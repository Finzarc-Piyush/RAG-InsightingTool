# Vercel Python wrapper — MUST stay in sync with ../../python-service/requirements.txt.
# CI (.github/workflows/ci.yml) diffs these two files and fails on drift.
-r ../../python-service/requirements.txt
