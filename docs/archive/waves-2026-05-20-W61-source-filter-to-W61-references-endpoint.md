# Archive — Marico RAG Insighting Tool wave history (2026-05-20 W61-source-filter → 2026-05-20 W61-references-endpoint)

> Rotated 2026-05-21 (post-Wave W61-detail-schema) to keep `docs/WAVES.md` under the 600-line / 300 KB / 50-entry rotation thresholds (WAVES.md was at 633 lines / 283 KB / 17 entries before this rotation).
> Contains 10 entries, newest-first within the rotated batch: W61-references-endpoint → W61-references-scan → W61-detail-extract → W61-audit-history-tab → W61-audit-history-client → W61-audit-revert → W61-audit-history-api → W61-audit-log → W61-filter-persist → W61-source-filter.
>
> These are the older waves rotated out of `docs/WAVES.md` to keep the active changelog readable. Same prose voice — newest-first; full per-wave entries with files / why / tests / verified / out-of-scope.

---

- **2026-05-20** — **Wave W61-references-endpoint · GET endpoint that wires the W61-references-scan scanner into a downstream-reference counter exposed over the wire.** Pairs with the prior W61-references-scan wave's pure-function scanner: that wave shipped the field-walking module without an HTTP surface; this wave wraps it in a thin Express handler so the future W61-delete-entry client UI can query "how many charts reference 'net_sales_value'?" before the destructive operation. `GET /admin/semantic-models/:sessionId/references?entry=<name>` returns `{ sessionId, entry, chartCount, totalOccurrences }`. Server-only — no client-side helpers, no route consumers yet; the next wave **W61-delete-entry** will both DELETE the entry and consume this endpoint for the confirmation copy. Pure-controller wave: ~75 LOC of handler + ~12 LOC of route registration + ~12 behavioural tests; the scanner that does the real work was already pinned by W61-references-scan's 19 tests, so this wave's tests focus on the controller surface (admin gate, parameter validation, 404 / 500 paths, envelope shape).

  **What landed.** Three files modified, one new test file:
    - **Modified: [`server/controllers/adminSemanticModelController.ts`](../server/controllers/adminSemanticModelController.ts)** (+105 LOC). New `import { countSemanticModelReferences } from "../lib/semantic/semanticModelReferences.js"`. New exported interface `AdminSemanticModelReferencesResponse` (`{ sessionId, entry, chartCount, totalOccurrences }`) with a doc-comment explaining that the envelope echoes the server-trimmed `entry` value (not the raw query value) so the client can compare against its own state without re-implementing the trim rule. New `getSemanticModelReferences(req, res)` async handler at the end of the file with the same shape as `getSemanticModel` / `getSemanticModelAuditLog`: admin-gate 403, sessionId trim → 400 on empty, `entry` query-param coercion to single string + trim → 400 on missing / whitespace / non-string, `_detailFetcher(sessionId)` (re-used; same Cosmos read), 404 on missing doc, 404 on missing `doc.semanticModel`, `countSemanticModelReferences(entry, doc.charts ?? [])`, envelope response. 500 with wave-specific `admin_semantic_model_references_failed` code on fetcher throw, with `console.error` logging the underlying error for ops.
    - **Modified: [`server/routes/admin.ts`](../server/routes/admin.ts)** (+12 LOC). `getSemanticModelReferences` added to the import. New `router.get("/admin/semantic-models/:sessionId/references", getSemanticModelReferences)` registration with a doc-comment explaining that the endpoint walks `doc.charts[]` only (not the blob-stored `chartReferences[]`) — that choice is load-bearing because fetching blob-stored charts would turn a free count into a multi-second request. Positioned between the audit-log GET and the PATCH so related semantic-model endpoints stay co-located in the file.
    - **New: [`server/tests/adminSemanticModelReferencesW61ReferencesEndpoint.test.ts`](../server/tests/adminSemanticModelReferencesW61ReferencesEndpoint.test.ts)** (451 LOC, 12 behavioural tests, node:test). Test harness mirrors `adminSemanticModelAuditLogW61AuditHistoryApi.test.ts`: the same `fakeReq` / `fakeRes` helpers (extended `fakeReq` to also accept `query`), the same `__setSuperadminEmailsForTesting` / `__setSemanticModelDetailFetcherForTesting` injectables for stand-up without Cosmos. Coverage: 5 admin-gate / param validation (403 non-admin, 400 missing sessionId, 400 missing `entry`, 400 whitespace `entry`, 400 array `entry`) + 2 404 paths (session_not_found, semantic_model_not_inferred) + 4 200 envelope checks (zero-counts on empty charts, correct counts for 2-of-3-charts-matching with `chartCount: 2` + `totalOccurrences: 3` since one chart contributes 2 occurrences, server-trimmed entry echoed via `?entry=%20net_sales_value%20`, undefined `doc.charts` coerced to empty without throwing) + 1 500 path (fetcher throw → 500 with wave-specific error code; `console.error` captured to keep test output clean).
    - **Modified: [`server/package.json`](../server/package.json)**. New test file appended to the explicit `test` list per invariant #4 (right after `tests/semanticModelReferencesW61ReferencesScan.test.ts` so the scanner pair stays adjacent in the run order).

  **Why this design.**
    - **Why a GET, not a POST.** This is an idempotent read — the scan walks the doc's persisted chart list and returns a count; nothing mutates. GET semantics let the client cache the response via standard HTTP rules, let the admin paste the URL to share "this metric is used N places" without a body, and let DevTools / curl exercise the endpoint without crafting JSON. POST would imply state change.
    - **Why a query param (`?entry=foo`), not a path param (`/.../references/foo`).** Entry names can in principle contain characters that are awkward in path positions (slashes most obviously; `metric.label` strings can have anything). `metric.name` itself is snake_case in practice, but the endpoint accepts any string the admin might type. Query-param is the canonical Express shape for an idempotent read filter; the path identifies the resource (the session), and the query is the filter on it. A future variant that wants a different filter (e.g. by entry kind) lands as another query param without re-shaping the path.
    - **Why an array `?entry=a&entry=b` produces a 400 (not "pick first" / "scan all").** Express's `qs` parser turns repeated keys into a string array; the handler's `typeof entryRaw === "string"` narrow rejects arrays. Picking the first (or scanning all) would silently mask a malformed client request — better to 400 so the client surfaces the bug immediately. A future bulk-scan endpoint (`?entries=a,b,c`) is a separate shape that opts into multi-entry semantics explicitly.
    - **Why walk `doc.charts[]` and NOT `doc.messages[].charts[]`.** [`chat.model.ts:55`](../server/models/chat.model.ts) describes `doc.charts: ChartSpec[]` as "All charts generated for this chat". The chat-model save path (around lines 713-743) dedup-merges per-message charts into the top-level list, so `doc.charts[]` is the authoritative full list. Walking both would double-count any chart that appears in both arrays.
    - **Why blob-stored `chartReferences[]` are NOT fetched.** Pulling each blob would turn a free in-memory count into a multi-second multi-request fetch; the cost grows linearly with the session's chart count, and most admin sessions don't have blob-stored charts in the first place (the threshold is large data arrays only). A future enhancement could load blob charts on-demand for sessions that exceeded the threshold; that's its own wave because it changes the endpoint's latency profile.
    - **Why re-use `_detailFetcher` rather than introducing a new injectable.** Same Cosmos read as `getSemanticModel` and `getSemanticModelAuditLog`. A separate `_referencesFetcher` would be the same function under a different name and would double the test-fake surface. The W61-audit-history-api wave established the "share the detail fetcher across read endpoints" pattern; this wave continues it.
    - **Why echo the server-trimmed `entry` in the envelope.** `?entry=%20net_sales_value%20` arrives as `" net_sales_value "` and the server trims it before the scan. Echoing the trimmed form (not the raw query value) lets the client compare the response's `entry` against its own state without re-implementing the trim rule. The W61-references-scan test pin against substring matching reinforces the same exact-equality contract end-to-end.
    - **Why the 500 catch uses `admin_semantic_model_references_failed`, not a shared error code.** Each W61 endpoint has its own wave-specific 500 code (`admin_semantic_model_list_failed`, `admin_semantic_model_detail_failed`, `admin_semantic_model_patch_failed`, etc.) so ops can grep server logs by the failing surface without correlating against handler stack traces. Continuing the pattern keeps the per-endpoint error taxonomy consistent.

  **Tests.** 12 behavioural tests in [`adminSemanticModelReferencesW61ReferencesEndpoint.test.ts`](../server/tests/adminSemanticModelReferencesW61ReferencesEndpoint.test.ts), all pass in ~340 ms standalone. **Admin-gate / param validation (5):** 403 non-admin; 400 missing sessionId (whitespace-only); 400 missing `entry` query param; 400 whitespace-only `entry`; 400 array `entry` (`?entry=a&entry=b`). **404 paths (2):** `session_not_found` when fetcher returns null; `semantic_model_not_inferred` when the doc exists but has no `semanticModel` field. **200 envelope checks (4):** zero counts on empty `doc.charts` array; correct counts on a 3-chart fixture where 1 chart matches once (y) + 1 chart matches twice (x + y) + 1 doesn't match → `{ chartCount: 2, totalOccurrences: 3 }`; `?entry=%20net_sales_value%20` returns `entry: "net_sales_value"` in the envelope plus correct `chartCount: 1` against a matching chart; `delete doc.charts` (legacy-shape session) coerces to empty without throwing. **500 path (1):** fetcher throws `Error("cosmos boom")` → 500 with `admin_semantic_model_references_failed`; `console.error` captured during the test to keep the runner output clean.

  **Verified.** Server tests: 4662 / 4666 pass (was 4650 / 4654; +12 new tests landed cleanly). The same 4 pre-existing failures in `wd2WiringEcharts` Treemap + Sunburst + `wd2DimEchartsTreemap` carry forward unchanged. Total duration ~39.8 s. Server typecheck `npx tsc --noEmit` = 98 errors (baseline preserved; zero new errors in the handler, route registration, or test file). Client untouched.

  **Conventions added.** None new. This wave continues the W61 controller patterns established by W61-list / W61-detail / W61-audit-history-api: admin-gate → sessionId trim → injectable detail fetcher → 404 on session / model missing → wave-specific 500 code. The "shared `_detailFetcher` across read endpoints" convention codified by W61-audit-history-api is reused here.

  **Out of scope.**
    - **Walking `doc.messages[].charts[]`** in addition to `doc.charts[]` — would double-count given the chart-model dedup-merge.
    - **Dashboard scanning** — dashboards live in a separate Cosmos container partitioned by username; deferred to W61-references-dashboards.
    - **Fetching blob-stored `chartReferences[]`** — turns a free in-memory count into a multi-second multi-request fetch; defer until session count of blob-stored charts is non-trivial in practice.
    - **Bulk endpoint** that counts references for a list of names in one request (`?entries=a,b,c`) — defer until the future "audit all unused entries" UI surfaces a need.
    - **Returning matching chart titles / IDs** alongside the counts (for a "view the affected charts" affordance) — the current scanner returns counts only; if the UX needs detail-level matches, a future scanner enhancement + endpoint widening would land together.
    - **Caching** the count server-side (per `(sessionId, entry)`) — the chart list rarely changes between admin sessions and the count is cheap; defer until profiling shows it's a bottleneck.
    - **Authorization beyond admin-gate** (e.g. per-session ACL beyond the admin allowlist) — admin endpoints are already gated by `SUPERADMIN_EMAILS`; additional session-level auth is unrelated to this wave's scope.

- **2026-05-20** — **Wave W61-references-scan · pure-function scanner for downstream chart references to a semantic-model entry name.** Foundation for the upcoming W61-delete-entry wave's "removing this metric will break N charts that reference it" confirmation. The prior session brief packaged delete + add-new together as a single ~300-LOC client + ~50-LOC server wave (W61-add-delete); this wave splits the load-bearing scanner foundation into its own ~480-LOC wave (code + tests) so its semantics are exercised in isolation before the controller endpoint, the per-row delete UI, and the add-new affordance build on top. Without this scanner the future delete is still functionally correct but loses the safety-net UX that the prior brief flagged as the dominant remaining surface on the W61 polish trail. Pure module — no controller endpoint, no route registration; the next wave **W61-references-endpoint** will wire the GET surface.

  **What landed.** Two new files plus a one-line `server/package.json` update:
    - **New: [`server/lib/semantic/semanticModelReferences.ts`](../server/lib/semantic/semanticModelReferences.ts)** (179 LOC). Exports `SemanticModelReferenceCount` interface (`{ chartCount: number; totalOccurrences: number }` — distinct charts that contain at least one match + total field-position matches across all charts, with a chart whose `x === name && y === name` contributing 2 to `totalOccurrences`). Three pure functions: `countReferencesInChartSpec(chart, name)` walks every advertised v1 field (`x` / `y` / `z` / `seriesColumn` / `y2` / `y2Series[]` / `_agentProvenance.columnsUsed[]` / `_agentProvenance.rangeFilters[].column`); `countReferencesInChartSpecV2(chart, name)` walks every `.field` location on the v2 encoding (`x` / `y` / `x2` / `y2` / `y2Series[].field` / `color` / `size` / `shape` / `pattern` / `opacity` only on the channel branch — the `{ value: number }` branch is correctly skipped via `"field" in enc.opacity` narrowing / `facetRow` / `facetCol` / `detail` / `text` / `order` / `tooltip[].field`) plus the transform discriminated union's identifier-bearing branches (`aggregate.groupby` + `aggregate.ops[].field`, `fold.fields[]`, `bin.field`, `window.ops[].field` + `window.groupby[]` + `window.sort[]`, `regression.on`) plus the same `_agentProvenance.{columnsUsed,rangeFilters}` shape carried into v2 via `chartSpecSchema.shape._agentProvenance`. The top-level `countSemanticModelReferences(name, charts)` accepts `ReadonlyArray<unknown>` and uses [`isChartSpecV2`](../server/shared/schema.ts) to discriminate v1 vs v2 at runtime — future-proofs against the planned v1→v2 migration. Non-object array items (defensive against stray `null` / primitive / `undefined` values in `doc.charts[]`) are silently skipped without throwing.
    - **New: [`server/tests/semanticModelReferencesW61ReferencesScan.test.ts`](../server/tests/semanticModelReferencesW61ReferencesScan.test.ts)** (308 LOC, 19 behavioural tests, node:test). Top-level guards (empty `name` returns zero — defensive against malformed semantic-model entries; empty array returns zero; non-object items silently skipped). v1 field walks (single-field match, multiple-field match in one chart counts twice, parametric walk of `z` / `seriesColumn` / `y2` / `y2Series[]`, `_agentProvenance.columnsUsed[]` with duplicates counted, `_agentProvenance.rangeFilters[].column`, no-match returns zero). v2 channel walks (parametric 13-channel walk covering every `.field` location, separate `y2Series[].field` test with mixed match / no-match, `tooltip[].field`, opacity-channel-vs-literal-value-branch test). v2 transform walks (parametric for `aggregate` / `fold` / `bin` / `window` / `regression`, **negative pin that `filter.expr` + `calculate.expr` are NOT walked** — load-bearing for the exact-identifier-only semantics, `_agentProvenance` carried into v2). Mixed v1+v2 array aggregation (3 distinct charts hit, 4 total occurrences). Empty-name short-circuit in per-spec scanners (defense in depth — both the top-level and each per-spec function guard against `name === ""`).
    - **Modified: [`server/package.json`](../server/package.json)** (+1 file entry). The new test file is appended to the explicit `test` list right after `tests/semanticModelAuditLogW61AuditLog.test.ts` so semantically-related W61 tests stay adjacent in the run order. Required per invariant #4 (server `npm test` is an explicit file list; new files not appended are silently skipped by CI).

  **Why this design.**
    - **Why exact-identifier-only matching, no substring or fuzzy.** Substring matching would false-positive across similarly-named columns ("sales" matches "sales_volume" AND "net_sales_value" AND "sales_amount"); too noisy to be load-bearing for a destructive delete prompt. The contract is `name === field` with no normalisation, no case-folding, no trim. A future enhancement could add opt-in expression-substring mode behind a flag once a real false-negative surfaces — but the v1 surface stays strict.
    - **Why `transform.filter.expr` and `transform.calculate.expr` are NOT walked.** Those fields are SQL-like strings (e.g. `"net_sales_value > 0"`); substring matching would inherit the noise from above. A test pins the negative behaviour so a future "let's just substring-match expressions" refactor surfaces immediately as a test break. The right path to expression support is a SQL identifier tokenizer + opt-in flag, not silent string contains.
    - **Why empty `name` short-circuits at three layers.** Top-level `countSemanticModelReferences("", ...)` returns zero; each per-spec scanner ALSO guards on empty name. Defense in depth: a malformed semantic-model entry with an empty `name` field would otherwise chart-count every chart that happens to have an empty `seriesColumn` or empty `y2`. The schema's `.max(...)` bounds don't prevent empty strings at the type level — they prevent oversized strings. Empty-name handling is a runtime concern.
    - **Why the top-level function accepts `ReadonlyArray<unknown>`, not `(ChartSpec | ChartSpecV2)[]`.** The caller is the future `getSemanticModelReferences` controller handler; it hands us whatever lives at `doc.charts` — which is typed as `ChartSpec[]` but the runtime values may be v2 specs after the planned migration AND may contain stray null / undefined / primitive slots from older data shapes. The defensive `typeof === "object"` + `value !== null` guard at the top of the loop means a malformed doc doesn't 500 the endpoint; the unreadable chart is silently skipped, which is the right UX for a "how many" question (an unparseable chart can't be destructively-affected by removing a metric we don't know it references).
    - **Why discriminate v1 vs v2 via [`isChartSpecV2`](../server/shared/schema.ts) at runtime rather than at the type level.** The chart storage path types as `ChartSpec[]` (v1) but [`isChartSpecV2`](../server/shared/schema.ts) is the canonical runtime discriminator (`value.version === 2 && typeof value.mark === "string"`). Walking the right field set per shape produces accurate counts; falling back to v1 scanner on a v2 chart would silently undercount (v1 has no `encoding.color.field` to walk). Future-proofs against the v1→v2 migration without needing a rebuild of the scanner.
    - **Why count `totalOccurrences` AND `chartCount`, not just one.** The UI's delete prompt only needs `chartCount` ("removing this metric will break N charts"). `totalOccurrences` is a debug / observability signal — a metric used in 1 chart but referenced 4 times (x + y + facetRow + tooltip) signals a different reliability concern than the same chart count with 1 occurrence each. Both fields are cheap to compute in the same pass; emitting both leaves room for a future detail-level UX without a scanner change.
    - **Why a separate wave from the endpoint rather than bundling.** The brief estimated ~50 LOC for the reference-scan endpoint, which is mostly the controller handler wiring + 403 / 400 / 404 / 200 plumbing. Bundled with the scanner the wave would be ~600 LOC + tests — at the upper edge of the wave budget. Splitting keeps each sub-wave small (~500 LOC scanner + tests this wave; ~150 LOC endpoint + tests next wave), and the scanner's pure-function semantics get the focused test coverage they deserve before the controller layer adds Express plumbing on top.

  **Tests.** 19 new behavioural tests in [`server/tests/semanticModelReferencesW61ReferencesScan.test.ts`](../server/tests/semanticModelReferencesW61ReferencesScan.test.ts). All pass in ~137 ms standalone. **Top-level guards (3):** empty name → zero; empty array → zero; non-object items skipped. **v1 field walks (5):** single-field match (`y === name`); multiple-field match (`x === y === name` → 2 occurrences in one chart); parametric walk of `z` / `seriesColumn` / `y2` / `y2Series[]` (with mixed match/no-match across the array); `_agentProvenance.columnsUsed[]` (duplicates DO count — the schema shape is what it is); `_agentProvenance.rangeFilters[].column`; no-match → zero. **v2 channel walks (4):** parametric 13-channel walk; `y2Series[].field` with mixed match/no-match; `tooltip[].field`; opacity-channel-vs-literal-value-branch (the literal-value branch must NOT be walked since `value` is a number). **v2 transform walks (3):** parametric `aggregate` (groupby + ops, 1+1 = 2 occurrences); parametric `fold` / `bin` / `window` (ops + groupby + sort sum to 3 occurrences) / `regression.on`; **negative pin against `filter.expr` + `calculate.expr` being walked** (load-bearing — a future "include expressions" mode must be opt-in); `_agentProvenance` v2-carry. **Integration (2):** mixed v1+v2 array aggregation (3 distinct charts, 4 total occurrences); empty-name short-circuit in per-spec scanners.

  **Verified.** Server tests: 4650 / 4654 pass (was 4631 / 4635; +19 new tests landed cleanly). Same 4 pre-existing failures in `wd2WiringEcharts` / `wd2DimEchartsTreemap` Sunburst + `streamingNarratorW38` suites unchanged. Total duration ~39.9 s. Server typecheck `npx tsc --noEmit` = 98 errors (baseline preserved). None of the 98 errors mention `semanticModelReferences.ts` or its test file. Client untouched.

  **Conventions added.** None as a hard rule. A soft pattern emerges that's worth naming if it recurs: **"foundation scanner waves first, then the endpoint that wires them"**. The shape is the same as W61-source-bump → W61-source-badge → W61-source-filter (each sub-wave was the smallest atomic unit before consumers built on top). Promote to `docs/conventions/` if a third such split appears.

  **Out of scope.**
    - **GET endpoint** `GET /admin/semantic-models/:sessionId/references?entry=<name>` — deferred to W61-references-endpoint.
    - **Walking `doc.messages[].charts[]`** in addition to `doc.charts[]` — the endpoint wave will decide which arrays to walk; the pure scanner takes any `ReadonlyArray<unknown>` so the choice doesn't leak into this module.
    - **Dashboard scanning** — dashboards live in a separate Cosmos container partitioned by username; deferred to W61-references-dashboards.
    - **Substring / SQL-expression matching** (e.g. detect `name` inside `transform.filter.expr`) — defer until a real false-negative appears in production.
    - **Sample / per-match detail return values** (e.g. "the matching chart's title is X" for a "view affected charts" affordance) — v1 surface returns counts only.
    - **Case-insensitive / trim normalisation** — the contract is strict equality; column names are exact identifiers, not user-typed labels.
    - **Counting references inside the semantic model itself** (e.g. metrics that reference a dimension via the `references` array) — that's a different scan with different semantics (semantic-model self-consistency vs persisted-chart impact); defer to a separate semantic-self-consistency wave if needed.

- **2026-05-20** — **Wave W61-detail-extract · sibling-file extraction of SourceFilterChips + AuditHistoryCard from AdminSemanticModelDetail.tsx.** Pre-emptive zero-behavioural-risk refactor that relieves file-size pressure on [`client/src/pages/Admin/AdminSemanticModelDetail.tsx`](../client/src/pages/Admin/AdminSemanticModelDetail.tsx) before the projected W61-add-delete wave (+~300 LOC) would push it past the 1,500-LOC sub-component-extract threshold flagged in the prior session brief. The host file had grown to 1,295 LOC after W61-audit-history-tab landed its +197-LOC inline audit-trail Card; this wave carves the two largest extractable presentational pieces — the `SourceFilterChips` chip row used 3× on the page and the entire `AuditHistoryCard` collapsible Card — into sibling files under a new [`client/src/pages/Admin/components/`](../client/src/pages/Admin/components/) directory. Host drops to 1,131 LOC (−164 LOC), leaving ~370 LOC of headroom before W61-add-delete would re-approach the threshold. Pure file-move-and-import-rewire wave; no JSX nodes, prop semantics, or runtime behaviour change; no new tests because the underlying pure helpers consumed by the extracted components were already pinned by their respective W61 helper-test files. See [`/Users/tida/.claude/plans/w61-detail-extract.md`](/Users/tida/.claude/plans/w61-detail-extract.md) for the pre-implementation plan.

  **What landed.** Three files touched:
    - **New: [`client/src/pages/Admin/components/SourceFilterChips.tsx`](../client/src/pages/Admin/components/SourceFilterChips.tsx)** (76 LOC). Exported named component + `SourceFilterChipsProps` interface. Renders the "Show only X" chip row with Auto / User / Domain / All filter chips per the existing W61-source-filter design (active chip carries the source's badge variant; inactive chips render outline; the "All" sentinel uses outline even when active so it doesn't compete with source-tinting). Imports `Badge` + `cn` from the shared UI lib + `getSourceBadgeVariant` from `../lib/semanticModelSourceBadge` + `SOURCE_FILTER_ORDER` / `getFilterLabel` / `SemanticEntryFilter` from `../lib/semanticModelSourceFilter`. The full W61-source-filter doc comment migrates verbatim with a `/ W61-detail-extract` suffix so a future reader sees the provenance.
    - **New: [`client/src/pages/Admin/components/AuditHistoryCard.tsx`](../client/src/pages/Admin/components/AuditHistoryCard.tsx)** (166 LOC). Exported named component + `AuditHistoryCardProps` interface. Props: `historyOpen`, `onOpenChange`, `historyLog`, `historyLoading`, `historyError`, `revertError`, `reverting`, `saving`, `onRevert`. State stays in the parent because the per-row Revert button's disabled triple-gate (`saving || reverting !== null || historyLoading`) coordinates against the existing parent-owned save flow; lifting the state would force a callback-through-render-bridge for `saving` and break the gate's atomicity. All five render branches (`revert-error` banner / `loading` / `error` / `empty` / `list`) are byte-identical to the pre-extract inline JSX; every `data-testid` (`admin-semantic-model-audit-history-{trigger,loading,error,empty,list,revert-error,revert-N}`) is preserved verbatim so the future Playwright / RTL smoke test remains valid without any rebinding. Imports: `ChevronDown` from `lucide-react`, `Card` + `Button` + the three Radix `Collapsible` primitives, `cn`, types `AdminSemanticModelAuditEntry` / `AdminSemanticModelAuditLog` from `@/lib/api/admin`, `buildAuditEntrySummary` from `../lib/semanticModelAuditHistory`.
    - **Modified: [`client/src/pages/Admin/AdminSemanticModelDetail.tsx`](../client/src/pages/Admin/AdminSemanticModelDetail.tsx)** (1,295 → 1,131 LOC, −164 LOC). Imports trimmed: `ChevronDown`, the three `Collapsible` primitives, `SOURCE_FILTER_ORDER`, `getFilterLabel`, and `buildAuditEntrySummary` all move out to the extracted files; `SourceFilterChips` + `AuditHistoryCard` imports added. `buildRevertConfirmation` stays in the host (still consumed by `handleRevert`). Removed: the 42-LOC inline `SourceFilterChips` function declaration (lines 218–259 of the pre-extract host) and the 104-LOC inline audit-history `<Card>` JSX block (lines 1187–1291 of the pre-extract host). Replaced: the audit-history JSX block becomes a 12-line `<AuditHistoryCard ... />` invocation with all nine props threaded explicitly; the three `<SourceFilterChips ...>` use-sites in the Metrics / Dimensions / Hierarchies card headers are byte-identical (the symbol is now imported instead of declared in-file).

  **Why this design.**
    - **Why a single extract wave covering both components, not two waves.** Both targets are presentational shells with no internal state; both live in the same host file; both are touched by the same import-cleanup arithmetic; and a future reader scanning `git log --oneline` sees one "carve out before W61-add-delete" entry rather than two stacked "moved-only" commits. The cost of folding them into one wave is ~70 LOC over the typical sub-300-LOC wave budget for this stream — well within budget for an extraction that touches no behaviour and ships no new tests. Splitting would also force two intermediate states of the host file with stale import sets, each requiring a separate typecheck round; one wave keeps the host in a known-good state across the full move.
    - **Why a new `components/` sibling directory, not co-locating in `lib/`.** The existing `client/src/pages/Admin/lib/` directory holds pure functions: `semanticModelEditValidation.ts`, `semanticModelSourceBadge.ts`, `semanticModelSourceFilter.ts`, `semanticModelFilterUrlSync.ts`, `semanticModelAuditHistory.ts`. Each is a `*.ts` file (not `.tsx`) with a paired `*.test.ts` consumed by `node:test` via the explicit list in `server/package.json`. React components have a different shape: `*.tsx`, jsdom mount when tested, and a future barrel re-export pattern that pure helpers don't need. Splitting along the React boundary keeps a future reader's mental model clean — `lib/` is "the math", `components/` is "the JSX". The two extracted files are the first inhabitants of `components/`; the directory will grow as more sub-component extracts happen in subsequent W61 polish waves.
    - **Why state stays in the parent rather than lifting into `AuditHistoryCard`.** Six pieces of audit-history state (`historyOpen`, `historyLog`, `historyLoading`, `historyError`, `reverting`, `revertError`) plus the `useEffect` that fetches the log on open + `lastUpdatedAt` bump + the `handleRevert` async function all reference `data` / `sessionId` / `saving` from the parent's scope. Lifting them into `AuditHistoryCard` would require either (a) a five-prop callback bridge for parent-owned values + a return-value-bridge for the in-flight `reverting` index, or (b) replacing the parent's `data` state with a Context Provider, both of which add load-bearing surface for zero behavioural improvement. The parent-owned-state pattern is also stylistically consistent with the surrounding host file (the existing `saving` / `saveError` / `sourceFilter` are all parent-owned too). The wave's contract is "move the JSX, not the architecture"; lifting state would violate that contract.
    - **Why the inline `{/* W61-audit-history-tab · ... */}` block comment moves to a `/** ... */` JSDoc on the extracted component.** The inline comment described why the Card was opt-in + Cosmos-round-trip-gated; the same context is more discoverable on the extracted component's declaration than buried inside the parent's render body, and tooling (TypeScript LSP, IDE hover) surfaces JSDoc but not inline JSX comments. The W61-source-filter doc comment migrates the same way for the same reason.
    - **Why `SourceBadge` stays inline in the host.** `SourceBadge` is 11 LOC and consumed by `MetricRow` + `DimensionRow` (both inline in the host at 60–95 LOC each). The W61-add-delete wave will need to touch `MetricRow` / `DimensionRow` to add per-row Delete buttons; extracting `SourceBadge` now would force W61-add-delete to either re-import it from a sibling or replicate it inline. Deferred until W61-add-delete makes the row-extraction trade-off concrete.
    - **Why no new tests.** The extracted components contain the same render branches that already worked when inline. The pure helpers they consume (`buildAuditEntrySummary`, `buildRevertConfirmation`, `getFilterLabel`, `getSourceBadgeVariant`, `SOURCE_FILTER_ORDER`) are already pinned by the W61-audit-history-client (15 tests) + W61-source-filter + W61-source-badge + W61-filter-persist test files. The parent's behavioural contract (PATCH success bumps `lastUpdatedAt`, revert refreshes the log, the disabled triple-gate is race-safe) is unchanged because the parent itself is unchanged. Adding jsdom mount tests would re-test the underlying helpers via a different surface without changing the signal — deferred until a real component-level bug appears.

  **Tests.** No new test file. The 199 cumulative W61 admin + client tests + the 420 client vitest tests continue to pass byte-identical on the post-wave tree because no helper module was touched. Client vitest = 420 / 420 across 46 test files (zero rebinds; the extracted components' helper imports flow through the same `@/lib/api/admin` + `./lib/*` paths the host previously used). Server suite ran 4631 / 4635 (same 4 pre-existing failures: `wd2WiringEcharts` Treemap dispatch + `wd2WiringEcharts` Sunburst dispatch + `wd2DimEchartsTreemap` dim triplet + `streamingNarratorW38` — all confirmed pre-existing on the W61-audit-history-tab baseline; unrelated to this wave's surface).

  **Verified.** Client typecheck: `npx tsc --noEmit` = 53 errors (baseline preserved). All 53 errors are in non-Admin paths (`ChartOnlyModal`, `Dashboard`, `DataPreview`, `MessageBubble`, `useHomeMutations`) — none mention `AdminSemanticModelDetail.tsx`, `SourceFilterChips.tsx`, or `AuditHistoryCard.tsx`. Server tests: 4631 / 4635 (server typecheck unchanged at 98 — no server file touched). Client vitest: 420 / 420 across 46 files (983 ms). Net LOC delta: −164 in host file + 76 (SourceFilterChips) + 166 (AuditHistoryCard) = +78 LOC across the wave; the AuditHistoryCard file picks up a bit more LOC than the inline block had because the doc comment promoted from `{/* ... */}` to `/** ... */` adds ~20 lines + the import block + the props interface declaration adds ~25 lines, but neither pays for runtime cost.

  **Conventions added.** None new as a hard rule, but this wave establishes a soft pattern worth tracking: **when a single host React component grows past ~1,200 LOC and contains presentational sub-components with no internal state, extract them into a sibling `components/` directory before the next feature-adding wave lands**. The recommendation is not a hard threshold (1,295 was the post-W61-audit-history-tab LOC and the wave was deferred; the actionable threshold is 1,500 LOC per the sub-component-extract trigger). Promote to `docs/conventions/<slug>.md` if a second similar extract surfaces with the same shape; informal until then.

  **Out of scope.**
    - **jsdom mount tests for the extracted components.** No new behaviour to pin; the helpers they consume are already tested at the pure-function level. Add when a real component-level bug surfaces (e.g. a render-branch regression that a helper test can't catch).
    - **Extraction of `MetricRow` / `DimensionRow` / `HierarchyRow` / `SourceBadge` / `ExposedToggle` / `EditableText` / `EditableSelect`.** Deferred to W61-add-delete, which will touch `MetricRow` / `DimensionRow` to add Delete buttons. Extracting now and touching again next wave is friction without payoff. `SourceBadge` is 11 LOC and intra-row-only.
    - **A `client/src/pages/Admin/components/index.ts` barrel re-export.** Two files don't need a barrel; promote when the directory grows past ~5 files.
    - **Migration of the existing `lib/` helpers into per-feature subdirectories** (e.g. `lib/sourceFilter/`, `lib/auditHistory/`). Not warranted while the count stays small (~5 modules).
    - **Lifting parent state into `AuditHistoryCard` via Context / callback bridges.** Architectural refactor unrelated to file-size pressure; the parent-owned state is stylistically consistent with the rest of the host.
    - **Promoting the "1,200 LOC soft extract threshold" to a CLAUDE.md invariant.** Informal precedent until a second similar extract appears with the same shape; codify when a pattern emerges.

- **2026-05-20** — **Wave W61-audit-history-tab · collapsible Audit-history Card on the admin semantic-model detail page.** Closes the W61 audit-trail trilogy end-to-end on the client by mounting the four helpers shipped last wave (W61-audit-history-client) onto [`AdminSemanticModelDetail.tsx`](../client/src/pages/Admin/AdminSemanticModelDetail.tsx) as a fourth Card under the existing Hierarchies card. The three audit-trail server waves (W61-audit-log write → W61-audit-history-api read → W61-audit-revert revert) plus the W61-audit-history-client API + formatter foundation were all individually testable, but until this wave landed the admin had no UI surface to actually see what had been saved or trigger a revert — every prior model was sitting in the Cosmos doc invisible to anything but a curl / DevTools fetch. Pure-mounting wave: no new code outside the host component, no new tests (the helpers were already pinned by 15 unit tests in W61-audit-history-client), browser verification per CLAUDE.md's UI policy is the acceptance signal.

  **What landed.** All edits in [`client/src/pages/Admin/AdminSemanticModelDetail.tsx`](../client/src/pages/Admin/AdminSemanticModelDetail.tsx) (1098 → 1295 LOC, +197 LOC):
    - **Imports.** Add `ChevronDown` from `lucide-react` (rotates 180° when the section opens); the three Radix Collapsible primitives (`Collapsible` / `CollapsibleTrigger` / `CollapsibleContent`) from `@/components/ui/collapsible`; four new symbols from `@/lib/api/admin` (`fetchSemanticModelAuditLog`, `revertSemanticModel`, type `AdminSemanticModelAuditEntry`, type `AdminSemanticModelAuditLog`); two pure helpers from `./lib/semanticModelAuditHistory` (`buildAuditEntrySummary`, `buildRevertConfirmation`). No new top-level dep — every package was already on the client.
    - **State.** Six new pieces of component-local state alongside the existing `saving` / `saveError` / `sourceFilter` block: `historyOpen: boolean` (closed by default — the audit-log Cosmos fetch is opt-in), `historyLog: AdminSemanticModelAuditLog | null` (cached after first open), `historyLoading: boolean`, `historyError: string | null`, `reverting: number | null` (the index of the row whose Revert button is in flight — see "per-row reverting state" below), `revertError: string | null`. Each piece carries a 2-line doc-comment so the next reader sees the role at a glance.
    - **Effect.** New `useEffect` keyed on `[sessionId, historyOpen, lastUpdatedAt]` (with `lastUpdatedAt = data?.lastUpdatedAt` extracted to a stable local first so React's dep-array shallow-compares numbers, not the `data` object reference). Gated by `!sessionId || !historyOpen` so closed-section visits skip the Cosmos round-trip entirely. Inside: standard `cancelled` flag pattern (matches the existing detail-fetch effect at the same scope), `fetchSemanticModelAuditLog` then setState. The `lastUpdatedAt` dep is the key wiring: when the admin saves via the existing PATCH or completes a revert, `data.lastUpdatedAt` bumps (PATCH success path already does `setData({ ...prior, lastUpdatedAt: res.lastUpdatedAt, model: res.model })`); the audit-log effect re-fires and re-reads the now-grown buffer without an explicit cache-busting call at every mutation site.
    - **Handler.** `handleRevert(entry, indexFromNewest, total)` is an async function that runs `buildRevertConfirmation(entry, indexFromNewest, total)` through `window.confirm`, bails on cancel, then `revertSemanticModel(sessionId, indexFromNewest)`. On success: `setData({ ...data, lastUpdatedAt: res.lastUpdatedAt, model: res.model })` — byte-identical to the existing PATCH-success path because the server's revert envelope is byte-identical to the save envelope (codified by W61-audit-revert reusing `PatchSemanticModelResponse`). On failure: `setRevertError`; the row stays clickable for retry. `finally`: `setReverting(null)`. Re-entry guard: `if (!data || reverting !== null) return;` so a fast double-click can't queue two reverts.
    - **Render.** New `<Card className="p-0 overflow-hidden">` between the existing Hierarchies card's closing tag and the page-container's closing `</div>`. Wrapped in a `Collapsible open={historyOpen} onOpenChange={setHistoryOpen}`. Trigger is a `<button>` (Radix `asChild`) with a left-aligned headline + caption ("Prior model versions (newest first, last 10 saves). Revert to restore a snapshot — the current model will be appended to this log first.") and a right-aligned chevron with `rotate-180` toggled on `historyOpen`. Content renders one of five states: revert-error banner above the rest (if `revertError`), then loading (if `historyLoading && !historyLog`), error (if `historyError`), empty (if `historyLog && entries.length === 0`), or list (otherwise). The list is a `<ul className="divide-y divide-border">` with one `<li>` per entry — each shows `summary.headline` at base font + `summary.subhead` muted + tabular-nums (so the timestamps + version numbers line up visually across rows), and a Revert `<Button>` on the right with text flipping to "Reverting…" when `reverting === idx`. Disabled-gate on each button: `saving || reverting !== null || historyLoading` — race-safe against the existing PATCH save path AND against another revert in flight. Test-IDs (`admin-semantic-model-audit-history-{trigger,loading,error,empty,list,revert-error,revert-N}`) wired throughout so a future Playwright / RTL smoke test can drive the surface without scraping text.

  **Why this design.**
    - **Direct `fetch + useState` rather than TanStack Query.** The session brief proposed `useQuery({ enabled: tabOpen })` because that's the natural shape for "fetch when the tab opens", but the host component uses direct `useEffect + useState` throughout (the existing detail-fetch effect at line 633-670 is the same pattern). Introducing TanStack Query mid-component would be a stylistic discontinuity without a behavioural payoff — the audit log is fetched at most once per page visit (gated by `historyOpen`) and refreshed on save-bump. A future broader-scope refactor that converts the whole admin viewer to TanStack Query is a separate consideration; that refactor would also restructure the PATCH path's optimistic-update flow, which is out of scope here.
    - **`historyOpen` closed by default.** Open by default would fetch the audit log on every detail-page mount even though most visits are "look up something, leave" without consulting history. Closed-by-default is opt-in: the fetch happens only when the admin explicitly clicks the trigger. The closed state is on the component (not URL-synced) because the deep-link use case ("share me the open history view") is speculative — can promote to a `?history=open` toggle later if a user-driven need appears, but adding two more places that read/write search params now would just be friction.
    - **Per-row `reverting: number | null` state, not a global boolean.** A list of up to 10 entries with a single `reverting: boolean` would disable every row's Revert button while one is in flight. With the per-index flag, all other rows remain readable AND queue-able (a re-entry guard on the handler still prevents the second click from launching a second revert, but the admin can read the others while waiting). The server's `withSessionWriteLock` (invariant #9) makes any concurrent attempt safe — only one mutation lands per session at a time; the others see fresh state on retry. Tradeoff acknowledged: a Revert click while another is in flight is silently dropped by the handler's guard rather than queued; this is fine because the admin almost always wants to inspect the result of the first revert before deciding the second.
    - **Effect dep on `lastUpdatedAt`, not `data`.** React's dep-array compares by reference; `data?.lastUpdatedAt` is a number (or undefined), comparable by value, and changes exactly on save or revert. Using `data` directly would re-trigger the effect on every `setData` even when only an optimistic toggle bumped — and the optimistic-toggle path on the PATCH inside `persistModel` rebuilds the `data` object reference but keeps `lastUpdatedAt` until the server response. Extracting `lastUpdatedAt` to a local variable before the effect spares the re-render storm and reads naturally as "re-fetch when the doc has been written".
    - **Effect cleanup flag matches the existing pattern.** The host component already uses `let cancelled = false; ... return () => { cancelled = true; };` in the detail-fetch effect. Repeating the pattern in the history effect keeps two effects in this file stylistically identical so a future reader doesn't trip on "wait, why does this one use AbortController and that one uses a flag?". `fetch()` itself is unaborted on cancel — the host detail fetch doesn't abort either, and a 200 KB audit-log response that lands after navigation just gets garbage-collected without the setState landing. Adding `AbortController` for the audit-log fetch but not the detail fetch would create an inconsistency without a measurable benefit.
    - **Revert success path reuses the existing PATCH-success shape.** Both PATCH and revert produce `{ sessionId, lastUpdatedAt, model }` (W61-audit-revert deliberately reuses `PatchSemanticModelResponse`'s envelope so the client's success handler doesn't fork). The revert handler does the same `setData({ ...data, lastUpdatedAt: res.lastUpdatedAt, model: res.model })` as `persistModel`, and the `lastUpdatedAt` bump rolls into the same effect that PATCH success rolls into — meaning the audit log refreshes automatically with the newly-appended (the just-overwritten) entry at index 0. The W61-audit-revert wave's audit-write-on-revert design is what makes this work: the just-overwritten model becomes the new newest entry, and "undo this revert" is naturally `auditEntryIndex: 0` on the next click.
    - **Disabled-gate triple `saving || reverting !== null || historyLoading`.** Three sources can race: an in-flight PATCH from the existing save UI (saving=true), an in-flight revert (reverting≠null), and an in-flight audit-log refetch (historyLoading=true). Disabling all Revert buttons during any of the three prevents a second mutation against a stale read or against an in-progress write. The cost is mild UI friction (Revert is disabled during the brief window between save-success and the audit-log-refetch landing), but that's correct: clicking Revert during that window would target an index against the pre-save audit log shape.
    - **`<button>` for the trigger, not the page-existing `<Card><header>` shape.** Other cards on the page (`Metrics`, `Dimensions`, `Hierarchies`) use a non-interactive `<header>` because there's no toggle. The Audit-history card needs the whole header row to be clickable for the collapse toggle; a `<button>` element is the semantically-correct interactive role for that, and `Collapsible asChild` lets Radix forward the trigger props onto the `<button>`. Tailwind classes match the other cards' header padding / border / typography so visually the cards read as a family.
    - **Test-IDs everywhere.** Each branch (`trigger`, `loading`, `error`, `empty`, `list`, `revert-error`, `revert-${idx}`) gets a stable `data-testid` so a future smoke test can drive the surface without text-scraping. The cost is ~80 chars of attribute markup; the benefit is that the surface stays mechanically queryable even if the copy changes.

  **Tests.** No new test file. The helpers wired here were already pinned by 15 unit tests in `semanticModelAuditHistoryW61AuditHistoryClient.test.ts` (W61-audit-history-client) and the API helpers by integration tests against the W61-audit-history-api / W61-audit-revert server endpoints. The wave-level acceptance signal per CLAUDE.md's "For UI changes: actually use the feature in a browser" is in-browser verification (see below). Adding a jsdom mount of the full component would re-test the helpers without exercising the actual flow; deferred until the first real bug surfaces. The 199 prior W61 admin + client tests (8 server files + 6 client files) still pass byte-identical in ~354 ms (no helper module touched). Server typecheck = **98 errors**, identical to the W61-audit-history-client baseline (no server touched). Client typecheck = **53 errors**, identical to the W61-audit-history-client baseline (the new `useState` / `useEffect` / handler / render block is fully typed; `AdminSemanticModelAuditLog` and `AdminSemanticModelAuditEntry` flow through `setHistoryLog` / `historyLog.entries[]` without any-casts).

  **Verified.** Server tests: 4631 / 4635 pass (4 pre-existing failures in `wd2WiringEcharts` / `wd2DimEchartsTreemap` / `wd2WiringEcharts` Sunburst suites — confirmed pre-existing by `git stash` + re-run on the unmodified tree, both runs produce the same 4 failures in the same suites; unrelated to this wave's surface). Server typecheck baseline preserved at 98. Client typecheck baseline preserved at 53. 199 cumulative W61 admin + client tests green on the combined isolated run (~354 ms). In-browser verification: deferred to the user — the session is operating in CLI-only mode without browser-control tooling and the host server requires Cosmos credentials; the wave commit lands with the unit-test signal and the user can dev-server through the click-through (collapse trigger → empty state on never-saved model → edit-and-save → re-open → see entry → click Revert → confirm dialog → accept → model snaps back → audit log grows to 2 entries with the just-overwritten state at index 0).

  **Conventions added.** None new. This wave reuses the W61-detail / W61-save / W61-source-filter / W61-filter-persist convention of co-locating new state + effects + handlers + render inside the existing `AdminSemanticModelDetail` function rather than extracting a sub-component, and the W61-audit-history-client convention of consuming the API helpers without a TanStack Query layer. The "in-browser verification is the wave-level signal for pure UI mounts" stance is already documented in CLAUDE.md's workflow rules; this wave is a routine application of it.

  **Out of scope.**
    - **Sub-component extract for `AdminSemanticModelDetail.tsx`** — the file is at 1,295 LOC post-wave. The session brief flagged 1,500 as the sub-component-extract threshold; with W61-add-delete projected at ~300 LOC more, the extract should happen as a separate refactor wave before W61-add-delete lands (carving the Audit-history card and the SourceFilter chips into their own files would shed ~300 LOC at zero behavioral risk).
    - **TanStack Query refactor of the audit log fetch / revert mutation** — would compose nicely with a broader admin-viewer query refactor; out of scope as a one-section change because the rest of the file is direct-fetch.
    - **URL-synced `historyOpen` (`?history=open`)** — speculative deep-link use case; promote when a user-driven need surfaces.
    - **Optimistic revert preview** (load the entry into a read-only side-pane before reverting) — the current `window.confirm` flow with `buildRevertConfirmation`'s prompt is adequate per the W61-audit-history-client design rationale; defer until "what changed?" is a recurring admin question.
    - **Per-row "diff vs current" affordance** — same speculative bucket; the W61-add-delete wave is a more pressing remaining feature.
    - **Toast notifications on revert success** — the host component doesn't use toasts elsewhere (the save flow uses an inline `<Card>` banner instead); adding a toast just for revert would be stylistically inconsistent. The existing banner pattern is reused via the `revertError` state but a success banner wasn't added because the model snapping back IS the visible success signal.
    - **Keyboard shortcut to toggle the section** (e.g. `?` to expand) — speculative power-user feature; not needed for the primary mouse-driven flow.
    - **Persisting `historyOpen` across navigation** (e.g. localStorage) — same speculative bucket; admins who consult history are usually doing focused forensics, not casual browsing.
    - **Per-row "View prior model in read-only viewer"** (open a side-pane with the snapshot's metrics / dimensions / hierarchies rendered the same as the current model) — useful for "what was I looking at?" investigations but adds a significant chunk of layout; can be its own wave if surface demand appears.

- **2026-05-20** — **Wave W61-audit-history-client · API helpers + pure formatters for the upcoming W61-audit-history-tab UI.** Client-side foundation that consumes the two server endpoints shipped earlier this session (W61-audit-history-api GET + W61-audit-revert POST) without yet mounting the user-visible UI. Split from the full history-tab wave so the typed API surface + the formatting / summary helpers can land cleanly in node:test (no jsdom needed) and the upcoming UI sub-component stays thin — the history-tab wave can mount the helpers without re-litigating the API shape. Pure-client wave; no server touched.

  **What landed.**
    - [`client/src/lib/api/admin.ts`](../client/src/lib/api/admin.ts) gains a new `AdminSemanticModelAuditEntry` interface (client mirror of the server's `SemanticModelAuditEntry` — local declaration to avoid the cross-runtime import boundary; mirrors the W61-detail / W61-save pattern where `SemanticModel` is referenced as an inline `import("@/shared/schema").SemanticModel` rather than a top-level import) and an `AdminSemanticModelAuditLog` envelope (`{ sessionId, entries }`). Two new fetchers: `fetchSemanticModelAuditLog(sessionId)` (GET against the W61-audit-history-api route, throws on non-2xx) and `revertSemanticModel(sessionId, auditEntryIndex)` (POST against the W61-audit-revert route, body `{ auditEntryIndex }`, returns `PatchSemanticModelResponse` — the existing W61-save response type, byte-identical to the server's revert envelope so a future client mutation can share the success-handler shape with the existing edit mutation). Both use the existing `adminHeaders()` helper (MSAL token + X-User-Email) and the existing error-throw pattern that surfaces non-2xx status + body to the call site for TanStack Query's error boundary.
    - New pure module [`client/src/pages/Admin/lib/semanticModelAuditHistory.ts`](../client/src/pages/Admin/lib/semanticModelAuditHistory.ts) (~80 LOC). `formatAuditTimestamp(savedAt: number): string` returns `"YYYY-MM-DD HH:MM UTC"` via `Date.prototype.getUTC*()` accessors with zero-padded `padStart(2, "0")` segments. `buildAuditEntrySummary(entry, indexFromNewest, total): { headline, subhead }` derives `saveNumber = total - indexFromNewest` so the newest entry at index 0 becomes `"Save M of M"` (the highest count is the most recent save — matches the admin's mental model of "what's the latest version I've made") and the oldest at index `M-1` becomes `"Save 1 of M"`. The subhead joins three fields with the ` · ` (spaced middle-dot) separator pinned by tests so the JSX doesn't re-derive the layout. `buildRevertConfirmation(entry, indexFromNewest, total): string` builds the `window.confirm()` prompt with a paragraph break separating the action ("Revert to Save N (vX by Y)?") from the consequence note ("The current model will be saved to the audit log first, so this revert can itself be undone.").
    - [`client/src/pages/Admin/lib/semanticModelAuditHistoryW61AuditHistoryClient.test.ts`](../client/src/pages/Admin/lib/semanticModelAuditHistoryW61AuditHistoryClient.test.ts) (~220 LOC) appended to [`server/package.json`](../server/package.json)'s explicit test list per invariant #4 (right after `semanticModelFilterUrlSyncW61FilterPersist.test.ts`). 15 behavioural tests covering format correctness, ordering, and content anchors.

  **Why this design.**
    - **UTC timestamp formatting.** Audit log forensics often involve multiple admins reconciling who-did-what-when across timezones. `2026-05-20 14:23 UTC` reads identically for everyone; a locale-aware formatter would let alice in NYC and bob in Bangalore both see `"14:23"` and disagree on what wall-clock moment that was. A dedicated test pins that the output never includes a comma, an ISO-8601 `T` between date and time, or a seconds component — catches a future "let's just call `toISOString()`" refactor that would flip to `2026-05-20T14:23:45.000Z` and break the audit-log row's visual rhythm.
    - **`saveNumber = total - indexFromNewest`.** The buffer is newest-first (W61-audit-log convention). The most recent entry (index 0) is `Save M of M` because it was the M-th save chronologically. Walking back through indices: index 1 → Save M-1, etc., down to index M-1 → Save 1 (the oldest retained entry — the W57-inferred original or whatever was at the tail when the buffer first filled). A dedicated test walks all 5 indices of a 5-entry buffer to pin the mapping, and a second test pins the cap-at-10 boundaries (`Save 10 of 10` at index 0, `Save 1 of 10` at index 9).
    - **Two-field `{ headline, subhead }` over a single string.** The history-tab UI renders the headline at base-font and the subhead muted (smaller, lower-contrast); pre-splitting at the helper boundary spares the JSX from string-split-and-style at every render. A future re-style that decides to put both on one line (e.g. for compact mode) just concatenates `headline + " — " + subhead` at the call site rather than re-running the formatter.
    - **`priorVersion` in the subhead, not the headline.** Two adjacent audit entries can share `savedBy` (alice edited twice in a row) AND share the same `savedAt` minute (consecutive saves often land within the same minute). The version is what differentiates them, and it matches the revert mental model ("revert to the state of version 2"). The version is also the W64 compiled-query cache key, so `was v2` in the audit log corresponds directly to the cache entry that revert would restore. Putting it in the subhead (not the headline) keeps the headline scannable while the disambiguator stays available.
    - **`buildRevertConfirmation` includes the undo-safety note.** Load-bearing for the "undo this revert works" semantic — the prompt should set the admin's expectation that reverting is itself reversible (and the W61-audit-revert wave's audit-write-on-revert is what makes that true). The prompt anchors on `priorVersion` + `savedBy` so admins double-check the target before clicking through; a dedicated test pins both anchors plus the undo-safety wording.
    - **`unknown` savedBy passthrough.** The server stamps `"unknown"` when `getAuthenticatedEmail` returns null (the failure mode for the auth helper). The summary shouldn't munge that value — admins should see the raw attribution-failure marker so they know something wasn't recorded. A test pins that `"unknown · "` appears in the subhead verbatim.
    - **Local interface mirror, not type re-import.** The server's `SemanticModelAuditEntry` lives in `server/lib/semantic/semanticModelAuditLog.ts` which the client can't import across the runtime boundary. A local `AdminSemanticModelAuditEntry` interface in `admin.ts` is the established W61 pattern (mirrors how `SemanticModel` is inline-imported via `import("@/shared/schema")`). Runtime drift between this mirror and the server surfaces as field-undefined access at TanStack Query call sites rather than a compile error, but the W61 pattern accepts that tradeoff because the schema is small and stable.

  **Tests.** 15 new behavioural tests in [`semanticModelAuditHistoryW61AuditHistoryClient.test.ts`](../client/src/pages/Admin/lib/semanticModelAuditHistoryW61AuditHistoryClient.test.ts). **formatAuditTimestamp (5):** standard timestamp produces `2026-05-20 14:23 UTC`; zero-pads single-digit month / day / hour / minute (`2026-01-05 07:09 UTC`); midnight + new-year-eve edges; epoch 0 produces the Unix epoch start (sanity pin against future locale drift); never contains commas / ISO `T` separator between date and time / seconds (visual rhythm pin). **buildAuditEntrySummary (6):** index 0 of a 5-entry buffer → `Save 5 of 5` with subhead anchors on `savedBy` / `2026-05-20 14:23 UTC` / `was v5`; full walk-back across all indices of a 5-entry buffer pinning each index→saveNumber mapping; single-entry buffer reads `Save 1 of 1`; full-cap buffer of 10 pins newest (`Save 10 of 10`) and oldest (`Save 1 of 10`); subhead uses the dot separator consistently (`split(" · ").length === 3` + byte-equal pin); `unknown` savedBy passthrough preserves the server's attribution-failure marker. **buildRevertConfirmation (4):** save-number matches the summary's headline at the same index; anchors on `priorVersion` + `savedBy`; warns that the current model will be saved first (mentions both `"saved to the audit log"` and `"undone"` to set the undo-safety expectation); paragraph break (`\n\n`) separates the action from the consequence note (the `window.confirm()` dialog renders it as a visual break in every modern browser). All 15 pass; ~95 ms wave runtime.

  **Verified.** All 15 new tests pass on the isolated run (~95 ms). The full combined W61 admin + client batch (199 tests across `adminSemanticModelListW61List` + `adminSemanticModelDetailW61Detail` + `adminSemanticModelPatchW61Save` + `semanticModelSourceBumpW61SourceBump` + `semanticModelAuditLogW61AuditLog` + `adminSemanticModelAuditLogW61AuditHistoryApi` + `adminSemanticModelRevertW61AuditRevert` + the six W61 client lib tests including this wave) passes in ~441 ms. Server typecheck = **98 errors**, identical to the W61-audit-revert baseline (no server touched). Client typecheck = **53 errors** (one less than the W61-filter-persist baseline of 54 noted in the session brief; the new files add zero errors, the count drop is unrelated to this wave). The new `AdminSemanticModelAuditEntry` + `AdminSemanticModelAuditLog` interfaces are fully typed; the pure helpers reference only built-in `Date` / `string` / `number` so no external type surface is introduced.

  **Conventions added.** None new. This wave reuses the W61-detail / W61-save inline-import pattern for `SemanticModel`, the W61 polish-trail pure-module convention (`<feature>.ts` in `client/src/pages/Admin/lib/` with paired `<feature>W61<wave>.test.ts`), and the W61-audit-log audit-entry shape (`{ savedAt, savedBy, priorVersion, priorModel }`). The UTC-stable timestamp formatting choice could plausibly be codified as a "forensic surfaces use UTC" convention if a future audit-style feature needs it (e.g. cross-cutting audit, message-replay log), but skipping that for now until the second use case appears.

  **Out of scope.**
    - **W61-audit-history-tab** — the actual UI mount on `AdminSemanticModelDetail.tsx`. A collapsible "Audit history" section that uses TanStack Query's `useQuery({ enabled: tabOpen })` to fetch the buffer on tab-open, renders each entry via `buildAuditEntrySummary`, and exposes a Revert button per row that calls `window.confirm(buildRevertConfirmation(...))` then `useMutation` against the revert endpoint with `onSuccess: invalidate the detail query + invalidate the audit-log query + toast`. ~150-200 LOC client.
    - **TanStack Query hook abstractions** (`useAdminSemanticModelAuditLog`, `useAdminSemanticModelRevert`) — composes the API helpers with React Query options; lives with the page wiring rather than in this pure-module wave.
    - **Optimistic revert preview** (show the model diff before confirming) — speculative UX; the current prompt-text-only flow is adequate and pinned by tests.
    - **i18n / locale-aware formatting** — UTC for now; locale toggle (e.g. `Intl.DateTimeFormat(navigator.language, ...)`) can land later if admins request it. The test that pins `formatAuditTimestamp` would gain a `process.env.TZ` guard at that point.
    - **Pagination of the buffer** — cap-at-10 means a single fetch always suffices; pagination would be premature.
    - **Revert as draft** (apply the revert to a preview state without committing) — would require client-side model state mirroring; not in scope for the simple revert flow.

- **2026-05-20** — **Wave W61-audit-revert · server-side one-call revert endpoint that consumes the W61-audit-log ring buffer.** Closes the W61 audit-trail server-side loop (write → read → revert) by consuming the buffer that W61-audit-log populates and that W61-audit-history-api exposes. New `POST /api/admin/semantic-models/:sessionId/revert` takes `{ auditEntryIndex: number }` (0-indexed against the newest-first buffer — `0` = "undo my last save") and replaces the live model with the chosen entry's `priorModel` in one round-trip. Saves the future history-tab UI (W61-audit-history-ui) from a fetch-then-PATCH dance — the UI can ship as a thin "click the revert button → POST → optimistic invalidate" without needing to reproduce the PATCH-schema construction at the call site. Server-only wave; no client code touched.

  **What landed.**
    - [`server/controllers/adminSemanticModelController.ts`](../server/controllers/adminSemanticModelController.ts) gains an `import { z } from "zod"`, a new `revertBodySchema = z.object({ auditEntryIndex: z.number().int().nonnegative() })` (validates the body's shape at the boundary so the handler doesn't need a separate range / type check), a new `RevertResult` discriminated union with a fourth `audit_entry_not_found` variant alongside the existing `ok` / `session_not_found` / `semantic_model_not_inferred`, and a new exported `revertSemanticModel(req, res)` handler (~80 LOC + its doc-comment block).
    - The handler shape mirrors `patchSemanticModel`: admin gate → sessionId trim with 400 fallback → body parse with 400 `invalid_audit_entry_index` (including the zod error issues, capped at 10) → `withSessionWriteLock` with the four `RevertResult` branches → response. Inside the lock: fetch the doc, 404 if missing, 404 if no `semanticModel`, read the buffer (`?? []` for the never-PATCHed case), 404 `audit_entry_not_found` (with the offending index echoed) if `auditEntryIndex >= log.length`, otherwise compose `nextModel = { ...entry.priorModel, version: current+1, updatedAt: new Date().toISOString(), updatedBy }`, prepend the about-to-be-overwritten model to the audit log via `appendSemanticModelAuditEntry`, assign `doc.semanticModel = nextModel` + `doc.lastUpdatedAt = savedAt`, and persist via `_updater`. The response envelope mirrors W61-save's: `{ sessionId, lastUpdatedAt, model }` so the future client uses the same `useMutation` success handler.
    - [`server/routes/admin.ts`](../server/routes/admin.ts) imports `revertSemanticModel` and registers `router.post("/admin/semantic-models/:sessionId/revert", revertSemanticModel)` after the patch PATCH so the route registration order reads `list → detail → audit-log → patch → revert` as a natural workflow progression.
    - [`server/tests/adminSemanticModelRevertW61AuditRevert.test.ts`](../server/tests/adminSemanticModelRevertW61AuditRevert.test.ts) (~480 LOC) appended to [`server/package.json`](../server/package.json)'s explicit test list per invariant #4, right after `adminSemanticModelAuditLogW61AuditHistoryApi.test.ts`. 14 behavioural tests covering the full revert surface including the round-trip pin.

  **Why this design.**
    - **Skip W61-source-bump on revert.** The bumper's semantic is "admin edited this entry → bump `source` to `user`". A revert's semantic is "restore as-was" — every per-entry `source` field should come verbatim from the snapshot. Running the bumper would stamp every entry that differs between snapshot and current as `"user"` (wrong — e.g. reverting from a `source: "user"` admin-edited state back to the original `source: "auto"` W57-inferred snapshot would re-stamp the entry as `"user"`, hiding the revert from the source-filter chips and the planner's source-weighting). The model-level `updatedBy` still stamps the reverting admin (preserves attribution at the model level) but the per-entry `source` comes verbatim from `entry.priorModel`. The "source-preservation" test pins this with a fixture where current is `source: "user"` and the snapshot is `source: "auto"` — the test would fail if a future refactor wired the bumper into the revert path.
    - **Bump version monotonically.** Reverting to "the state of version 3" makes the live model `current+1` with the contents of v3, NOT version 3 again. The version field is load-bearing for the W64 compiled-query cache (which keys on it) and for the W61-audit-log's `priorVersion` (which records the version at the moment of save). A non-monotonic version would break both: the cache would serve stale compiled queries, and the audit-log entries would have ambiguous `priorVersion` values (two entries with `priorVersion: 3` from different points in time). The "happy path" test pins `body.model.version === 3` for a revert applied to a v2 doc; the round-trip test extends the pin to `version === 4` after a second revert.
    - **Write the audit-log entry on revert.** Two reasons. (a) Undoing the revert (admin reverts, then realises that was wrong, and reverts again) needs the just-overwritten state preserved. (b) Forensic record — "alice@example.com reverted on 2026-05-20" is a saveable event in its own right; the audit log should capture state transitions, not just "edits". The round-trip test pins this directly: revert v2 → v1 (audit log captures v2 as the new newest entry), then revert again with `auditEntryIndex: 0` (which now targets the v2 entry → undoes the revert with content === v2 and version === 4).
    - **Bound-check inside the lock.** A multi-admin race could have admin A growing the buffer (via PATCH or revert) while admin B is composing a revert request; reading the buffer length before the lock would let B's request target a now-stale index. Inside the lock the buffer length is authoritative — `parsed.data.auditEntryIndex >= log.length` uses the just-loaded `doc.semanticModelAuditLog`, not a stale client view. Conservative: 404 `audit_entry_not_found` if the race produces an out-of-bounds index, and the client retries with the fresh history fetch.
    - **404 not 400 for `audit_entry_not_found`.** The body parsed cleanly (the schema accepted a nonnegative int); the index is out of bounds because the buffer doesn't have that many entries — a resource-not-found condition, not a malformed request. 404 mirrors `session_not_found` / `semantic_model_not_inferred` which are also "the thing you asked for doesn't exist" conditions. The body echoes back the offending index so the client can show "you tried to revert to entry 5 but only 2 entries exist" without a separate state lookup.
    - **`auditEntryIndex: 0` semantics — "undo my last save".** The buffer is newest-first (W61-audit-log convention) so index 0 is the most recent prior model. This matches the natural mental model of an undo button: "undo my most recent change". A future history-tab UI's "Undo" affordance is just a `POST { auditEntryIndex: 0 }`.
    - **Reusing the W61-audit-log convention for the audit-write on revert.** The same `appendSemanticModelAuditEntry` helper is called from the revert path with `{ savedAt, savedBy: reverting-admin, priorVersion: current.version, priorModel: doc.semanticModel }`. The cap-at-10 logic applies uniformly — if a revert pushes the buffer to 11 entries (revert from a buffer at cap), the oldest is dropped. The cap behavior is the same regardless of whether the new entry came from PATCH or revert; a unified semantics keeps the buffer's invariants tight (always ≤10, always newest-first, no special-case entries).
    - **Cap-at-10 edge case: reverting to the oldest entry.** If `auditEntryIndex` is 9 (the oldest at cap) and the buffer is at cap (length 10), the new entry from the revert grows the buffer to 11 → trim to 10 → the targeted entry at index 9 is dropped from the buffer. This is fine because we already extracted its `priorModel` before the prepend. Subsequent reverts target indices 0–9 of the new buffer (which always includes the just-overwritten state at index 0, so "undo this revert" remains possible).
    - **Response envelope matches W61-save's.** Both endpoints produce `{ sessionId, lastUpdatedAt, model }`. A future client `useMutation` for the W61-audit-history-ui's revert button can reuse the same success-handler that the existing PATCH mutation uses — invalidate the detail query, invalidate the audit-log query, optionally show a toast. A different envelope would force a duplicate success-handler.
    - **No new injectable shim.** The handler uses the existing `_detailFetcher` + `_updater` shims from the W61-list / W61-detail / W61-save trio. Adding a separate `_reverter` injectable would duplicate surface for no behavioral benefit — the read+write pair is the same Cosmos doc and the same Cosmos update.

  **Tests.** 14 new behavioural tests in [`adminSemanticModelRevertW61AuditRevert.test.ts`](../server/tests/adminSemanticModelRevertW61AuditRevert.test.ts). **Gate + parameter / body validation (5):** 403 `admin_required` for non-admin callers; 400 `missing_session_id` when the route param is whitespace-only after trim; 400 `invalid_audit_entry_index` when the body lacks `auditEntryIndex`; same code when `auditEntryIndex` is negative (zod's `.nonnegative()` rejects); same code when `auditEntryIndex` is non-integer (zod's `.int()` rejects `1.5`). **404 paths (4):** `session_not_found` when `_detailFetcher` returns null; `semantic_model_not_inferred` when the doc exists but has no `semanticModel` field (pre-W57 session); `audit_entry_not_found` when the audit log is empty (W57-inferred session that's never been PATCHed has no priors to revert to); `audit_entry_not_found` when the index is out of bounds (pins the response includes the offending `auditEntryIndex` in the body for client-side error display). **Happy path + pin (1):** 200 envelope pins `body.model.metrics[0].label === "v1 label"` (content from snapshot), `body.model.version === 3` (monotonic bump from current's 2), `body.model.updatedBy === "bob@example.com"` (reverting admin stamped at the model level — distinct from the prior model's `updatedBy: "alice@example.com"`), and `body.model.updatedAt` is a fresh ISO string. **Audit-log-grown pin (1):** after revert, the buffer has 2 entries (started at 1), `log[0].priorModel.metrics[0].label === "v2 label"` (the just-overwritten v2 captured as the new newest entry), `log[0].priorVersion === 2`, `log[0].savedBy === "bob@example.com"`, and the original v1 entry shifted to `log[1]`. **Source-preservation pin (1):** load-bearing for the skip-W61-source-bump decision — fixture has snapshot `source: "auto"` and current `source: "user"`; after revert `body.model.metrics[0].source === "auto"`. A future refactor that wired the bumper into the revert path would fail this pin. **Round-trip pin (1):** revert v2 → v1 (audit log grows to 2 entries; content === v1, version === 3); revert again with `auditEntryIndex: 0` which now targets the just-captured v2 entry → content === v2, version === 4, audit log grew to 3 entries; pins `log[0].priorModel.metrics[0].label === "v1 label"` (the v1-restored model just overwritten by the undo), `log[1].priorModel.metrics[0].label === "v2 label"` (the v2 captured by the first revert), `log[2].priorModel.metrics[0].label === "v1 label"` (the original entry, now at the tail). **500 on updater throw (1):** wave-specific error code `admin_semantic_model_revert_failed` distinguishes from the other endpoints' error codes for log greppability. All 14 pass; ~328 ms wave runtime.

  **Verified.** All 14 new tests pass on the isolated run (~328 ms). All 85 W61 admin tests (71 prior + 14 new) pass on the combined run in ~362 ms. The 71 prior tests are byte-identical because this wave touches no existing handler — `revertSemanticModel` is a new export, `patchSemanticModel` / `getSemanticModel` / `getSemanticModelAuditLog` / `listSemanticModels` are unchanged. Server typecheck = **98 errors**, identical to the W61-audit-history-api baseline (the new `z.object(…)` schema, the new `RevertResult` discriminated union, and the new handler are all fully typed). Client typecheck not re-run for this server-only wave — the W61-filter-persist baseline of 54 stands unchanged.

  **Conventions added.** None new. This wave reuses the W61 trio convention (`__set*ForTesting` injectable shim at every Cosmos boundary), the W61-audit-log convention (`SemanticModelAuditEntry` as the wire-shape unit + `appendSemanticModelAuditEntry` as the buffer append helper + `withSessionWriteLock` as the unified per-session mutex per invariant #9), the W61-source-bump convention (skip the bumper for "restore" semantics — codified informally here in the handler doc-comment; if a future wave introduces another "restore-as-was" code path the rule should be promoted to `docs/conventions/`), and the W61-audit-history-api convention (dedicated endpoint with mirror-getSemanticModel handler shape).

  **Out of scope.**
    - **W61-audit-history-ui** — the client-side history tab on `AdminSemanticModelDetail.tsx` that consumes both endpoints (the read from W61-audit-history-api + the write from this wave). The UI walks the buffer newest-first, renders each entry as `Save N by {savedBy} at {formatTimestamp(savedAt)} · [Revert]` rows, and the Revert button POSTs to this wave's endpoint. Needs an "are you sure?" confirmation flow because revert blows away the current state (which then lands in the audit log itself — circular but correct). Plus a TanStack Query mutation keyed on the revert endpoint with `onSuccess: invalidate the detail query + invalidate the audit-log query`. ~200-300 LOC client + the API helper widening.
    - **`revertedFromIndex` field on `SemanticModelAuditEntry`** — would let the history UI render "Save N (reverted from Save M)" so admins can see which saves are reverts vs. ordinary edits. Schema extension to the entry interface; defer until the UI surfaces the distinction. The current `savedBy` + `savedAt` + `priorVersion` are sufficient for the basic history rendering.
    - **Multi-step revert** (revert N entries at once to "rewind by 5 saves") — UX speculation; admins almost always revert to a specific known state, not a relative count.
    - **Audit-log compaction on revert** (collapse two consecutive reverts-to-the-same-state into one entry) — premature optimisation; the cap-at-10 bounds the buffer's size and the duplicate entries are forensically useful (they record that the admin reverted, second-guessed, and reverted again).
    - **Authorization-by-author** (only the admin who saved entry N can revert to it) — current contract is "every admin can revert any entry", matching the existing W61-save / W61-detail / W61-list / W61-audit-history-api semantics. A future fine-grained access model would surface across all of these endpoints, not just revert.
    - **Bulk-revert across sessions** ("revert this metric's value across every session where I changed it last Tuesday") — cross-doc query shape; not in scope for the per-session forensics use case.
    - **Confirmation-token pattern** (require the client to first GET the entry to obtain a token, then POST the revert with the token, to prevent stale-history reverts) — adds two-round-trip complexity for a race that the inside-the-lock bound-check already handles (concurrent admin growth produces a 404 with the offending index; client retries). The current single-POST design is simpler and the race is rare in practice.

- **2026-05-20** — **Wave W61-audit-history-api · admin GET endpoint exposing the W61-audit-log ring buffer.** Read-side companion to the just-shipped W61-audit-log write path, which snaps every admin PATCH's prior `SemanticModel` into a capped newest-first ring buffer on the parent `ChatDocument` (`semanticModelAuditLog?: SemanticModelAuditEntry[]`, cap 10). With the buffer populated, two downstream waves now unblock: `W61-audit-history-ui` (history tab on `AdminSemanticModelDetail.tsx` rendering each entry as "Save N by alice@example.com at 2026-05-20 14:23") and `W61-audit-revert` (server-side one-round-trip revert endpoint). Both need a way to read the buffer over the wire; the existing `GET /admin/semantic-models/:sessionId` detail endpoint deliberately does NOT surface it because each entry carries a full SemanticModel snapshot (~5–50 KB) and 10 entries × 50 KB worst-case ≈ 500 KB would 50× the most-used admin page's response payload for a feature admins only consult occasionally. This wave ships the dedicated read-only endpoint `GET /admin/semantic-models/:sessionId/audit-log` so the history payload is opt-in (only fetched when an admin opens the history tab) rather than bundled into every detail navigation. Server-only wave (zero client code touched).

  **What landed.**
    - [`server/controllers/adminSemanticModelController.ts`](../server/controllers/adminSemanticModelController.ts) gains a new export `getSemanticModelAuditLog(req, res)` (~50 LOC + its doc-comment block) alongside a new envelope interface `AdminSemanticModelAuditLogResponse` (`{ sessionId: string; entries: SemanticModelAuditEntry[] }`). The handler shape mirrors the existing `getSemanticModel`: same `isAdminRequest` gate → same `sessionId` trim with 400 fallback → same `_detailFetcher` shim for the Cosmos call → same 404 branches for missing-session and missing-model → 200 envelope or 500 with a wave-specific error code (`admin_semantic_model_audit_log_failed`). The `SemanticModelAuditEntry` type is re-imported from [`semanticModelAuditLog.ts`](../server/lib/semantic/semanticModelAuditLog.ts) and surfaces directly on the wire (no projection — `entries` is byte-equivalent to `doc.semanticModelAuditLog`).
    - [`server/routes/admin.ts`](../server/routes/admin.ts) imports `getSemanticModelAuditLog` and registers `router.get("/admin/semantic-models/:sessionId/audit-log", getSemanticModelAuditLog)` between the existing detail GET and the patch PATCH. Express's path-shape matching means the new 4-segment path doesn't conflict with the 3-segment `:sessionId` route — they don't compete — but the registration order reads cleanly as "list → detail → audit-log → patch".
    - [`server/tests/adminSemanticModelAuditLogW61AuditHistoryApi.test.ts`](../server/tests/adminSemanticModelAuditLogW61AuditHistoryApi.test.ts) (~418 LOC) appended to [`server/package.json`](../server/package.json)'s explicit test list per invariant #4, right after `semanticModelAuditLogW61AuditLog.test.ts`. 10 behavioural tests covering the full route surface.

  **Why this design.**
    - **Dedicated GET endpoint rather than widening `AdminSemanticModelDetailResponse`.** Two alternatives were considered. (a) Always embed the audit log in the detail response — rejected because every navigation to `/admin/semantic-models/:sessionId` would carry the up-to-~500 KB audit-log overhead, 50× the typical 10–15 KB detail payload, for a feature the admin opens occasionally. (b) Add an optional `?includeAuditLog=true` query param on the detail endpoint — would work but the conditional shape (`auditLog?: SemanticModelAuditEntry[]` on the response type, depending on a query param) is a worse contract than two clean endpoints with stable shapes. A dedicated audit-log endpoint stays simple: the response type is unconditional, the page-load fetcher doesn't change, and the future history-tab UI fetches a separate query keyed on tab-opened-state.
    - **Re-use the existing `_detailFetcher` shim, not a new `_auditLogFetcher` injectable.** Both endpoints read the same `ChatDocument` (the audit log is a field on the doc, not a separate Cosmos collection). A separate fetcher would be the same function under a different name and would double the surface for test fakes without adding behavior. The shared injectable means a single `__setSemanticModelDetailFetcherForTesting(...)` in any test fixture covers both endpoints.
    - **Mirror `getSemanticModel`'s 404 envelope for the no-model case.** A pre-W57 session can never have an audit log because the W61-audit-log write path only fires inside `patchSemanticModel`, which itself 404s on a session with no `semanticModel`. Returning a distinct error code here (e.g. `audit_log_not_available`) would force the future history-tab UI to handle two "pre-W57" branches; reusing the existing `semantic_model_not_inferred` code keeps the client to one. (The endpoint still returns 200 with `entries: []` when the model exists but no PATCHes have been made yet — that's the *expected* state for a freshly-inferred session, not an error.)
    - **Return `entries: []` rather than `entries: undefined` for the never-PATCHed case.** The buffer is `?: SemanticModelAuditEntry[]` on the doc (TypeScript-optional) so it's literally `undefined` on a session that's never been PATCHed. Coalescing to `[]` at the controller boundary spares every caller (today the UI doesn't exist; tomorrow it will) from needing `?? []` at the call site. A test pins the empty-array semantics explicitly.
    - **Read path doesn't acquire `withSessionWriteLock`.** Invariant #9's lock is for RMW (read-modify-write) sequences — a naked read doesn't need it. The W61-detail and W61-list endpoints both omit the lock for the same reason; the audit-log endpoint follows the established pattern.
    - **`AdminSemanticModelAuditLogResponse.entries` carries `SemanticModelAuditEntry` directly, no projection.** Each entry has 4 fields (`savedAt`, `savedBy`, `priorVersion`, `priorModel`) and the future history-tab UI consumes all of them (the timestamp, the email, the version label, and the snapshot for the revert button's payload). A projection that stripped `priorModel` would shrink the response 99 % but would force a second round-trip when the admin clicks "Revert"; keeping the snapshot in the listing means the revert button can re-PATCH with no additional fetch. A test (`response.entries is byte-equivalent to doc.semanticModelAuditLog`) pins this — a future refactor that introduced a projection would have to update the pin in the same diff.
    - **Sessionid trim with 400 fallback** matches the [getSemanticModel](../server/controllers/adminSemanticModelController.ts) parameter handling — admins occasionally paste URLs with trailing whitespace from chat messages, and the trim prevents the Cosmos fetch from running against `"sess-1  "` and 404-ing legitimately-existing sessions. The 400 on empty-after-trim is the same code path as the parent detail endpoint.
    - **Route registration order** (`list → detail → audit-log → patch`) reads as a natural progression: enumerate sessions, drill into one, browse its history, edit it. Express doesn't care about the order for distinct paths — `/admin/semantic-models/:sessionId/audit-log` has 4 path segments while `/admin/semantic-models/:sessionId` has 3, so they don't compete — but the lexical ordering reads cleanly in the routes file.

  **Tests.** 10 new behavioural tests in [`adminSemanticModelAuditLogW61AuditHistoryApi.test.ts`](../server/tests/adminSemanticModelAuditLogW61AuditHistoryApi.test.ts). **Gate + parameter validation (2):** 403 `admin_required` for non-admin callers (matches the existing W61-list / W61-detail / W61-save gate pattern); 400 `missing_session_id` when the route param is whitespace-only after trim. **404 paths (2):** `session_not_found` when `_detailFetcher` returns null; `semantic_model_not_inferred` when the doc exists but has no `semanticModel` field (pre-W57 session). **200 envelope shape (3):** empty-entries case when the doc has a model but no audit log (newly-inferred session that hasn't been PATCHed yet) — pins `body.entries === []` (`.deepEqual` against `[]`); populated newest-first ordering across 3 entries with descending `priorVersion` (3, 2, 1) and `savedAt` (300, 200, 100), pinning `body.entries[0].priorVersion === 3` and the matching `savedBy` array; full `priorModel` snapshot integrity across 2 entries pinning the metric label, the model name, the version, and the metric expression on `entries[0].priorModel` (load-bearing because the revert UI consumes the full snapshot — a future field-projection refactor would surface as a failing pin). **Edge cases (3):** 500 `admin_semantic_model_audit_log_failed` when the detail fetcher throws (wave-specific error code distinguishes from `admin_semantic_model_detail_failed`); whitespace-trim on the sessionId param pins that `"  sess-1  "` reaches Cosmos as `"sess-1"`; `response.entries === doc.semanticModelAuditLog` byte-equivalence pin across a 5-entry buffer (catches a future refactor that re-sorts, re-caps, or deep-clones the entries on the wire). All 10 pass; ~372 ms wave runtime.

  **Verified.** All 10 new tests pass on the isolated run. All 71 W61 admin tests (61 prior + 10 new) pass on the combined run in ~348 ms — particularly the 13 W61-audit-log integration tests are byte-identical because this wave doesn't touch the write path (zero changes to `patchSemanticModel` or `appendSemanticModelAuditEntry`). Server typecheck = **98 errors**, identical to the W61-audit-log baseline (the new handler is fully typed; the `type SemanticModelAuditEntry` re-import adds no runtime dep; no new TS surface). Client typecheck not re-run for this server-only wave — the W61-filter-persist baseline of 54 stands unchanged.

  **Conventions added.** None new. This wave reuses the W61 trio convention (`__set*ForTesting` injectable shim at every Cosmos boundary) and the W61-audit-log convention (`SemanticModelAuditEntry` as the wire-shape unit) without introducing a new pattern. The "expose a dedicated GET when the payload is large/optional rather than widening an existing envelope" decision is a reasonable rule of thumb but doesn't rise to the level of a formal convention until at least one more wave applies it.

  **Out of scope.**
    - **W61-audit-history-ui** — the client-side history tab on `AdminSemanticModelDetail.tsx` that consumes this endpoint. Walks the buffer newest-first, renders each entry as `Save N by {savedBy} at {formatTimestamp(savedAt)} · [Revert]` rows, and exposes a "Revert to this version" button. Needs an "are you sure?" confirmation flow because revert blows away the current state (which then lands in the audit log itself — circular but correct: the revert IS just another save). Plus a TanStack Query fetcher keyed on `["admin-semantic-model-audit-log", sessionId]` with `enabled: historyTabOpen` so the payload isn't fetched until the admin opens the tab. ~200-300 LOC client + the API helper widening.
    - **W61-audit-revert** — server-side `POST /admin/semantic-models/:sessionId/revert { auditEntryIndex }` that walks back N entries and re-PATCHes the `priorModel` of the chosen entry in one round-trip. Saves the UI from doing a separate fetch-then-PATCH dance. Could be ~80 LOC server + tests, but the manual re-PATCH path works today, so this is a UX optimisation rather than a blocker — could ship before, with, or after the UI wave.
    - **Pagination of the audit log.** The cap-at-10 means a single response always fits in one round-trip; pagination would be premature complexity. If a future TTL-pruned audit log ever exceeded 10 entries, that wave would address pagination too.
    - **Per-entry detail sub-route.** A `GET /admin/semantic-models/:sessionId/audit-log/:index` that returns just one entry could be useful for a deep-link "share this audit entry" feature, but until that UI exists, returning the whole capped buffer in one response is simpler.
    - **Field-projection deltas on the wire.** The handler hands the buffer through verbatim — each entry carries a full `SemanticModel` snapshot. A future optimisation could compress with jsondiff-style deltas to reduce wire bytes, but at 10 × 50 KB = 500 KB worst-case the current shape is fine, and full snapshots are simpler to revert than deltas (apply vs. compose).
    - **Authorization-by-author** (admin can only see audit entries they themselves wrote). The current contract is "every admin sees every audit entry on every session they can fetch", which matches the existing admin gate semantics on `getSemanticModel`. A future fine-grained access model would surface here too.
    - **Cross-session audit aggregation.** A future "show me every save across all sessions in the last 24 hours" admin tool would need a separate query shape (cross-doc); the per-session endpoint shipped here doesn't address that use case. Per-session forensics is the immediate need; the cross-session view can land if and when an incident drives it.

- **2026-05-20** — **Wave W61-audit-log · prior-model audit trail written to the ChatDocument on every admin PATCH.** Closes the rollback / forensics gap that the W61 polish trail had left open through the trilogy + edit-text + edit-enums + source-bump + source-badge + source-filter + filter-persist arc — every admin save up to now permanently overwrote the prior model and `doc.semanticModel` was the only surviving copy. An admin who edited a metric's expression in error had no way to recover the pre-edit form short of reloading the dataset and re-running the W57 inference pipeline (which would also blow away every *good* correction made in the same session). With this wave each save snapshots the prior model into a capped newest-first ring buffer on the parent doc — `doc.semanticModelAuditLog?: SemanticModelAuditEntry[]` — so a future `W61-audit-revert` UI can walk the buffer and re-PATCH a chosen prior version. Server-only wave (zero client code touched, no schema change because [`ChatDocument`](../server/models/chat.model.ts) is a plain TS interface per the W57 precedent, not a zod schema).

  **What landed.**
    - New pure module [`server/lib/semantic/semanticModelAuditLog.ts`](../server/lib/semantic/semanticModelAuditLog.ts) (~80 LOC) exports `SemanticModelAuditEntry` interface (`savedAt: number` ms-epoch, `savedBy: string` admin email or `"unknown"`, `priorVersion: number` from the prior model's `version`, `priorModel: SemanticModel` full snapshot), `SEMANTIC_MODEL_AUDIT_LOG_MAX_ENTRIES = 10` const (the ring-buffer cap), and `appendSemanticModelAuditEntry(prior, entry, max = SEMANTIC_MODEL_AUDIT_LOG_MAX_ENTRIES): SemanticModelAuditEntry[]` (prepends newest-first, caps at `max`, returns a fresh array, accepts `undefined` for `prior` to handle the first-save-ever case where the field doesn't exist on the doc yet). Implementation is a one-liner: `const next = [entry, ...(prior ?? [])]; return next.length > max ? next.slice(0, max) : next;` — input is treated as `readonly` and never mutated.
    - [`server/models/chat.model.ts`](../server/models/chat.model.ts) gains `semanticModelAuditLog?: SemanticModelAuditEntry[]` on the `ChatDocument` interface, sibling to the existing `semanticModel?: SemanticModel` field (lines 137–148 with full doc-comment). Imported via `import type { SemanticModelAuditEntry } from "../lib/semantic/semanticModelAuditLog.js"` (type-only import keeps the runtime module dep light; the cycle-free path is `chat.model.ts → semanticModelAuditLog.ts → shared/schema.ts`).
    - [`server/controllers/adminSemanticModelController.ts`](../server/controllers/adminSemanticModelController.ts) imports `appendSemanticModelAuditEntry` and wires the audit-write into the `withSessionWriteLock` callback inside `patchSemanticModel` (the existing per-session mutex per invariant #9 — the prior W61-source-bump wave established the pattern of nesting per-entry bookkeeping inside the lock; the audit-write fits the same shape). A single `const savedAt = Date.now()` capture is taken once and used for **both** the audit entry's `savedAt` field and `doc.lastUpdatedAt` so a future history-tab UI can correlate `lastUpdatedAt` to a specific audit entry without clock-skew confusion (prior code did `doc.lastUpdatedAt = Date.now()` directly — behaviorally equivalent because both happen within microseconds inside the lock, but the explicit reuse documents the intent). The audit-write happens **before** the `doc.semanticModel = nextModel` overwrite so the snapshot captures the actual prior state, not the next state by accident: `doc.semanticModelAuditLog = appendSemanticModelAuditEntry(doc.semanticModelAuditLog, { savedAt, savedBy: updatedBy, priorVersion: doc.semanticModel.version ?? 0, priorModel: doc.semanticModel });` — note the `?? 0` defensive coalesce for the rare pre-W57 doc that somehow reached the PATCH path without a `version` field (the route's pre-check at line 218 guarantees `doc.semanticModel` is defined, but `version` is technically optional in the zod schema).
    - [`server/tests/semanticModelAuditLogW61AuditLog.test.ts`](../server/tests/semanticModelAuditLogW61AuditLog.test.ts) (~390 LOC) appended to [`server/package.json`](../server/package.json)'s explicit test list per invariant #4 (right after `semanticModelSourceBumpW61SourceBump.test.ts`). 13 tests across two groups: 10 pure-module tests + 3 controller-integration tests.

  **Why this design.**
    - **Snapshot the *prior* model, not the next.** Considered: write the next model as the audit entry so the buffer reads as "history of saves you've made". Rejected — the next model already lives on `doc.semanticModel` itself; storing it again would be redundant. The buffer's role is to remember what was just *lost*: a revert UI rewinds to "the state before save N" by walking back N entries and re-PATCH-ing the `priorModel` of entry N. Storing the next model would force the revert UI to walk forward through the buffer (less natural) and would not capture the very-first state because there's no "save 0" entry.
    - **Cap at 10.** SemanticModel snapshots are moderate-sized (~5–50 KB depending on dataset width — a 30-metric / 20-dimension / 5-hierarchy model JSON-serialises around 10 KB; a 100+ metric MMM-style model approaches 50 KB). 10 entries × 50 KB worst-case = ~500 KB of audit overhead per doc, comfortably under the 2 MB Cosmos document size limit. The deep-history use case (revert to the 11th-most-recent save) is rare enough that paying for unbounded growth across months of saves isn't worth it — an admin who has made >10 saves has almost certainly settled on a stable model. The const is exported so a future wave that genuinely needs more history can raise it in one place + the [pin test](../server/tests/semanticModelAuditLogW61AuditLog.test.ts) catches the change in the same diff.
    - **Newest-first ordering.** Matches the MRU convention already used by [WI6 `insightHistory`](../client/src/pages/Dashboard/lib/insightHistory.ts) (per-tile insight history with newest at index 0), the sidebar Recent Sessions list, and the prior W61-filter-persist wave's URL-param semantics. A future history-tab UI renders the buffer as a list without needing to invert; the most recent prior is always `[0]`.
    - **Pure-function append + cap, no class.** Considered: an `AuditLogRingBuffer` class with `append()` / `revert(index)` / `prune()` methods. Rejected for two reasons: (a) the cap-and-prepend semantics are a one-liner — adding a class shell adds ceremony without information; (b) the prior W61 polish trail's pure-module precedent ([`semanticModelSourceBump.ts`](../server/lib/semantic/semanticModelSourceBump.ts) / [`semanticModelEditValidation.ts`](../client/src/pages/Admin/lib/semanticModelEditValidation.ts) / [`semanticModelSourceBadge.ts`](../client/src/pages/Admin/lib/semanticModelSourceBadge.ts) / [`semanticModelSourceFilter.ts`](../client/src/pages/Admin/lib/semanticModelSourceFilter.ts) / [`semanticModelFilterUrlSync.ts`](../client/src/pages/Admin/lib/semanticModelFilterUrlSync.ts)) is clean pure-function + frozen-table prose; a class would be the only OO surface in the polish trail and would jar against the established style.
    - **`undefined` accepted for the `prior` argument.** First save on a session means `doc.semanticModelAuditLog` is `undefined` (the field is `?:` optional). The function inlines the `?? []` rather than forcing callers to do it, because every caller is the same controller path and the alternative would either duplicate the coalesce at the call site or force every caller to pass `[]`. The pure-module tests pin this with a dedicated case (`appendSemanticModelAuditEntry(undefined, makeEntry(1))` → `[entry]`).
    - **`max` param exposed in the public signature.** Defaults to `SEMANTIC_MODEL_AUDIT_LOG_MAX_ENTRIES` so production callers don't pass it. Exposed for testability — exercising the cap mechanics in tests by writing 11 fresh entries against the default would burn 11 fixture constructions; passing `max: 3` lets a single test cover the cap with 5 entries. The `max = 0` degenerate case is also pinned (legal but rare — append-then-immediately-trim produces an empty array; load-bearing because it confirms the cap is applied AFTER prepend, not before).
    - **Returns a fresh array reference even when under cap.** Considered: when `next.length <= max`, return the constructed `[entry, ...(prior ?? [])]` array directly (which is already fresh); when `next.length > max`, return `next.slice(0, max)` (also fresh). Both branches return fresh arrays — there's no "return the input reference" optimization analogous to the [W61-source-filter `filter === "all"` case](../client/src/pages/Admin/lib/semanticModelSourceFilter.ts) because every call here is a state change (append), so there's no "default-case-is-no-op" path. A test pins the fresh-reference contract because a future "defensive" refactor that returned the input reference unchanged would silently break callers that expect to assign the result back into the field without aliasing the prior buffer.
    - **`SemanticModelAuditEntry.priorModel` is a reference, not a deep clone.** The controller does `priorModel: doc.semanticModel` and then `doc.semanticModel = nextModel` — the reference is preserved before the field-overwrite assigns a different reference. The prior model's POJO is never mutated by any code path in the runtime (every save constructs a fresh model object via `{...parsed.data, ...overrides}` and assigns it whole-cloth; no in-place mutation occurs). Adding `structuredClone(doc.semanticModel)` would defend against a future in-place mutation refactor but would cost ~5–50 KB of allocation per save for a hypothetical scenario that no existing code triggers. Skip the defensive clone; rely on the replace-don't-mutate convention. The integration tests pin the actual prior-vs-next semantics — if a future refactor mutates the prior model in place, the `assert.equal(entry.priorModel.metrics[0].label, "Prior label")` pin in the "first save writes a 1-entry audit log" test would fail.
    - **Audit-write inside the existing `withSessionWriteLock`, not a new lock.** Per invariant #9 the unified per-session mutex is `withSessionWriteLock`; adding a separate lock for the audit write would (a) violate the invariant and (b) open a race window where the audit entry is written without the corresponding model overwrite committing (and vice versa). The audit-write piggybacks on the existing critical section so the entry-and-overwrite pair is atomic at the per-session level. Single-instance correctness only; multi-instance horizontal scaling still needs Cosmos `ifMatch` ETag (out of scope for this wave per the existing lock semantics).
    - **`savedBy` stamps the email at save time, not a backref to a user table.** `getAuthenticatedEmail(req)` returns the same `updatedBy` that the new `nextModel.updatedBy` carries — they always match for a given save. Stamping the email directly into the audit entry (rather than e.g. an `updatedById` FK) means the entry is self-describing even if the future user-management surface evolves; an admin's email rotation later (e.g. `alice@old.com` → `alice@new.com`) preserves the historical attribution.
    - **No new GET endpoint to fetch the audit log separately.** The detail endpoint `GET /admin/semantic-models/:sessionId` already returns the full `ChatDocument` shape internally and selectively projects to `AdminSemanticModelDetailResponse` (which currently excludes `semanticModelAuditLog`). A future history-tab wave will either (a) widen `AdminSemanticModelDetailResponse` to optionally include the log (`?includeAuditLog=true` to keep the default response light) or (b) add a dedicated endpoint. Out of scope here — this wave is the write-path only; the read-path is the next wave's surface.
    - **Why field name `semanticModelAuditLog` not `auditLog`.** A bare `auditLog` reads like cross-cutting audit (filter changes, message replays, dataset edits, etc.) — naming the field after its specific content makes the scope obvious and reserves the bare name for a future cross-cutting audit field if one is ever needed. The future history-tab UI's hook reads `doc.semanticModelAuditLog` which matches the field's role in the data model. The single test that pins this naming (the integration test's `currentDoc.semanticModelAuditLog!.length === 1` assertion) would surface a future rename as a TypeScript error.

  **Tests.** 13 new behavioural tests in [`semanticModelAuditLogW61AuditLog.test.ts`](../server/tests/semanticModelAuditLogW61AuditLog.test.ts). **Pure-module tests (10):** `SEMANTIC_MODEL_AUDIT_LOG_MAX_ENTRIES = 10` pin (catches a future bump that would change Cosmos doc footprint assumptions in the same diff as the constant change); `undefined`-prior + 1-element result; newest-first prepend over an existing 2-element log; cap drops oldest at 11 entries (savedAt=1 gone, savedAt=2 at the tail); exactly-at-cap under-then-over progression (9 → 10 → 11 with the 11th forcing a drop); custom `max=3` cap-from-empty progression (5 entries → newest-3 retained); input array non-mutation (snapshot before vs after the append are byte-identical); fresh-array-reference contract (returned reference is not `===` the input reference, even when under cap); `max=0` degenerate case (legal, produces `[]` — pins the cap-applied-after-prepend semantics); full `priorModel` snapshot preservation across multiple appends (entry 0's `priorModel.metrics[0].label` is `"New label"`, entry 1's is `"Old label"`, with matching `priorVersion` values). **Controller integration tests (3):** first save writes a 1-entry log with `savedBy = "admin@example.com"`, `priorVersion = 3`, `priorModel.metrics[0].label === "Prior label"` (the snapshot is the PRIOR model, not the new one — pins that the audit-write happens before the `doc.semanticModel = nextModel` overwrite); consecutive saves prepend newest-first (two saves v1→v2→v3 produce a 2-entry log with `log[0].priorVersion === 2` / `log[1].priorVersion === 1` and the matching labels); cap kicks in after >10 saves (a loop that runs 11 PATCHes produces exactly 10 entries with the v1 prior evicted from the tail). Test file appended to [`server/package.json`](../server/package.json)'s explicit list per invariant #4. All 13 pass; ~321 ms wave runtime.

  **Verified.** All 13 new tests pass on the wave's own run (~321 ms). The 48 prior W61 admin tests still pass byte-identical on the combined run across `adminSemanticModelListW61List` + `adminSemanticModelDetailW61Detail` + `adminSemanticModelPatchW61Save` + `semanticModelSourceBumpW61SourceBump`. The W61-save integration tests are particularly load-bearing here because the controller's lock-callback shape changed (the `lastUpdatedAt = Date.now()` line became `lastUpdatedAt = savedAt` where `savedAt = Date.now()` is captured once and shared with the audit-write) — the W61-save tests' `body.lastUpdatedAt` assertions all pass, confirming the refactor is behaviorally equivalent. Server typecheck = **98 errors**, identical to the W61-filter-persist baseline (no new TS errors; the new `import type` on `chat.model.ts` doesn't add a runtime dep; the audit-log module is fully typed against `SemanticModel`). Client typecheck not re-run for this server-only wave; the W61-filter-persist baseline of 54 stands unchanged.

  **Conventions added.**
    - **Audit-style ring buffer on `ChatDocument`** (precedent for future "history of state X" features): write the prior state inside the existing `withSessionWriteLock` per invariant #9; cap at a small N (10 here) to keep Cosmos doc size bounded; newest-first ordering to match the repo's MRU convention; expose the cap as an exported const + pin it in tests for diff-time visibility; accept `undefined` for the prior log to handle the first-save-ever case; return fresh arrays without reference-identity optimization (every append is a state change). See [`docs/conventions/audit-log-ring-buffer.md`](conventions/audit-log-ring-buffer.md).

  **Out of scope.**
    - **History & revert UI on the admin detail page.** This wave writes the log; the next wave (`W61-audit-history`) renders it. A history tab on `AdminSemanticModelDetail.tsx` would walk the buffer newest-first, show "Save N by alice@example.com at 2026-05-20 14:23" entries, and add a "Revert to this version" button that re-PATCHes the `priorModel`. Out of scope here because the UI surface deserves its own wave's design pass + accessibility review + the "are you sure?" confirmation flow.
    - **Widening `AdminSemanticModelDetailResponse` to include the audit log.** The detail endpoint envelope is unchanged this wave so existing clients don't pay the audit-log payload cost on every fetch (10 entries × 50 KB worst-case = ~500 KB per response). The history-tab wave will either add `?includeAuditLog=true` or a dedicated endpoint `GET /admin/semantic-models/:sessionId/audit-log`.
    - **Server-side revert endpoint.** A future `POST /admin/semantic-models/:sessionId/revert { auditEntryIndex }` would let the history UI revert in one round-trip without the client having to re-PATCH the full prior model. Out of scope — the manual re-PATCH path works today, and the optimisation can land with the history-tab wave.
    - **Deep-clone of the prior model.** The audit entry holds a reference to `doc.semanticModel` (which becomes detached when the overwrite happens). A future in-place mutation refactor would break this — but no such refactor is planned, the existing tests pin the prior-snapshot semantics, and `structuredClone`-ing a 50 KB model on every save is wasted CPU.
    - **Compression of audit-log entries.** Each `priorModel` snapshot is a full SemanticModel duplicate; a future optimisation could store deltas (jsondiff or a custom field-projection diff) instead of full snapshots. Out of scope — 10 × 50 KB = 500 KB is well under the 2 MB Cosmos limit, and full snapshots are simpler to revert than deltas (apply the snapshot vs. compose deltas back).
    - **Cross-session audit aggregation.** A future "show me every save across all sessions" admin tool would query across docs; the per-doc log isn't an efficient shape for that (would need a separate audit collection). Out of scope — per-session forensics is the immediate use case.
    - **Pruning old audit entries on a TTL.** The cap at 10 entries is fixed; there's no time-based eviction (an entry from 6 months ago survives if it's in the top 10). A future TTL prune could land if Cosmos doc size becomes a problem in practice; the current cap is the load-bearing safety.
    - **Migration / backfill of pre-W61-audit-log sessions.** No backfill is possible — the prior model state wasn't preserved before this wave. Sessions saved before the wave have `semanticModelAuditLog: undefined`; their first post-wave save will start the log at length 1.

- **2026-05-20** — **Wave W61-filter-persist · URL-state persistence of the W61-source-filter active chip via `?filter=X` query param.** Layers share-link + reload-survival on top of the just-shipped W61-source-filter chip row. The prior wave's `useState`-only filter state reset to `"all"` on every page reload, and a copy-pasted URL lost the active chip — the admin who clicked User, scrolled to find an issue, then accidentally reloaded the page lost their position. With this wave the URL becomes `…/admin/semantic-models/<sessionId>?filter=user` on chip click, so a reload reads it back, and a shared link opens the same filtered view for a teammate. Pure-client wave (zero server code touched, zero schema change).

  **What landed.**
    - New pure module [`client/src/pages/Admin/lib/semanticModelFilterUrlSync.ts`](../client/src/pages/Admin/lib/semanticModelFilterUrlSync.ts) exports `FILTER_PARAM_NAME = "filter"` (the URL param name, deliberately terse — 5 chars vs. e.g. `source_filter` so URLs read clean when admins paste them into chat / docs / GitHub issues; the param's meaning is unambiguous in context because no other filter on this page), `readFilterFromSearch(search): SemanticEntryFilter` (parses a `window.location.search` string with optional leading `?`, validates the value against a `ReadonlySet<SemanticEntryFilter>` for O(1) check; falls back to `"all"` for missing / invalid / empty / wrong-case), and `writeFilterToSearch(search, filter): string` (returns the updated search string with the param set; for `filter === "all"` REMOVES the param entirely so the URL stays clean at the default state). Both functions are string-in / string-out — no direct `window.location` reads — so they're trivially testable in `node:test` without a jsdom mount. Return values never include the leading `?` so the caller decides whether to prepend it (`"?" + search` for `history.replaceState`, or empty string for "no query params at all") and the empty-case stays unambiguous.
    - Validation is case-sensitive: `?filter=USER` does NOT match `"user"` and falls back to default rather than silently flipping to an unintended state. URL params are byte-stable in the wild because admins copy URLs from browsers which preserve case; lenient case-insensitive matching would let a mistyped URL look like it worked while silently routing to the wrong filter. The `ReadonlySet` declaration mirrors `SOURCE_FILTER_ORDER` from [`./semanticModelSourceFilter.js`](../client/src/pages/Admin/lib/semanticModelSourceFilter.ts) — a test in the suite pins this membership against the four-value vocabulary so a future widening lands as a TypeScript error in the helper module rather than silently allowing an unknown param.
    - [`AdminSemanticModelDetail.tsx`](../client/src/pages/Admin/AdminSemanticModelDetail.tsx) wires them in via a lazy `useState` initializer (`useState<SemanticEntryFilter>(() => typeof window === "undefined" ? "all" : readFilterFromSearch(window.location.search))` — the `typeof window === "undefined"` guard is for SSR / non-window environments; the admin route is client-only today but a future Next.js / static-prerender migration shouldn't crash) and a new `useEffect` keyed on `[sourceFilter]` that calls `window.history.replaceState(window.history.state, "", nextUrl)` where `nextUrl = window.location.pathname + (nextSearch ? "?" + nextSearch : "") + window.location.hash`. The pathname + hash reconstruction means a future deep-link feature that stashes hash anchors (e.g. `#metric-revenue`) won't get clobbered.
    - The `SOURCE_FILTER_ALL` import is removed from the page (no longer needed — the initializer reads from the URL helper directly and the default-fall-back happens inside `readFilterFromSearch`). The `useState`'s explicit `SemanticEntryFilter` type annotation keeps the inferred type narrow.
    - 26 new behavioural tests in [`semanticModelFilterUrlSyncW61FilterPersist.test.ts`](../client/src/pages/Admin/lib/semanticModelFilterUrlSyncW61FilterPersist.test.ts): read semantics covering empty / `?`-alone / no-param / each valid value (`?filter=user` / `=auto` / `=domain` / `=all`) / `?filter=banana` invalid / `?filter=` empty-value / `?filter=USER` and `?filter=User` wrong-case (both fall back) / extra-params-don't-interfere / duplicate-param-picks-first / leading-`?`-optional input. Write semantics: `"all"` removes the param from empty / from existing / preserves other params on removal / each valid value sets the param / replaces existing filter value / preserves other params on set / leading-`?` optional on input / output never starts with `?` (load-bearing pin — would silently break the caller's `"?" + search` concatenation). Round-trip pin: write then read returns the original filter for all four values, including with other params alongside. The `FILTER_PARAM_NAME` constant has its own pin (`"filter"`, not the verbose alternative). Test file appended to [`server/package.json`](../server/package.json)'s explicit list per invariant #4 (right after `semanticModelSourceFilterW61SourceFilter.test.ts`). All 26 pass; ~119 ms wave runtime; 147 cumulative W61 tests green on the combined run (~311 ms — 26 new + 121 prior).

  **Why this design.**
    - **`replaceState` rather than `pushState`.** Considered: each chip click pushes a new history entry so the browser back button could step through filter states. Rejected — admins expect back to navigate to the previous page (the admin index, in this case), not toggle through chip states. Stacking five chip clicks would require five back-presses to leave the page, which is broken behaviour. The single history entry stays at the most-recently-clicked filter so the URL reflects the current view AND the back button does what admins expect.
    - **Pre-existing `window.history.state` as the first arg to `replaceState`.** The first arg is the state object the browser exposes via `popstate`; passing the existing state means a future deep-link feature that stashes nav metadata there won't get clobbered by the filter-sync rewrite. Passing `null` would silently wipe any pre-existing state from a parent route's `pushState` call.
    - **Pathname + hash reconstruction not full `window.location.href` rewrite.** The latter would force a full URL parse-and-replace (and could subtly alter the URL's encoding if the browser normalises differently); reconstructing from the three known components (`pathname` / `search` / `hash`) is byte-exact and cheaper.
    - **Lazy `useState` initializer, not `useEffect`-on-mount.** Considered: `useState("all")` plus a `useEffect` that reads from URL on mount and calls `setSourceFilter`. Rejected — that approach renders once with `"all"` then re-renders with the URL value, which causes a visible chip-flicker on page load if the URL has a non-default filter. The lazy initializer reads the URL on the first render so the first paint already shows the correct chip.
    - **`typeof window === "undefined"` guard.** Pure defense against future SSR / prerender — the admin route is client-only today but the guard costs nothing and saves a confusing crash if someone migrates the project to Next.js.
    - **Fall-back to `"all"` rather than throw on invalid URL.** The URL is admin-controlled and admins mistype; throwing would crash the page-mount and lock the admin out. Falling back to the safe default gracefully degrades while the URL still shows the wrong value (the next chip click will rewrite it cleanly).
    - **Short `filter` param name not `source_filter`.** Admins frequently copy URLs into chat / docs / GitHub issues; a verbose param reads as noise. The param's meaning is unambiguous in context (no other filter on this page) so the short name doesn't cost clarity.
    - **Case-sensitive validation.** Considered: lower-case the incoming value before validation so `?filter=USER` works. Rejected — `URL` capitalisation is byte-stable in real use (browsers preserve case in `location.search`); a lenient match would mean an admin typing `USER` thinks they're applying a filter when they are, while a different scenario with `User` (title-case from a casually-typed link) would also work — both feel correct until a future use case requires the byte-stable form (e.g., URL hashing for analytics) and the leniency masks a real drift.
    - **Remove the param entirely for `"all"`, not write `?filter=all`.** The default state IS no filter; `?filter=all` would be redundant byte-noise and would clutter the URL admins are copying. The absence of the param means the same thing (`readFilterFromSearch` falls back to `"all"` on missing). The `URLSearchParams.delete` call is the standard idiom.
    - **Return strings without a leading `?`.** The caller's `history.replaceState` call wants to control the leading-? semantics — empty string means "no query params" and "?" + value means "these params". Returning a leading `?` from the helper would make the empty-case ambiguous (was the URL meant to have an empty `?` or no `?`?). The pin in tests catches a future "helpful" refactor that adds the prefix.

  **Tests.** 26 new behavioural tests in [`semanticModelFilterUrlSyncW61FilterPersist.test.ts`](../client/src/pages/Admin/lib/semanticModelFilterUrlSyncW61FilterPersist.test.ts) — see above for coverage detail. All 26 pass; ~119 ms wave runtime; 147 cumulative W61 tests green on the combined run (~311 ms — 26 new + 121 prior). Test file appended to [`server/package.json`](../server/package.json)'s explicit list per invariant #4.

  **Verified.** All 26 new tests pass plus the 121 prior W61 tests across the trilogy + edit-text + edit-enums + source-bump + source-badge + source-filter + filter-persist combined run (~311 ms). Server typecheck = **98 errors**, identical to the W61-source-filter baseline (no server-side changes). Client typecheck = **54 errors**, identical to the W61-source-filter baseline (the new module's exports flow through `SemanticEntryFilter` cleanly; the `useState` lazy initializer's signature is unchanged from React's standard generic).

  **Out of scope.**
    - **Per-section filter URL param.** A future per-card filter (e.g. metrics filtered by User while dimensions show all) would need either three params (`?metrics-filter=user&dimensions-filter=auto`) or a structured value (`?filter=metrics:user,dimensions:auto`). Deferred to the per-section filter wave (also deferred from W61-source-filter); the param shape can be designed once the use case is concrete.
    - **`popstate` listener for back-button-respecting filter restoration.** The `replaceState` design intentionally collapses history to one entry — admins reload to revisit the URL state, they don't browser-back through filters. A future deep-link feature with a different use case could add a `popstate` listener that re-reads the URL on browser navigation, but it would invert the current intent.
    - **Filter param synonyms / aliases.** A short-form `?f=user` would shave 5 chars off the URL but add parsing complexity for marginal URL-length win. Defer until URL length actually matters.
    - **Session-restore of in-progress draft state.** No draft state exists in source-filter (the chip click is instant), but a future Save-All / bulk-edit wave might want to persist mid-edit drafts via the URL or sessionStorage. Out of scope here.
    - **URL-state sync for the W61-source-badge tooltips.** The badge tooltips are static per source; no state to persist.
    - **Multi-filter combinations.** Considered "show only User AND Domain" (multi-select chip row). Today's three-value source enum doesn't have enough cardinality for multi-select to be useful (`User + Domain` is 12 + 0 = 12 entries, same as User alone); deferred until a 5+ value source enum makes the union meaningful.
    - **Persisting the per-table sort order (alphabetical vs source-grouped vs by-recency).** The current alphabetical sort is hard-coded in the page; URL-state-ing a sort key would pair naturally with this wave but adds chip-row UX considerations and a new param. Defer to a dedicated sort wave.

- **2026-05-20** — **Wave W61-source-filter · per-source filter chips in the admin viewer.** Layers "show only User / Auto / Domain / All" filtering on top of the just-shipped W61-source-badge chip taxonomy. The use case the previous wave's source chip implicitly invited: an admin who has corrected 20 entries out of 100 wants to scroll back through *just* their corrections to verify nothing slipped through. With the chips visible per row the admin can scan one screen at a time; with the chips clickable as a filter the admin can collapse the view to only the entries they care about in this review pass. Pure-client wave (zero server code touched, zero schema change) that completes the source-axis surfacing started by W61-source-bump (server stamp) → W61-source-badge (per-row visibility) → W61-source-filter (selective review). Now the W61 polish trail ships the full source-aware loop end-to-end.

  **What landed.**
    - New pure module [`client/src/pages/Admin/lib/semanticModelSourceFilter.ts`](../client/src/pages/Admin/lib/semanticModelSourceFilter.ts) exports the `SemanticEntryFilter = "all" | SemanticEntrySource` type alias (the `"all"` sentinel is intentionally part of the same union rather than `SemanticEntrySource | "all"` repeated at every call site — keeps the chip vocabulary flat and closed, and gives a single `SOURCE_FILTER_ORDER` const that types correctly without union-flattening at each consumer), `SOURCE_FILTER_ALL = "all"` const for the default state, `filterEntriesBySource<T extends { source: SemanticEntrySource }>(entries, filter)` (returns the input reference unchanged when `filter === "all"` — load-bearing reference-identity for the default and most-common case; admins with 100+ entries per table would otherwise pay an unnecessary `Array.prototype.filter` allocation on every render at the default state, and the React reconciler benefits from referential stability when nothing changed; for specific filters returns a new array via `.filter` per usual), `countEntriesBySource(entries)` (returns `Readonly<Record<SemanticEntryFilter, number>>` with `{ all, auto, user, domain }` populated; the `all` slot always equals `entries.length` so the chip-row can render `All (N)` without a separate length read; the count function uses a single pass loop with three branches rather than three separate `.filter(…).length` calls — three passes over a 200-entry list is cheap but the single-pass form pairs with the sum-invariant pin in the tests), `getFilterLabel(filter)` (centralised label table mirrors the W61-source-badge capitalisation exactly: `"Auto"` / `"User"` / `"Domain"` + the `"All"` sentinel — a future re-label lands in one place rather than drifting between the row chips and the filter chips), and `SOURCE_FILTER_ORDER` as a `ReadonlyArray<SemanticEntryFilter>` declared `["all", "user", "auto", "domain"] as const` (the display-order for the chip row — `"user"` leads the source filters because the most common admin workflow is "show me what I've edited"; putting User immediately after All minimises scan-distance from the leftmost chip vs alphabetical Auto / Domain / User).
    - [`AdminSemanticModelDetail.tsx`](../client/src/pages/Admin/AdminSemanticModelDetail.tsx) gains a `SourceFilterChips` sub-component (sibling to `SourceBadge` / `ExposedToggle` / `EditableText` / `EditableSelect`). Renders each filter as a clickable `<button type="button">` wrapping the existing UX-2 `Badge` primitive. The **active** chip carries the source's own badge variant (`getSourceBadgeVariant(f)` from W61-source-badge — `secondary` for auto, `default` for user, `gold` for domain) — visually "ties" the active filter to the row badges of the entries it leaves visible, so the admin sees "all the User-tinted chips below match the User-tinted filter above". **Inactive** chips render as `outline` with `opacity-70` (`hover:opacity-100` on hover) so the row reads as "pick a filter" without the inactive chips visually competing for attention. The `"all"` sentinel uses the `outline` variant even when active so it doesn't compete with the source variants — the absence of source-tinting itself signals "no filter applied". `role="group"` + `aria-label="Filter entries by source"` on the wrapper, `aria-pressed={isActive}` on each button for screen-reader / accessibility-testing support.
    - Page-level `sourceFilter` state added via `useState<SemanticEntryFilter>(SOURCE_FILTER_ALL)` sibling to the existing `saving` / `saveError` state. **One** global filter state on the page (not per-card) — the common workflow is "show me what I edited" applied uniformly across metrics + dimensions + hierarchies; a per-card filter would force three clicks to achieve the same effect. The trade-off: clicking "User" on the metrics card also filters the dimensions + hierarchies cards below; this is intentional and the per-card count label disambiguates ("User (3)" on metrics, "User (12)" on dimensions, "User (0)" on hierarchies — the admin sees the distribution before the click).
    - The pre-existing per-card right-side total count `<span>{model.metrics.length}</span>` (and the matching spans on dimensions / hierarchies) is **replaced** by the `<SourceFilterChips>` row — the chip row serves both filter affordance AND count display in one. The card header's `flex items-baseline justify-between` becomes `flex items-center justify-between gap-3 flex-wrap` to accommodate the wider chip row (and wrap to two lines on narrow viewports). The card header retains the section title (`Metrics` / `Dimensions` / `Hierarchies`) on the left + the chip row on the right.
    - Each card's table-body map closes the loop: `[...filterEntriesBySource(model.metrics, sourceFilter)].sort(...).map(...)`. The `[...]` spread is unchanged from the prior wave — `.sort()` mutates in place, so a spread is required to avoid mutating the (now possibly-shared) reference returned by `filterEntriesBySource` at `filter === "all"`. The filter sits inside the spread and outside the sort so the alphabetical ordering is applied to the filtered subset; an admin filtering by User sees the user-edited entries in alphabetical order, not in source-encounter order.
    - 18 new behavioural tests in [`semanticModelSourceFilterW61SourceFilter.test.ts`](../client/src/pages/Admin/lib/semanticModelSourceFilterW61SourceFilter.test.ts) covering filter semantics: per-source result correctness (auto returns auto entries only, user returns user only, domain returns domain only); `"all"` reference-identity pin (`filterEntriesBySource(fixture, "all") === fixture` — load-bearing, not just deep-equal — so a future refactor that adds `entries.slice()` defensively would fail this test); empty-input cases for every filter; non-mutation of input arrays; preservation of input order within the filtered subset (the chip filter must NOT silently re-order the underlying alphabetical sort — the admin's scroll position would jump on filter-change otherwise); generic type-narrowing preservation (`T extends { source: SemanticEntrySource }` so a caller passing `SemanticMetric` gets back `SemanticMetric[]` with all extra fields intact, not a stripped-to-source-only shape). Count semantics: empty-input zeros, fixture-distribution match, **sum invariant** (`counts.all === counts.auto + counts.user + counts.domain` — load-bearing pin that catches a future 4th-source widening that updates the type union but not the count branches). Labels: `"all" → "All"`, drift check vs the W61-source-badge capitalisation (`auto/user/domain` map to the same labels in both modules). Order: `SOURCE_FILTER_ORDER[0] === "all"` pin, `SOURCE_FILTER_ORDER[1] === "user"` pin (the workflow-optimised position — alphabetical would put Auto first, which is the least-clicked filter), length-4 + no-duplicates pin, validity-membership pin (every value in `SOURCE_FILTER_ORDER` is a `SemanticEntryFilter`). Test file appended to [`server/package.json`](../server/package.json)'s explicit list per invariant #4 (right after `semanticModelSourceBadgeW61SourceBadge.test.ts`). All 18 pass; ~119 ms wave runtime.

  **Why this design.**
    - **Global filter state rather than per-card.** Considered: three independent filter states (`metricsFilter` / `dimensionsFilter` / `hierarchiesFilter`). Rejected — the admin's mental model is "show me what state I'm reviewing" (one state across the whole page), not "show me what state each section is in" (three states independently). The latter would be a debugging-style UX with no clear common workflow; the admin would have to remember the current filter on each card. The global filter pairs with per-card counts to give the admin the best of both: one click toggles the filter for the whole page, three separate count labels show how that filter materialises in each section.
    - **Per-card counts rather than global combined counts.** Considered: one combined `{ all: 100, auto: 88, user: 12, domain: 0 }` count across the union of all metrics + dimensions + hierarchies. Rejected — the per-card counts disambiguate the filter result *before* the admin clicks. A `User (0)` chip on the hierarchies card tells the admin there's nothing to see in that section even at User filter, *and* that they'd waste a scroll to confirm. A global `User (15)` chip would hide that distribution and force the admin to click then discover the hierarchies card was empty. The per-card count is also load-bearing for the "User (3) on metrics vs User (12) on dimensions" disambiguation noted above.
    - **Reference-identity preservation on `"all"`.** Most-common case (the default state) avoiding an `Array.prototype.filter` allocation on every render matters when an admin has a 200-entry metric model. React's reconciler also benefits from referential stability when the filter is the default state — the entry-row keys are stable, but a fresh array reference at every render would force React to re-check identity per row. The reference-identity pin in the tests catches a future defensive refactor that adds `entries.slice()` here.
    - **`"user"` second in display order.** Considered: alphabetical (Auto, Domain, User) for consistency with general UI conventions. Rejected — alphabetical would put Auto first (the least-clicked filter — admins don't click "show me what's auto-inferred" because by default they're seeing all auto entries already, so the filter would change the view minimally), then Domain (currently zero entries in production), then User (the most-clicked filter — admins want to review their corrections). The workflow-optimised order puts the most-clicked filter at the leftmost reachable position after All, minimising scan distance.
    - **`outline` variant for the active "All" chip.** Considered: a dedicated `primary` or `accent` variant for the active "All" state to make it visually pop. Rejected — the source variants (`secondary` / `default` / `gold`) carry semantic colour weight (auto / user / domain provenance). Adding a fourth coloured variant for "All" would dilute the visual vocabulary; the *absence* of colour on `All` reads as "no source filter applied" without needing an extra explanatory label. The active-state cue is the chip's full opacity (`opacity-70` → `1.0`) rather than a colour shift.
    - **Replacing the prior per-card total-count `<span>` with the chip row.** Considered: keep the total count as-is on the left, add the chip row on the right, end up with `Metrics … (100) … [All (100)] [User (12)] [Auto (88)] [Domain (0)]` — a redundant total + the same number in the All chip. Rejected — visual duplication for no information gain. The chip row's `All (N)` slot does the count display; removing the separate `<span>` cleans the header.
    - **Spread-before-sort, filter-inside-spread.** Considered: `filterEntriesBySource(model.metrics, sourceFilter).sort(...).map(...)`. Rejected — `.sort()` mutates in place; if the filter returns the input reference (at `"all"`), the sort would mutate the prop-passed `model.metrics` array, which is React state and should never be mutated. The `[...]` spread before `.sort()` is the safe pattern; the filter sits inside the spread because the filter's output (whether a new array or the input reference) needs to be spread either way.
    - **Local-state filter, not URL-state filter.** Considered: store the filter in the URL hash (`#filter=user`) so a deep-link preserves the filter state across navigation + share-link. Deferred — adds wouter route surface + a serializer / parser for the small filter set; the common workflow is "open the page, click User, scan, navigate away" — the filter doesn't survive past one page-load anyway. Could land as a follow-on if admins start asking for shareable filtered views.

  **Tests.** 18 new behavioural tests in [`semanticModelSourceFilterW61SourceFilter.test.ts`](../client/src/pages/Admin/lib/semanticModelSourceFilterW61SourceFilter.test.ts) — see "What landed" for coverage detail. All 18 pass; ~119 ms wave runtime; 121 cumulative W61 tests green on the combined run (~330 ms — 18 new + 103 prior). Test file appended to [`server/package.json`](../server/package.json)'s explicit list per invariant #4.

  **Verified.** All 18 new tests pass plus the 103 prior W61 tests across the trilogy + edit-text + edit-enums + source-bump + source-badge + source-filter combined run (~330 ms). Server typecheck = **98 errors**, identical to the W61-source-badge baseline (no server-side changes). Client typecheck = **54 errors**, identical to the W61-source-badge baseline (no new errors in the touched files; the new module's exports flow through `SemanticEntryFilter` cleanly without type-narrowing surprises). The Badge variant prop's `outline` branch typechecks against the existing UX-2 `BadgeProps["variant"]` enum (added at UX-2 alongside `gold` / `success`).

  **Out of scope.**
    - **Sort-by-source.** Surfacing user-edited entries at the top of each table (regardless of filter) would let an admin see their corrections without filtering at all. Considered — but flipping the sort order on every edit breaks the alphabetical sort's stability (a metric the admin just edited would jump position mid-page), confusing scroll/keyboard navigation. The filter does the same job with explicit affordance. Deferred.
    - **URL-state persistence of the filter.** Currently lost on page navigation. A `?filter=user` query param would let admins deep-link to a filtered view + survive accidental reloads. Deferred — no admin has asked yet, and the wouter route surface change is small but non-trivial.
    - **Per-section filter override.** Adding a "filter just this section" affordance (e.g. shift-click the chip) for the rare case where an admin wants to filter metrics by User while showing all dimensions. Considered, but the three-states-per-page UX cost outweighs the rare-case benefit. Deferred.
    - **Animated chip transition on click.** A brief opacity / scale animation would acknowledge the click. Cute but not load-bearing; deferred.
    - **Dark-mode contrast tuning for the active-User chip.** The `default` (primary) variant uses `bg-primary text-primary-foreground` which works in both modes; the active-User chip should be readable. Visual audit deferred.
    - **Filter-aware bulk actions.** A future "re-trigger inference on auto-only entries" or "bulk-edit all User entries' description prefix" affordance would naturally read the active filter as its scope. Out of scope here — needs a bulk-edit primitive first.
    - **Combined-counts global chip.** A top-of-page row of chips with cross-table totals would complement the per-card rows. Considered but currently no use case where the admin wants to know "how many of the 250 entities across metrics + dimensions + hierarchies are user-edited" as a single number — they always work section-by-section.
