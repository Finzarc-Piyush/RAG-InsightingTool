# Shared schemas (server ↔ client mirror)

## Purpose

Every schema the wire touches is defined **twice** — once in
`server/shared/schema.ts`, once in `client/src/shared/schema.ts` — with
byte-for-byte equivalent shape. Zod's default `strip` means a
mismatched shape silently drops fields on re-validate, which has burned
us before.

## Key files

- `server/shared/schema.ts` — authoritative server-side schemas.
- `client/src/shared/schema.ts` — client mirror, imported as
  `@shared/schema` via the `@shared/*` alias.
- `scripts/check-shared-schemas.mjs` (if present) — the drift gate that
  hashes both files' structural shape; CI fails on deltas beyond the
  allowlist.

## Mirror pairs

| Schema | Purpose | Both files? |
|---|---|---|
| `messageSchema` | Chat message (user + assistant) | ✅ |
| `agentWorkbenchSchema` | Workbench entry rows streamed over SSE | ✅ |
| `chartSpecSchema` | Chart rendering contract | ✅ |
| `dashboardSpecSchema` | Dashboard spec from autogen / user | ✅ |
| `dashboardPatchSchema` | Incremental dashboard edits (add/remove/reorder tiles) | ✅ |
| `analysisBriefSchema` | Planner brief (outcome metric, filters, comparisonPeriods) | ✅ |
| `thinkingStepSchema` / `thinkingSnapshotSchema` | Live thinking panel | ✅ |
| `pivotDefaultsSchema` | Query-derived pivot fields | ✅ |

**`agentTrace`** on `messageSchema` is carried as
`z.record(z.unknown()).optional()` in both files — it is **not** a
strict schema today. The server writes a capped `AgentTrace` blob; the
client reads it opportunistically. Typing it strictly is a future
improvement (would also let the drift gate check trace shape), not a
bug.

## Data contracts

- **Nothing on the wire should come from `any` alone.** If a field
  isn't in the mirrored schema, the drift gate should flag it.
- **Optional vs strict.** Prefer `.optional()` over `.passthrough()`;
  prefer `.strict()` for request bodies parsed by route handlers.
- **Shape mirrors, not identity.** `server/shared/schema.ts` and
  `client/src/shared/schema.ts` are physically separate modules — each
  service imports its local copy. Don't attempt to `import` one from
  the other (the Vite SPA cannot see the server filesystem at build
  time).

## Extension points

- **Add a shared schema**: define it in `server/shared/schema.ts`, copy
  the same declaration into `client/src/shared/schema.ts`, and run the
  drift gate if present. Types via `z.infer<typeof X>` travel through
  `@shared/schema` on both sides.
- **Add a field to `messageSchema`**: apply to both files in the same
  commit. The drift gate should catch any asymmetry.

## Known pitfalls

- **`strip` silently drops.** If you forget to mirror a field, the
  parser simply removes it on round-trip — no error, no warning. Always
  mirror.
- **Enum values must match.** `z.enum([...])` compares literals
  case-sensitively; `"pass"` ≠ `"Pass"`.
- **Default values live in schemas, not in handlers.** A schema's
  `.default(x)` runs at parse time; a handler's `?? x` runs after
  validation. Prefer the schema unless you need request-context.

## Recent changes

- Wave W61-references-endpoint (2026-05-20) — server-only follow-on to W61-references-scan. No schema change. Wires the pure-function scanner into `GET /admin/semantic-models/:sessionId/references?entry=<name>` returning `{ sessionId, entry, chartCount, totalOccurrences }`. New exported `AdminSemanticModelReferencesResponse` interface in [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts); new handler shape mirrors `getSemanticModel` / `getSemanticModelAuditLog` (admin-gate 403 → sessionId trim 400 → `entry` query-param coercion + trim 400 → re-uses `_detailFetcher` → 404 on session / model not-found → `countSemanticModelReferences(entry, doc.charts ?? [])` → envelope → 500 with `admin_semantic_model_references_failed`). Walks `doc.charts[]` only (not `doc.messages[].charts[]` — would double-count given the chat-model dedup-merge; not blob-stored `chartReferences[]` — would turn a free in-memory count into a multi-second multi-request fetch). 12 behavioural tests in [`adminSemanticModelReferencesW61ReferencesEndpoint.test.ts`](../../server/tests/adminSemanticModelReferencesW61ReferencesEndpoint.test.ts) — admin gate, param validation (including `?entry=a&entry=b` → 400 since Express's qs parser turns repeated keys into arrays and the handler rejects them rather than silently picking one), 404 paths, 200 envelope with server-trimmed entry echo, undefined-charts coerce-to-empty, 500 path with `console.error` capture. See `docs/WAVES.md` for full entry.
- Wave W61-references-scan (2026-05-20) — server-only foundation for the upcoming W61-delete-entry confirmation. No schema change. New pure module [`server/lib/semantic/semanticModelReferences.ts`](../../server/lib/semantic/semanticModelReferences.ts) (179 LOC) exports `SemanticModelReferenceCount` (`{ chartCount, totalOccurrences }`) and three counter functions: `countReferencesInChartSpec` walks v1 fields (`x` / `y` / `z` / `seriesColumn` / `y2` / `y2Series[]` / `_agentProvenance.{columnsUsed,rangeFilters}`); `countReferencesInChartSpecV2` walks every `.field` location on the v2 encoding (13 channels + `y2Series[].field` + `tooltip[].field` + opacity-channel-only) plus the transform discriminated union's identifier-bearing branches (`aggregate.{groupby,ops}` / `fold.fields[]` / `bin.field` / `window.{ops,groupby,sort}` / `regression.on` — `filter.expr` + `calculate.expr` are deliberately skipped to avoid SQL-substring noise); and the top-level `countSemanticModelReferences(name, charts)` accepts `ReadonlyArray<unknown>` and discriminates v1 vs v2 at runtime via [`isChartSpecV2`](../../server/shared/schema.ts) so the planned v1→v2 chart migration doesn't break the scanner. Exact-identifier equality only (no substring / fuzzy / case-folding); empty `name` short-circuits to zero at three layers. 19 behavioural tests in [`semanticModelReferencesW61ReferencesScan.test.ts`](../../server/tests/semanticModelReferencesW61ReferencesScan.test.ts). Reads chart-schema fields but does not change the schema itself — listed here because the schemas subsystem is the closest map for a pure-function reader of chart structure. See `docs/WAVES.md` for full entry.
- Wave W61-detail-extract (2026-05-20) — pure-client refactor. No schema change. Sibling-file extraction of `SourceFilterChips` + `AuditHistoryCard` (collapsible audit-history Card) out of [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx) into new sibling files under [`client/src/pages/Admin/components/`](../../client/src/pages/Admin/components/) (`SourceFilterChips.tsx` 76 LOC + `AuditHistoryCard.tsx` 166 LOC). Host drops 1,295 → 1,131 LOC (−164 LOC); relieves file-size pressure before the projected W61-add-delete wave (+~300 LOC) would push past the 1,500 sub-component-extract threshold. Zero behavioural risk — no JSX, prop semantics, or runtime behaviour changes. Every `data-testid` preserved verbatim. No new tests (the underlying pure helpers were already pinned by W61 helper-test files). See `docs/WAVES.md` for full entry.
- Wave W61-audit-history-tab (2026-05-20) — pure-client UI mount that consumes the W61-audit-history-client helpers + the W61-audit-history-api / W61-audit-revert server endpoints. No schema change. [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx) gains a fourth `<Card>` under Hierarchies wrapped in `Collapsible open={historyOpen}` (closed by default — the audit-log fetch is opt-in). The new effect is keyed on `[sessionId, historyOpen, data?.lastUpdatedAt]` so save / revert bumps refresh the open list automatically without explicit cache-busting at every mutation site. Per-row Revert handler runs `buildRevertConfirmation` through `window.confirm`, calls `revertSemanticModel`, and updates `data` with the response envelope — byte-identical to the PATCH-success path because W61-audit-revert deliberately reuses `PatchSemanticModelResponse`. Per-row `reverting: number | null` index keeps non-clicked rows readable while one revert is in flight (`withSessionWriteLock` makes concurrent attempts safe per invariant #9). Disabled triple gate `saving || reverting !== null || historyLoading` on every Revert button prevents racing the existing PATCH save path, another revert in flight, or the audit-log refetch landing. Test-IDs (`admin-semantic-model-audit-history-{trigger,loading,error,empty,list,revert-error,revert-N}`) wired throughout. +197 LOC to the host file (1,098 → 1,295 LOC; under the 1,500 sub-component-extract threshold). No new tests — the helpers were already pinned by 15 W61-audit-history-client unit tests. See `docs/WAVES.md` for full entry.
- Wave W61-audit-history-client (2026-05-20) — pure-client follow-on to W61-audit-history-api + W61-audit-revert. [`client/src/lib/api/admin.ts`](../../client/src/lib/api/admin.ts) gains `AdminSemanticModelAuditEntry` + `AdminSemanticModelAuditLog` interfaces (client mirror of the server's `SemanticModelAuditEntry` shape; local declaration to avoid the cross-runtime import boundary — mirrors the W61-detail / W61-save inline-import pattern for `SemanticModel`), `fetchSemanticModelAuditLog(sessionId)`, and `revertSemanticModel(sessionId, auditEntryIndex)` (re-uses the existing `PatchSemanticModelResponse` type because the server envelope is byte-identical to W61-save's). New pure module [`semanticModelAuditHistory.ts`](../../client/src/pages/Admin/lib/semanticModelAuditHistory.ts) ships `formatAuditTimestamp` (UTC-stable for multi-TZ forensics), `buildAuditEntrySummary` (`{ headline: "Save N of M", subhead: "{savedBy} · {ts} · was v{priorVersion}" }` with `saveNumber = total - indexFromNewest` so newest entry reads `"Save M of M"`), and `buildRevertConfirmation` (`window.confirm()` prompt with `\n\n` paragraph break separating action from the undo-safety note). No schema change — the client mirror is structural-only and stays in lockstep with the server via runtime undefined-field detection. See `docs/WAVES.md` for full entry.
- Wave W61-audit-revert (2026-05-20) — server-only follow-on to W61-audit-history-api that closes the audit-trail server loop (write → read → revert). New `POST /admin/semantic-models/:sessionId/revert` body schema `revertBodySchema = z.object({ auditEntryIndex: z.number().int().nonnegative() })` (zod) validates at the boundary; upper-bound check against `log.length` happens inside `withSessionWriteLock` so it sees the authoritative state. New `RevertResult` discriminated union adds a fourth `audit_entry_not_found` variant alongside `ok` / `session_not_found` / `semantic_model_not_inferred`. Response envelope `{ sessionId, lastUpdatedAt, model }` mirrors W61-save's so the future client `useMutation` reuses the same success handler. **`nextModel = { ...entry.priorModel, version: current+1, updatedAt, updatedBy }`** — version stays monotonic (W64 compiled-query cache key), about-to-be-overwritten model appended to the audit log so "undo this revert" works. **W61-source-bump is NOT run on revert** — per-entry `source` field comes verbatim from the snapshot ("restore as-was" semantics, not "edit"). See `docs/WAVES.md` for full entry.
- Wave W61-audit-history-api (2026-05-20) — server-only follow-on to W61-audit-log that exposes the prior-model ring buffer over a dedicated read-only endpoint `GET /admin/semantic-models/:sessionId/audit-log`. New envelope [`AdminSemanticModelAuditLogResponse`](../../server/controllers/adminSemanticModelController.ts) = `{ sessionId, entries: SemanticModelAuditEntry[] }` with entries newest-first (`[]` when the session has a model but no PATCHes yet — coalesced at the controller boundary so callers don't need `?? []`). Handler shape mirrors `getSemanticModel`: same admin gate / sessionId trim / `_detailFetcher` shim / 404 codes (`session_not_found`, `semantic_model_not_inferred`) / wave-specific 500 code (`admin_semantic_model_audit_log_failed`). Re-uses the existing `_detailFetcher` injectable (audit log is a field on the same `ChatDocument`, not a separate Cosmos collection). The endpoint surfaces `SemanticModelAuditEntry` directly with no projection — the full `priorModel` snapshot lets the future revert UI re-PATCH in one round-trip without a second fetch. Pairs with the upcoming history-tab UI (W61-audit-history-ui) and one-call revert endpoint (W61-audit-revert). See `docs/WAVES.md` for full entry.
- Wave W61-audit-log (2026-05-20) — server-only follow-on that adds a prior-model audit trail on every admin PATCH. `ChatDocument` gains optional `semanticModelAuditLog?: SemanticModelAuditEntry[]` sibling to `semanticModel`, populated by [`patchSemanticModel`](../../server/controllers/adminSemanticModelController.ts) inside the existing `withSessionWriteLock` callback (per invariant #9) **before** the `doc.semanticModel = nextModel` overwrite so the snapshot captures the prior state. The entry interface (`savedAt` / `savedBy` / `priorVersion` / `priorModel`) and the ring-buffer helper live in the new pure module [`server/lib/semantic/semanticModelAuditLog.ts`](../../server/lib/semantic/semanticModelAuditLog.ts) with the cap exported as `SEMANTIC_MODEL_AUDIT_LOG_MAX_ENTRIES = 10` (chosen so 10 × ~50 KB worst-case SemanticModel snapshots ≈ 500 KB fits comfortably under the 2 MB Cosmos doc limit). No zod schema mirror needed — `ChatDocument` is a plain TS interface per the W57 precedent. The audit-write piggybacks on the existing per-session mutex so the entry-and-overwrite pair is atomic at the per-session level (single-instance correctness only; multi-instance horizontal scaling still needs Cosmos `ifMatch` ETag). The new "audit-style ring buffer on `ChatDocument`" pattern is codified in [`docs/conventions/audit-log-ring-buffer.md`](../conventions/audit-log-ring-buffer.md). See `docs/WAVES.md` for full entry.
- Wave W61-filter-persist (2026-05-20) — pure-client follow-on that persists the W61-source-filter active chip to the URL via `?filter=X`. New pure module [`semanticModelFilterUrlSync.ts`](../../client/src/pages/Admin/lib/semanticModelFilterUrlSync.ts) ships `readFilterFromSearch(search)` / `writeFilterToSearch(search, filter)` — string-in / string-out so testable without jsdom. Validation is case-sensitive (URL params are byte-stable in real use) and falls back to `"all"` on invalid / missing / wrong-case; the `"all"` default REMOVES the param entirely so the URL stays clean. Helpers return strings without a leading `?` so the caller controls the empty-vs-prefix semantics. Wired into the page via a lazy `useState` initializer + `replaceState`-on-change to avoid browser-history accumulation. Zero schema change — the URL contract is local to the admin viewer. See `docs/WAVES.md` for full entry.
- Wave W61-source-filter (2026-05-20) — pure-client follow-on that layers a per-source filter chip row above each table in [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx). New pure module [`semanticModelSourceFilter.ts`](../../client/src/pages/Admin/lib/semanticModelSourceFilter.ts) exports `SemanticEntryFilter = "all" | SemanticEntrySource` (sentinel-extends the W61-source-badge type without re-declaring the source values), `filterEntriesBySource` (reference-identity preservation on `"all"` to avoid allocation on the most-common case), `countEntriesBySource` (single-pass per-source counts + the `all = entries.length` slot for the chip-row labels), and `SOURCE_FILTER_ORDER` with `"user"` second in display order (workflow-optimised — most-clicked filter at the leftmost reachable position after `All`). Zero schema change — the filter is a read-only render over the existing `source` field; the helper module is the chip-row's data layer. The `{T extends { source: SemanticEntrySource }}` generic constraint lets `SemanticMetric` / `SemanticDimension` / `SemanticHierarchy` flow through without per-type duplication. See `docs/WAVES.md` for full entry.
- Wave W61-source-badge (2026-05-20) — pure-client follow-on that surfaces the per-entry `source` field as a colored chip in [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx). New pure module [`semanticModelSourceBadge.ts`](../../client/src/pages/Admin/lib/semanticModelSourceBadge.ts) maps the `SemanticEntrySource = "auto" | "user" | "domain"` local alias to label / Badge variant / tooltip via three frozen `Readonly<Record<SemanticEntrySource, …>>` lookups. The alias is intentionally local (not re-exported from `@/shared/schema`) so a future split (e.g. metric_source vs dimension_source diverging) forces a deliberate sync. Zero schema change — the chip is a read-only render of the existing `source` field that W57 / W61-save / W61-source-bump already populate. The `Readonly<Record<…>>` keying gives the TypeScript type checker a single shape to widen against when a fourth source value lands. See `docs/WAVES.md` for full entry.
- Wave W61-source-bump (2026-05-20) — closes the W61 source-weighting gap. Adds a per-entry content-hash diff in [`server/lib/semantic/semanticModelSourceBump.ts`](../../server/lib/semantic/semanticModelSourceBump.ts) that the W61-save controller invokes inside `withSessionWriteLock` between the `safeParse` and the version stamp. Unchanged entries preserve prior `source` (auto / domain / user); changed entries bump to `"user"`; new entries preserve client-sent source. Content hashes project each entry to its content fields (everything except `source`) so source-only diffs don't trigger the bump. No schema change — `SemanticMetric.source` / `SemanticDimension.source` / `SemanticHierarchy.source` are unchanged; only their write-path semantics on PATCH evolved. See `docs/WAVES.md` for full entry.
- Wave W61-edit-enums (2026-05-20) — second polish wave on the W61 trilogy. Adds an `EditableSelect` UI on top of the W61-edit-text validators with three frozen option lists declared via `as const satisfies ReadonlyArray<{ value: T; label: string }>` against `SemanticMetric["format"]` / `SemanticDimension["kind"]` literal types. The compile-time `satisfies` guard catches enum-value drift between the admin UI and the zod schema at `tsc --noEmit` time rather than at runtime PATCH-time. New `validateCurrencyCode` (`/^[A-Z]{3}$/` after trim; empty allowed for the optional field) joins the existing label / description / expression / isMeaningfulChange validators in [`client/src/pages/Admin/lib/semanticModelEditValidation.ts`](../../client/src/pages/Admin/lib/semanticModelEditValidation.ts). The format/currencyCode and kind/temporalGrain cross-field coupling is enforced server-side by `semanticModelSchema.safeParse`, with the orphan field cleared client-side at the parent handler boundary so the planner manifest doesn't render misleading `currency (USD)` hints on a percent metric. See `docs/WAVES.md` for full entry.
- Wave W61-edit-text (2026-05-20) — first polish wave on the W61 trilogy. Adds client-side per-field validators in [`client/src/pages/Admin/lib/semanticModelEditValidation.ts`](../../client/src/pages/Admin/lib/semanticModelEditValidation.ts) that mirror `semanticMetricSchema.label.min(1).max(120)` / `.description.max(1000)` / `.expression.min(1).max(2000)` bounds. The expression validator adds three checks beyond the schema's free-text `z.string().min(1).max(2000)`: no semicolons (multi-statement guard), no SQL comments (`--` / `/*`), no top-level `SELECT` / `FROM` / `JOIN` / `WHERE` / `GROUP BY` / `ORDER BY` / `UNION` keywords (padded-uppercase scan so `SUM(selector_count)` is NOT rejected). The schema itself is unchanged — these are a cheap obvious-broken filter ahead of the W58 compiler's authoritative parse; if the schema bounds change, a drift here just routes the save to the server's authoritative rejection. No new schema mirror needed. See `docs/WAVES.md` for full entry.
- Wave W61-save (2026-05-20) — admin PATCH endpoint adds `PATCH /admin/semantic-models/:sessionId` body validation via `semanticModelSchema.safeParse` in [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts). Server overrides three schema fields after parse: `version: prior + 1`, `updatedAt: new Date().toISOString()`, `updatedBy: getAuthenticatedEmail() ?? "unknown"`. Client-sent values for those keys are ignored (a client-set version would let an admin bump backwards or jump forward; the W64 compiled-query cache reads `version` as cache key so monotonic bump is load-bearing). RMW routes through [`withSessionWriteLock`](../../server/lib/sessionWriteLock.ts) per invariant #9 — single-instance correctness only; multi-instance horizontal scaling needs Cosmos `ifMatch` ETag separately. `__setSemanticModelUpdaterForTesting` injectable shim completes the trio (lister / detailFetcher / updater) for route tests. See `docs/WAVES.md` for full entry.
- Wave W61-detail (2026-05-20) — admin detail endpoint adds `AdminSemanticModelDetailResponse` envelope (`{ sessionId, fileName, username, lastUpdatedAt, model: SemanticModel }`) in [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts). Client mirrors via `import("@/shared/schema").SemanticModel` inline type in [`lib/api/admin.ts`](../../client/src/lib/api/admin.ts) so the API helper stays free of structural schema deps. No zod schema change — `SemanticModel` is consumed verbatim from the W56 foundation. See `docs/WAVES.md` for full entry.
- Wave W61-list (2026-05-20) — `chat.model.ts` exports a new lightweight `AdminSemanticModelListEntry` (index-row projection over `ChatDocument.semanticModel`) + `ADMIN_SEMANTIC_MODEL_LIST_SELECT` (the Cosmos SELECT) + `finalizeAdminSemanticModelEntry` (pure coercion) + `getAllSessionsWithSemanticModel` (network boundary). The projection cherry-picks root fields + nested `semanticModel.{version,name,updatedAt,updatedBy}` + per-collection counts via the defensive `IIF(IS_DEFINED ... AND IS_ARRAY, ARRAY_LENGTH, 0)` triple. `WHERE IS_DEFINED(c.semanticModel)` filters pre-W57 docs; `ORDER BY c.lastUpdatedAt DESC` surfaces the active sessions. Powers the admin index page at `/admin/semantic-models` (first of three W61 sub-waves; W61-detail surfaces the full payload via `formatMetricCatalog`; W61-save lands the PATCH endpoint). The `enableCrossPartitionQuery: true` FeedOption was deliberately omitted because Cosmos SDK v4 dropped it from `FeedOptions` and including it would have added a 6th instance of the existing `TS2769` overload baseline (lines 583/979/1063/1493/1762). See `docs/WAVES.md` for full entry.
- Wave W57 (2026-05-16) — `ChatDocument` gains optional `semanticModel?: SemanticModel` field in [server/models/chat.model.ts](../../server/models/chat.model.ts) (ChatDocument is a TS interface, not a zod schema, so no mirror needed). Populated at the upload understanding-ready checkpoint via [`server/lib/semantic/inferModel.ts`](../../server/lib/semantic/inferModel.ts).
- Wave W56 (2026-05-16) — Semantic & metrics layer type foundation. Adds `semanticMetricSchema`, `semanticDimensionSchema`, `semanticHierarchySchema`, `semanticModelSchema` to [server/shared/schema.ts](../../server/shared/schema.ts). Auto-mirrored to client via the W5 re-export. Foundation for W57–W64 per the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md). See `docs/WAVES.md` for full entry.
- Initial seed of this doc.

**Caveat — out-of-date description above.** The "every schema defined twice" line at the top of this doc predates Wave W5, which collapsed the client mirror into a single re-export (`export * from "../../../server/shared/schema"` in [client/src/shared/schema.ts](../../client/src/shared/schema.ts)). The drift gate is no longer load-bearing; manual mirroring is no longer possible. Refresh in a future doc-hygiene wave.
