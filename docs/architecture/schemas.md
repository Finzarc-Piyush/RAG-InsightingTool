# Shared schemas (server ↔ client mirror)

> ℹ Line numbers in this doc are indicative and known to drift (the zod *shapes*
> stay accurate). For an exact current location of any schema symbol, grep it or
> look it up in `docs/index/symbols.generated.tsv`.

## Purpose

Every schema the wire touches is defined **twice** — once in
`server/shared/schema.ts`, once in `client/src/shared/schema.ts` — with
byte-for-byte equivalent shape. Zod's default `strip` means a
mismatched shape silently drops fields on re-validate, which has burned
us before.

## Key files

- `server/shared/schema.ts` — authoritative server-side schemas.
- `client/src/shared/schema.ts` — client mirror, imported as
  `@shared/schema` via the `@shared/*` alias.
- A drift gate (historical) hashed both files' structural shape and
  failed CI on deltas beyond the allowlist. It is no longer present — see
  the caveat at the end of this doc: Wave W5 collapsed the client mirror
  into a single re-export, so the gate is no longer load-bearing.

## Mirror pairs

| Schema | Purpose | Both files? |
|---|---|---|
| `messageSchema` | Chat message (user + assistant) | ✅ |
| `agentWorkbenchSchema` | Workbench entry rows streamed over SSE | ✅ |
| `chartSpecSchema` | Chart rendering contract | ✅ |
| `dashboardSpecSchema` | Dashboard spec from autogen / user | ✅ |
| `dashboardPatchSchema` | Incremental dashboard edits (add/remove/reorder tiles) | ✅ |
| `analysisBriefSchema` | Planner brief (outcome metric, filters, comparisonPeriods) | ✅ |
| `thinkingStepSchema` / `thinkingSnapshotSchema` | Live thinking panel | ✅ |
| `pivotDefaultsSchema` | Query-derived pivot fields | ✅ |

**`agentTrace`** on `messageSchema` is carried as
`z.record(z.unknown()).optional()` in both files — it is **not** a
strict schema today. The server writes a capped `AgentTrace` blob; the
client reads it opportunistically. Typing it strictly is a future
improvement (would also let the drift gate check trace shape), not a
bug.

## Data contracts

- **Nothing on the wire should come from `any` alone.** If a field
  isn't in the mirrored schema, the drift gate should flag it.
- **Optional vs strict.** Prefer `.optional()` over `.passthrough()`;
  prefer `.strict()` for request bodies parsed by route handlers.
- **Shape mirrors, not identity.** `server/shared/schema.ts` and
  `client/src/shared/schema.ts` are physically separate modules — each
  service imports its local copy. Don't attempt to `import` one from
  the other (the Vite SPA cannot see the server filesystem at build
  time).

## Extension points

- **Add a shared schema**: define it in `server/shared/schema.ts`, copy
  the same declaration into `client/src/shared/schema.ts`, and run the
  drift gate if present. Types via `z.infer<typeof X>` travel through
  `@shared/schema` on both sides.
- **Add a field to `messageSchema`**: apply to both files in the same
  commit. The drift gate should catch any asymmetry.

## Known pitfalls

- **`strip` silently drops.** If you forget to mirror a field, the
  parser simply removes it on round-trip — no error, no warning. Always
  mirror.
- **Enum values must match.** `z.enum([...])` compares literals
  case-sensitively; `"pass"` ≠ `"Pass"`.
- **Default values live in schemas, not in handlers.** A schema's
  `.default(x)` runs at parse time; a handler's `?? x` runs after
  validation. Prefer the schema unless you need request-context.

## Recent changes

- Wave WD3-WI4-sheetId-telemetry (2026-05-21) — widening of the two inline zod request body schemas (`drillThroughTelemetryRequestSchema` + `explainSliceTelemetryRequestSchema`) declared in [`server/routes/telemetry.ts`](../../server/routes/telemetry.ts) to add `sheetId: z.string().min(1).optional()` as a sixth field on each, under the existing `.strict()`. No change to [`shared/schema.ts`](../../server/shared/schema.ts) (the `usageEventTypeSchema` enum doesn't widen — both endpoints continue writing their existing `dashboard.drill-through` / `dashboard.explain-slice` event types; the new field rides on the open-shape `metadata: z.record(z.unknown()).optional()` slot on `usageEventDocSchema`). Both controllers spread `sheetId` conditionally into recorder metadata via `...(sheetId ? { sheetId } : {})` — the explicit-omit invariant (NOT `sheetId: undefined`) preserves byte-identical Cosmos rows for callers that don't yet pass the field, and a doublet-drift-guard test pins both schemas + both controllers via regex-match-count = 2 at the source level. Third consumer of the strict-zod-on-observability-endpoints convention codified last session; no new convention introduced this wave — the "additive optional field on a strict observability endpoint" shape is a soft pattern with 1 instance, would promote on a second. Client-side payload interface mirrors in [`client/src/lib/telemetry.ts`](../../client/src/lib/telemetry.ts) gain matching `sheetId?: string` so the strict-zod gates pass when DashboardView listeners thread `activeSheetId ?? undefined` into the helper. See `docs/WAVES.md` for full entry.
- Wave WI4-telemetry (2026-05-21) — narrow schema-enum widening that adds a fifth `"dashboard.explain-slice"` member to the AD3 `usageEventTypeSchema` enum in [`server/shared/schema.ts`](../../server/shared/schema.ts) at position 5 (immediately after `"dashboard.drill-through"`, before `"analysis.shared"` — lexical grouping for the dashboard-family events stays intact), so the new client-fired POST `/api/telemetry/explain-slice` endpoint can write structured rows into the `usage_events` Cosmos container via the existing `recordUsageEvent` fire-and-forget pattern proven for `dashboard.opened` / `dashboard.shared` / `dashboard.drill-through`. The new POST handler is co-located in the existing [`server/routes/telemetry.ts`](../../server/routes/telemetry.ts) module — the WD3-telemetry route module hosts BOTH endpoints, both controllers route through the SAME `__setUsageEventRecorderForTesting` / `__resetUsageEventRecorderForTesting` route-level recorder seam first introduced by WD3-telemetry, which is the second-instance promotion that codifies the seam at [`docs/conventions/route-level-recorder-seam.md`](../conventions/route-level-recorder-seam.md). No widening of the `usageEventDocSchema` companion — the new event uses `metadata: { chartId, column, regionKind }` which already fits the existing `metadata: z.record(z.unknown()).optional()` slot; `dashboardId` rides on the existing top-level optional field. **Load-bearing PII contract** documented inline on both the route and the client helper: column NAMES go on the wire (dataset schema, public, observability-safe), column VALUES and brush bounds never do — only the `BrushRegion` discriminant rides on the wire (`"numeric" | "temporal" | "categorical" | "box2d"`). Strict zod body schema with `z.enum()` on `regionKind` (stronger than WD3's free-form `valueType: z.string().min(1)` — the enum narrowing is additional defense in depth against future callers smuggling non-discriminant tokens through the schema layer) — the strict-rejection itself promotes to [`docs/conventions/strict-zod-on-observability-endpoints.md`](../conventions/strict-zod-on-observability-endpoints.md) as the second-instance codification. Three conventions codified this wave from the soft patterns WD3-telemetry shipped (each at the second-instance promotion threshold): route-level-recorder-seam, pii-safe-discriminant-tag-on-observability-events, strict-zod-on-observability-endpoints. See `docs/WAVES.md` for full entry.
- Wave WD3-telemetry (2026-05-21) — narrow schema-enum widening that adds a fourth `"dashboard.drill-through"` member to the AD3 `usageEventTypeSchema` enum in [`server/shared/schema.ts`](../../server/shared/schema.ts) so the new client-fired POST `/api/telemetry/drill-through` endpoint can write structured rows into the `usage_events` Cosmos container via the existing `recordUsageEvent` fire-and-forget pattern proven for `dashboard.opened`. The enum position is intentional (right after `dashboard.shared`, before `analysis.shared`) so the dashboard-family events stay grouped — alphabetisation isn't load-bearing because the enum is consumed by zod via membership-check, not by SQL ORDER BY, but the lexical grouping helps future readers spot related events. No widening of the `usageEventDocSchema` companion — the new event uses `metadata: { chartId, column, valueType }` which already fits the existing `metadata: z.record(z.unknown()).optional()` slot; `dashboardId` rides on the existing top-level optional field. **Load-bearing PII contract** documented inline on the new route ([`server/routes/telemetry.ts`](../../server/routes/telemetry.ts)) and on the client helper ([`client/src/lib/telemetry.ts`](../../client/src/lib/telemetry.ts)): column NAMES go on the wire (dataset schema, public), column VALUES never do (only their JS `typeof` tag — `"string"|"number"|"boolean"|"object"|"undefined"`). Strict zod body schema (`drillThroughTelemetryRequestSchema` exported from the route, `chartId.min(1) + column.min(1) + valueType.min(1) + dashboardId.optional()` under `.strict()`) rejects any extra top-level key so a future caller that accidentally widens the payload to include the raw value surfaces as a 400 immediately. New injectable `__setUsageEventRecorderForTesting` / `__resetUsageEventRecorderForTesting` seam on the route module — first instance of "inject the model writer on the route layer instead of the model layer"; the W61 admin-controller-setter precedent is the closest analogue but those setters live on controller exports rather than route exports. Pattern would promote on a second instance. See `docs/WAVES.md` for full entry.
- Wave W61-per-section-filter (2026-05-21) — pure-client UI wave that closes the last W61 polish trail item end-to-end by adding a per-section filter override on top of the W61-source-filter global chip row. No server schema change — the existing `SemanticEntryFilter` taxonomy (`"all" | SemanticEntrySource`) from W61-source-filter is reused verbatim; the new module composes on top rather than widening the union. New pure module [`semanticModelSectionFilters.ts`](../../client/src/pages/Admin/lib/semanticModelSectionFilters.ts) exports the `SemanticModelSectionFilters` interface (`{ readonly global: SemanticEntryFilter; readonly overrides: Readonly<Record<"metrics"|"dimensions"|"hierarchies", SemanticEntryFilter | null>> }`) — a hybrid shape that preserves the synced-global default (which serves the dominant admin workflow) while adding an opt-in per-section override path via the shift-click modifier. The `applyChipClick(filters, section, next, modifier)` pure reducer routes both branches: modifier=true (shift-click) sets only `overrides[section]=next`; modifier=false (plain-click) sets `global=next` AND clears ALL overrides — the "single exit path" semantics so an admin can never accidentally leave a stale override hidden by switching cards. `getEffectiveFilter` selector returns `overrides[section] ?? global` per card. `SourceFilterChips.onChange` widens to `(next, modifier: boolean) => void` (the chip button captures `e.shiftKey` via inline event handler — keeps the host's pure reducer agnostic of React's synthetic event); each card threads an optional `isSectionOverridden` prop into `<SourceFilterChips isOverridden={...}>` which renders an "(overridden)" italic muted-foreground hint at the end of the chip row. Host swaps `sourceFilter: SemanticEntryFilter` state for `sectionFilters: SemanticModelSectionFilters`; URL persistence (W61-filter-persist) keys only on `sectionFilters.global` — per-section overrides are session-local opt-in advanced state that doesn't survive reload (share-link recipients probably want the synced default anyway). No new convention codified — the "global + per-section override" hybrid filter shape is a soft pattern with one instance; a future hybrid filter on a different admin page would promote it. The W61 polish trail is now CLOSED end-to-end: the admin `/admin/semantic-models` page ships every common-admin correction surface (CRUD + audit trail + safety net + schema-aware edit + per-section filter override). See `docs/WAVES.md` for full entry.
- Wave W61-edit-column-refs (2026-05-21) — pure-client UI wave that closes the W61 edit surface for `SemanticDimension.column` rebinding and `SemanticMetric.references[]` editing by consuming the W61-detail-schema `datasetSchema` envelope (shipped earlier this session). No server schema change — the existing `semanticDimensionSchema.column` (`z.string().min(1).max(200)`) and `semanticMetricSchema.references` (`z.array(z.string().min(1).max(200)).max(20).default([])`) at [`server/shared/schema.ts`](../../server/shared/schema.ts) and [`server/shared/schema.ts`](../../server/shared/schema.ts) are mirrored client-side by new validators in [`semanticModelEditValidation.ts`](../../client/src/pages/Admin/lib/semanticModelEditValidation.ts): `validateColumn(value): string | null` (empty + 200-char cap), `validateReferences(refs): string | null` (cardinality-only against the exported `REFERENCES_MAX = 20` constant), and the `REFERENCES_MAX` constant itself exported so the tag-list UI can disable the "+ Add" affordance at the cap rather than letting the admin add a 21st tag and surfacing rejection only after blur — same cross-runtime-boundary local-mirror precedent as W61-add-client / W61-hierarchy-edit (drift surfaces at the call site as a server 400, correctness-preserving). The cards consume the W61-detail-schema envelope's `datasetSchema.columns[].name` projection via a new `datasetColumns: ReadonlyArray<string> | null` prop threaded from the host's `data.datasetSchema?.columns.map(c => c.name) ?? null` derivation; both `EditableColumnPicker` (DimensionsCard, ~70 LOC private component) and `EditableColumnTagList` (MetricsCard, ~150 LOC private component) branch on `datasetColumns === null` between a Radix `<Select>` of the dataset's columns (schema-aware path) and a free-text `EditableText` / `<Input>` fallback (legacy path for pre-`dataSummary` sessions). The picker shows the current value as an option even when not in `datasetColumns`, with a `(not in dataset)` italic muted-foreground hint so stale-state drift is visible. The tag list dedupes against both the dropdown options AND the free-text add path (two defenses for the same set-semantic invariant). Edits route through the existing W61-save PATCH path via `patchMetric(metricName, { references: nextRefs })` and `patchDimension(dimensionName, { column: nextCol })` — no new mutation paths, no new endpoints, no new envelope; the W61-cache-invalidate version-bump hook fires automatically on every successful save. New convention NOT codified — the schema-aware-or-free-text edit-cell shape has two instances inside this single wave but the "two precedents across separate waves" rule applies; a future wave introducing a third schema-aware edit cell would promote the pattern then. See `docs/WAVES.md` for full entry.
- Wave W61-host-extract (2026-05-21) — pure-client refactor. No schema change — the existing `semanticMetricSchema` / `semanticDimensionSchema` / `semanticHierarchySchema` are consumed verbatim by the new card siblings (the cards re-render the same rows that were previously inlined in the host, with no structural change to the entry shapes). Sibling-file extraction of `MetricsCard` + `DimensionsCard` + `HierarchiesCard` + shared `semanticModelCells` out of [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx) into new sibling files under [`client/src/pages/Admin/components/`](../../client/src/pages/Admin/components/): [`MetricsCard.tsx`](../../client/src/pages/Admin/components/MetricsCard.tsx) (275 LOC), [`DimensionsCard.tsx`](../../client/src/pages/Admin/components/DimensionsCard.tsx) (250 LOC — also exports the `TEMPORAL_GRAIN_AUTO` sentinel so the host's `handleTemporalGrainChange` and the option list stay co-located), [`HierarchiesCard.tsx`](../../client/src/pages/Admin/components/HierarchiesCard.tsx) (172 LOC), and [`semanticModelCells.tsx`](../../client/src/pages/Admin/components/semanticModelCells.tsx) (287 LOC — exports `ExposedToggle` / `SourceBadge` / `EditableText` / `EditableSelect` / `RowDeleteButton` shared across all three card siblings). Host drops 1,545 → 802 LOC (−743 LOC); relieves the 1,500-LOC sub-component-extract threshold pressure introduced by W61-hierarchy-edit (+103 LOC) and unblocks the queued `W61-edit-column` + `W61-edit-references` bundle + `W61-per-section-filter` polish wave. Zero behavioural risk — no JSX, prop semantics, or runtime behaviour changes; every `data-testid` preserved verbatim. No new tests (the underlying pure helpers `validateLabel` / `validateDescription` / `validateExpression` / `validateCurrencyCode` / `isMeaningfulChange` / `getSourceBadgeLabel|Tooltip|Variant` / `countEntriesBySource` / `filterEntriesBySource` are already pinned by their respective W61 helper-test files). Second instance of the host-pre-extract-at-1500-loc pattern after W61-detail-extract (2026-05-20); now codified as new convention [`docs/conventions/host-pre-extract-at-1500-loc.md`](../conventions/host-pre-extract-at-1500-loc.md). See `docs/WAVES.md` for full entry.
- Wave W61-hierarchy-edit (2026-05-21) — pure-client UI wave that closes the last gap on the W61 admin-edit surface by giving `SemanticHierarchy.levels` an inline-edit modal. No schema change — the existing `semanticHierarchySchema.levels` regex + bounds (`z.array(z.string().min(1).max(80).regex(SNAKE_CASE)).min(2).max(8)` at [`server/shared/schema.ts`](../../server/shared/schema.ts)) are mirrored client-side by the new [`semanticModelHierarchyLevels.ts`](../../client/src/pages/Admin/lib/semanticModelHierarchyLevels.ts) module's exported constants (`SNAKE_CASE_LEVEL_RE` matching the server regex byte-for-byte; `MAX_LEVEL_NAME_LENGTH = 80`; `MIN_LEVELS = 2`; `MAX_LEVELS = 8`) — same cross-runtime-boundary local-mirror precedent as W61-add-client's `validateName` mirror of the server's `SNAKE_CASE` regex. Drift surfaces at the call site as a server 400 (correctness-preserving). The edit routes through the existing W61-save PATCH path (no dedicated `/hierarchies/:name` endpoint per the brief — the full model is small enough that wholesale-replace is acceptable), so the W61-cache-invalidate version-bump hook fires automatically on every successful levels edit. The wave introduces no new server schema, no new client-server envelope, no new endpoint — just helpers + a sibling component + host wire-up — but the schemas subsystem changelog still records it per the W61 every-wave-touches-schemas convention. See `docs/WAVES.md` for full entry.
- Wave W61-detail-schema (2026-05-21) — server-only foundation wave that widens the `GET /admin/semantic-models/:sessionId` detail-endpoint envelope with a `datasetSchema: { columns: { name, type }[] } | null` field projected from `doc.dataSummary?.columns`. No new zod schema — the existing `dataSummarySchema.columns` array (in [`server/shared/schema.ts`](../../server/shared/schema.ts)) is consumed verbatim; the wave just exports two new TS interfaces (`AdminSemanticModelDatasetColumn` + `AdminSemanticModelDatasetSchema`) declared inline in [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts) and the matching client mirrors in [`client/src/lib/api/admin.ts`](../../client/src/lib/api/admin.ts) per the W61 cross-runtime-boundary local-interface-mirror convention. The projection is a deliberate subset of `DataSummary.columns[]` — only `{ name, type }` make the wire payload — because the full DataSummary column shape carries `sampleValues` / `topValues` / `dateRange` / `temporalFacet*` / `indicator` / `answersQuestions` fields that the future picker UIs don't need; a strict `Object.keys(col).sort() === ["name", "type"]` test pins the deliberate slimness so a future widening that re-bloats the envelope surfaces immediately (load-bearing because `sampleValues` could carry sensitive cell data, and a 60-column dataset's full DataSummary column shape with `sampleValues` is ~50 KB vs the projection's ~2 KB). New exported pure helper `projectDatasetSchema(doc: Pick<ChatDocument, "dataSummary">): AdminSemanticModelDatasetSchema | null` returns `null` for both undefined-dataSummary and empty-columns cases so the consumer has a single null check. A dedicated controller test pins that `null` JSON-serialises distinctly from `undefined` (the field key MUST appear in the response body even when the value is null, so the client can distinguish "no schema available for this session" from "field missing on a stale-server response"). See `docs/WAVES.md` for full entry.
- Wave W61-cache-invalidate (2026-05-21) — server-only observability + contract-pin wave that codifies the "version bump = invalidate" contract across every semantic-model mutation path. No schema change — the existing `semanticModelSchema` and the `chatDocument.semanticModel.version` field (a `z.number()` already present on the model since W56) are consumed verbatim; nothing on the schemas subsystem moved. New pure module [`server/lib/semantic/semanticModelInvalidate.ts`](../../server/lib/semantic/semanticModelInvalidate.ts) exports the `SemanticModelInvalidationEvent` interface (a typed envelope, NOT a zod schema — declared inline because the hook payload is internal and never round-trips across the runtime boundary; widening to a zod schema would buy nothing because there's no parse step) and the `SemanticModelInvalidator` callback type. The four [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts) mutation paths (`patchSemanticModel` / `revertSemanticModel` / `deleteSemanticModelEntry` / `addSemanticModelEntry`) each insert `onSemanticModelVersionBumped({ sessionId, priorVersion: nextVersion - 1, nextVersion })` immediately after the `_updater(doc)` write resolves; the existing version-bump logic in each function is untouched (`const nextVersion = (doc.semanticModel.version ?? 0) + 1;` still computes the next monotone integer from `semanticModel.version`). New convention codified in [`docs/conventions/version-bump-invalidator-hook.md`](../conventions/version-bump-invalidator-hook.md): every code path that bumps `semanticModel.version` MUST call the hook from inside its `withSessionWriteLock` callback AFTER the persist resolves; error paths must NOT fire the hook. The convention is load-bearing because the future W64 cache-key wave will key on `(sessionId, semanticModel.version)` and depend on every mutation participating in the bump — a future contributor adding a W61-edit-column / W61-edit-references / W61-hierarchy-edit mutation path that forgets the hook would silently regress the contract; the new controller integration test file's "every mutation path fires the hook on success and not on any error path" coverage is the canary. See `docs/WAVES.md` for full entry.
- Wave W61-references-dashboards (2026-05-21) — server-side foundation wave that widens the W61 references scanner to also count tiles across the session-owner's dashboards. No schema change — the existing chart-spec schemas (v1 `chartSpecSchema` + v2 `chartSpecV2Schema`) are consumed verbatim by the new [`server/lib/semantic/semanticModelDashboardReferences.ts`](../../server/lib/semantic/semanticModelDashboardReferences.ts) module via the existing v1/v2 field walkers (`countReferencesInChartSpec` / `countReferencesInChartSpecV2`) and the `isChartSpecV2` runtime discriminator. The `Dashboard` + `DashboardSheet` types are imported from [`server/shared/schema.ts`](../../server/shared/schema.ts) as type-only references for the defensive guards (`Partial<Dashboard>` cast against the `ReadonlyArray<unknown>` input). [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts) widens the local `AdminSemanticModelReferencesResponse` interface with two new fields (`dashboardCount` / `dashboardTileCount`); not a zod schema — this is a typed envelope declared inline in the controller, mirrored client-side in [`client/src/lib/api/admin.ts`](../../client/src/lib/api/admin.ts) per the W61 cross-runtime-boundary local-interface-mirror convention (the client doesn't import the server type; it declares structurally identical fields locally so runtime drift surfaces as field-undefined access at the call site rather than compile errors). The widening preserves byte-compatibility with prior clients — the old envelope was a strict subset of the new one — so the rollout doesn't need coordination. New "one injectable per Cosmos container" convention codified in [`docs/conventions/injectable-per-cosmos-container.md`](../conventions/injectable-per-cosmos-container.md): chat docs use `_detailFetcher`, dashboards use the new `_dashboardListerForUser`, future shared-dashboards / past-analyses / shared-analyses read-paths get their own injectables rather than overloading existing ones. See `docs/WAVES.md` for full entry.
- Wave W61-add-client (2026-05-21) — pure-client UI wave that closes the W61 additive-edit loop end-to-end. No schema change — the existing `semanticMetricSchema` / `semanticDimensionSchema` / `semanticHierarchySchema` are consumed via the W61-add-server endpoint shipped the same day; the client constructs the per-kind entry payload via inline object literals (no new client-side schema imports beyond the existing type imports for `SemanticMetric` / `SemanticDimension` / `SemanticHierarchy`). New `validateName` validator in [`semanticModelEditValidation.ts`](../../client/src/pages/Admin/lib/semanticModelEditValidation.ts) mirrors the server's `SNAKE_CASE` regex `/^[a-z][a-z0-9_]*$/` + 1-80 length bounds — duplicated literal because importing the regex across the runtime boundary isn't possible and the bounds are small / stable enough that drift is correctness-preserving (round-trips to a 400 instead of catching client-side). New `NameAlreadyExistsError extends Error` class in [`client/src/lib/api/admin.ts`](../../client/src/lib/api/admin.ts) is the first non-generic-Error variant in the W61 API helper family — pattern would promote to a documented convention if a second typed-error variant lands (e.g. a future W61-edit-rename returning 409 on rename-to-existing-name). Sibling component [`AddEntryForm.tsx`](../../client/src/pages/Admin/components/AddEntryForm.tsx) (530 LOC) uses `<Dialog>` not `<AlertDialog>` — Dialog is Radix's canonical form-modal (multi-input + primary submit); AlertDialog is confirmation-only (single message + Cancel/Confirm). The wave continues all W61 client patterns established in W61-edit-text / W61-source-badge / W61-source-filter / W61-audit-history-client / W61-delete-client (cross-runtime-boundary client interface mirror, pure-helper + sibling-component + paired test file shape, envelope-reuse of `PatchSemanticModelResponse` across save / revert / delete / add, parent-owned mutation + modal-owned draft with `e.preventDefault()` to keep the modal open during in-flight, single global write-mutation gate). 23 behavioural tests in [`semanticModelAddFormW61AddClient.test.ts`](../../client/src/pages/Admin/lib/semanticModelAddFormW61AddClient.test.ts) cover the validator's snake_case boundaries + the per-kind formatters + the hierarchy levels parser. See `docs/WAVES.md` for full entry.
- Wave W61-add-server (2026-05-21) — server-only foundation for the future W61-add-client UI. No schema change — the existing `semanticMetricSchema` / `semanticDimensionSchema` / `semanticHierarchySchema` are imported into [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts) and used to validate request bodies on the new `POST /admin/semantic-models/:sessionId/entries/:kind` endpoint per kind, but no new schema is declared. New `AddResult` discriminated union sentinel (`ok` / `session_not_found` / `semantic_model_not_inferred` / `name_already_exists`) maps inside-the-lock outcomes to HTTP codes — same precedent as `PatchResult` / `RevertResult` / `DeleteResult`; the novel `name_already_exists` variant surfaces as 409 Conflict (the first non-404 client-error variant in the W61 endpoint family — collision semantic vs absence semantic; the 409 envelope includes `{ sessionId, kind, name }` echo so the client can render a typed "this name is already taken" message). Handler shape mirrors W61-delete-server: audit-write-before-mutation inside the existing `withSessionWriteLock` (per invariant #9) so "undo this add via revert" works (the audit snapshot captures the pre-add state); bumps `version` monotonically for W64 cache key invalidation (W64's compiled-query cache keys on version; even a new metric that no existing chart references could affect future compilation, so bumping makes the cache miss explicit); returns the W61-save envelope `{ sessionId, lastUpdatedAt, model }` byte-identical to save / revert / delete so the future client mutation reuses the existing success handler. Kind-appropriate body schema picked via ternary (not `Record<kind, schema>`) to preserve per-kind type narrowing for the cast inside the lock; the three zod schemas have different inferred entry types (`SemanticMetric` vs `SemanticDimension` vs `SemanticHierarchy`) and a `Record` lookup would resolve to a union that the `.some((m) => m.name === entry.name)` line would have to re-narrow. **Cross-kind name collisions ARE allowed** (metric "x" + dimension "x" coexist) — the uniqueness check is scoped to the kind's own collection because the planner addresses entries via `{kind, name}` not `{name}` alone, and the SemanticModel schema doesn't forbid the collision across collections. A dedicated load-bearing test pins this (metric "revenue" exists; POST /entries/dimension with body.name === "revenue" → 200, both coexist in the model). Source-bumper skipped on both survivors (byte-identical, no edits) and the new entry (no prior version to content-diff against; the bumper's `priorByName.get(newName)` returns undefined → new-entry branch returns the entry unchanged with client-sent source preserved); schema's `.default("user")` on each source field populates new entries by default; client-sent `"domain"` preserved verbatim for pack-import flows. 18 behavioural tests in [`adminSemanticModelAddW61AddServer.test.ts`](../../server/tests/adminSemanticModelAddW61AddServer.test.ts) cover admin-gate (403), param validation (400 across sessionId / kind / body schema with the canonical allowed-values list in the envelope + zod issues array including the bad path), 404 paths (session / model — the W61-add wave can't bootstrap a model, only append to an existing one), 409 same-kind collision per kind + **cross-kind allowance as a load-bearing positive test**, 200 success per kind with W61-save envelope + version bump + survivors' source preserved (`alpha: "user"` stays user, `beta: "auto"` stays auto), audit-log integration (priorModel captures pre-add state, consecutive adds grow buffer newest-first with monotonic priorVersion for revert chain-walking), client-sent source preservation (a metric with `source: "domain"` is not overridden to "user"), and 500 with wave-specific `admin_semantic_model_add_failed` code. See `docs/WAVES.md` for full entry.
- Wave W61-delete-client (2026-05-21) — pure-client UI wave that closes the W61 destructive-op loop end-to-end. No schema change. [`client/src/lib/api/admin.ts`](../../client/src/lib/api/admin.ts) gains `AdminSemanticModelEntryKind` (local mirror of the server's exported alias — declared client-side to avoid the cross-runtime import boundary per the W61 cross-runtime-boundary convention; runtime drift surfaces as field-undefined access at the call site rather than compile errors, accepted tradeoff because the literal-union shape is small / stable), `AdminSemanticModelReferencesResponse` interface (mirrors the server's envelope), `fetchSemanticModelReferences(sessionId, entry)`, and `deleteSemanticModelEntry(sessionId, kind, name)` (returns `PatchSemanticModelResponse` because the server's delete envelope is byte-identical to W61-save / W61-audit-revert per the envelope-reuse convention). New pure module [`semanticModelDeleteConfirmation.ts`](../../client/src/pages/Admin/lib/semanticModelDeleteConfirmation.ts) ships `buildDeleteHeadline` / `buildDeleteGenericConfirmation` / `buildDeleteReferencesWarning` (returns `null` for `chartCount === 0`, singular / plural correctness at the 1 / N boundaries, subhead suppression when `totalOccurrences === chartCount`) + the `DELETE_AUDIT_LOG_REASSURANCE` constant pinning the audit-log restore-via-revert copy in one place so tests anchor it and the modal renders the literal string. New sibling component [`DeleteEntryConfirmation.tsx`](../../client/src/pages/Admin/components/DeleteEntryConfirmation.tsx) — controlled `<AlertDialog>` that owns its own references fetch via mount-scoped `useEffect` (stale-fetch guard via `cancelled` closure + `res.entry !== pendingName` belt-and-braces check) while the parent owns the delete mutation; `e.preventDefault()` on `<AlertDialogAction>` suppresses Radix's default close-on-action so the modal stays open with `deleting=true` until the parent resolves the mutation (success closes it via `setPendingDelete(null)`; failure populates `deleteError` for inline display). Host [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx) gains a shared `RowDeleteButton` presentational helper (`Trash2` lucide icon + ghost-variant `Button` + destructive accent) consumed by all three Row components, a trailing Actions column with an `sr-only` header on each of Metrics / Dimensions / Hierarchies, three new state slots (`pendingDelete` / `deletingEntry` / `deleteError`), and `handleDelete` async handler mirroring `handleRevert` — single global in-flight guard, success branch reuses the W61-save `setData` shape so the audit-history-tab's `lastUpdatedAt`-keyed effect re-fetches the buffer automatically (the just-deleted entry's snapshot lands in the buffer for revert-as-undo). The destructive-op + envelope-reuse + cross-runtime-boundary patterns established in W61-save / W61-audit-revert / W61-delete-server flow through unchanged. Host file 1,131 → 1,316 LOC (under the 1,500 sub-component-extract threshold). 17 new behavioural tests in [`semanticModelDeleteConfirmationW61DeleteClient.test.ts`](../../client/src/pages/Admin/lib/semanticModelDeleteConfirmationW61DeleteClient.test.ts) pin the singular / plural boundaries (`1 chart that references it` vs `3 charts that reference it`, subhead present iff `totalOccurrences > chartCount`), the "non-null warnings never include the literal 0" invariant, and the audit-log reassurance anchor words. See `docs/WAVES.md` for full entry.
- Wave W61-delete-server (2026-05-20) — server-only foundation for the future W61-delete-client UI. No schema change. New `DELETE /admin/semantic-models/:sessionId/entries/:kind/:name` endpoint in [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts) (handler shape mirrors W61-audit-revert: audit-write-before-mutation inside the existing `withSessionWriteLock` so "undo this delete via revert" works; bumps `version` monotonically for W64 cache key invalidation; returns the W61-save envelope `{ sessionId, lastUpdatedAt, model }` so the future client mutation reuses the existing success handler). New `deleteEntryKindSchema = z.enum(["metric", "dimension", "hierarchy"])` zod literal-union validates `:kind` at the path-param boundary; exported `AdminSemanticModelEntryKind` type alias for the future client API helper. New `DeleteResult` discriminated union sentinel (`ok` / `session_not_found` / `semantic_model_not_inferred` / `entry_not_found`) maps inside-the-lock outcomes to HTTP codes without throws — same pattern as `PatchResult` / `RevertResult`. Survivors keep their prior `source` byte-identical (no spurious bump-to-user) because the source-bumper isn't called on a delete — surviving entries are content-equal to their prior selves so running it would be a no-op and skipping is clearer about intent. **Why explicit `:kind`** — name collisions across kinds are legal (the semantic model schema doesn't forbid a metric and dimension sharing a name); requiring `:kind` in the URL prevents accidental cross-kind deletes (a dedicated test pins this: `kind=dimension` + `name=alpha` when alpha is only a metric → 404, NOT silent metric delete). 14 behavioural tests in [`adminSemanticModelDeleteW61DeleteServer.test.ts`](../../server/tests/adminSemanticModelDeleteW61DeleteServer.test.ts) cover admin-gate (403), param validation (400 across sessionId / kind / name including the allowed-values list in the 400 envelope), 404 paths (session / model / entry / cross-kind no-fuzzy), 200 success per kind, audit-log integration (priorModel captures pre-delete state; consecutive deletes grow buffer newest-first with monotonic priorVersion for revert chain-walking), and 500 with wave-specific `admin_semantic_model_delete_failed` code. See `docs/WAVES.md` for full entry.
- Wave W61-references-endpoint (2026-05-20) — server-only follow-on to W61-references-scan. No schema change. Wires the pure-function scanner into `GET /admin/semantic-models/:sessionId/references?entry=<name>` returning `{ sessionId, entry, chartCount, totalOccurrences }`. New exported `AdminSemanticModelReferencesResponse` interface in [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts); new handler shape mirrors `getSemanticModel` / `getSemanticModelAuditLog` (admin-gate 403 → sessionId trim 400 → `entry` query-param coercion + trim 400 → re-uses `_detailFetcher` → 404 on session / model not-found → `countSemanticModelReferences(entry, doc.charts ?? [])` → envelope → 500 with `admin_semantic_model_references_failed`). Walks `doc.charts[]` only (not `doc.messages[].charts[]` — would double-count given the chat-model dedup-merge; not blob-stored `chartReferences[]` — would turn a free in-memory count into a multi-second multi-request fetch). 12 behavioural tests in [`adminSemanticModelReferencesW61ReferencesEndpoint.test.ts`](../../server/tests/adminSemanticModelReferencesW61ReferencesEndpoint.test.ts) — admin gate, param validation (including `?entry=a&entry=b` → 400 since Express's qs parser turns repeated keys into arrays and the handler rejects them rather than silently picking one), 404 paths, 200 envelope with server-trimmed entry echo, undefined-charts coerce-to-empty, 500 path with `console.error` capture. See `docs/WAVES.md` for full entry.
- Wave W61-references-scan (2026-05-20) — server-only foundation for the upcoming W61-delete-entry confirmation. No schema change. New pure module [`server/lib/semantic/semanticModelReferences.ts`](../../server/lib/semantic/semanticModelReferences.ts) (179 LOC) exports `SemanticModelReferenceCount` (`{ chartCount, totalOccurrences }`) and three counter functions: `countReferencesInChartSpec` walks v1 fields (`x` / `y` / `z` / `seriesColumn` / `y2` / `y2Series[]` / `_agentProvenance.{columnsUsed,rangeFilters}`); `countReferencesInChartSpecV2` walks every `.field` location on the v2 encoding (13 channels + `y2Series[].field` + `tooltip[].field` + opacity-channel-only) plus the transform discriminated union's identifier-bearing branches (`aggregate.{groupby,ops}` / `fold.fields[]` / `bin.field` / `window.{ops,groupby,sort}` / `regression.on` — `filter.expr` + `calculate.expr` are deliberately skipped to avoid SQL-substring noise); and the top-level `countSemanticModelReferences(name, charts)` accepts `ReadonlyArray<unknown>` and discriminates v1 vs v2 at runtime via [`isChartSpecV2`](../../server/shared/schema.ts) so the planned v1→v2 chart migration doesn't break the scanner. Exact-identifier equality only (no substring / fuzzy / case-folding); empty `name` short-circuits to zero at three layers. 19 behavioural tests in [`semanticModelReferencesW61ReferencesScan.test.ts`](../../server/tests/semanticModelReferencesW61ReferencesScan.test.ts). Reads chart-schema fields but does not change the schema itself — listed here because the schemas subsystem is the closest map for a pure-function reader of chart structure. See `docs/WAVES.md` for full entry.
- Wave W61-detail-extract (2026-05-20) — pure-client refactor. No schema change. Sibling-file extraction of `SourceFilterChips` + `AuditHistoryCard` (collapsible audit-history Card) out of [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx) into new sibling files under [`client/src/pages/Admin/components/`](../../client/src/pages/Admin/components/) (`SourceFilterChips.tsx` 76 LOC + `AuditHistoryCard.tsx` 166 LOC). Host drops 1,295 → 1,131 LOC (−164 LOC); relieves file-size pressure before the projected W61-add-delete wave (+~300 LOC) would push past the 1,500 sub-component-extract threshold. Zero behavioural risk — no JSX, prop semantics, or runtime behaviour changes. Every `data-testid` preserved verbatim. No new tests (the underlying pure helpers were already pinned by W61 helper-test files). See `docs/WAVES.md` for full entry.
- Wave W61-audit-history-tab (2026-05-20) — pure-client UI mount that consumes the W61-audit-history-client helpers + the W61-audit-history-api / W61-audit-revert server endpoints. No schema change. [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx) gains a fourth `<Card>` under Hierarchies wrapped in `Collapsible open={historyOpen}` (closed by default — the audit-log fetch is opt-in). The new effect is keyed on `[sessionId, historyOpen, data?.lastUpdatedAt]` so save / revert bumps refresh the open list automatically without explicit cache-busting at every mutation site. Per-row Revert handler runs `buildRevertConfirmation` through `window.confirm`, calls `revertSemanticModel`, and updates `data` with the response envelope — byte-identical to the PATCH-success path because W61-audit-revert deliberately reuses `PatchSemanticModelResponse`. Per-row `reverting: number | null` index keeps non-clicked rows readable while one revert is in flight (`withSessionWriteLock` makes concurrent attempts safe per invariant #9). Disabled triple gate `saving || reverting !== null || historyLoading` on every Revert button prevents racing the existing PATCH save path, another revert in flight, or the audit-log refetch landing. Test-IDs (`admin-semantic-model-audit-history-{trigger,loading,error,empty,list,revert-error,revert-N}`) wired throughout. +197 LOC to the host file (1,098 → 1,295 LOC; under the 1,500 sub-component-extract threshold). No new tests — the helpers were already pinned by 15 W61-audit-history-client unit tests. See `docs/WAVES.md` for full entry.
- Wave W61-audit-history-client (2026-05-20) — pure-client follow-on to W61-audit-history-api + W61-audit-revert. [`client/src/lib/api/admin.ts`](../../client/src/lib/api/admin.ts) gains `AdminSemanticModelAuditEntry` + `AdminSemanticModelAuditLog` interfaces (client mirror of the server's `SemanticModelAuditEntry` shape; local declaration to avoid the cross-runtime import boundary — mirrors the W61-detail / W61-save inline-import pattern for `SemanticModel`), `fetchSemanticModelAuditLog(sessionId)`, and `revertSemanticModel(sessionId, auditEntryIndex)` (re-uses the existing `PatchSemanticModelResponse` type because the server envelope is byte-identical to W61-save's). New pure module [`semanticModelAuditHistory.ts`](../../client/src/pages/Admin/lib/semanticModelAuditHistory.ts) ships `formatAuditTimestamp` (UTC-stable for multi-TZ forensics), `buildAuditEntrySummary` (`{ headline: "Save N of M", subhead: "{savedBy} · {ts} · was v{priorVersion}" }` with `saveNumber = total - indexFromNewest` so newest entry reads `"Save M of M"`), and `buildRevertConfirmation` (`window.confirm()` prompt with `\n\n` paragraph break separating action from the undo-safety note). No schema change — the client mirror is structural-only and stays in lockstep with the server via runtime undefined-field detection. See `docs/WAVES.md` for full entry.
- Wave W61-audit-revert (2026-05-20) — server-only follow-on to W61-audit-history-api that closes the audit-trail server loop (write → read → revert). New `POST /admin/semantic-models/:sessionId/revert` body schema `revertBodySchema = z.object({ auditEntryIndex: z.number().int().nonnegative() })` (zod) validates at the boundary; upper-bound check against `log.length` happens inside `withSessionWriteLock` so it sees the authoritative state. New `RevertResult` discriminated union adds a fourth `audit_entry_not_found` variant alongside `ok` / `session_not_found` / `semantic_model_not_inferred`. Response envelope `{ sessionId, lastUpdatedAt, model }` mirrors W61-save's so the future client `useMutation` reuses the same success handler. **`nextModel = { ...entry.priorModel, version: current+1, updatedAt, updatedBy }`** — version stays monotonic (W64 compiled-query cache key), about-to-be-overwritten model appended to the audit log so "undo this revert" works. **W61-source-bump is NOT run on revert** — per-entry `source` field comes verbatim from the snapshot ("restore as-was" semantics, not "edit"). See `docs/WAVES.md` for full entry.
- Wave W61-audit-history-api (2026-05-20) — server-only follow-on to W61-audit-log that exposes the prior-model ring buffer over a dedicated read-only endpoint `GET /admin/semantic-models/:sessionId/audit-log`. New envelope [`AdminSemanticModelAuditLogResponse`](../../server/controllers/adminSemanticModelController.ts) = `{ sessionId, entries: SemanticModelAuditEntry[] }` with entries newest-first (`[]` when the session has a model but no PATCHes yet — coalesced at the controller boundary so callers don't need `?? []`). Handler shape mirrors `getSemanticModel`: same admin gate / sessionId trim / `_detailFetcher` shim / 404 codes (`session_not_found`, `semantic_model_not_inferred`) / wave-specific 500 code (`admin_semantic_model_audit_log_failed`). Re-uses the existing `_detailFetcher` injectable (audit log is a field on the same `ChatDocument`, not a separate Cosmos collection). The endpoint surfaces `SemanticModelAuditEntry` directly with no projection — the full `priorModel` snapshot lets the future revert UI re-PATCH in one round-trip without a second fetch. Pairs with the upcoming history-tab UI (W61-audit-history-ui) and one-call revert endpoint (W61-audit-revert). See `docs/WAVES.md` for full entry.
- Wave W61-audit-log (2026-05-20) — server-only follow-on that adds a prior-model audit trail on every admin PATCH. `ChatDocument` gains optional `semanticModelAuditLog?: SemanticModelAuditEntry[]` sibling to `semanticModel`, populated by [`patchSemanticModel`](../../server/controllers/adminSemanticModelController.ts) inside the existing `withSessionWriteLock` callback (per invariant #9) **before** the `doc.semanticModel = nextModel` overwrite so the snapshot captures the prior state. The entry interface (`savedAt` / `savedBy` / `priorVersion` / `priorModel`) and the ring-buffer helper live in the new pure module [`server/lib/semantic/semanticModelAuditLog.ts`](../../server/lib/semantic/semanticModelAuditLog.ts) with the cap exported as `SEMANTIC_MODEL_AUDIT_LOG_MAX_ENTRIES = 10` (chosen so 10 × ~50 KB worst-case SemanticModel snapshots ≈ 500 KB fits comfortably under the 2 MB Cosmos doc limit). No zod schema mirror needed — `ChatDocument` is a plain TS interface per the W57 precedent. The audit-write piggybacks on the existing per-session mutex so the entry-and-overwrite pair is atomic at the per-session level (single-instance correctness only; multi-instance horizontal scaling still needs Cosmos `ifMatch` ETag). The new "audit-style ring buffer on `ChatDocument`" pattern is codified in [`docs/conventions/audit-log-ring-buffer.md`](../conventions/audit-log-ring-buffer.md). See `docs/WAVES.md` for full entry.
- Wave W61-filter-persist (2026-05-20) — pure-client follow-on that persists the W61-source-filter active chip to the URL via `?filter=X`. New pure module [`semanticModelFilterUrlSync.ts`](../../client/src/pages/Admin/lib/semanticModelFilterUrlSync.ts) ships `readFilterFromSearch(search)` / `writeFilterToSearch(search, filter)` — string-in / string-out so testable without jsdom. Validation is case-sensitive (URL params are byte-stable in real use) and falls back to `"all"` on invalid / missing / wrong-case; the `"all"` default REMOVES the param entirely so the URL stays clean. Helpers return strings without a leading `?` so the caller controls the empty-vs-prefix semantics. Wired into the page via a lazy `useState` initializer + `replaceState`-on-change to avoid browser-history accumulation. Zero schema change — the URL contract is local to the admin viewer. See `docs/WAVES.md` for full entry.
- Wave W61-source-filter (2026-05-20) — pure-client follow-on that layers a per-source filter chip row above each table in [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx). New pure module [`semanticModelSourceFilter.ts`](../../client/src/pages/Admin/lib/semanticModelSourceFilter.ts) exports `SemanticEntryFilter = "all" | SemanticEntrySource` (sentinel-extends the W61-source-badge type without re-declaring the source values), `filterEntriesBySource` (reference-identity preservation on `"all"` to avoid allocation on the most-common case), `countEntriesBySource` (single-pass per-source counts + the `all = entries.length` slot for the chip-row labels), and `SOURCE_FILTER_ORDER` with `"user"` second in display order (workflow-optimised — most-clicked filter at the leftmost reachable position after `All`). Zero schema change — the filter is a read-only render over the existing `source` field; the helper module is the chip-row's data layer. The `{T extends { source: SemanticEntrySource }}` generic constraint lets `SemanticMetric` / `SemanticDimension` / `SemanticHierarchy` flow through without per-type duplication. See `docs/WAVES.md` for full entry.
- Wave W61-source-badge (2026-05-20) — pure-client follow-on that surfaces the per-entry `source` field as a colored chip in [`AdminSemanticModelDetail.tsx`](../../client/src/pages/Admin/AdminSemanticModelDetail.tsx). New pure module [`semanticModelSourceBadge.ts`](../../client/src/pages/Admin/lib/semanticModelSourceBadge.ts) maps the `SemanticEntrySource = "auto" | "user" | "domain"` local alias to label / Badge variant / tooltip via three frozen `Readonly<Record<SemanticEntrySource, …>>` lookups. The alias is intentionally local (not re-exported from `@/shared/schema`) so a future split (e.g. metric_source vs dimension_source diverging) forces a deliberate sync. Zero schema change — the chip is a read-only render of the existing `source` field that W57 / W61-save / W61-source-bump already populate. The `Readonly<Record<…>>` keying gives the TypeScript type checker a single shape to widen against when a fourth source value lands. See `docs/WAVES.md` for full entry.
- Wave W61-source-bump (2026-05-20) — closes the W61 source-weighting gap. Adds a per-entry content-hash diff in [`server/lib/semantic/semanticModelSourceBump.ts`](../../server/lib/semantic/semanticModelSourceBump.ts) that the W61-save controller invokes inside `withSessionWriteLock` between the `safeParse` and the version stamp. Unchanged entries preserve prior `source` (auto / domain / user); changed entries bump to `"user"`; new entries preserve client-sent source. Content hashes project each entry to its content fields (everything except `source`) so source-only diffs don't trigger the bump. No schema change — `SemanticMetric.source` / `SemanticDimension.source` / `SemanticHierarchy.source` are unchanged; only their write-path semantics on PATCH evolved. See `docs/WAVES.md` for full entry.
- Wave W61-edit-enums (2026-05-20) — second polish wave on the W61 trilogy. Adds an `EditableSelect` UI on top of the W61-edit-text validators with three frozen option lists declared via `as const satisfies ReadonlyArray<{ value: T; label: string }>` against `SemanticMetric["format"]` / `SemanticDimension["kind"]` literal types. The compile-time `satisfies` guard catches enum-value drift between the admin UI and the zod schema at `tsc --noEmit` time rather than at runtime PATCH-time. New `validateCurrencyCode` (`/^[A-Z]{3}$/` after trim; empty allowed for the optional field) joins the existing label / description / expression / isMeaningfulChange validators in [`client/src/pages/Admin/lib/semanticModelEditValidation.ts`](../../client/src/pages/Admin/lib/semanticModelEditValidation.ts). The format/currencyCode and kind/temporalGrain cross-field coupling is enforced server-side by `semanticModelSchema.safeParse`, with the orphan field cleared client-side at the parent handler boundary so the planner manifest doesn't render misleading `currency (USD)` hints on a percent metric. See `docs/WAVES.md` for full entry.
- Wave W61-edit-text (2026-05-20) — first polish wave on the W61 trilogy. Adds client-side per-field validators in [`client/src/pages/Admin/lib/semanticModelEditValidation.ts`](../../client/src/pages/Admin/lib/semanticModelEditValidation.ts) that mirror `semanticMetricSchema.label.min(1).max(120)` / `.description.max(1000)` / `.expression.min(1).max(2000)` bounds. The expression validator adds three checks beyond the schema's free-text `z.string().min(1).max(2000)`: no semicolons (multi-statement guard), no SQL comments (`--` / `/*`), no top-level `SELECT` / `FROM` / `JOIN` / `WHERE` / `GROUP BY` / `ORDER BY` / `UNION` keywords (padded-uppercase scan so `SUM(selector_count)` is NOT rejected). The schema itself is unchanged — these are a cheap obvious-broken filter ahead of the W58 compiler's authoritative parse; if the schema bounds change, a drift here just routes the save to the server's authoritative rejection. No new schema mirror needed. See `docs/WAVES.md` for full entry.
- Wave W61-save (2026-05-20) — admin PATCH endpoint adds `PATCH /admin/semantic-models/:sessionId` body validation via `semanticModelSchema.safeParse` in [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts). Server overrides three schema fields after parse: `version: prior + 1`, `updatedAt: new Date().toISOString()`, `updatedBy: getAuthenticatedEmail() ?? "unknown"`. Client-sent values for those keys are ignored (a client-set version would let an admin bump backwards or jump forward; the W64 compiled-query cache reads `version` as cache key so monotonic bump is load-bearing). RMW routes through [`withSessionWriteLock`](../../server/lib/sessionWriteLock.ts) per invariant #9 — single-instance correctness only; multi-instance horizontal scaling needs Cosmos `ifMatch` ETag separately. `__setSemanticModelUpdaterForTesting` injectable shim completes the trio (lister / detailFetcher / updater) for route tests. See `docs/WAVES.md` for full entry.
- Wave W61-detail (2026-05-20) — admin detail endpoint adds `AdminSemanticModelDetailResponse` envelope (`{ sessionId, fileName, username, lastUpdatedAt, model: SemanticModel }`) in [`adminSemanticModelController.ts`](../../server/controllers/adminSemanticModelController.ts). Client mirrors via `import("@/shared/schema").SemanticModel` inline type in [`lib/api/admin.ts`](../../client/src/lib/api/admin.ts) so the API helper stays free of structural schema deps. No zod schema change — `SemanticModel` is consumed verbatim from the W56 foundation. See `docs/WAVES.md` for full entry.
- Wave W61-list (2026-05-20) — `chat.model.ts` exports a new lightweight `AdminSemanticModelListEntry` (index-row projection over `ChatDocument.semanticModel`) + `ADMIN_SEMANTIC_MODEL_LIST_SELECT` (the Cosmos SELECT) + `finalizeAdminSemanticModelEntry` (pure coercion) + `getAllSessionsWithSemanticModel` (network boundary). The projection cherry-picks root fields + nested `semanticModel.{version,name,updatedAt,updatedBy}` + per-collection counts via the defensive `IIF(IS_DEFINED ... AND IS_ARRAY, ARRAY_LENGTH, 0)` triple. `WHERE IS_DEFINED(c.semanticModel)` filters pre-W57 docs; `ORDER BY c.lastUpdatedAt DESC` surfaces the active sessions. Powers the admin index page at `/admin/semantic-models` (first of three W61 sub-waves; W61-detail surfaces the full payload via `formatMetricCatalog`; W61-save lands the PATCH endpoint). The `enableCrossPartitionQuery: true` FeedOption was deliberately omitted because Cosmos SDK v4 dropped it from `FeedOptions` and including it would have added a 6th instance of the existing `TS2769` overload baseline (lines 583/979/1063/1493/1762). See `docs/WAVES.md` for full entry.
- Wave W57 (2026-05-16) — `ChatDocument` gains optional `semanticModel?: SemanticModel` field in [server/models/chat.model.ts](../../server/models/chat.model.ts) (ChatDocument is a TS interface, not a zod schema, so no mirror needed). Populated at the upload understanding-ready checkpoint via [`server/lib/semantic/inferModel.ts`](../../server/lib/semantic/inferModel.ts).
- Wave W56 (2026-05-16) — Semantic & metrics layer type foundation. Adds `semanticMetricSchema`, `semanticDimensionSchema`, `semanticHierarchySchema`, `semanticModelSchema` to [server/shared/schema.ts](../../server/shared/schema.ts). Auto-mirrored to client via the W5 re-export. Foundation for W57–W64 per the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md). See `docs/WAVES.md` for full entry.
- Initial seed of this doc.

**Caveat — out-of-date description above.** The "every schema defined twice" line at the top of this doc predates Wave W5, which collapsed the client mirror into a single re-export (`export * from "../../../server/shared/schema"` in [client/src/shared/schema.ts](../../client/src/shared/schema.ts)). The drift gate is no longer load-bearing; manual mirroring is no longer possible. Refresh in a future doc-hygiene wave.
