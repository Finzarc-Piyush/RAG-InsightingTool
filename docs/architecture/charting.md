# Charting — behavior contract & v2 architecture

This doc has two roles:

1. **Behavior contract** for the legacy
   [client/src/pages/Home/Components/ChartRenderer.tsx](../../client/src/pages/Home/Components/ChartRenderer.tsx)
   (1,820 LOC). It enumerates every prop, state, special case, and
   downstream side effect — the parity checklist that gates Phase 9.4
   deletion.
2. **Forward architecture** for the v2 charting layer (`<PremiumChart>`,
   `<ChartCanvas>`, `<ChartGrid>`, `<ChatChartCard>`,
   `<RawDataProvider>`, ChartSpecV2 grammar). Filled in incrementally
   as waves land.

The full plan and wave breakdown lives in
`/Users/tida/.claude/plans/are-2-things-ever-deep-duckling.md`.

---

## 1. Behavior contract: legacy `ChartRenderer.tsx`

### 1.1 Public props (`ChartRendererProps`)

Defined as the `ChartRendererProps` interface in [ChartRenderer.tsx](../../client/src/pages/Home/Components/ChartRenderer.tsx) (grep the symbol, or look it up in `docs/index/symbols.generated.tsv` — line numbers move).

| Prop | Type | Purpose | v2 parity requirement |
|------|------|---------|---|
| `chart` | `ChartSpec` (v1) | Spec to render. | ChartShim adapter converts v1 → v2 transparently. |
| `index` | `number` | Position in a multi-chart row (used for color cycling and aria labels). | `<PremiumChart>` accepts an `index` for the same purposes. |
| `isSingleChart` | `boolean?` | When `true`, disables IntersectionObserver lazy-render and uses a larger height (400px vs 250px). | `<PremiumChart>` prop `isSingleChart` keeps both behaviors. |
| `showAddButton` | `boolean?` | Controls visibility of the "Add to dashboard" button in the chart header. | Preserved on `<ChatChartCard>` and `<ChartCanvas>` headers. |
| `useChartOnlyModal` | `boolean?` | Switches the expand-on-click modal between [ChartModal](../../client/src/pages/Home/Components/ChartModal.tsx) (rich) and [ChartOnlyModal](../../client/src/pages/Dashboard/Components/ChartOnlyModal.tsx) (pure). | `<PremiumChart>` `expandModal: 'rich' \| 'only'`. |
| `fillParent` | `boolean?` | When `true`, chart fills its parent container (used by dashboard tiles). Disables lazy-render and compact X-tick mode. | `<PremiumChart>` prop `fillParent` keeps both side-effects. |
| `enableFilters` | `boolean?` | Surfaces the per-chart filter UI (categorical filter chips below the chart). Excluded for line/bar/area when seriesKeys present (legend handles series visibility). | `<FilterChips>` component, only rendered for non-series-multi charts unless explicit `enableFilters`. |
| `filters` | `ActiveChartFilters?` | Controlled filter state. | `<ChartCanvas>` owns this state when uncontrolled; `<ChatChartCard>` passes through if controlled. |
| `onFiltersChange` | `(f: ActiveChartFilters) => void?` | Callback when filters change. | Same. |
| `isLoading` | `boolean?` | Loading state, used by correlation charts during background computation. | Preserved verbatim. |
| `loadingProgress` | `{ processed, total, message? }?` | Progress info for long-running correlation chart loads. | Surfaced as `<SkeletonState progress={…}>` in `<PremiumChart>`. |
| `keyInsightSessionId` | `string \| null?` | Enables on-demand "Key Insight" LLM fetch when the chart has no inline insight, triggered from inside `<ChartModal>`. | `<PremiumChart>` accepts `keyInsightSessionId` and forwards to its modal. Must call same backend route. |

### 1.2 Internal state ([line 172+](../../client/src/pages/Home/Components/ChartRenderer.tsx))

| State | Initial | Used by | v2 parity |
|-------|---------|---------|-----------|
| `isModalOpen` | `false` | Click-to-expand on the chart header. | Same in `<PremiumChart>`. |
| `isDashboardModalOpen` | `false` | "Add to dashboard" button. | Same. |
| `hideOutliers` | `false` | Scatter mark only — toggle in the chart header to hide IQR-detected outliers. | Preserved in scatter renderer; layer toggle in `<LayersPanel>` for `<ChartCanvas>`. |
| `pointOpacity` | `'medium'` | Scatter mark — three-step opacity slider in `<ChartModal>`. | Preserved in scatter renderer settings. |
| `pointDensity` | `'medium'` | Scatter mark — four-step density slider (`low`/`medium`/`high`/`all`) controlling `MAX_RENDER_POINTS`. | Preserved, with same enum values. |
| Legend hidden-series set | (via callback) | Legend toggle (single + toggle-all). | `<ChartLegend>` props `onToggleSeries` and `onToggleAll`. |

### 1.3 Constants

| Constant | Value | Where | v2 parity |
|----------|------:|-------|-----------|
| `MAX_COMPACT_X_TICKS` | `6` | [line 75](../../client/src/pages/Home/Components/ChartRenderer.tsx) | Same constant in `<PremiumChart>` axis config. |
| `LINE_AREA_MAX_X_TICKS` | imported from shared | Used for line/area charts in non-fill mode. | Same. |
| Outlier IQR threshold | `0.1` (paddingFraction default) | [line 201](../../client/src/pages/Home/Components/ChartRenderer.tsx) | Constant in `dataEngine.ts:detectOutliers`. |
| `MAX_RENDER_POINTS` per density | dynamic (`getMaxRenderPoints()`) | [line 437](../../client/src/pages/Home/Components/ChartRenderer.tsx) | Ported into `dataEngine.ts:sample`. |

### 1.4 Per-mark behaviors

#### `bar`

- **Compact X-axis** when `!fillParent && !isSingleChart && chartData.length > MAX_COMPACT_X_TICKS`
  ([line 448](../../client/src/pages/Home/Components/ChartRenderer.tsx)). Picks the
  first 6 categories or the largest 6 by value.
- **Date-detection in compact mode** ([line 454-466](../../client/src/pages/Home/Components/ChartRenderer.tsx)):
  if X looks like dates, takes a chronological slice; otherwise sorts by Y desc.
- **Stacked / grouped** via `chart.barLayout`. Series defined by
  `seriesColumn` + `seriesKeys`.
- **15-series "Others" merging** (per server pipeline; ChartRenderer expects
  pre-merged data). v2: do the merge in `encodingResolver.ts` so the client
  can re-do it after a filter or shelf change.

#### `line`

- **Dual-axis (Y2)** ([line 864-869](../../client/src/pages/Home/Components/ChartRenderer.tsx)):
  when `chart.y2` is set, renders a second left-axis series with a different color
  (`hsl(var(--chart-1))` for left, `chartColor` for right).
- **Multi-series** when `seriesColumn` + `seriesKeys` present; uses
  `RechartsWideLegendContent` with toggle-series and toggle-all callbacks.
- **Trend line** ([line 189](../../client/src/pages/Home/Components/ChartRenderer.tsx)):
  optional `chart.trendLine` array (two points). Rendered as overlay.
- **Dynamic Y domain** ([line 781](../../client/src/pages/Home/Components/ChartRenderer.tsx))
  via `getDynamicDomain(values, paddingFraction=0.1)`. Override with `chart.yDomain`.
- **Dot toggle** in modal — controls `showDots` flag.

#### `area`

- Same dual-axis + multi-series + dynamic domain rules as `line`.
- Stacked area is currently the only mode (no diverging stacks).

#### `scatter`

- **Outlier hiding** (state `hideOutliers`) — toggle in header.
  IQR-based detection ([line 392](../../client/src/pages/Home/Components/ChartRenderer.tsx)).
- **Stratified sampling** ([line 437-445](../../client/src/pages/Home/Components/ChartRenderer.tsx)):
  when point count > `MAX_RENDER_POINTS`, samples every Nth point and slice-caps.
  Density slider (`low`/`medium`/`high`/`all`) controls `MAX_RENDER_POINTS`.
- **Opacity slider** (`low` 0.3 / `medium` 0.6 / `high` 0.9). Used to mitigate
  overplot.
- **Trend line** support same as line.
- **Z-axis (size encoding)** for bubble-style scatter via `chart.z`.
- **Loading progress** when `_isCorrelationChart` flag present
  ([line 1727](../../client/src/pages/Home/Components/ChartRenderer.tsx)) — shows
  progress overlay while correlation matrix computes server-side.

#### `pie`

- Single-level only.
- "Others" merging server-side.
- Custom tooltip via `formatChartTooltipValue`.

#### `heatmap` ([line 1088](../../client/src/pages/Home/Components/ChartRenderer.tsx))

- **Custom HSL color scale** based on cell value vs. min/max range.
- **Custom low-to-high legend** below the chart (gradient strip with min/max
  labels).
- **Empty-state** "No numeric values for heatmap." when no Z column.
- Max 40 rows × 24 cols (downsampling beyond this happens server-side in
  [chartGenerator.ts](../../server/lib/chartGenerator.ts)).

### 1.5 Cross-cutting behaviors

#### Lazy-render via IntersectionObserver

[ChartRenderer.tsx](../../client/src/pages/Home/Components/ChartRenderer.tsx)
+ [line 1566](../../client/src/pages/Home/Components/ChartRenderer.tsx).
Renders an empty container until the chart enters viewport. Disabled when
`isSingleChart || fillParent || isLoading`. **v2 parity**: same behavior in
`<PremiumChart>` via the same hook (`useIntersectionObserver`).

#### Filter UI

[line 222](../../client/src/pages/Home/Components/ChartRenderer.tsx) +
[line 230](../../client/src/pages/Home/Components/ChartRenderer.tsx) +
[line 275](../../client/src/pages/Home/Components/ChartRenderer.tsx).

- Only enabled for `line`/`bar`/`area` when `enableFilters` is true.
- Filter chips are derived from the original (pre-aggregation) data set.
- Series keys are excluded from filter UI when `seriesColumn` is set —
  the legend handles series visibility instead.

**v2 parity**: `<FilterChips>` component takes `(rows, columns, activeFilters,
onChange)`. Logic ported from `applyChartFilters`.

#### Legend (multi-series)

[RechartsWideLegendContent](../../client/src/lib/rechartsWideLegend.tsx)
component, used at [line 851-856](../../client/src/pages/Home/Components/ChartRenderer.tsx).

Behaviors:
- Wide horizontal layout that wraps when narrow.
- Click-toggle individual series.
- "Toggle all" button (show all / hide all).
- Wide legend shows up only when `seriesKeys.length > 4` or so.

**v2 parity**: `<ChartLegend>` component with `mode: 'compact' | 'wide'`,
`onToggleSeries`, `onToggleAll` props.

#### Tooltip

`formatChartTooltipValue` and `rechartsTooltipValueFormatter` from
[client/src/lib/chartNumberFormat.ts](../../client/src/lib/chartNumberFormat.ts).
Format rules:
- Currency-detection by column name keywords.
- K/M/B suffixes.
- Date formatting via `formatDate` (date-fns).

**v2 parity**: same formatters reused from a shared module
(`client/src/lib/charts/format.ts`). Tooltip itself is a React component
(`<ChartTooltip>`) styled with semantic tokens.

#### Modal expansion

Three modal types:
- [ChartModal](../../client/src/pages/Home/Components/ChartModal.tsx) — rich
  modal with inline insights, settings sliders (scatter density/opacity), and
  optional Key Insight fetch.
- [ChartOnlyModal](../../client/src/pages/Dashboard/Components/ChartOnlyModal.tsx)
  — pure expand-only modal used by dashboard tiles.
- [DashboardModal](../../client/src/pages/Home/Components/DashboardModal/DashboardModal.tsx)
  — "Add to dashboard" button target.

**v2 parity**: `<PremiumChart>` carries a passive `keyInsightSessionId`
prop for a future "expand to modal" caller (the v2 expand modal isn't
wired yet — the prop is preserved so a flag flip doesn't lose the legacy
Key Insight feature). `<ChartCanvas>` mounts its own larger view inline
(no modal needed for editing).

#### Dynamic axis domain

`getDynamicDomain` (local helper in [ChartRenderer.tsx](../../client/src/pages/Home/Components/ChartRenderer.tsx))
adds 10% padding above/below the value range. Used when `yDomain` not
explicitly set. Returns `[min - pad, max + pad]`.

**v2 parity**: still a local helper — no separate charts/scales module
was ever extracted.

#### Series sort + topN

When `seriesKeys` is provided by the spec, ChartRenderer trusts it as the
canonical render order. Server pre-sorts and merges low-volume series into
"Others" (15-series cap).

**v2 parity**: `<EncodingResolver>` (and `dataEngine.ts:topNAndOther`) handle
this client-side now, so a filter or shelf change can re-derive the
ordering without a server roundtrip.

### 1.6 External dependencies (must be replicated or imported)

| Module | Purpose |
|--------|---------|
| `recharts` | Renderer (will be removed in Phase 9). |
| [@/lib/chartNumberFormat](../../client/src/lib/chartNumberFormat.ts) | Tooltip + axis number formatting. **Reused as-is.** |
| [@/lib/rechartsWideLegend](../../client/src/lib/rechartsWideLegend.tsx) | Wide multi-series legend. Logic re-implemented in `<ChartLegend>`. |
| [@/lib/chartFilters](../../client/src/lib/chartFilters.ts) | Filter state shape + `applyChartFilters`. **Reused as-is** (or behind `dataEngine.ts`). |
| [@/hooks/useIntersectionObserver](../../client/src/hooks/useIntersectionObserver.ts) | Lazy-render. **Reused as-is.** |
| `chartRechartsShared` | Shared Recharts config (color cycle, axis defaults). **Replaced** by `<PremiumChart>` config in v2. |
| `LINE_AREA_MAX_X_TICKS` | Constant. **Reused as-is.** |
| `formatDate` (date-fns) | Date formatting. **Reused as-is.** |
| `getDynamicDomain` (local helper) | Axis padding. Remains local to ChartRenderer — no separate `scales.ts` module exists. |

---

## 2. Parity checklist for Phase 9.4 deletion

Before deleting `ChartRenderer.tsx`, every item below must pass under
`<PremiumChart>` for **all six existing marks** with **production session
log payloads** (pulled from real chat answers / pivot panels / dashboard
tiles).

### Visual parity
- [ ] Pixel diff vs. legacy ≥ 90% similarity at default settings.
- [ ] Light theme + dark theme both pass.
- [ ] Tooltip number formatting matches (currency, K/M/B, dates).
- [ ] Axis tick count matches in both `fillParent=true` and default.
- [ ] Compact X-axis behavior at `chartData.length > 6 && !fillParent && !isSingleChart`.
- [ ] Heatmap HSL color scale + custom legend gradient.
- [ ] Pie chart "Others" merging at server cap.

### Interaction parity
- [ ] Lazy-render under IntersectionObserver (verify with off-screen chart not rendering).
- [ ] Click chart header → opens correct modal (rich vs. only) per `useChartOnlyModal`.
- [ ] "Add to dashboard" button visible per `showAddButton`.
- [ ] Filter chip toggle under `enableFilters`.
- [ ] Filter chips exclude series keys in multi-series mode.
- [ ] Multi-series legend toggle (individual + toggle-all).
- [ ] Scatter outlier toggle + IQR-based hide/show.
- [ ] Scatter density slider four-step + `MAX_RENDER_POINTS` honored.
- [ ] Scatter opacity slider three-step.
- [ ] Dual-axis line (Y2) renders both axes with distinct colors.
- [ ] Trend line overlay when `chart.trendLine` set.
- [ ] Loading progress overlay on correlation scatter.

### Data parity
- [ ] Same row count after filtering.
- [ ] Same series ordering.
- [ ] Same numeric values (no rounding regressions).
- [ ] Stratified sampling reproduces same point count for given density.

### Modal / hook parity
- [ ] Key Insight fetch works under `keyInsightSessionId` (Network tab confirms request).
- [ ] Dashboard modal opens correctly from "Add to dashboard".
- [ ] Modal scatter sliders persist state across open/close.

### Performance parity
- [ ] Render time of a 5K-row scatter not regressed.
- [ ] Bundle size delta net-positive after Recharts removal (-90KB) vs.
      Visx + ECharts gain.
- [ ] No memory leak on repeated re-renders (DevTools heap snapshot).

### Storybook gallery
- [ ] One story per mark × at least 3 production payloads.
- [ ] Side-by-side legacy vs. v2 view, with pixel-diff overlay.
- [ ] Two weeks of zero per-mark feature-flag flips before deletion (per WC9.4 risk plan).

---

## 3. Forward architecture (filled in by waves)

### 3.1 Component tree

> Filled in by WC0.3 (`<PremiumChart>` core), WC2.6 (`<ChartCanvas>`),
> WC2.7 (`<ChatChartCard>` + Fork to Explorer), WC6.2 (`<ChartGrid>`).

### 3.2 ChartSpecV2 grammar

> Filled in by WC0.2. Schema lives in
> [server/shared/schema.ts](../../server/shared/schema.ts).

### 3.3 Data engine

> Filled in by WC2.1 (`<RawDataProvider>`) and WC2.2 (`dataEngine.ts`).

### 3.4 Theme tokens

> Filled in by WC1.4 (palette CSS variables in
> [client/src/index.css](../../client/src/index.css)) and updates to
> [client/THEMING.md](../../client/THEMING.md).

### 3.5 Per-mark feature flags

> Filled in by WC0.1 (`client/src/lib/charts/featureFlags.ts`). Each mark
> defaults `false` (use legacy ChartRenderer) until the v2 renderer for
> that mark passes parity tests, then flips to `true`. Two weeks of zero
> flips before deleting the legacy renderer in WC9.4.

---

## 4. Outstanding questions surfaced during the contract audit

1. **Heatmap legend ownership**: legacy renders a custom HSL gradient
   strip below the chart. v2 should do this in `<ChartLegend>` with a
   `mode: 'gradient'` variant — confirm this works for ECharts heatmap
   too (or does ECharts heatmap own its own legend?).
2. **`_isCorrelationChart` private flag** on the spec drives the loading
   progress overlay for correlation charts. v2 should formalize this as
   a `config.loadingState: 'computing'|'sampling'|'idle'` instead of an
   underscore-prefixed flag.
3. **Scatter density slider** persists per-modal-open in legacy; v2
   should consider per-message persistence (so reopening the chart in
   the same chat turn keeps the user's setting).
4. **15-series "Others" cap** is currently server-side; moving it
   client-side (so post-filter re-aggregation can re-merge) requires
   the agent's spec to ship the *unmerged* series count or all series
   keys. Confirm with WC7.1 (visualPlanner v2 migration).

These should be resolved in WC0.2 (schema design), WC0.3 (PremiumChart
core), or escalated to the user if they change the v2 contract shape.

---

## 5. Activation knobs (Fix-6)

### 5.1 Per-mark feature flag

Every chart in chat / pivot / dashboards flows through `<ChartShim>`,
which routes to either the legacy `ChartRenderer` or the new
`<PremiumChart>` based on a per-mark feature flag. Resolution order:

1. **Build-time env**: `VITE_USE_PREMIUM_<TYPE>=true` (set in
   `client/client.env`).
2. **Runtime localStorage** (dev / QA / staged rollout):
   `localStorage.setItem('chart.premium.<type>', 'true')` (where
   `<type>` is `bar`, `line`, `area`, `scatter`, `pie`, `heatmap`).
3. **Default** in
   [client/src/lib/charts/featureFlags.ts](../../client/src/lib/charts/featureFlags.ts)
   — currently `false` for every type, so legacy is the default.

Flip a single key, reload; that chart type renders through the v2
pipeline (`<PremiumChart>` + Visx / lazy ECharts) wherever it appears.

### 5.2 Server `_autoLayers`

The server emits `_autoLayers` on v1 ChartSpec via
[server/lib/charts/autoAttachLayers.ts](../../server/lib/charts/autoAttachLayers.ts).
Triggered after `enrichCharts` in
[server/services/chat/chatStream.service.ts](../../server/services/chat/chatStream.service.ts);
each chart on every chat answer is offered up for auto-layer
attachment.

What the regex looks for in the user's question:

| Layer | Trigger phrase example | Mark gate |
|------|----------------------|-----------|
| `reference-line` | *"target of $5M"*, *"100K threshold"* | line / area / bar / scatter only (Fix-2) |
| `trend` | *"how is X trending"* | line / area / scatter only |
| `forecast` | *"forecast next 4 quarters"* | line / area / scatter only |
| `outliers` | *"any anomalies / spikes / dips"* | line / area / scatter only |
| `comparison` | *"vs prior period"*, *"YoY"* | line / area / scatter only |

False-positive guards (Fix-2):
- Reference-line target detection requires either a currency symbol
  *or* a K/M/B/T magnitude suffix on the captured number.
- "dip(?:s|ped|ping)?" word boundary so "diplomat" / "diploma" /
  "diphenyl" don't trigger outlier callouts.

Kill switch:
```
AUTO_ATTACH_LAYERS_ENABLED=false
```
in `server/server.env` disables the entire feature globally if regex
misfires in production.

The legacy renderer ignores `_autoLayers` — it's an opaque optional
field on the v1 ChartSpec. The client `<ChartShim>` v1→v2 converter
forwards it into `ChartSpecV2.layers` so `<PremiumChart>` renders
the inferred analytical overlays.

### 5.3 Fork-to-Explorer

Chat charts are read-only. The
[ChatChartCard](../../client/src/components/charts/ChatChartCard.tsx)
ships a `Fork` button that encodes the active spec as a URL hash and
navigates to `/explore` — a `<ChartCanvas>` with full editing:

- `MarkPicker` (one-click chart-type swap)
- `EncodingShelves` (click-to-pick column shelves for X / Y / Color /
  Size / Shape / Facet)
- `SuggestedAlts` (heuristic recommendations when current chart shape
  is suboptimal)
- `ExportMenu` (PNG / SVG / CSV)
- All analytical layers from the agent's `_autoLayers` are preserved.

URL hash format: `#spec=<base64-json>`. Inline data is *omitted* from
the hash to keep URLs sane; rows are pulled from `<RawDataProvider>`
when the source kind is `session-ref`.

### 5.3.1 Chat ↔ pivot capability parity

Chat charts now flow through
[InteractiveChartCard](../../client/src/components/charts/InteractiveChartCard.tsx)
→ `ChartShim` → (v2 `PremiumChart` | v1 `ChartRenderer`). The wrapper
adds an in-card toolbar that mutates a local copy of the spec — no
server roundtrip — to give chat surface parity with the pivot
preview's controls:

- **Mark switch** (bar / line / area). Hidden when the original mark
  isn't switchable (pie, scatter, heatmap stay as the agent chose).
  Switching strips bar-only fields (`barLayout`) but preserves
  `x` / `y` / `seriesColumn` / `seriesKeys`.
- **Stacked / grouped** layout toggle. Only rendered when the active
  mark is `bar` AND `seriesKeys.length > 1` (single-series bars have
  no stack to toggle).
- **Filter chips** remain owned by `ChartRenderer.enableFilters`. The
  wrapper does not duplicate that UI.

Pivot's preview keeps its existing external selectors for chart-type
and bar-layout (`DataPreviewTable.tsx`); it does not yet wrap through
`InteractiveChartCard` because the external controls also drive the
server pivot query (e.g. switching to scatter changes the pivot
request shape). Consolidating to a single source of truth is a later
refactor; today both surfaces expose the same capability set, just
through different controls.

### 5.3.2 Pivot field list — capped available zone

[PivotFieldPanel](../../client/src/pages/Home/Components/pivot/PivotFieldPanel.tsx)
caps the "Choose fields to add" zone with internal scroll
(`maxAvailableVisible` prop, default 5; expanded pivot dialog passes
10) so the selected zones (Filters / Columns / Rows / Values) stay
visible without panel-wide scrolling. `@dnd-kit` auto-scroll is
configured at the `DndContext` level so dragging from a deep unused
item into a selected zone walks both the inner and outer scrollers.

### 5.3.3 Chart key insight — client-side cache

[DataPreviewTable](../../client/src/pages/Home/Components/DataPreviewTable.tsx)
fires `POST /api/sessions/:sessionId/chart-key-insight` after the
chart preview settles, keyed by the existing `chartConfigHash` (which
already encodes the pivot query and filter selections). The insight
renders directly below the chart in both the small and expanded
chart views and survives Chart↔Pivot tab toggles without refetching;
only a config change bumps the hash and triggers a new call. A
sequence ref drops stale responses when the user changes config
mid-flight.

### 5.5 Recent changes — dashboard global filter bar

- **Wave WHov-line-crosshair (2026-05-22)** — add a hover-time vertical cross-hair indicator to the [`LineRenderer`](../../client/src/lib/charts/visxRenderers/LineRenderer.tsx) so the user sees a clear on-surface visual anchor for which x-bucket the existing nearest-x snap is reporting against, mirroring the financial-chart temporal-indicator pattern. Pre-wave the LineRenderer's `onMouseMove` handler at line 529 already snapped to the nearest data point (writes `tooltipData.xRaw` + `tooltipLeft` via `useTooltip`) and the tooltip displayed an all-series snapshot at the snapped x, but there was NO on-surface visual indicator showing WHICH x the tooltip was anchored to. New indicator block (+32 LOC, immediately after the brush-rectangle block before the zero-line) adds a single `<line>` at `xPx(tooltipData.xRaw)` — the SNAPPED x bucket, NOT the raw cursor pixel (pinned by negative test in [`lineRendererCrosshair.test.ts`](../../client/src/pages/Dashboard/lib/lineRendererCrosshair.test.ts)) — spanning full innerHeight (`y1=0` to `y2=innerHeight`), dashed `hsl(var(--muted-foreground))` at 0.45 opacity with `strokeDasharray="3 3"` so it reads as a hint not a divider. Gated on `tooltipOpen && tooltipData && brushStart === null` so (a) no stray indicator outside hover, (b) the brush rectangle owns the visual during an active drag (the brush has its own dashed border at `hsl(var(--primary))` — overlaying the cross-hair would visually compete), (c) the TS narrowing on `tooltipData` is clean. `pointerEvents="none"` so the line can't capture mouse events meant for the brush/hover surface (same guard the brush rect uses for the same reason). `Number.isFinite(cx)` defensive guard returns null when the snapped xRaw isn't in the current scale's domain mid-zoom (without the guard React would emit `<line x1="NaN" x2="NaN" ...>` which is a visual no-op but pollutes the DOM). Placed UNDER the data lines (rendered earlier in the Group, before the comparison/series/reference/trend/forecast layers) so the series strokes visually pass OVER the indicator — standard layering for this pattern. Explore-subagent disambiguation confirmed: (i) visx renderers have NO existing cross-hair overlay code (zero results across the search terms crosshair / vertical-rule / bisector / indicator-line) — this wave establishes the pattern; (ii) all visx renderers already use `@visx/tooltip`'s `useTooltip` with `tooltipLeft` / `tooltipData` state, so the indicator is a single `<line>` element binding to existing state with no new state plumbing; (iii) the echarts `axisPointer.type:'cross'` approach would have covered only the 8 specialty renderers (Treemap / Sunburst / Sankey / Calendar / Candlestick / Choropleth / Gauge / Parallel — non-temporal-x marks where cross-hair is a category error) missing the 8 primary visx trend renderers (Line / Area / Bar / Combo / Point / Arc / Box / Regression) where the affordance is most-expected — custom visx overlay wins on coverage. LineRenderer is the FIRST visx renderer to surface a hover-time cross-hair; subsequent waves can mirror this exact shape onto AreaRenderer / BarRenderer / ComboRenderer / PointRenderer one at a time. 7 new behavioural tests in new [`lineRendererCrosshair.test.ts`](../../client/src/pages/Dashboard/lib/lineRendererCrosshair.test.ts) via `node:test` source-inspection (positive pin on the `<line>` shape with `x1={cx} x2={cx} y1={0} y2={innerHeight}`; positive pin on the `const cx = xPx(tooltipData.xRaw)` snap binding to defend against a refactor that accidentally binds to the raw cursor pixel — the load-bearing snap invariant; positive pin on the conjunction guard with whitespace-tolerant separators; scoped `pointerEvents="none"` pin via regex-match on the WHov-line-crosshair block only (NOT the brush rect's `pointerEvents`); scoped `strokeDasharray` pin to confirm the indicator is dashed-not-solid as a visual contract; defensive `Number.isFinite(cx)` guard pin; wave-marker pin). Test file appended to [`server/package.json`](../../server/package.json)'s explicit list per invariant #4 (positioned right after `lineRendererCursorParity.test.ts` so the LineRenderer source-inspection cluster stays grouped). **No new convention codified** — the "hover-time vertical cross-hair indicator on a brush-capable visx trend renderer" shape is a new soft pattern (1 instance — this wave); promotes to a documented convention on the second instance when another visx renderer mirrors it. Per the second-instance-promotion rule, codification waits for the AreaRenderer mirror (most likely the next instance — same `tooltipData.xRaw` snap, byte-equivalent indicator block). Server tests 5013/5013 (+7 net new — zero pre-existing failures preserved, 100% pass rate from WD2-echarts-test-realign carried). Out of scope: cross-hair propagation to AreaRenderer / BarRenderer / ComboRenderer / PointRenderer (mechanical mirrors; one per wave); horizontal cross-hair / cross-shape (snap is per-X-bucket not per-Y-value); echarts `axisPointer.type` on specialty renderers (Treemap / Sunburst / etc. are non-temporal-x — category error); configurable cross-hair style via spec (premature). See `docs/WAVES.md` for full entry.
- **Wave WD2-echarts-test-realign (2026-05-22)** — realign the 4 long-carrying source-inspection test failures in the WD2 echarts wiring + dim test files ([`wd2WiringEcharts.test.ts`](../../client/src/pages/Dashboard/lib/wd2WiringEcharts.test.ts) + [`wd2DimEchartsTreemap.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimEchartsTreemap.test.ts)) to the current (correct) renderer code shape, closing a 13+-session carry-forward. Pre-wave the wd2WiringEcharts test at lines 88, 130 pinned the literal-line import `/import \{ dispatchCrossFilter, toFilterValue \} from "@\/pages\/Dashboard\/lib\/crossFilter"/` but the WD2-dim-echarts-treemap + WD2-dim-echarts-rest waves added `isCrossFilterActive` to BOTH the [`TreemapRenderer`](../../client/src/lib/charts/echartsRenderers/TreemapRenderer.tsx) and [`SpecialtyRenderers`](../../client/src/lib/charts/echartsRenderers/SpecialtyRenderers.tsx) import lists for the dim functionality AND prettier reformatted to multi-line, breaking the literal-line pin; line 137 pinned the Sunburst `onSunburstClick` useCallback's deps as literal `[dashboardTile, labelCh.field]` but the WD3-wiring-echarts wave correctly widened to `[dashboardTile, labelCh.field, dashboardFilters]` for the drill-through filters closure capture; the wd2DimEchartsTreemap test at lines 168-178 asserted `dispatchIdx < dimIdx` but the WD3-wiring-echarts wave deliberately LIFTED the dim triplet ABOVE the click handler so the click handler's useCallback closure could capture `dashboardFilters` (per the [`TreemapRenderer.tsx`](../../client/src/lib/charts/echartsRenderers/TreemapRenderer.tsx) inline comment at lines 78-86). Fix is purely test-side, zero code changes: (i) widen the import regex to a symbol-set membership shape `/import \{[\s\S]*?\bdispatchCrossFilter\b[\s\S]*?\btoFilterValue\b[\s\S]*?\} from "...crossFilter"/` that pins "these two symbols ARE imported" without forbidding sibling imports; (ii) widen the Sunburst deps suffix to `[dashboardTile, labelCh.field(?:,[^\]]*)?]` so the load-bearing contract becomes "deps START with these symbols"; (iii) flip the dim-triplet ordering assertion from `dispatchIdx < dimIdx` to `dimIdx < dispatchIdx` with updated description + comment explaining the WD3-wiring-echarts closure-capture rationale. Each affected test carries a new inline `WD2-echarts-test-realign ·` comment explaining the drift + the preserved intent of the original pin. No new convention codified — test-regex realignment to drift-corrected code is too narrow to promote. Server tests 5006/5006 = ZERO failures for the first time in 13+ sessions (was 5002/5006 post-WD2-line-cursor-parity; +4 newly-passing). See `docs/WAVES.md` for full entry.
- **Wave WD2-line-cursor-parity (2026-05-22)** — bring the [`LineRenderer`](../../client/src/lib/charts/visxRenderers/LineRenderer.tsx) svg-level cursor up to parity with [`AreaRenderer`](../../client/src/lib/charts/visxRenderers/AreaRenderer.tsx)'s WI4-wiring-area three-branch ternary (`brushStart ? ew-resize : dashboardTile ? pointer : undefined`) so the WD2 dashboard-tile click affordance reads consistently across the two trend renderers. Pre-wave the LineRenderer svg at line 658 carried `style={{ cursor: brushStart !== null ? "ew-resize" : "default" }}` even though its `onClick` was gated on `dashboardTile` (line 429) and dispatched cross-filter / drill-through via `dispatchCrossFilter` / `dispatchDrillThrough` (lines 449, 457) — outside a brush the cursor read `"default"` so the user got no visual signal that clicking the line surface would do anything, breaking the WD2 click-affordance parity with the eight other WD2-wired renderers (Arc / Bar / Box / Funnel / Combo / Rect / Waterfall / Area, all of which surface `cursor: pointer` when mounted inside a dashboard tile via the conditional-on-`dashboardTile` pattern). AreaRenderer was updated to the richer 3-branch ternary in the WI4-wiring-area wave (which added brush mechanics from scratch and took the opportunity to fix the cursor at the same time) but LineRenderer was missed because its brush pre-existed WI4 and the `"default"` cursor was a pre-WD2 holdover. Client-only: [`LineRenderer.tsx`](../../client/src/lib/charts/visxRenderers/LineRenderer.tsx) +17 LOC for the new ternary + 9-line inline comment cross-referencing AreaRenderer line ~432 as the parity-source; `dashboardTile` was already in scope from WD2-wiring-rest-trend, so no new state plumbing; no behavioural change to the brush itself nor outside a dashboard tile (the new `undefined` branch is browser default, cursor-byte-equivalent to `"default"` for an svg). 5 new behavioural tests in new [`lineRendererCursorParity.test.ts`](../../client/src/pages/Dashboard/lib/lineRendererCursorParity.test.ts) via `node:test` source-inspection (3 LineRenderer cases — positive pin on the 3-branch ternary, negative pin against the pre-wave hardcoded `cursor: "default"` shape, wave-marker pin; 2 AreaRenderer parity-source cases — drift-defense pin on the parity-source ternary, wave-marker pin on AreaRenderer's WI4-wiring-area cursor comment). The AreaRenderer parity-pin pair is load-bearing as drift-defense: any future change to AreaRenderer's cursor shape (a wave reflowing WI4-wiring-area, or a refactor consolidating cursor logic into a shared helper) surfaces drift BEFORE LineRenderer accidentally falls behind again; treat failures on that branch as a signal the two renderers need a re-sync wave, not a bug in the test. No new convention codified — the 3-branch cursor ternary is already documented inline in AreaRenderer's WI4-wiring-area comment block (lines 428-431) which serves as the de-facto canonical-source reference; second-instance-promotion to a documented convention waits for a third instance on a different renderer (e.g. a future brush-capable Heatmap or Geo). Server tests 5002/5006 (+5 net new). See `docs/WAVES.md` for full entry.
- **Wave WI4-client-sheetId-resolution (2026-05-22)** — mirror of the prior WD3-server-sheetId-resolution wave on the WI4 explain-slice path. Closes the chartId-collision concern end-to-end on BOTH click-intent paths (drill-through + explain-slice). Pre-wave the ExplainSlicePanel chart-resolution IIFE at [`DashboardView.tsx`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) lines 1170-1176 read `activeSheet.charts[idx]` against whatever sheet was active at panel-render time, so brushing Sheet 1's chart-0 then navigating to Sheet 2 before panel render silently re-resolved to Sheet 2's chart-0 → `useInsightRegen` fired against Sheet 2's data with Sheet 1's brush region → panel showed regenerated insight for a chart the user never intended. Structurally identical to the prior wave but with a smaller scope because (i) the resolution surface is local to DashboardView (no server changes — explain-slice has no server-side chartId resolver, the panel only consumes event detail + the resolved chart spec to drive `useInsightRegen` in-process), and (ii) no hook URL plumbing is needed (the regen call POSTs spec + filtered rows directly). Client-only: [`ExplainSliceEvent.sheetId?: string`](../../client/src/pages/Dashboard/lib/explainSlice.ts) added with JSDoc pinning the brush-time-vs-render-time invariant + the stale-sheetId predictable-failure contract (cross-references the server-side WD3 resolver's `chart_not_found` for symmetry across click-intent paths). DashboardView WI4 listener at lines 504-513 changes `setExplainSliceEvent(detail)` to `setExplainSliceEvent(activeSheetId ? { ...detail, sheetId: activeSheetId } : detail)` — direct mirror of the WD3 listener wave; `activeSheetId` was already in scope from the prior WD3-WI4-sheetId-telemetry wave's effect-deps widening, so no new state plumbing. ExplainSlicePanel mount IIFE at lines 1170-1192 widens from `if (!explainSliceEvent || !activeSheet) return null; ... return activeSheet.charts[idx] ?? null;` to a branched resolution: outer guard collapses to `!explainSliceEvent` only (so a valid sheetId can resolve even if `activeSheet` is null mid-mount), scoped branch on `if (explainSliceEvent.sheetId)` does `sheets.find((s) => s.id === explainSliceEvent.sheetId)?.charts[idx] ?? null` (returns null on stale/missing sheet via optional-chain — predictable-failure, NOT silent fallback to `activeSheet`), fallback branch preserves the pre-wave `activeSheet.charts[idx] ?? null` verbatim for events without `sheetId` (backwards-compat with any panel mount that pre-dates this wave). **Both soft patterns from the prior WD3-server-sheetId-resolution wave reach their second instance on this wave and get codified as documented conventions**: [`docs/conventions/predictable-failure-on-optional-disambiguator.md`](../conventions/predictable-failure-on-optional-disambiguator.md) (server `findChartByTileId` returning null on stale sheetId → client ExplainSlicePanel resolver returning null via optional-chain on `sheets.find` miss) + [`docs/conventions/click-time-context-capture-on-event-detail.md`](../conventions/click-time-context-capture-on-event-detail.md) (WD3 listener spreading `activeSheetId` onto DrillThroughEvent detail → WI4 listener spreading `activeSheetId` onto ExplainSliceEvent detail with the byte-identical conditional-spread shape). 12 new behavioural tests in new [`wi4SheetIdResolution.test.ts`](../../client/src/pages/Dashboard/lib/wi4SheetIdResolution.test.ts) (3 interface + 3 listener + 1 negative pin + 5 resolver); one existing test widened in-place ([`wi4Panel.test.ts`](../../client/src/pages/Dashboard/lib/wi4Panel.test.ts) "setExplainSliceEvent" regex widened to the new conditional-spread shape — mirrors the wd3Sheet widening from the prior wave). Server tests 4997/5001 (+12 net new). Out of scope: hook-level queryKey widening on `useInsightRegen` (key is `(tileId, filterHash, brushRegion)` — tileId already disambiguates); WD2 cross-filter sheetId-resolution (cross-filter doesn't carry chartId, structurally immune); ExplainSlicePanel.chart prop signature widening (two-prop-drift risk; resolution stays on DashboardView). See `docs/WAVES.md` for full entry.
- **Wave WD3-server-sheetId-resolution (2026-05-21)** — closes the user-facing chartId-collision on the WD3 drill-through resolver path that the prior WD3-WI4-sheetId-telemetry wave only made observable in Cosmos. Pre-wave [`findChartByTileId`](../../server/services/dashboardDrillThrough.service.ts) at lines 153-167 walked `dashboard.sheets` in declaration order and returned the first chart matching `"chart-${idx}"` across ALL sheets, so cmd-clicking Sheet 2's `chart-0` silently rendered Sheet 1's data in [`DrillThroughSheet`](../../client/src/pages/Dashboard/Components/DrillThroughSheet.tsx) (the `"chart-${index}"` convention in [`DashboardView.tsx`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) lines 219-228 resets index per-sheet, so chart-N is locally unique within a sheet but globally collides across sheets on every multi-sheet dashboard). This wave plumbs the optional `sheetId` end-to-end: server [`DrillThroughRequest`](../../server/services/dashboardDrillThrough.service.ts) gains `sheetId?: string`; `findChartByTileId(dashboard, tileId, sheetId?)` gains a scoped-lookup branch (`dashboard.sheets.find` by id then index into `targetSheet.charts`) when sheetId is present, returns null on stale / unknown sheetId rather than falling back, preserves the legacy walk-across-sheets when sheetId is undefined for backwards-compat with shareable URLs predating this wave; `resolveDrillThrough` threads `request.sheetId`; [`dashboardDrillThroughController.ts`](../../server/controllers/dashboardDrillThroughController.ts) reads `sheetId` from `req.query` via the `typeof === "string"` guard (NOT in the 400-missing guard — sheetId is optional). Client: [`DrillThroughEvent.sheetId?: string`](../../client/src/pages/Dashboard/lib/drillThrough.ts) with JSDoc pinning the click-time-vs-render-time invariant; [`useDrillThroughRows`](../../client/src/pages/Dashboard/hooks/useDrillThroughRows.ts) grows `event?.sheetId ?? ""` as the seventh queryKey piece + conditional `if (event.sheetId) url.searchParams.set("sheetId", event.sheetId)` URL gate (negative-pin tests confirm NO unconditional set — preserves byte-identical URL shape for legacy share-links); DashboardView listener at lines 426-459 changes `setDrillThroughEvent(detail)` to `setDrillThroughEvent(activeSheetId ? { ...detail, sheetId: activeSheetId } : detail)` — the conditional injection captures the user's intent at click time (NOT panel-render time), so subsequent sheet navigation while the side-sheet is open doesn't re-resolve to a different chart. The predictable-failure invariant on stale sheetId (returns null / 404 rather than silently falling back to legacy walk) is the load-bearing defense against silent mis-resolution from deleted-sheet share-links or hand-crafted URLs with wrong sheet ids — pinned by source-inspection in the new test file. The WI4 explain-slice resolution at DashboardView lines 1144-1158 has the same collision shape but is OUT OF SCOPE for this wave — it's a separate sibling wave (next-wave-order #2) that mirrors the WD3 fix on the explain-slice surface. 22 new behavioural tests in new [`dashboardDrillThroughWD3SheetIdResolution.test.ts`](../../server/tests/dashboardDrillThroughWD3SheetIdResolution.test.ts) (6 service-runtime + 3 resolver-runtime + 4 service source + 3 controller source + 1 event-interface + 3 hook source + 2 listener source); two existing tests updated in-place ([`dashboardDrillThroughWD3Server.test.ts`](../../server/tests/dashboardDrillThroughWD3Server.test.ts) "delegates to resolveDrillThrough" regex widened to include sheetId; [`wd3Sheet.test.ts`](../../client/src/pages/Dashboard/lib/wd3Sheet.test.ts) "sets drillThroughEvent" regex widened to the new conditional-spread shape). Two soft patterns shipped (1 instance each, would promote on a second): **predictable-failure on optional disambiguator** (null/404 when provided-but-unknown, not silent fallback) + **click-time context capture on event detail** (NOT render-time prop). Server tests 4985/4989 (+22 net new). Out of scope: client-side ExplainSlicePanel chart resolution (separate WI4-client-sheetId-resolution wave); streaming row payload (NDJSON + useInfiniteQuery — deferred unless performance is actively a complaint); backfilling pre-wave share-link URLs (legacy walk handles them fine). See `docs/WAVES.md` for full entry.
- **Wave WD3-WI4-sheetId-telemetry (2026-05-21)** — observability disambiguation on the two DashboardView click-intent listeners. Pre-wave both `useEffect` listeners (the WD3 `DRILL_THROUGH_EVENT` at lines 426-455 + the WI4 `EXPLAIN_SLICE_EVENT` at lines 462-491 in [`DashboardView.tsx`](../../client/src/pages/Dashboard/Components/DashboardView.tsx)) called the matching telemetry helper with `{chartId, column, valueType|regionKind, dashboardId}` — but `chartId` is the `dashboardTile.tileId` field that DashboardView itself stamps at line 219-228 (`id: "chart-${index}"` where `index` is the position within `activeSheet.charts`), which resets to N=0 on every sheet switch. The doublet-listener-doublet-telemetry-helper symmetry that WD3-telemetry + WI4-telemetry codified last session made this collision observable in Cosmos for the first time but didn't fix it — `Sheet 1: chart-0` + `Sheet 2: chart-0` + `Sheet 3: chart-0` all wrote rows with identical `chartId` strings into the `usage_events` container, and any `GROUP BY chartId` aggregation silently combined them. This wave threads `sheetId: activeSheetId ?? undefined` through both listeners into the matching helper (`activeSheetId` is the existing `useState<string | null>` slot at line 88; `?? undefined` omits the field entirely when no sheet is active, NOT `null` which would fail the server-side `z.string()` type guard). Both effects' deps arrays widen from `[dashboard.id]` to `[dashboard.id, activeSheetId]` for ESLint exhaustive-deps cleanliness — the listener re-binds on sheet switch (cheap: `removeEventListener` + `addEventListener` on `window`, once per sheet navigation); without the dep widening the listener would close over the initial `activeSheetId` (typically `null` on first mount) and every subsequent click on a non-initial sheet would record telemetry with `sheetId: undefined` even though the user is clearly on a different sheet, silently reverting the entire observability benefit. The renderer surface (15 visx + ECharts renderers that emit `dispatchDrillThrough` / `dispatchExplainSlice` via `dashboardTile.tileId`) and the server-side `findChartByTileId` regex resolver (`server/services/dashboardDrillThrough.service.ts` lines 153-167, which has the same collision on the user-facing fetch path — DrillThroughSheet renders Sheet 2's chart-0 when the user clicked Sheet 1's chart-0 after a navigation) are NOT touched by this wave. The additive-sheetId shape was chosen over a `chartId` widening (`chart-N` → `chart-<sheetId>-N`) because the widening would break every existing test pinning literal `"chart-N"` values across 4+ test files, create a mixed-format Cosmos aggregation surface across the wave boundary, and propagate through the renderer surface + server resolver + `ExplainSlicePanel` chart resolution at DashboardView lines 1144-1150 — all of which would need to learn the new format. Out of scope: server-side `findChartByTileId` collision fix (separate wave); client-side `ExplainSlicePanel` resolution against `activeSheet.charts` after a navigation (separate wave); widening of other dashboard-family telemetry events (`dashboard.opened` / `dashboard.shared` / `dashboard.exported`) with sheetId — those events are per-dashboard, not per-chart-on-a-sheet; backfilling pre-wave Cosmos rows with sheetId (fundamentally lossy because dashboard sheet structure drifts as users edit). See `docs/WAVES.md` for full entry.
- **Wave WI6-persist (2026-05-20)** — cross-session persistence for the WI6 insight history store via sessionStorage. Widens the WI6 contract from "history dies with the `DashboardView` mount" to "history survives route-change remounts but dies with the tab" — `sessionStorage`, not `localStorage`, because analytical insight context shouldn't outlive the user's exploration thread. [`insightHistory.ts`](../../client/src/pages/Dashboard/lib/insightHistory.ts) gains version-suffixed `STORAGE_KEY_PREFIX = "marico-insight-history-v1"` + private `SCHEMA_VERSION = 1` constant baked into every persisted payload (two-level versioning: key suffix isolates the storage slot; payload field catches manual-edit pollution). New `InsightHistoryStorage` interface (`read` / `write` / `remove`) abstracts the browser-only `window.sessionStorage` for testability; default adapter wraps each native call in try/catch (quota / private-mode / blocked-storage failures degrade silently). `createInsightHistoryStore` accepts two new options on `InsightHistoryStoreOptions`: `storage?: InsightHistoryStorage | null` (default = the sessionStorage adapter; pass `null` to opt out explicitly even in a browser context; pass an in-memory fake in tests) and `storageScope?: string` (suffix appended to `STORAGE_KEY_PREFIX` with `::` delimiter — typically a dashboard id, so two dashboards on the same tab don't share storage slots). The factory uses `opts.storage !== undefined ? opts.storage : default` rather than `??` because the explicit `null` opt-out must be distinguishable from `undefined`. Hydration on construction: `storage.read()` wrapped in try/catch, raw passed to `hydrateFromStorage(raw)`. Defensive shape validation drops the payload on JSON parse failure, version mismatch, missing `tiles` field, or non-object `tiles`; the per-entry `isValidHistoryEntry` predicate keeps valid siblings when one slot is corrupt (required shape: `filterHash: string`, `filters: object`, `entry: object` with `entry.text: string`, `recordedAt: number`). Write-through on `record` and `clear(tileId)` via a private `persist()` helper; `clear()` (no arg) calls `storage.remove()` directly so the key is freed rather than written as an empty payload. `persist()` wraps both `JSON.stringify` (cyclic-ref defense) and `storage.write` (quota defense) in try/catch — every failure path keeps in-memory record semantics intact. [`DashboardView.tsx`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) passes `storageScope: dashboard.id` via the existing `useMemo([], )` lifetime (empty deps capture initial id; dashboards remount on route change in practice). 20 new behavioural tests across 7 suites in [`insightHistoryPersistenceWI6Persist.test.ts`](../../client/src/pages/Dashboard/lib/insightHistoryPersistenceWI6Persist.test.ts): constant pin, explicit `storage: null` opt-out, default-non-window env, full round-trip via in-memory adapter (single-tile + multi-tile + MRU bubble + capacity cap survive write-A / read-B handoff), defensive hydration (parse error, version mismatch, missing tiles, non-array slot, per-entry mixed-quality filter, immediate-read on hydrated payload), storage scope isolation across two adapter instances, `clear(tileId)` + `clear()` write-through, adapter failure tolerance (quota on write + throwing remove + throwing read at construction all degrade gracefully). Test file appended to [`server/package.json`](../../server/package.json)'s explicit list per invariant #4 (right after `insightHistory.test.ts`). All 18 pre-existing WI6 tests still green; 157 of 157 pass on the broader WI suite. Server typecheck = 98 errors / client typecheck = 53 errors — both identical to the WV4-bucket baseline. Out of scope: cross-tab persistence via `BroadcastChannel`; cross-dashboard history sharing (each `dashboard.id` has its own scope); schema-migration helper for `-v1 → -v2` transitions (next bump just starts the user empty); active eviction of orphan tile entries on hydrate; UI surface for "Restored from previous session". See `docs/WAVES.md` for full entry.
- **Wave WI6 (2026-05-20)** — per-tile insight history dropdown — MRU navigator for prior filter combos. **Closes Workstream 7 to 6 of 6 (CLOSED).** New pure module [`insightHistory.ts`](../../client/src/pages/Dashboard/lib/insightHistory.ts) sibling to [`insightRegenCache.ts`](../../client/src/pages/Dashboard/lib/insightRegenCache.ts) ships `createInsightHistoryStore()` with `MAX_HISTORY_PER_TILE = 3`, `record(tileId, filters, entry)` resolving the de-dup key via `hashGlobalFilters(filters)` and applying MRU bubble-to-top semantics (existing slot moves to index 0 with refreshed entry + recordedAt; new slot unshifts; oversize trimmed), `get(tileId)` returning a defensive-copy newest-first list, `clear(tileId?)` per-tile or store-wide. **Two separate stores by design**: the cache is keyed `(tileId, filterHash[, regionHash])` → entry, LRU + TTL — read-performance-oriented; the history is keyed `tileId` → ordered list of `(filterHash, filters, entry, recordedAt)` slots, MRU-bubble + cap — UX-navigation-oriented. Co-located in `lib/` so a future maintainer finds both side-by-side. No TTL on history (unlike the 5-min cache TTL) — bound is `MAX × tile_count` slots per `DashboardView` mount; navigation surface deliberately outlives the cache's prose-freshness window. **Interpretation decision**: the brief's "Keep last 3 insights (for each unique filter combo)" + example labels "Insights for: this view (now), {prior filter combo}, baseline" picked interpretation (b) most-recent-N-DISTINCT-filter-combos navigator over (a) per-key Re-explain-attempts ring; the WI4-rexplain button already serves the same-combo bypass-cache affordance. [`DashboardView.tsx`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) holds one store per mount via `useMemo(() => createInsightHistoryStore(), [])` alongside the existing `insightRegenCache`; threaded through [`DashboardTiles.tsx`](../../client/src/pages/Dashboard/Components/DashboardTiles.tsx) into every [`ChartTileBody.tsx`](../../client/src/pages/Dashboard/Components/ChartTileBody.tsx). The body's `useEffect` keyed on `[insightHistoryStore, tile.id, regen.entry, filters]` records on every fresh `regen.entry?.regeneratedAt` — the load-bearing signal is `regeneratedAt` being a fresh ISO string per fetch, so the effect fires both for new regenerations AND for filter-toggle paint-from-cache transitions (where `cache.get(newKey)` returns a different entry → `regen.entry` identity changes → MRU bubble fires). A `historyVersion` `useState<number>` bumped inside the record effect is the load-bearing re-render trigger for the dropdown — React doesn't observe the store's Map mutation otherwise, so without the version bump the dropdown would never re-render after a record. `useMemo<InsightHistoryEntry[]>(() => store.get(tile.id), [insightHistoryStore, tile.id, historyVersion])` reads the latest list. `handleHistorySelect = useCallback((entry) => onFiltersChange(entry.filters), [onFiltersChange])` routes a click into the dashboard's filter-change pipeline (single source of truth — filters drive what every tile renders, cache then resolves on the new key); the warm regen cache paints the cached entry instantly when the filter combo is still within the 5-min TTL. We deliberately don't auto-fire a regen on history-select to keep the navigator cheap — if the cache slot has TTL-expired, the existing manual Re-explain path is the user's next move. [`TileInsightFooter.tsx`](../../client/src/pages/Dashboard/Components/TileInsightFooter.tsx) gains optional `history?: InsightHistoryEntry[]` + `onHistorySelect?: (e) => void` props + inline `formatHistoryLabel(filters)` helper (`"Baseline"` for empty filters, `"<column>: <value>"` truncated to 30 chars for a single filter, `"<N> filters"` for 2+). Renders a Radix `<DropdownMenu>` block inline with the Re-explain button (sibling of `regen.onRegenerate()`), gated on `history && history.length > 0 && onHistorySelect`. Trigger: `Button variant="outline" size="sm" h-7 px-2 text-[11px]` with `<History>` Lucide icon + "Recent insights (N)" label. Menu: `<DropdownMenuLabel>` "Insights for:" header + `<DropdownMenuSeparator>` + one `<DropdownMenuItem>` per entry keyed by `entry.filterHash`, each rendering the label + a smaller `formatRelativeShort(entry.entry.regeneratedAt)` line below. `e.stopPropagation()` on trigger + content keeps the parent footer chevron from toggling (mirrors the WI5 chip row's stopPropagation). 18 new behavioural tests across 10 suites in [`insightHistory.test.ts`](../../client/src/pages/Dashboard/lib/insightHistory.test.ts) — real import + runtime assertions, NOT source-inspection (same shape as `tileRecommendations.test.ts` / `explainSlice.test.ts`). Coverage: `MAX_HISTORY_PER_TILE` constant pin; empty-store `get` returns `[]`; single record shape + empty-filters empty-string `filterHash` sentinel; two/three distinct combos newest-first; same combo recorded twice → length 1 with entry + recordedAt updated AND filterHash stable; same combo re-recorded after newer combos bubbles to index 0 (MRU); four distinct combos drops the oldest at length 3; custom `maxPerTile` option respected; empty vs non-empty filter slots distinct; multi-tile isolation; `clear(tileId)` + `clear()` + `clear(unknownTile)` semantics; defensive-copy on `get`; injectable `now()` deterministic. Test file appended to [`server/package.json`](../../server/package.json)'s explicit `test` list per invariant #4 (right after `tileRecommendations.test.ts`). 4374 of 4378 server tests pass (4 pre-existing ECharts treemap+sunburst carry-forwards unchanged from the WI4-cache-key baseline); client typecheck baseline preserved (53). The dropdown is purely additive — tiles whose `ChartTileBody` doesn't receive an `insightHistoryStore` prop, or that have `history.length === 0`, render byte-identically to the pre-WI6 footer. **Workstream 7 — insight engine 2.0 — is now CLOSED end-to-end** (WI1 schema + WI2 cache/server/wire/wire-bind trilogy + WI3 citations + WI4 family + WI5 recommendations + WI6 history). Out of scope: cross-session persistence (would need a serialiser for `InsightRegenEntry` + key-version bump); brush-region history slots in the same dropdown (the `formatHistoryLabel` only looks at filters today and would need a region-aware branch); per-key Re-explain-attempts ring as interpretation (a). See `docs/WAVES.md` for full entry.
- **Wave WI5 (2026-05-20)** — per-tile "Try this" recommendation chips under the regen prose. Closes Workstream 7 to 5 of 6 — the recommendations infra was already partially built in [`AnalysisSummaryPanel.tsx`](../../client/src/pages/Dashboard/Components/AnalysisSummaryPanel.tsx) at the *whole-analysis* scope (LLM-generated, grouped by horizon); WI5 is the per-tile counterpart: deterministic, single-click CTAs derived synchronously from the tile's current spec + filtered rows + active filters. No LLM, no server round-trip. New pure module [`tileRecommendations.ts`](../../client/src/pages/Dashboard/lib/tileRecommendations.ts) exports `deriveTileRecommendations(spec, rows, activeFilters): TileRecommendation[]` with three deterministic rules: **filter-bottom** (lowest x-bucket < median × `FILTER_BOTTOM_RATIO = 0.5` → "Focus lowest: <value>"), **filter-top** (highest > median × `FILTER_TOP_RATIO = 2.0` → "Focus highest: <value>"), and **clear-filters** (always-on when any filter is active, slotted last). Only fires for categorical-x chart types (`bar` / `pie`) with ≥3 buckets — a median is meaningless with fewer datapoints; `line` / `area` / `scatter` / `heatmap` deliberately skip the filter rules (continuous or 2D x-encodings). Aggregator drops null/undefined/empty-string x AND y rows BEFORE the `Number()` coercion to match the convention in `filterRowsByBrushRegion` (without the y-drop, `Number(null) === 0` would silently create phantom buckets at 0 and shift the median down — caught during test development). Sums y-values per bucket; threshold is median-anchored so a different chart aggregator (mean / count) doesn't affect relative ordering. `isValueAlreadyFiltered` check prevents re-surfacing a CTA that would be a no-op (a categorical filter on the same column already pinning the value); date / numeric filters on the same column do NOT count, since those narrow ranges without pinning a specific value. Capped at `MAX_TILE_RECOMMENDATIONS = 3` so the chip row stays single-line. Rec ids encode `kind + column + value` (`filter-bottom:region:A`) so two recs on the same tile never collide as React keys. [`TileInsightFooter.tsx`](../../client/src/pages/Dashboard/Components/TileInsightFooter.tsx) gains optional `recommendations?: TileRecommendation[]` + `onRecommendationClick?: (rec) => void` props and a new "Try this:" chip row between the Sources row and the action row (order: prose → metadata → sources → try-this → actions), gated on `recommendations?.length > 0`. Chip styling: `Button variant="outline" size="sm" h-6 px-2 text-[11px]` with `e.stopPropagation()` to keep the expand/collapse chevron from toggling on chip click. [`ChartTileBody.tsx`](../../client/src/pages/Dashboard/Components/ChartTileBody.tsx) computes `recommendations` via `useMemo(() => deriveTileRecommendations(specLite, filteredRows, filters ?? {}), [specLite, filteredRows, filters])` and routes each kind via `handleRecommendationClick`: filter-bottom / filter-top → `onFiltersChange({...filters, [column]: { type: "categorical", values: [value] }})` (clobber any prior categorical filter on the column, safe because the helper guard prevents re-surfacing already-pinned values); clear-filters → `onFiltersChange({})`. 30 new behavioural tests across 12 suites in [`tileRecommendations.test.ts`](../../client/src/pages/Dashboard/lib/tileRecommendations.test.ts) (real import + runtime assertions, not source-inspection — same shape as `explainSlice.test.ts`): constants pinned, empty/balanced/extreme data cases, each chart-type gate (bar + pie fire, line + area + scatter + heatmap don't), <3 bucket short-circuit, already-filtered skip, clear-filters appearance + ordering, MAX cap, aggregator hygiene (null/empty x; null / NaN / non-numeric y; sum semantics), id stability + uniqueness. Test file appended to [`server/package.json`](../../server/package.json)'s explicit list per invariant #4. 375 tests across 123 suites all green (the WI4 family + WD2/WD3 regression + the new tileRecommendations suite). Client typecheck baseline preserved (53). The chip row is purely additive — tiles whose data triggers no rule and have no active filters render zero chips, matching the pre-wave footer byte-for-byte. See `docs/WAVES.md` for full entry.
- **Wave WI4-wiring-point (2026-05-20)** — closes the WI4 widening end-to-end at 4 of 4 brush-capable kinds. [`PointRenderer.tsx`](../../client/src/lib/charts/visxRenderers/PointRenderer.tsx) had NO pre-existing brush mechanics — only per-point onClick handlers powering WD2 cross-filter + WD3 drill-through, both gated on `colorCh` — so this wave adds mouse-down / move / up state + a 2D rectangular overlay from scratch. **First 2D brush wiring in the WI4 family**: scatter is the only chart kind with TWO independently-continuous axes, so the brush captures `{ x, y }` pixel-coord pairs (distinct from the 1D brushes' bare-number `brushStart`). The three 1D constructors (`makeNumericRegion` / `makeTemporalRegion` / `makeCategoricalRegion`) are DELIBERATELY NOT imported — negative pin in tests catches a future drift that would downgrade the scatter brush to 1D. `useEffect(() => { setBrushStart(null); setBrushEnd(null); }, [data])` resets on data change. Three handlers gated on `dashboardTile`: `onBrushDown` reads `localPoint`, computes `(x, y)` in inner-plot space, guards BOTH axes' bounds (`x < 0 || x > innerWidth` AND `y < 0 || y > innerHeight`), captures `e.altKey === true` into `brushExplainRef.current`, sets brushStart AND brushEnd to the same `{x, y}` pair. `onBrushMove` short-circuits on `brushStart === null` / `!(e.buttons & 1)`, clamps `pt.x` / `pt.y` independently to `[0, innerWidth]` / `[0, innerHeight]` via `Math.max(0, Math.min(...))` on each axis. `onBrushUp`'s click-vs-drag gate requires **BOTH axes** to cross `BRUSH_MIN_PX` (`isBrushDrag(brushStart.x, brushEnd.x, BRUSH_MIN_PX) && isBrushDrag(brushStart.y, brushEnd.y, BRUSH_MIN_PX)`) — a 100×3 sliver is still a click in the y-dimension; a 3×100 sliver is a click in the x-dimension. Drag branch gates on `brushExplainRef.current && dashboardTile`, inverts pixel coords via `xScale.invert(brushStart.x)` / `xScale.invert(brushEnd.x)` / `yScale.invert(brushStart.y)` / `yScale.invert(brushEnd.y)` (yScale's `range = [innerHeight, 0]` inverts low-pixel-y to high-data-y; the constructor normalises so xMin/yMin/xMax/yMax come out correctly regardless of drag direction), and builds the region via `makeBox2dRegion(x1Data, x2Data, y1Data, y2Data, yCh.field)` — yCh.field rides on the region's `yColumn` per WI4-foundation-box2d. Dispatch shape is the 5-field `{ chartId, column: xCh.field, region, sourceTileId, filters: dashboardFilters }`, guarded by `if (region)` so a null constructor return (zero-area) doesn't fire a malformed event. SVG element gains `onMouseDown` / `onMouseMove` / `onMouseUp` (gated on dashboardTile) + a widened `onSvgMouseLeave` (does BOTH `hideTooltip()` AND a brush state clear if active — safety net for cursor-leaves-svg-mid-drag). svg cursor toggles to `crosshair` during an active brush — the canonical 2D-rect-selection affordance, distinct from the 1D brushes' `ew-resize` / `ns-resize`. Brush `<rect>` overlay inserted inside `<Group>` right after the zero-line: anchored at `(Math.min(brushStart.x, brushEnd.x), Math.min(brushStart.y, brushEnd.y))` with `width = Math.abs(brushEnd.x - brushStart.x)` and `height = Math.abs(brushEnd.y - brushStart.y)` so reverse drags render correctly; same primary-tint dashed-stroke styling as the 1D brushes (primary at 0.1 fill + 0.4 stroke + dashed `3 3`) + `pointerEvents="none"` so the overlay never intercepts mouse events. Per-point `onMouseMove` tooltip handler gains a single-line `if (brushStart !== null) return;` guard at the top so the tooltip doesn't flicker between points during a brush drag (mirrors BarRenderer's onMouseMove guard). Pre-existing WD2 cross-filter + WD3 drill-through per-point onClick handlers preserved byte-identical (the SVG-level brush + per-point click handle different event paths and never conflict). 38 source-inspection tests across 15 suites in [`wi4WiringPoint.test.ts`](../../client/src/pages/Dashboard/lib/wi4WiringPoint.test.ts) — including NEGATIVE pins against the three 1D constructors (scatter is box2d-only) + against `setZoomRange` (no zoom on scatter). Test file appended to [`server/package.json`](../../server/package.json)'s explicit list per invariant #4. 366 tests across 122 suites all green across the WI4 family + WD2 + WD3 Point regression suite. Client typecheck baseline preserved (53). The WI4 master-plan series is now CLOSED end-to-end at 4 of 4 brush-capable kinds — alt-drag on any line / area / bar / scatter chart inside a dashboard tile opens the ExplainSlicePanel with regenerated insight prose for the brushed sub-region. See `docs/WAVES.md` for full entry.
- **Wave WI4-foundation-box2d (2026-05-20)** — opens the load-bearing foundation for a 2D scatter brush by widening [`BrushRegion`](../../client/src/pages/Dashboard/lib/explainSlice.ts) from 3 kinds to 4 with a new `box2d` variant: `{ kind: "box2d"; xMin: number; xMax: number; yMin: number; yMax: number; yColumn: string }`. The y-column rides on the region (unlike the three 1D variants whose y-axis is irrelevant to row matching) so `ExplainSliceEvent`'s `column` field stays "x-axis column" for all four kinds and `filterRowsByBrushRegion`'s 3-arg signature is unchanged. New `makeBox2dRegion(ax, bx, ay, by, yColumn)` constructor mirrors the three 1D constructors (normalises `xMin <= xMax` AND `yMin <= yMax`, returns `null` on zero-area or non-finite inputs); `filterRowsByBrushRegion`'s box2d branch reads `column` (x-axis) AND `region.yColumn` (y-axis) with the same null/empty-string drop + `Number()` coercion as the numeric branch so string-typed CSV / JSON numerics match cleanly. `hashBrushRegion` emits `b2:<xMin>..<xMax>x<yMin>..<yMax>;y:<yColumn>` — the `;y:<yColumn>` suffix is load-bearing because two scatters whose y-encoding changes (e.g. `sales` → `revenue`) describe DIFFERENT sub-regions even at identical pixel-rectangle bounds, and `tileId` alone can't disambiguate since the tile id is stable across encoding edits. [`ExplainSlicePanel`](../../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx)'s `formatRegion` widens to an exhaustive switch on `region.kind` — the categorical branch now has an explicit `kind === "categorical"` check (typecheck-load-bearing because the union widening would otherwise narrow `region.values` to `categorical | box2d` where box2d has no `values` field) + a new box2d branch displays `[xMin, xMax] × yColumn [yMin, yMax]` Cartesian-product notation with the y-column label between the two ranges so the bounds aren't ambiguous when read out of context. The formatRegion branch was bundled into the foundation wave (originally planned as a third "WI4-format-box2d" wave) because the union widening cascades into formatRegion's typecheck — splitting would force a compile-safe stub in foundation that the format wave would polish, vs the bundle: one clean wave, baseline preserved. 20 new tests across three existing test files (no new test file → server/package.json's explicit list unchanged): 5 in `makeBox2dRegion` + 5 in `filterRowsByBrushRegion — box2d region` (explainSlice.test.ts); 5 in `hashBrushRegion — box2d kind` + 3 in `buildCacheKey collision avoidance — box2d` (insightRegenCache.test.ts); 2 in `formatRegion discriminates on region.kind` (wi4Panel.test.ts). The pre-existing `dispatchExplainSlice` discriminated-union pin updated to include the 4th variant. Cumulative WI4 family: 280 tests across 92 suites all green. Client typecheck baseline preserved (53). Next wave WI4-wiring-point adds PointRenderer's 2D brush mechanics from scratch. See `docs/WAVES.md` for full entry.
- **Wave WI4-citations (2026-05-19)** — ports the WI3 footer's `Sources:` row of `CitationHoverCard` chips into [`ExplainSlicePanel`](../../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx) — discoverable, scan-at-a-glance list of every cited pack for the brushed-slice regen. New `<div>` row inside the "Regenerated insight" `<section>`, sibling between the prose `<div>` and the WI4-rexplain button (three regen affordances stack in source order: read → sources → refresh). Gates on `regen.entry?.citations && regen.entry.citations.length > 0` so the row only appears when there are pack ids to chip. Byte-for-byte parity with WI3 footer's row styling (`mt-2 flex flex-wrap items-center gap-1 text-[11px] text-muted-foreground` + `<span>Sources:</span>` label + `.map((packId, i) => <CitationHoverCard key={packId} packId={packId} index={i + 1} />)`) so a future visual tweak lands in both files together. Inline backtick-wrapped pack ids inside the regen prose continue to render as `[N]` superscript hover-cards via `MarkdownRenderer`'s WQ3 integration — that path is unchanged; this row is the sibling that pulls the full citation set up to the top level. 8 source-inspection tests across 3 suites in [`wi4Citations.test.ts`](../../client/src/pages/Dashboard/lib/wi4Citations.test.ts) — including a three-landmark source-order pin and an "intervening `</section>` absent" pin (Sources lives inside the Regenerated insight `<section>`, not its own). Test file appended to [`server/package.json`](../../server/package.json)'s explicit list per invariant #4. 3 of 4 WI4 dormancy-debt polish items shipped this session — only box-2D scatter brush remains. See `docs/WAVES.md` for full entry.
- **Wave WI4-rexplain (2026-05-19)** — gives the user an explicit bypass-cache button in [`ExplainSlicePanel`](../../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx) so a fresh regeneration of the same slice is one click away (the panel auto-fires once per fresh event then serves from cache for the 5-minute TTL; the button calls `regen.regenerate(specLite, narrowedRows, { bypassCache: true })`). New `<Button>` block inside the "Regenerated insight" `<section>`, sibling below the prose `<div>`, gated on `regen.entry?.text && chart && specLite` (only appears once a regeneration has actually landed AND the chart + spec are resolved). Sparkles (idle) / Loader2 spin (loading); disabled while loading; `e.stopPropagation()` keeps the Radix Sheet open on click — mirrors the WI2 footer's button shape (variant=outline + size=sm + h-7 + text-[11px]). Hook already supported `bypassCache: true` since WI2-wire; this wave is purely the call-site surface. NEGATIVE pin in tests against widening `ExplainSlicePanelProps` with `onRegenerate?` / `bypassCache?` so a future refactor that tries to lift regen control to the parent surfaces immediately. 12 source-inspection tests across 3 suites in [`wi4Rexplain.test.ts`](../../client/src/pages/Dashboard/lib/wi4Rexplain.test.ts). Test file appended to [`server/package.json`](../../server/package.json)'s explicit list per invariant #4. The WI4 master-plan series now has 2 of 4 dormancy-debt polish items shipped this session (remaining: citation chips, box-2D scatter brush). See `docs/WAVES.md` for full entry.
- **Wave WI4-cache-key (2026-05-19)** — closes a silent cache collision in the WI4 panel: two brushes on the same `(chartId, filters)` but different sub-regions were sharing one cache slot, so the second brush silently painted the first brush's prose. [`buildCacheKey`](../../client/src/pages/Dashboard/lib/insightRegenCache.ts) widens to take an optional third `regionHash` arg; new `hashBrushRegion(region: BrushRegion | undefined): string` helper mirrors `hashGlobalFilters`'s byte-stability contract per kind (`n:<start>..<end>` / `t:<startMs>..<endMs>` / `c:<v1>|<v2>|…`) and returns `""` for undefined so non-brush call sites get byte-identical keys to the pre-wave 2-arg shape — the WI2 footer's existing cached entries remain reachable through the widening. [`useInsightRegen`](../../client/src/pages/Dashboard/hooks/useInsightRegen.ts) takes an optional `brushRegion?: BrushRegion` arg and threads it through the cache-key `useMemo` (`buildCacheKey(tileId, hashGlobalFilters(filters), hashBrushRegion(brushRegion))`) + deps `[tileId, filters, brushRegion]`. [`ExplainSlicePanel`](../../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx) passes `event?.region` at the call site between `filters` and `cache`. Categorical value order preserved (not sorted) because the band-scale slice the renderer dispatches is itself ordered upstream via `distinctOrdered(data, enc.x.accessor)`; a NEGATIVE test pin catches a future drift to sorting. Footer + panel cache slots are deliberately split post-wave: footer = 2 segments (whole-chart insight), panel = 3 segments (brushed-region insight). 17 new tests in [`insightRegenCache.test.ts`](../../client/src/pages/Dashboard/lib/insightRegenCache.test.ts) (byte stability + backwards compat + per-kind collision avoidance) + 4 new source-inspection pins across [`useInsightRegen.test.ts`](../../client/src/pages/Dashboard/hooks/useInsightRegen.test.ts) + [`wi4Wire.test.ts`](../../client/src/pages/Dashboard/lib/wi4Wire.test.ts), plus 2 pre-existing pins broadened to match the widened call shape. No new test file — appended to an existing file already in [`server/package.json`](../../server/package.json)'s explicit list. Client typecheck at baseline (53). The WI4 master-plan series is now CLOSED at 3 of 3 brush-capable kinds + 1 of 4 dormancy-debt polish items shipped (remaining: Re-explain button, citation chips, box-2D scatter brush). See `docs/WAVES.md` for full entry.
- **Wave WI4-wiring-bar (2026-05-19)** — closes the WI4 widening series at 3 of 3 brush-capable kinds (Line + Area + Bar). [`BarRenderer.tsx`](../../client/src/lib/charts/visxRenderers/BarRenderer.tsx) had no pre-existing brush mechanics — only per-bar onClick handlers powering WD2 cross-filter + WD3 drill-through — so this wave adds mouse-down / move / up state + a `<rect>` overlay from scratch (mirroring WI4-wiring-area). Categorical-only — bars use a band scale on `enc.x.field`, no temporal branch; the `makeTemporalRegion` / `makeNumericRegion` constructors are DELIBERATELY NOT imported (negative pin in tests catches future drift). **First orientation-aware WI4 wiring**: vertical bars brush horizontally along the x-axis (`innerWidth`, `ew-resize` cursor); horizontal bars brush vertically along the y-axis (`innerHeight`, `ns-resize` cursor). The categorical-axis math collapses both orientations into a single 1D brush via three derived locals (`isVertical = orientation === "vertical"` / `brushAxisSize = isVertical ? innerWidth : innerHeight` / `brushAxisOffset = isVertical ? MARGIN.left : MARGIN.top`) so the three handlers (`onBrushDown` / `onBrushMove` / `onBrushUp`) stay orientation-agnostic. `onBrushDown` reads `coord = (isVertical ? pt.x : pt.y) - brushAxisOffset`, guards `coord < 0 || coord > brushAxisSize`, sets brushStart/brushEnd to coord, captures `e.altKey === true` into `brushExplainRef.current`. `onBrushUp` derives `i0` / `i1` from `(lo|hi / brushAxisSize) * xValues.length` and dispatches `dispatchExplainSlice` with `{ chartId, column: enc.x.field, region, sourceTileId, filters: dashboardFilters }` gated on `brushExplainRef.current && dashboardTile`. The svg's pre-existing `onMouseLeave={hideTooltip}` widens to an arrow that does BOTH `hideTooltip()` AND a brush state clear if `brushStart !== null`. Brush `<rect>` overlay is orientation-aware (full-height column in vertical mode, full-width band in horizontal mode), same primary tint / dashed stroke styling as LineRenderer + AreaRenderer + `pointerEvents="none"`. Per-bar `onMouseMove` tooltip handler gains a single-line `if (brushStart !== null) return;` guard so the tooltip doesn't flicker between bars during a brush. Pre-existing per-bar onClick (WD3 cmd-click drill / chat-explorer grid.toggleFilter / dashboard cross-filter) preserved byte-identical. 38 source-inspection tests across 16 suites in [`wi4WiringBar.test.ts`](../../client/src/pages/Dashboard/lib/wi4WiringBar.test.ts) — including NEGATIVE pins against the temporal / numeric region imports (categorical-only) and `setZoomRange` (no zoom on Bar). The WI4 master-plan series is now CLOSED — alt-drag on any line / area / bar chart inside a dashboard tile opens the ExplainSlicePanel with regenerated insight prose for the brushed sub-region. See `docs/WAVES.md` for full entry.
- **Wave WI4-wiring-area (2026-05-19)** — widens the WI4 wired surface from 1 of 3 brush-capable kinds to 2 by adding brush mechanics to [`AreaRenderer.tsx`](../../client/src/lib/charts/visxRenderers/AreaRenderer.tsx) from scratch. Unlike LineRenderer, AreaRenderer had no pre-existing brush mechanics — only an `onClick` handler powering WD2 cross-filter + WD3 drill-through — so this wave adds the mouse-down / move / up state + a `<rect>` overlay from scratch. New state declarations: `brushStart` / `brushEnd` useState (pixel x-coords in inner-plot space) + `brushExplainRef` useRef (alt flag, ref because it doesn't drive re-render). New `useEffect` resets brush state on `[data]` change (same shape as LineRenderer's Fix-5 effect). Three new handlers gated on `dashboardTile`: `onBrushDown` captures `e.altKey === true` into the ref + sets brushStart/brushEnd to the click x (with `[0, innerWidth]` bounds guard); `onBrushMove` short-circuits on `brushStart === null` or `!(e.buttons & 1)` (no active drag), clamps to `[0, innerWidth]` and updates brushEnd; `onBrushUp` gates on `isBrushDrag(brushStart, brushEnd, BRUSH_MIN_PX)` — click branch resets state and yields to existing `onClick` (the browser doesn't fire `click` after a drag so the brush and click paths never conflict), drag branch dispatches `dispatchExplainSlice` gated on `brushExplainRef.current && dashboardTile`. Temporal axis builds `makeTemporalRegion(startMs, endMs)` via `domMin + (lo / innerWidth) * (domMax - domMin)` interpolation; categorical axis builds `makeCategoricalRegion(xs.slice(i0, i1))` via the same Math.max/floor/min/ceil shape LineRenderer uses. **NO brush-to-zoom** is a deliberate scope decision — Area charts rarely warrant zoom and keeping the brush exclusively for explain-slice avoids the plain-drag-vs-alt-drag disambiguation complexity LineRenderer has. SVG element gains `onMouseDown` / `onMouseMove` / `onMouseUp` / `onMouseLeave` (safety net clearing brush state if cursor leaves mid-drag), cursor toggles between `ew-resize` (active brush) and the pre-existing `pointer` (idle in dashboard). `<rect>` overlay inserted inside `<Group>` after `GridRows` with same styling as LineRenderer (primary tint at 0.1 fill + 0.4 stroke + dashed `3 3` + `pointerEvents="none"`). Pre-existing WD2/WD3 click handlers preserved byte-identical. 37 source-inspection tests across 14 suites in [`wi4WiringArea.test.ts`](../../client/src/pages/Dashboard/lib/wi4WiringArea.test.ts) including NEGATIVE pins against `setZoomRange` / `useState<[number, number]>` (Area must not acquire a brush-to-zoom by accident). Companion [`wd2WiringRestTrend.test.ts`](../../client/src/pages/Dashboard/lib/wd2WiringRestTrend.test.ts) had two pins loosened — the stale `Math.abs(hi - lo) < 6` pin (pre-existing carry-forward from WI4-wiring-trend's missed cleanup) and the AreaRenderer cursor pin (direct consequence of this wave's brush-aware cursor expression). 96 tests green across WD2-trend + WD3-trend + WI4-trend + WI4-area + explainSlice + WI4-panel + WI4-wire regression suite. Client typecheck baseline preserved (53). WI4 wired surface now spans 2 of 3 brush-capable kinds (Line + Area); remaining WI4-wiring-bar adds BarRenderer brush mechanics from scratch (categorical-only). See `docs/WAVES.md` for full entry.
- **Wave WI4-wire (2026-05-19)** — closes the WI4 master-plan loop end-to-end on LineRenderer with real regenerated insight prose. [`ExplainSlicePanel.tsx`](../../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx) swaps the WI4-panel "Regenerated insight" placeholder for a `useInsightRegen` integration that narrows the brushed rows through the WI2 pipeline (`applyChartFilters(rows, event.filters)` → `filterRowsByBrushRegion(rows, event.column, event.region)`) and POSTs to the existing `/api/insight/regen` endpoint. Two new optional props: `chart?: ChartSpec | null` (the resolved chart spec) + `insightRegenCache?: InsightRegenCache` (the shared LRU+TTL cache lifted at DashboardView mount). `specLite` derivation mirrors [`ChartTileBody`](../../client/src/pages/Dashboard/Components/ChartTileBody.tsx)'s WI2-wire-bind shape field-for-field. `narrowedRows` is a two-stage `useMemo` (global filters first, brush region second — predicate-AND commutative; ordering matches `ChartTileBody`). `useInsightRegen` called unconditionally with `tileId: event?.chartId ?? IDLE_TILE_ID` (stable fallback `"__wi4_idle__"`) so React's rules-of-hooks are honoured. `useEffect` fires `regen.regenerate(specLite, narrowedRows)` keyed on `[event, specLite, narrowedRows]` — `regen` deliberately omitted from deps with an `eslint-disable` comment because the hook captures `regenerate` via `useRef` internally (a stable function reference); including it would render-loop. Four state-aware render branches replace the placeholder: `!chart` → "Could not resolve…"; `regen.loading` → "Regenerating insight for N brushed row(s)…" (singular / plural matched); `regen.error` → `role="alert"` with the error string (hook types error as `string | null`); `regen.entry?.text` → `whitespace-pre-wrap` entry prose; default → "Waiting for the first regeneration…". [`DashboardView`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) resolves the chart inline at the panel mount via an IIFE that parses `event.chartId` with `/^chart-(\d+)$/.exec(...)` and indexes `activeSheet.charts[idx] ?? null` (defensive — null if the tile was removed between brush-up and mount). `insightRegenCache={insightRegenCache}` threaded into the panel — same instance threaded into every per-tile `ChartTileBody` for WI2 regen, so a brushed slice can hit a cache slot populated by the per-tile "Re-explain" if filter hashes match. Zero new server-side code — the wave reuses the WI2 `/api/insight/regen` endpoint (cleaner than shipping a new endpoint; the brush is just an additional `applyChartFilters`-style filter applied client-side before the request). 29 source-inspection tests across 9 suites in [`wi4Wire.test.ts`](../../client/src/pages/Dashboard/lib/wi4Wire.test.ts). 103 cumulative tests across the full WI4 family (41 suites) all green. Client typecheck baseline preserved (53). The WI4 master-plan series is functionally complete on LineRenderer (1 of 3 brush-capable kinds); remaining waves WI4-wiring-area + WI4-wiring-bar widen the surface. See `docs/WAVES.md` for full entry.
- **Wave WI4-panel (2026-05-19)** — closes the user-visible explain-this-slice loop end-to-end on LineRenderer: alt-drag on a line chart now slides open a Radix right-side Sheet showing the brushed-slice pin metadata. New component [`ExplainSlicePanel.tsx`](../../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx) (~150 LOC, pure render, owns no state) mirrors [`DrillThroughSheet`](../../client/src/pages/Dashboard/Components/DrillThroughSheet.tsx)'s shape so the three click-intent receivers stay structurally parallel: WD2 → no panel (cross-filter applies in-place), WD3 → DrillThroughSheet (row table), WI4 → ExplainSlicePanel (regenerated insight). 2-prop interface (`event: ExplainSliceEvent | null` + `onOpenChange`); open state derives from `event !== null` (single source of truth). Three body sections: "Pinned slice" (chartId / column / region with a uppercase region.kind chip + `formatRegion(region)` for the bounds — numeric `[start, end]`, temporal `<startISO> → <endISO>` full canonical form via `new Date(ms).toISOString()`, categorical comma-joined with `MAX_CATEGORY_PREVIEW = 10` cap + `… +N more` suffix); "Filter context at brush time" (`summariseFilters(...)` lines, local copy of WD3-sheet's shape because the two receivers may diverge on filter display in future waves); "Regenerated insight" (WI4-wire placeholder pinning the `applyChartFilters` → `filterRowsByBrushRegion` → `useInsightRegen` pipeline via inline `<code>` references — same precedent as WD3-sheet's placeholder → WD3-sheet-fetch's swap). [`DashboardView`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) gains a sibling `explainSliceEvent` state (independent of `drillThroughEvent` so a future wave can hold both panels open at once), a `useEffect` window listener for `EXPLAIN_SLICE_EVENT` mirroring the `DRILL_THROUGH_EVENT` listener byte-for-byte (with stricter 3-field validation requiring `chartId` + `column` + `region` — the third field catches a malformed dispatch the foundation's `makeXRegion` helpers should have short-circuited), and the `<ExplainSlicePanel>` mount at the bottom of the JSX tree alongside `<DrillThroughSheet>`. Design choices: right-side Sheet (not Dialog) preserves the dashboard underneath; placeholder body (not "Loading…") avoids misleading messaging until WI4-wire lands; sibling state (not a discriminated union) keeps both panels independently mountable. 23 source-inspection tests across 10 suites in [`wi4Panel.test.ts`](../../client/src/pages/Dashboard/lib/wi4Panel.test.ts). 113 cumulative tests across WI4-foundation + WI4-wiring-trend + WI4-panel + WD3-sheet + WD3-wiring-rest-trend (46 suites) all green. Client typecheck baseline preserved (53). The WI4 master-plan series spans 3 of 5 waves shipped — round-trip works on LineRenderer with a placeholder body. Remaining waves: WI4-wire (swap placeholder for real `useInsightRegen` call), WI4-wiring-area + WI4-wiring-bar (add brush mechanics to Area + Bar renderers). See `docs/WAVES.md` for full entry.
- **Wave WI4-wiring-trend (2026-05-19)** — first per-renderer consumer of the WI4-foundation explainSlice helper. [`LineRenderer.tsx`](../../client/src/lib/charts/visxRenderers/LineRenderer.tsx) gains an alt-drag → explain-this-slice branch BEFORE the pre-existing brush-to-zoom branch. Plain drag continues to zoom; cmd/ctrl-click continues to drill (WD3 wiring intact); the three intents are mutually exclusive at brush-down time via two independent refs (`brushModifierRef` for cmd/ctrl, `brushExplainRef` for alt). New `brushExplainRef = useRef<boolean>(false)` captures `e.altKey === true` at brushDown AFTER the brushModifier capture (canonical order); consumed in the parameterless `onBrushUp`. Click-vs-drag threshold now derives from the foundation: `if (!isBrushDrag(brushStart, brushEnd, BRUSH_MIN_PX))` replaces the prior inline `Math.abs(hi - lo) < 6` (the literal is REMOVED — negative pin in tests catches re-introduction). Explain branch gated on `brushExplainRef.current && dashboardTile`; temporal axis computes `makeTemporalRegion(startMs, endMs)` from the existing `domMin + (lo / innerWidth) * (domMax - domMin)` interpolation, categorical axis computes `makeCategoricalRegion(xs.slice(i0, i1))` from the same Math.max/floor/min/ceil shape as the zoom branch (alt-dragging brushes the SAME range plain-dragging would zoom to — user's mental model survives). Dispatch shape: `{ chartId, column: xCh.field, region, sourceTileId, filters: dashboardFilters }`, guarded by `if (region)` so a null constructor return (zero-width brush) doesn't fire a malformed event. Both refs reset to false after dispatch + after the click path. The two existing zoom branches (temporal `setZoomRange([newMin, newMax])` + categorical `setZoomRange([i0, i1])`) remain untouched. Alt-drag chosen over shift / cmd because cmd is owned by WD3 (drill), shift is reserved for future multi-select intents, and alt is widely available across keyboards (option on macOS); two independent refs (not a tri-state enum) because the intents are orthogonal and ordering matters (explain checked first because it's the "wider" intent — a real drag — vs drill's tiny-drag click). 23 source-inspection tests across 11 suites in [`wi4WiringTrend.test.ts`](../../client/src/pages/Dashboard/lib/wi4WiringTrend.test.ts). The companion WD3 test (`wd3WiringRestTrend.test.ts`) had ONE over-tight regex loosened to allow the new sibling reset between `brushModifierRef = false` and `setBrushStart(null)`. 69 cumulative tests across WD3-trend + WI4-trend + foundation (29 suites) all green. Client typecheck baseline preserved (53). Remaining WI4 waves: WI4-panel (receiver UI), WI4-wire (`useInsightRegen` integration with the brushed filteredRows), WI4-wiring-area + WI4-wiring-bar (Area + Bar brush mechanics — both renderers currently lack brush). See `docs/WAVES.md` for full entry.
- **Wave WI4-foundation (2026-05-19)** — opens the WI4 (explain-this-slice) master-plan series with a new pure-fn module [`explainSlice.ts`](../../client/src/pages/Dashboard/lib/explainSlice.ts) (~285 LOC), the THIRD click-intent on chart marks alongside WD2's [`crossFilter.ts`](../../client/src/pages/Dashboard/lib/crossFilter.ts) and WD3's [`drillThrough.ts`](../../client/src/pages/Dashboard/lib/drillThrough.ts). Where WD2 captures a single click and WD3 captures a single cmd/ctrl-click, WI4 captures a RECT-DRAG (mouse-down + mouse-up at ≥ 6 px apart) and dispatches an `ExplainSliceEvent` that re-runs the WI2 insight generator on JUST the brushed sub-region — the cmd/ctrl modifier is irrelevant because a brush drag is already distinct from a click by virtue of cursor travel. The module exports: **`BRUSH_MIN_PX = 6`** — lifts [`LineRenderer.tsx`](../../client/src/lib/charts/visxRenderers/LineRenderer.tsx)'s inline `Math.abs(hi - lo) < 6` click-vs-drag threshold into a single shared constant so subsequent WI4-wiring waves replace their inline checks with the helper. **`BrushRegion`** discriminated union covering the three x-axis flavours every WI4-wiring target binds to: `{ kind: "numeric"; start; end }` (continuous quantitative axis), `{ kind: "temporal"; startMs; endMs }` (Date / ms axis on Line / Area when `isTemporal`), `{ kind: "categorical"; values }` (discrete axis on Bar / cat-x Line / cat-x Area). Storing ms (not Date) on `temporal` keeps the event JSON-serialisable for downstream hash-key debouncing. **`ExplainSliceEvent { chartId, column, region, sourceTileId?, filters? }`** — field shape mirrors `DrillThroughEvent` (`chartId` / `column` / `sourceTileId` / `filters`) so the three click-intent events stay co-located in the renderer's mental model; the load-bearing new field is `region`. **`EXPLAIN_SLICE_EVENT = "marico:explain-slice"`** — the canonical CustomEvent name, same `marico:` prefix as the WD2/WD3 events. **`isBrushDrag(start, end, minPx = BRUSH_MIN_PX)`** — pure pixel-distance gate; null/undefined for either bound returns false (mid-state-reset defensive); zero-width returns false; direction-agnostic. **`makeNumericRegion` / `makeTemporalRegion` / `makeCategoricalRegion`** — constructor helpers enforcing `start <= end` normalisation, zero-width rejection (returns `null` — caller should fall back to click path), non-finite numeric rejection. Categorical helper clone-copies the input array so post-construct caller mutation doesn't leak. **`filterRowsByBrushRegion<T>(rows, column, region)`** — pure post-filter, commutative-AND with `applyChartFilters` so the WI4 panel composes them as `applyChartFilters(rows, filters)` → `filterRowsByBrushRegion(…, region)`. Numeric branch explicitly drops null / undefined / empty-string BEFORE coercion (otherwise `Number(null) === 0` would silently match `[0, N]`); temporal branch coerces via private `asTimeMs` (Date / ISO / ms) — un-parseable dates drop; categorical branch coerces via private `asCategoryString` mirroring `crossFilter.toFilterValue`'s null-bucket semantics (`null` → `"null"`). **`dispatchExplainSlice`** — SSR-safe CustomEvent dispatcher mirroring the WD2 / WD3 dispatchers. 28 runtime tests across 11 suites in [`explainSlice.test.ts`](../../client/src/pages/Dashboard/lib/explainSlice.test.ts) — actual import + runtime assertions, not source-inspection (the helper has no React dependency). Test file appended to `server/package.json` per invariant #4. Client typecheck baseline preserved (53). Remaining WI4 waves: WI4-wiring-trend (LineRenderer + AreaRenderer brush), WI4-wiring-bar (BarRenderer new brush mechanics), WI4-panel (Radix Sheet/Dialog receiver UI), WI4-wire (`useInsightRegen` integration with the brushed `filteredRows`). See `docs/WAVES.md` for full entry.
- **Wave WD3-sheet-fetch (2026-05-19)** — DrillThroughSheet fetches real rows from the WD3-server endpoint, closing the user-visible WD3 round-trip end-to-end. New hook [`useDrillThroughRows({ dashboardId, event })`](../../client/src/pages/Dashboard/hooks/useDrillThroughRows.ts) wraps `useQuery` from @tanstack/react-query — queryKey serialises the full event payload (6 pieces) so distinct drill requests get distinct cache slots; `enabled: !!event && !!dashboardId` keeps the query idle when no drill is active; queryFn POSTs to `/api/dashboards/:id/drill?chartId=&column=&value=` with body `{ filters, extraPins }`. `stringifyForKey(v)` mirrors the server's `stringifyForComparison`. New component [`<DrillThroughRowTable response={...} />`](../../client/src/pages/Dashboard/Components/DrillThroughRowTable.tsx) renders the response as a compact `<table>` inside `overflow-x-auto` (Radix Sheet is sm:max-w-md = 28rem so wide rows scroll); empty-state on `rows.length === 0`; cap-applied affordance shows "Showing X of Y matching rows"; `formatCell(v)` handles null/undefined → `—`, Date → ISO, other → String. [`DrillThroughSheet`](../../client/src/pages/Dashboard/Components/DrillThroughSheet.tsx) gains a REQUIRED `dashboardId: string` prop (threaded from DashboardView as `dashboard.id`); the dashed-border placeholder section is REPLACED by a new "Underlying rows" `<section>` whose body branches on the hook state (`isLoading` → "Loading rows…"; `isError` → `role="alert"` with the error message; `data` → `<DrillThroughRowTable response={...} />`). 30 source-inspection tests across 8 suites in [`wd3SheetFetch.test.ts`](../../client/src/pages/Dashboard/lib/wd3SheetFetch.test.ts). Full WD3 family regression suite (10 test files): 248 tests across 73 suites all green. WD3 master-plan series is functionally complete — a cmd/ctrl-click on any of the 15 wired chart marks slides open the sheet, fetches matching rows from the server (filtered by active filters + primary pin + heatmap extra pins), and renders them in a compact table. See `docs/WAVES.md` for full entry.
- **Wave WD3-server (2026-05-19)** — `POST /api/dashboards/:id/drill` endpoint closes the WD3 round-trip end-to-end. Server-side resolver for the WD3 drill-through family. New pure-fn service [`dashboardDrillThrough.service.ts`](../../server/services/dashboardDrillThrough.service.ts) exports `DrillThroughPin` + `DrillThroughRequest` + `DrillThroughResponse` types (server-side mirror of the client `drillThrough.ts` shape); `findChartByTileId(dashboard, tileId)` parses `chart-${idx}` and walks all sheets (same convention as `DashboardView.tsx` tile derivation, first-match-across-sheets for multi-sheet dashboards); `filterChartRowsForDrill(rows, request)` applies active filters FIRST (categorical / numeric / date — ported semantics from the client's `applyChartFilters`), then primary pin, then each `extraPin` (AND-intersection), caps at `DRILL_ROW_CAP = 1000`, returns `{ rows, totalMatched, capApplied }`; `resolveDrillThrough(dashboard, chartId, request)` top-level resolver returns the full response with chart metadata (throws `chart_not_found:<id>` for the controller to map to 404). `stringifyForComparison(v)` mirrors the client's `toFilterValue` so server-side comparison agrees with wire-storage semantics (null / undefined → `"null"`; numbers / booleans / Dates → `String(v)`). New controller [`dashboardDrillThroughController.ts`](../../server/controllers/dashboardDrillThroughController.ts) (auth via `getAuthenticatedEmail`; 401 missing, 400 missing chartId/column, 404 dashboard/chart missing, 500 unexpected). POST (not GET) so the body can carry a non-trivial `ActiveChartFilters` object — query-string encoding would force JSON-in-URL escapes. Route registered in [`server/routes/dashboards.ts`](../../server/routes/dashboards.ts) as `router.post('/dashboards/:id/drill', drillDashboardController)`. 30 tests across 10 suites in [`dashboardDrillThroughWD3Server.test.ts`](../../server/tests/dashboardDrillThroughWD3Server.test.ts): primary pin filter (3); + categorical / numeric / date filter composition (5); + extraPins AND-intersection (3); row cap (2); findChartByTileId (5); resolveDrillThrough (3); controller source contract (6); route registration (3). WD3 family closes the round-trip end-to-end: wiring (15 of 15) + receiver sheet + server endpoint. The sheet's placeholder body swaps to a TanStack-Query-fetched row list in the follow-on WD3-sheet-fetch wave. See `docs/WAVES.md` for full entry.
- **Wave WD3-wiring-echarts (2026-05-19)** — closes the WD3 wiring family at 15 of 15 chart kinds. 5 ECharts marks ([`TreemapRenderer`](../../client/src/lib/charts/echartsRenderers/TreemapRenderer.tsx) + [`SunburstRenderer`](../../client/src/lib/charts/echartsRenderers/SpecialtyRenderers.tsx) + [`SankeyRenderer`](../../client/src/lib/charts/echartsRenderers/SpecialtyRenderers.tsx) + [`CalendarRenderer`](../../client/src/lib/charts/echartsRenderers/SpecialtyRenderers.tsx) + [`CandlestickRenderer`](../../client/src/lib/charts/echartsRenderers/SpecialtyRenderers.tsx)) gain a top-of-handler modifier branch inside their existing `onChartClick(params)` callback. ECharts wraps the native MouseEvent at `params.event.event` (the ZRender event object wrapping the DOM event); `isModifierClick(p?.event?.event)` reads metaKey/ctrlKey through the foundation's sparse-event-shape contract with NO foundation changes. Per-renderer wiring follows the WD2 dispatch shape (column-symmetry): Treemap `labelCh.field` × leaf name (parents skipped); Sunburst same; Sankey `sourceCh.field` × `p.name` (edges skipped); Calendar `dateCh.field` × the `[date]` tuple element (raw ISO string); Candlestick `xCh.field` × `xs[idx]` (the categorical x-axis label, NOT the OHLC tuple values). All values pass RAW (NOT toFilterValue-coerced) — server canonicaliser picks Date / number / categorical comparison per inferred column type. Each renderer's modifier branch fires BEFORE the pre-existing `dispatchCrossFilter` call with `return;` after dispatch (single-intent). In 4 of 5 renderers (Treemap / Sankey / Calendar / Candlestick), `dashboardFilters = dashboardTile?.filters` was lifted ABOVE the click handler so the useCallback closure can capture it for the drill `filters` snapshot — Sunburst was already correctly ordered. Each handler's useCallback deps gain `dashboardFilters`. Params cast widens with `event?: { event?: { metaKey?: boolean; ctrlKey?: boolean } }` so the optional-chaining narrows correctly. Parallel + Choropleth + Gauge stay un-wired (no meaningful categorical click target — same skip-list as WD2-wiring-echarts). 41 source-inspection tests across 9 suites in [`wd3WiringEcharts.test.ts`](../../client/src/pages/Dashboard/lib/wd3WiringEcharts.test.ts). WD3 family closes the wiring side at 15 of 15 chart kinds wired (10 visx + 5 ECharts) + receiver — the user-visible cmd / ctrl-click → side-sheet round-trip works end-to-end across every renderer with a categorical filter target. Remaining: WD3-server (the `/api/dashboards/:id/drill` endpoint replacing the sheet's placeholder body). See `docs/WAVES.md` for full entry.
- **Wave WD3-wiring-rest-rect (2026-05-19)** — extends WD3 to RectRenderer heatmap cells, with a minimal foundation widening. [`drillThrough.ts`](../../client/src/pages/Dashboard/lib/drillThrough.ts) exports a new `DrillThroughPin = { column: string; value: unknown }` interface and adds optional `extraPins?: DrillThroughPin[]` to `DrillThroughEvent` so a single event can carry a multi-dim drill target (row × col intersection). Three design options were considered: (1) widen `column` to a union shape — breaks every consumer; (2) fire 2 events with receiver de-duping — timing-fragile + AND/OR semantics ambiguous; (3) additive optional `extraPins` — chosen, smallest blast radius, fully backwards-compat, N-pin-capable. [`RectRenderer.tsx`](../../client/src/lib/charts/visxRenderers/RectRenderer.tsx) widens its cell onClick to accept `React.MouseEvent<SVGRectElement>` and gains a top-of-handler `if (isModifierClick(event))` branch BEFORE the pre-existing two-event WD2 cross-filter dispatch. Drill payload: `chartId` = tile id; `column` = `rowCh.field` (primary = row dim); `value` = `rowRawByKey.get(row)` RAW; `extraPins: [{ column: colCh.field, value: colRawByKey.get(col) }]` (col dim); `sourceTileId` = tile id; `filters` = `dashboardFilters` snapshot. Row is primary because it mirrors the pre-existing row-first WD2 cross-filter ordering. Both row + col values pass RAW (NOT `toFilterValue`-coerced) — server canonicaliser picks Date / number / categorical comparison per inferred type. The `return;` after dispatch is single-intent-load-bearing (drill XOR the two cross-filter events). [`DrillThroughSheet`](../../client/src/pages/Dashboard/Components/DrillThroughSheet.tsx) gains a new conditional "Additional pins" section (gated on `event.extraPins && event.extraPins.length > 0`) that iterates `extraPins.map((pin) => <Fragment key={pin.column}>...</Fragment>)` rendering each pin's column + canonicalised value in a parallel `<dl>` grid, with a one-line AND-filter explanation below. Primary "Pinned slice" section unchanged — single-pin events render byte-identical. 22 source-inspection tests across 8 suites in [`wd3WiringRestRect.test.ts`](../../client/src/pages/Dashboard/lib/wd3WiringRestRect.test.ts) (foundation widening + RectRenderer modifier branch + 6-field payload + WD2 two-event regression + sheet Additional-pins rendering + single-pin backwards-compat + column-symmetry + drill count = 1 / cross-filter count = 2). WD3 family now spans 10 of 15 chart kinds wired — all 10 visx renderers + receiver. Remaining: 5 ECharts marks. See `docs/WAVES.md` for full entry.
- **Wave WD3-wiring-rest-point (2026-05-19)** — extends WD3 to PointRenderer (scatter / bubble) at the per-mark level. [`PointRenderer.tsx`](../../client/src/lib/charts/visxRenderers/PointRenderer.tsx)'s existing `onPointClick` arrow widens from parameterless `() => {...}` to `(e: React.MouseEvent<SVGElement>) => {...}` and gains a top-of-handler `if (isModifierClick(e)) { dispatchDrillThrough({...}); return; }` branch BEFORE the pre-existing `dispatchCrossFilter` call — same single-event-handler shape as the WD3-wiring-rest-cat family. The conditional `crossFilterReady ? ... : undefined` shape is preserved (the WD2 gate): drill inherits the SAME opt-in domain as cross-filter — pure quantitative scatters with no `colorCh` have no categorical filter target either, so the WD3 wiring doesn't accidentally fire drill on cmd-click for those. 5-field payload: `chartId` = `dashboardTile!.tileId`; `column` = `colorCh!.field` (per-mark filter target — PointRenderer is unique in keying on `colorCh` not `xCh` because its filter target is the color group, not the quantitative x axis); `value` = `p.rawColor` RAW (NOT `toFilterValue`-coerced); `sourceTileId` = `dashboardTile!.tileId`; `filters` = `dashboardFilters` snapshot. Both render paths (`<Circle>` default + `<path>` glyph) bind the same widened `onClick={onPointClick}` so the dispatch shape is uniform. WD2-dim per-point factor (`isDashboardDimmed` / `dimMul`) composes unchanged. 16 source-inspection tests across 7 suites in [`wd3WiringRestPoint.test.ts`](../../client/src/pages/Dashboard/lib/wd3WiringRestPoint.test.ts) (imports + handler widening + modifier branch order + 5-field payload + raw-value pin + return-after-dispatch + WD2 regression + cursorStyle gate + WD2-dim contract preserved + both render paths bind handler + WD3 marker + cross-cutting column-symmetry across drill / cross-filter / dim on `colorCh.field` + drill dispatch count = 1). WD3 family now spans 9 of 15 chart kinds wired (bar + cat-5 + line + area + point) — every visx renderer with a per-mark categorical filter target now drills. See `docs/WAVES.md` for full entry.
- **Wave WD3-wiring-rest-trend (2026-05-19)** — extends WD3 to trend renderers. [`AreaRenderer`](../../client/src/lib/charts/visxRenderers/AreaRenderer.tsx) reuses the existing nearest-x lookup inside `onClick`; the WD3 wiring is an inline `if (isModifierClick(e)) { dispatchDrillThrough({...}); return; }` branch inside the `if (nearest)` block, before the existing `dispatchCrossFilter` call. [`LineRenderer`](../../client/src/lib/charts/visxRenderers/LineRenderer.tsx) differs because `dispatchCrossFilter` fires inside the PARAMETERLESS `onBrushUp` (brush-zoom plumbing piggybacks the click semantic — a `< 6 px` brush is treated as a click). The brushUp has no MouseEvent access, so a new `brushModifierRef = useRef<boolean>(false)` captures `isModifierClick(e)` at `onBrushDown` time and the brushUp click path branches on `brushModifierRef.current` (drill XOR cross-filter, drill branch FIRST). `useRef` not `useState` because the flag doesn't drive a re-render. Reset to `false` BEFORE `setBrushStart(null)` / `setBrushEnd(null)` so a brush-zoom mouseup with cmd held doesn't leave the flag dirty. Both renderers' WD2 nearest-x lookup is preserved verbatim; only the dispatcher diverges on the modifier flag. 5-field payload mirrors prior waves (`chartId` = tile id; `column` = `xCh.field`; `value` = `nearest.x` RAW; `sourceTileId` = tile id; `filters` = `dashboardFilters` snapshot). 18 source-inspection tests across 7 suites in [`wd3WiringRestTrend.test.ts`](../../client/src/pages/Dashboard/lib/wd3WiringRestTrend.test.ts) — AreaRenderer's inline branch shape + LineRenderer's brushModifierRef declare/capture/consume/reset triad + 5-field payload + raw-value pin + WD2 regression + column-symmetry + drill-count=1 single-shot enforcement. WD3 family now spans 8 of 15 chart kinds wired (bar + cat-5 + line + area) + receiver. See `docs/WAVES.md` for full entry.
- **Wave WD3-sheet (2026-05-19)** — closes the user-visible loop for the WD3 drill-through family. New [`DrillThroughSheet.tsx`](../../client/src/pages/Dashboard/Components/DrillThroughSheet.tsx) is a pure render component wrapping the Radix [`Sheet`](../../client/src/components/ui/sheet.tsx) primitive (right-side slide-in). Props `{ event: DrillThroughEvent | null; onOpenChange: (open: boolean) => void }` — `open` is DERIVED from `event !== null` inside (single source of truth; the two-prop `open` + `event` shape is an anti-pattern). Body shows chartId / column / value via `<dl>` grid + a "Filter context at click time" section iterating `event.filters` via a local `summariseFilters()` helper that branches on each `ChartFilterSelection` type (categorical → `column ∈ {v1, v2}`; date → `column between start and end`; numeric → `column in [min, max]`) + a dashed placeholder pinning the WD3-server endpoint name so a future fetch wiring can grep "WD3-server" and swap. Value display canonicalises via `toFilterValue` so null / Date / number / boolean stringify uniformly with the cross-filter wire-storage format. [`DashboardView.tsx`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) gains: named imports of `DRILL_THROUGH_EVENT` + `DrillThroughEvent` type + `DrillThroughSheet`; a `drillThroughEvent` state slice (`DrillThroughEvent | null`); a `useEffect` subscribing to `DRILL_THROUGH_EVENT` (mirrors the cross-filter listener's shape — SSR-safe `typeof window` guard, addEventListener + return-cleanup; STRICTER detail validation requires BOTH `chartId` AND `column` to be strings because the future server fetch needs chartId); JSX mount after `<ShareDashboardDialog>` at the dialog cluster locality. 21 source-inspection tests across 7 suites in [`wd3Sheet.test.ts`](../../client/src/pages/Dashboard/lib/wd3Sheet.test.ts). WD3 family round-trip works end-to-end on 6 of 15 chart kinds (Bar + Arc + Funnel + Box + Waterfall + Combo). See `docs/WAVES.md` for full entry.
- **Wave WD3-wiring-rest-cat (2026-05-19)** — fans out the WD3-wiring-bar pattern to the 5 categorical visx renderers ([`ArcRenderer`](../../client/src/lib/charts/visxRenderers/ArcRenderer.tsx), [`FunnelRenderer`](../../client/src/lib/charts/visxRenderers/FunnelRenderer.tsx), [`BoxRenderer`](../../client/src/lib/charts/visxRenderers/BoxRenderer.tsx), [`WaterfallRenderer`](../../client/src/lib/charts/visxRenderers/WaterfallRenderer.tsx), [`ComboRenderer`](../../client/src/lib/charts/visxRenderers/ComboRenderer.tsx)). Each renderer's onClick widens to accept `(event: React.MouseEvent<SVGElement>)` and gains a top-of-handler `if (isModifierClick(event)) { dispatchDrillThrough({ chartId: dashboardTile.tileId, column: <renderer-specific>, value: <renderer-specific raw>, sourceTileId: dashboardTile.tileId, filters: dashboardFilters }); return; }` branch. Renderer-specific column × raw-value pairs mirror WD2-wiring-rest-cat: Arc `labelCh.field` × `arc.data.rawKey`; Funnel `enc.x.field` × `s.rawLabel`; Box `enc.x.field` × `s.rawCategory`; Waterfall `enc.x.field` × `b.rawCategory` (outer `clickable` gate AND-skips `b.isTotal` running-total rows so summary bars aren't drillable); Combo `xCh.field` × `rawX` (bars only — secondary-axis `<LinePath>` deliberately un-wired since a continuous trend has no per-mark categorical brush target). All renderers already lifted `dashboardFilters` for the WD2-dim concern so the WD3 payload reuses the same reference. Values pass RAW (NOT `toFilterValue`-coerced) — server-side canonicalisation picks the right Date / number / categorical comparison per column type. 44 parametric source-inspection tests across 11 suites in [`wd3WiringRestCat.test.ts`](../../client/src/pages/Dashboard/lib/wd3WiringRestCat.test.ts) (per-renderer imports + branch shape + 5-field payload + return + raw-value pin + WD2 regression; cross-cutting marker / column-symmetry / Waterfall-gate / Combo-bars-only). WD3 family now spans 6 of 15 chart kinds wired (bar + cat-5); matches WD2-wiring-rest-cat scope wave-for-wave. See `docs/WAVES.md` for full entry.
- **Wave WD3-wiring-bar (2026-05-19)** — first per-renderer consumer of WD3-foundation. [`BarRenderer.tsx`](../../client/src/lib/charts/visxRenderers/BarRenderer.tsx)'s `onClick` widens from `() => {...}` to `(event: React.MouseEvent<SVGElement>) => {...}` so the modifier check can read the event flags. New top-of-handler branch: `if (dashboardTile && isModifierClick(event)) { dispatchDrillThrough({ chartId: dashboardTile.tileId, column: enc.x.field, value: c.outerRaw, sourceTileId: dashboardTile.tileId, filters: dashboardFilters }); return; }` — short-circuits BEFORE the pre-existing `grid.inGrid` (chat / explorer) and `dispatchCrossFilter` (dashboard) branches. The AND-gate is load-bearing: chat / explorer cmd-clicks fall through unchanged because drill-through is dashboard-only (no server endpoint for chat charts). The 5-field payload: `chartId` = tile id (dashboard's chart-by-position lookup convention; same as XLSX export); `column` = `enc.x.field` (shared with WD2-dim + WD2-wiring's dispatch column — symmetry pinned in a cross-cutting test); `value` = `c.outerRaw` RAW (NOT `toFilterValue(c.outerRaw)` — drill-through coercion is the server's job, the type-original raw lets the server's canonicaliser pick the right Date/number/categorical comparison); `sourceTileId` = same tile id (for tile-scoped UI); `filters` = `dashboardFilters` snapshot (captured at click time so a concurrent global-filter edit on another tile doesn't race with the drill request). 12 source-inspection tests across 3 suites in [`wd3WiringBar.test.ts`](../../client/src/pages/Dashboard/lib/wd3WiringBar.test.ts) (imports / event typing / branch order / dashboardTile gate / 5-field payload / `return;` after dispatch / value-passed-raw negative pin / WD2 cross-filter regression / chat-explorer regression / WD3 marker / dim+dispatch+drill column symmetry). See `docs/WAVES.md` for full entry.
- **Wave WD3-foundation (2026-05-19)** — drill-through pure helper module. New [`drillThrough.ts`](../../client/src/pages/Dashboard/lib/drillThrough.ts) mirrors [`crossFilter.ts`](../../client/src/pages/Dashboard/lib/crossFilter.ts)'s shape — `DrillThroughEvent` interface (`chartId`, `column`, `value: unknown`, optional `sourceTileId` + `filters` snapshot), `DRILL_THROUGH_EVENT = "marico:drill-through"` canonical CustomEvent name, `isModifierClick(event)` cross-platform check (macOS `metaKey` ⌘; Windows / Linux `ctrlKey`; either fires drill-through; sparse event shape accepts React synthetic / native DOM / ECharts onChartClick alike; null / undefined → false defensive), `dispatchDrillThrough(event)` SSR-safe dispatcher. The cmd / ctrl detection MUST be uniform across all 15 renderers (10 visx + 5 ECharts) so the modifier check lives in a single helper rather than duplicated per-renderer. The optional `filters` snapshot is the field that doesn't exist on `CrossFilterEvent` — drill-through is a server-side query that needs the entire filter context atomically at click time (no race with a concurrent global-filter edit). Renderer onClick handler shape (to be wired in subsequent waves): `if (isModifierClick(event)) { dispatchDrillThrough({...}); return; } dispatchCrossFilter({...});` — a clean two-line branch. 11 pure-function tests across 4 suites in [`drillThrough.test.ts`](../../client/src/pages/Dashboard/lib/drillThrough.test.ts) (DRILL_THROUGH_EVENT canonical name; metaKey / ctrlKey / both / neither / null / undefined / shift+alt-ignored; SSR-safe short-circuit; minimal vs. full event shape). Pairs naturally with the now-complete WD2-wiring + WD2-dim families (which provide the click capture + visual feedback respectively); WD3 layers the modifier-key intent on top. See `docs/WAVES.md` for full entry.
- **Wave WD2-dim-echarts-rest (2026-05-19)** — closes the WD2-dim-* family. SankeyRenderer + CalendarRenderer + CandlestickRenderer (in [`SpecialtyRenderers.tsx`](../../client/src/lib/charts/echartsRenderers/SpecialtyRenderers.tsx)) per-dataItem dim via separate `dimmedX` memos with identity short-circuit on dim-OFF. Sankey per-node dim on `sourceCh.field`, edges un-dimmed (same carve-out as WD2-wiring-echarts dispatch). Calendar per-cell dim on `dateCh.field` via mixed-shape array (matching cells stay as `[date, value]` tuples, non-matching promoted to `{ value: [date, value], itemStyle: { opacity: 0.4 } }`); range + visualMap min/max read pre-wave `series` so tuple destructuring stays safe. Candlestick per-bar dim on `xCh.field` via `xs[i]` membership (NOT OHLC tuple values — those are quantitative). `optionsKey` field name kept as pre-wave key (`nodes` / `series`) so dim-off JSON is byte-identical (no canvas re-render on mount). 23 source-inspection tests across 4 suites in [`wd2DimEchartsRest.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimEchartsRest.test.ts). WD2-dim-* family now spans 10 visx + 5 ECharts = 15 chart kinds with a categorical filter target — matches the WD2-wiring family end-to-end. See `docs/WAVES.md` for full entry.
- **Wave WD2-dim-echarts-treemap (2026-05-19)** — first wave on the WD2-dim-echarts series. [`TreemapRenderer.tsx`](../../client/src/lib/charts/echartsRenderers/TreemapRenderer.tsx) + SunburstRenderer (in [`SpecialtyRenderers.tsx`](../../client/src/lib/charts/echartsRenderers/SpecialtyRenderers.tsx)) leaf-only dim on `labelCh.field`. Diverges from the visx pack's post-render React-prop mutation mechanism: ECharts mounts its own canvas via `EChartsBase` and reads per-dataItem styling from `itemStyle.opacity` at series-construction time, so the dim factor must be injected inline into the dataItem objects inside each renderer's `tree` memo. Both `TreemapNode.itemStyle` + `SunburstNode` interfaces widened to allow `opacity?: number`. Triplet lifted BEFORE the `tree` memo (consumed at build time). A local `dimLeaf(name): NodeType["itemStyle"] | undefined` arrow returns `{ opacity: 0.4 }` for non-matching leaves, `undefined` for matching; both flat (`!groupCh`) and hierarchical projections use the conditional spread (`itemStyle ? { name, value, itemStyle } : { name, value }`) so matching leaves emit a byte-identical pre-wave shape — keeps `optionsKey = JSON.stringify({ tree, w, h })` stable on dim-off transitions. Tree memo deps widened to include `dashboardDimActive` + `dashboardFilters` so dim-state changes rebuild the tree and trigger ECharts re-render via `optionsKey`. Parents stay un-dimmed (structural hierarchy; mirrors the WD2-wiring-echarts leaf-only dispatch carve-out — parent-ring filtering would need a two-column dispatch shape, deferred). 19 source-inspection tests across 7 suites in [`wd2DimEchartsTreemap.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimEchartsTreemap.test.ts). WD2-dim-* family now spans 10 visx + 2 ECharts renderers. Remaining: WD2-dim-echarts-rest (Sankey + Calendar + Candlestick — each per-renderer dataItem shape: nodes-array promotion for Sankey, tuple → object promotion for Calendar, parallel itemStyle-by-index array for Candlestick OHLC tuples). See `docs/WAVES.md` for full entry.
- **Wave WD2-dim-point (2026-05-19)** — [`PointRenderer.tsx`](../../client/src/lib/charts/visxRenderers/PointRenderer.tsx) scatter per-point dim on `colorCh.field`. Closes the WD2-dim-* family for every visx renderer with a categorical filter target (10 of 10: bar + cat-5 + rect + line + area + point). Diverges from WD2-dim-trend's per-series shape because scatter marks are individually filter-targetable — WD2-wiring-rest-point's dispatch is per-point on `p.rawColor`, so the dim is per-point too. `isCrossFilterActive` added to the crossFilter named-import alongside `dispatchCrossFilter` + `toFilterValue`. Triplet lifted right after the existing `crossFilterReady` gate (dispatch + dim share the exact same applicability domain `!!dashboardTile && !!colorCh` so they stay opt-in together — co-located as a discoverable pair): `dashboardFilters = dashboardTile?.filters`; `colorFilterSel = colorCh ? dashboardFilters?.[colorCh.field] : undefined`; `dashboardDimActive = !!colorCh && !!colorFilterSel && colorFilterSel.type === "categorical" && colorFilterSel.values.length > 0`. Inside `points.map`, after `cx` / `cy` lift but BEFORE the `if (shapeCh)` render branching, per-point `isDashboardDimmed = dashboardDimActive && !isCrossFilterActive(dashboardFilters!, colorCh!.field, p.rawColor)` (non-null assertions safe because `dashboardDimActive` AND-gates on `!!colorCh`; `p.rawColor` not `p.colorKey` because non-string color dims stringify differently via `asString` vs. `toFilterValue`). Single `dimMul = isDashboardDimmed ? 0.4 : 1` lifted once per point so the fill (`0.7 * op`) and the stroke (`op`) both consume the same factor on whichever branch renders — a dimmed fill with a non-dimmed ring would render as a faded dot with a stark outline. Both render branches mutate to multiply `dimMul`: glyph `<path>` (shape-encoded scatter) gains `fillOpacity={0.7 * op * dimMul}` + `strokeOpacity={op * dimMul}`; plain `<Circle>` (default scatter) takes the identical composition. Opacity-only contract preserved across the family (no outline for matching points). Mirrors WD2-dim-area's `dimMul`-shared-between-fill-and-stroke pattern at per-iteration granularity (area dims once per series; point dims once per point). 19 source-inspection tests across 7 suites in [`wd2DimPoint.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimPoint.test.ts). Remaining: WD2-dim-echarts (5 ECharts marks via itemStyle.opacity per dataItem at the buildOptions level). See `docs/WAVES.md` for full entry.
- **Wave WD2-dim-trend (2026-05-19)** — [`LineRenderer.tsx`](../../client/src/lib/charts/visxRenderers/LineRenderer.tsx) + [`AreaRenderer.tsx`](../../client/src/lib/charts/visxRenderers/AreaRenderer.tsx) per-series dim on `colorCh.field`. Trend renderers have continuous-x marks (lines / stacked areas) so the per-mark dim shape doesn't apply — dim concept is per-SERIES: when an active categorical cross-filter on `colorCh.field` doesn't include a series's `rawColor`, the series's stroke / fill opacities multiply by 0.4. Both `Series` interfaces gain `rawColor?: unknown` (preserved from the first occurrence per colorCh group; the colorCh group builders refactored from per-row push into `Map<string, { points, rawColor }>` so the type-original color value survives). Triplet lifted on `colorCh.field` with `!!colorCh` gate so single-series trends skip dim entirely + the `colorCh!.field` non-null assertion inside the map is type-safe. LinePath strokeOpacity composes `op * (isDashboardDimmed ? 0.4 : 1)` preserving the legend-opacity factor. AreaRenderer lifts a single `dimMul` shared between `<AreaClosed fillOpacity={0.55 * op * dimMul}>` + `<LinePath strokeOpacity={op * dimMul}>` so a dimmed area and its outline stay visually coherent. AreaRenderer's stacked memo propagates `rawColor` via spread (`{ ...s, points: stackedPoints }`). 19 source-inspection tests across 5 suites in [`wd2DimTrend.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimTrend.test.ts). WD2-dim-* family now spans 9 of 13 visx renderers (bar + cat-5 + rect + line + area). Remaining: WD2-dim-point (scatter per-point on colorCh), WD2-dim-echarts (5 ECharts marks via itemStyle.opacity per dataItem). See `docs/WAVES.md` for full entry.
- **Wave WD2-dim-rect (2026-05-19)** — [`RectRenderer.tsx`](../../client/src/lib/charts/visxRenderers/RectRenderer.tsx) heatmap cells dim via OR-of-row-OR-col. Cells sit at the intersection of `rowCh` × `colCh`, so the dim contract diverges from every prior WD2-dim-* wave (single x-field). The wave lifts TWO independent triplets (`rowFilterSel` / `dashboardRowDimActive` on `rowCh.field`; `colFilterSel` / `dashboardColDimActive` on `colCh.field`) right after `useDashboardTileContext()`. Inside the per-cell `.map(...)`, `isRowDimmed = dashboardRowDimActive && !isCrossFilterActive(dashboardFilters!, rowCh.field, rowRawByKey.get(row))`; `isColDimmed = ...colCh.field, colRawByKey.get(col)`; `isDashboardDimmed = isRowDimmed || isColDimmed`. The OR contract gives "show cells surviving the filter intersection" — a cell at full opacity iff it passes BOTH the row AND col filters; cells failing either fade. Asymmetric with WD2-wiring-rest-rect's AND-dispatch shape (both events fire on click): dispatch models "the user picked this (row, col) intersection — apply both filters"; dim models "show me the surviving intersection". Reuses the `rowRawByKey` / `colRawByKey` maps WD2-wiring-rest-rect already built so non-string dims (Date / numeric / boolean) match correctly via raw lookups. Cell `<rect>` gains `fillOpacity={isDashboardDimmed ? 0.4 : 1}`; the 0.5-px background stroke + title tooltip stay untouched (structural grid lines + quantitative readout, not the filterable mark). 13 source-inspection tests across 6 suites in [`wd2DimRect.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimRect.test.ts). WD2-dim-* family now spans 7 of 13 visx renderers (bar + cat-5 + rect). Remaining: WD2-dim-trend (Line + Area per-series on colorCh), WD2-dim-point (scatter conditional on colorCh), WD2-dim-echarts (5 ECharts marks via itemStyle.opacity per dataItem). See `docs/WAVES.md` for full entry.
- **Wave WD2-dim-cat (2026-05-19)** — 5 categorical visx renderers ([`ArcRenderer`](../../client/src/lib/charts/visxRenderers/ArcRenderer.tsx), [`FunnelRenderer`](../../client/src/lib/charts/visxRenderers/FunnelRenderer.tsx), [`BoxRenderer`](../../client/src/lib/charts/visxRenderers/BoxRenderer.tsx), [`WaterfallRenderer`](../../client/src/lib/charts/visxRenderers/WaterfallRenderer.tsx), [`ComboRenderer`](../../client/src/lib/charts/visxRenderers/ComboRenderer.tsx)) fan out the WD2-dim-bar pattern. Each renderer lifts `dashboardFilters / xFilterSel / dashboardDimActive` on its primary x-field once per render (`labelCh.field` for Arc; `enc.x.field` for Funnel / Box / Waterfall; `xCh.field` for Combo); inside its per-mark map, `isDashboardDimmed = dashboardDimActive && !isCrossFilterActive(dashboardFilters!, <xField>, <rawValue>)` checks the renderer-specific raw value identifier (`arc.data.rawKey` / `s.rawLabel` / `s.rawCategory` / `b.rawCategory` / `rawX`); the mark's `fillOpacity` composes `baseline * (isDashboardDimmed ? 0.4 : 1)` preserving each mark's existing translucency baseline (Arc 1; Funnel 0.85; Box 0.4; Waterfall 0.85; Combo 0.85). Carve-outs mirror the WD2-wiring-rest-cat dispatch carve-outs: Waterfall running-total bars (`b.isTotal`) AND-gated out of dim (synthetic summary rows, not categorical marks); Combo secondary-axis `<LinePath>` stays untouched (a continuous trend dimmed by a categorical filter would render as a gappy interleave). Box whiskers + median stroke also stay full opacity — they're structural geometry the user reads for distribution comparison even on non-matching categories. 27 source-inspection tests across 6 suites in [`wd2DimCat.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimCat.test.ts). WD2-dim-* family now spans 6 of 13 visx renderers (bar + cat-5). Remaining: WD2-dim-rect (heatmap OR-of-row-OR-col), WD2-dim-trend (Line + Area per-series on colorCh), WD2-dim-point (scatter conditional on colorCh), WD2-dim-echarts (5 ECharts marks via itemStyle.opacity per dataItem at the buildOptions level). See `docs/WAVES.md` for full entry.
- **Wave WD2-dim-bar (2026-05-19)** — first renderer wave on the WD2-dim-* family. [`BarRenderer.tsx`](../../client/src/lib/charts/visxRenderers/BarRenderer.tsx) lifts `dashboardFilters = dashboardTile?.filters` + `xFilterSel = dashboardFilters?.[enc.x.field]` + `dashboardDimActive = !!xFilterSel && xFilterSel.type === "categorical" && xFilterSel.values.length > 0` once per render (right after the existing `useDashboardTileContext()` call); inside `cells.map`, `isDashboardDimmed = dashboardDimActive && !isCrossFilterActive(dashboardFilters!, enc.x.field, c.outerRaw)` runs per bar; the `fillOpacity` ternary extends from `(isFiltered ? 1 : grid.inGrid && grid.filter ? 0.4 : 1)` to `(isFiltered ? 1 : grid.inGrid && grid.filter ? 0.4 : isDashboardDimmed ? 0.4 : 1)`. The chat/explorer (`grid.inGrid`) branch stays first because `grid.inGrid` and `dashboardTile` are mutually exclusive contexts — so the new dashboard dim branch only fires when the chat/explorer one doesn't. [`isCrossFilterActive(global, column, value)`](../../client/src/pages/Dashboard/lib/crossFilter.ts) signature widened from narrow `string | number | boolean | null | undefined` to `unknown` (matches the WD2-wiring-bar `toFilterValue` widening precedent — the body already calls `toFilterValue(value)` which accepts `unknown`); lets renderers pass `BarCell.outerRaw` (typed `unknown`) without a cast. Opacity-only: no stroke / outline added for matching bars (the existing `isFiltered` stroke is chat/explorer-only) because outlining matched bars across every dashboard tile would be visually noisy. 16 source-inspection tests across 6 suites in [`wd2DimBar.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimBar.test.ts). Follow-on family: WD2-dim-cat (Arc/Funnel/Box/Waterfall/Combo), WD2-dim-rect (two-dim row OR col), WD2-dim-trend (Line/Area), WD2-dim-point (scatter on colorCh), WD2-dim-echarts (5 ECharts marks via itemStyle.opacity per dataItem). See `docs/WAVES.md` for full entry.
- **Wave WD2-dim-foundation (2026-05-19)** — [`dashboardTileContext`](../../client/src/pages/Dashboard/lib/dashboardTileContext.tsx) carries optional `filters: ActiveChartFilters`. `DashboardTileContextValue` + `DashboardTileProviderProps` both gain the new field; the `useMemo` body spreads `filters` conditionally (`undefined → omit`) so callers omitting filters get a value byte-identical to the pre-wave shape — important because upcoming dim-renderer guards can rely on `value.filters !== undefined` as a "dashboard-side context active" sentinel. Type-only `ActiveChartFilters` import keeps the runtime bundle untouched (dashboardTileContext is mounted on every dashboard tile and changes to its bundle ripple through SplitView code-splitting). [`ChartTileBody`](../../client/src/pages/Dashboard/Components/ChartTileBody.tsx) threads the existing `filters` prop into `<DashboardTileProvider filters={filters}>` — a two-character JSX edit; the prop was already on the body since WI2-wire-bind. Unlocks the WD2-dim-bar / -cat / -rect / -trend / -point / -echarts follow-on family where each renderer reads `dashboardTile.filters` via `useDashboardTileContext()`, calls [`isCrossFilterActive(global, column, value)`](../../client/src/pages/Dashboard/lib/crossFilter.ts) per mark, and multiplies opacity by ~0.4 for marks whose categorical value isn't in the active selection (the visual feedback half of the WD2-wiring family — "filtered tiles dim non-matching marks instead of removing them" per the 1000x master plan Workstream 4 UX). 13 source-inspection tests across 4 suites in [`wd2DimFoundation.test.ts`](../../client/src/pages/Dashboard/lib/wd2DimFoundation.test.ts); two existing assertions in `dashboardTileContext.test.ts` re-pinned to match the new memo shape + provider JSX. See `docs/WAVES.md` for full entry.
- **Wave WD2-wiring-echarts (2026-05-19)** — ECharts specialty pack wired to `dispatchCrossFilter`. [`EChartsBase`](../../client/src/lib/charts/echartsRenderers/EChartsBase.tsx) gains an optional `onChartClick?: (params: unknown) => void` prop with a ref-tracked callback so a single `inst.on('click', ...)` bind at mount picks up renderer-side identity changes without unbind/rebind. The bind is placed AFTER `inst.setOption(...)` in the init() async block — ECharts requires the instance to be initialised + options applied before event binding takes effect. Five specialty renderers (Treemap + Sunburst in [`TreemapRenderer.tsx`](../../client/src/lib/charts/echartsRenderers/TreemapRenderer.tsx) and [`SpecialtyRenderers.tsx`](../../client/src/lib/charts/echartsRenderers/SpecialtyRenderers.tsx); Sankey, Calendar, Candlestick in `SpecialtyRenderers.tsx`) each read `useDashboardTileContext()` once, build a per-renderer `useCallback` handler that translates the renderer-specific `params` shape into a `dispatchCrossFilter` call, and pass `onChartClick={dashboardTile ? handler : undefined}` so chat / explorer / share-preview surfaces stay click-inert at the ECharts instance level. Treemap + Sunburst dispatch leaf-only (`!Array.isArray(p.data.children) || .length === 0`) on `labelCh.field` — parent-group clicks are skipped because the dispatch column would diverge between rings (two-column dispatch a la RectRenderer is a follow-on). Sankey dispatches node-only (`params.dataType === 'node'`, edges skipped — no single categorical value to filter on) on `sourceCh.field` since same-dimension source/target is the dominant use case. Calendar reads `params.data[0]` (the ISO date string from the [date, value] tuple) and dispatches on `dateCh.field`. Candlestick looks up `xs[params.dataIndex]` (NOT `params.value`, which is the OHLC tuple) and dispatches on `xCh.field`. ParallelRenderer (continuous lines), ChoroplethRenderer (stub awaiting geo registration), and GaugeRenderer (single-value KPI) stay unwired — pinned by test that their function bodies contain no `dispatchCrossFilter(` call site. 23 tests across 7 source-inspection suites in [`wd2WiringEcharts.test.ts`](../../client/src/pages/Dashboard/lib/wd2WiringEcharts.test.ts). WD2-wiring family now spans 15 chart kinds: 10 visx (bar + cat-5 + rect + line + area + point) + 5 ECharts (treemap + sunburst + sankey + calendar + candlestick). KPI / Radar / Regression / Parallel / Choropleth / Gauge are all categorically inapplicable and remain deliberately skipped. See `docs/WAVES.md` for full entry.
- **Wave WI3 (2026-05-19)** — surfaces the regen `citations` array on [`TileInsightFooter`](../../client/src/pages/Dashboard/Components/TileInsightFooter.tsx) as a discoverable "Sources:" row of [`CitationHoverCard`](../../client/src/components/CitationHoverCard.tsx) chips. WI2-server's `extractInsightCitations(text)` (mirror of W22's `CITATION_TOKEN_RE` + hyphen rule) populates `InsightRegenEntry.citations: string[]` from backtick-wrapped pack ids in the regen prose; pre-WI3 that array landed in the cache but had no surface. WI3 inserts a `<div className="mt-1 flex flex-wrap items-center gap-1 text-[11px] text-muted-foreground">` row between the "Updated <relative> · <tier> confidence" metadata line and the Re-explain button, gated on `regen?.entry?.citations && regen.entry.citations.length > 0` so missing AND empty arrays short-circuit to no render. Each citation maps to `<CitationHoverCard key={packId} packId={packId} index={i + 1} />` — `key={packId}` is stable across array reorder, `index={i + 1}` matches the 1-based `[N]` superscript convention `MarkdownRenderer`'s `buildCitationIndex` already uses for inline rendering. The inline backtick-wrapped pack ids inside the regen prose continue to render as `[N]` superscript hover-cards via `MarkdownRenderer`'s WQ3 integration — a comment above the renderer call documents that path so future maintainers don't try to wire it again. `flex flex-wrap` keeps long citation lists from overflowing the narrow tile footer. 8 source-inspection tests in [`TileInsightFooterWI3.test.ts`](../../client/src/pages/Dashboard/Components/TileInsightFooterWI3.test.ts) pin the import, the gate, the literal label, the `.map(...)` shape, the structural ordering, the container class, the optional-chaining, and the documentation comment. See `docs/WAVES.md` for full entry.
- **Wave WI2-wire-bind (2026-05-19)** — closes the WI2 trilogy at the consumer level: [`ChartTileBody`](../../client/src/pages/Dashboard/Components/ChartTileBody.tsx) now calls `useInsightRegen` and ships the regenerated insight prose into [`TileInsightFooter`](../../client/src/pages/Dashboard/Components/TileInsightFooter.tsx). [`DashboardView`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) creates one shared `insightRegenCache = useMemo(() => createInsightRegenCache(), [])` and threads it through [`DashboardTiles`](../../client/src/pages/Dashboard/Components/DashboardTiles.tsx) as an optional `insightRegenCache?: InsightRegenCache` prop into every ChartTileBody mount, so re-toggling between explored filter combos in the dashboard session hits the cache instead of refiring the MINI-tier LLM. ChartTileBody derives `InsightChartSpecLite` from `tile.chart` (field-for-field subset via a memo over `[type, title, x, y, seriesColumn, aggregate]`, with `seriesColumn`/`aggregate` spread-conditional so undefined optionals don't ship to the strict zod request schema), computes `filteredRows` via `applyChartFilters(tile.chart.data ?? [], filters ?? {})` cast to `InsightRegenRow[]` — `ChartSpec.data` cells (`string | number | null`) are a structural subset of `InsightRegenRow` cells (`string | number | boolean | null`) so the cast is safe and ChartShim never has to expose its internal filteredData. Mounts `useInsightRegen({ tileId: tile.id, filters: filters ?? {}, cache: insightRegenCache })`; binds a no-arg `handleRegenerate = useCallback(() => { void regen.regenerate(specLite, filteredRows); }, [regen, specLite, filteredRows])` — the dynamic context flows in at click time, preserving the hook's over-fire-on-every-render guard. The four-key `regen={{ entry, loading, error, onRegenerate: handleRegenerate }}` prop is passed to `TileInsightFooter` only when `tile.chart.keyInsight` is present, so charts without a static insight remain visually unchanged (no empty footer). 19 tests across 3 source-inspection suites in [`ChartTileBody.test.ts`](../../client/src/pages/Dashboard/Components/ChartTileBody.test.ts). Domain-context plumbing is deferred — no domain-context provider/hook exists in `client/src/pages/Dashboard` yet (planned, not yet built); the hook's spread-conditional options keep the call site forwards-compatible. See `docs/WAVES.md` for full entry.
- **Wave WI2-wire (2026-05-19)** — closes the WI2 trilogy: per-tile insight regeneration is now a usable surface. New [`useInsightRegen({ tileId, filters, cache? })`](../../client/src/pages/Dashboard/hooks/useInsightRegen.ts) hook returns `{ entry, loading, error, regenerate, cacheKey }`. Cache key derives from `buildCacheKey(tileId, hashGlobalFilters(filters))` via `useMemo([tileId, filters])`; the cached entry reads synchronously every render so re-toggling between explored filter combos paints instantly. `regenerate(spec, filteredData, options?)` checks the cache first (unless `options.bypassCache`), POSTs to `/api/insight/regen` with `credentials: "include"`, and merges the parsed response straight into the cache via `cache.set(cacheKey, parsed)` — the WI2-server response shape was designed to be contract-compatible with `InsightRegenEntry`, so no transformation is needed. A `seqRef` guards against stale resolves clobbering newer state when multiple regenerate() calls fire back-to-back. The dynamic regen context (spec / filteredData / domainContext / datasetContextHint) flows in at CALL time rather than at hook-instantiation time — side-steps the over-fire-on-every-render trap of a `useEffect`-driven design. Per-mount fallback cache via `useMemo([])` when no shared cache is injected; the optional `cache` arg lets a future `DashboardView` mount share one cache across every tile. [`TileInsightFooter`](../../client/src/pages/Dashboard/Components/TileInsightFooter.tsx) gains an optional `regen?: { entry, loading, error, onRegenerate }` prop — when omitted, the legacy passive footer is unchanged; when provided: the regenerated entry text takes precedence over the static keyInsight (static is the fallback so the footer isn't empty mid-flight), a "✦ Re-explain this view" button renders with Sparkles (idle) / Loader2 (loading) icons, `disabled={regen.loading}` + `e.stopPropagation()` keeps the parent footer toggle quiet, errors render with `role="alert"` for a11y, and an "Updated <relative> · <tier> confidence" metadata line renders below via a local `formatRelativeShort(iso)` helper that covers s / min / h / d windows. 22 tests across 2 suites in [`useInsightRegen.test.ts`](../../client/src/pages/Dashboard/hooks/useInsightRegen.test.ts). The hook + footer extension are consumer-agnostic; the `WI2-wire-bind` follow-on wires `ChartTileBody` to call them (threads filteredData + spec + domainContext through `ChartShim` / `PremiumChart`). See `docs/WAVES.md` for full entry.
- **Wave WD2-wiring-rest-point (2026-05-19)** — PointRenderer (scatter / bubble) wired to `dispatchCrossFilter`, conditional on `colorCh`. Pure quantitative (x, y) scatters have no categorical filter target so the dispatch must NOT fire on them — a `crossFilterReady = !!dashboardTile && !!colorCh` local gates both the `cursor: pointer` style and the onClick uniformly. When `colorCh` IS set, clicking any point in a color group dispatches `{ column: colorCh!.field, value: toFilterValue(p.rawColor), sourceTileId }`, toggling that group's brush. Each point now preserves `rawColor` (type-original color value) alongside the stringified `colorKey`; `colorKey = asString(rawColor)` so the accessor runs once per row. Both render paths — `<Circle>` for plain scatters and glyph `<path>` for shape-encoded scatters — gain identical wiring so future maintainers can't introduce a divergence. The existing tooltip pathway (`onMouseMove` + `onMouseLeave`) is preserved on both render paths. 13 tests across 2 suites in [`wd2WiringRestPoint.test.ts`](../../client/src/pages/Dashboard/lib/wd2WiringRestPoint.test.ts). WD2-wiring-rest now spans 10 of 13 visx renderers (bar + cat-5 + rect + line + area + point); KPI / Radar / Regression have no meaningful categorical click target and are deliberately skipped. See `docs/WAVES.md` for full entry.
- **Wave WD2-wiring-rest-trend (2026-05-19)** — LineRenderer + AreaRenderer wired to `dispatchCrossFilter`. Trend marks (continuous x) have no per-mark click target; dispatch reads the click x-coord, scans every `series.points` for the nearest x, and fires `{ column: xCh.field, value: toFilterValue(nearest.x), sourceTileId }`. LineRenderer injects the dispatch into the existing `onBrushUp` click-treatment branch (`Math.abs(hi - lo) < 6`) — placed AFTER the 6-px guard but BEFORE the `setBrushStart(null)` reset so the recorded click position survives the lookup. The drag-to-zoom interaction never accidentally triggers a filter. AreaRenderer adds a fresh svg-level `onClick` using `localPoint(e)` to source click coords + `MARGIN.left` subtraction to map into the inner-plot origin used by `xPx` + a `[0, innerWidth]` clamp; reads from the pre-stack `series` (not `stacked`) because the stacked y values are cumulative but x values are identical, so pre-stack is the canonical original-data source. Two-level gating (`if (dashboardTile) { ... if (nearest) ... }`) avoids the O(series × points) scan cost on chat / explorer and silently handles empty-series edges. 18 tests across 2 suites in [`wd2WiringRestTrend.test.ts`](../../client/src/pages/Dashboard/lib/wd2WiringRestTrend.test.ts). See `docs/WAVES.md` for full entry.
- **Wave WD2-wiring-rest-rect (2026-05-19)** — RectRenderer heatmap cells wired to `dispatchCrossFilter`. A cell sits at the intersection of two categorical dims (`rowCh`, `colCh`); the cell `onClick` dispatches TWO `CROSS_FILTER_EVENT` events in row-first order, one per dim. `applyCrossFilter` is pure and event-driven so back-to-back dispatches each toggle their own column independently — the user sees the row + col filter applied, and re-clicking the same cell toggles both back off. The two parallel `Set` builders for the row / col domains collapse to a single `useMemo` returning `{ rows, rowRawByKey, cols, colRawByKey }` so the type-original raw value (Date / number / boolean / string) for each domain entry is recoverable at dispatch time — same shape as ArcRenderer's per-key aggregator from WD2-wiring-rest-cat. Row-first ordering is pinned by test via `indexOf("column: rowCh.field") < indexOf("column: colCh.field")` (a regex on the OUTER `dispatchCrossFilter` literal would return the same index for both searches — `String#search` returns the start of the overall match). 11 tests across 2 suites in [`wd2WiringRestRect.test.ts`](../../client/src/pages/Dashboard/lib/wd2WiringRestRect.test.ts). See `docs/WAVES.md` for full entry.
- **Wave WD2-wiring-rest-cat (2026-05-19)** — 5 categorical-by-construction visx renderers ([`ArcRenderer`](../../client/src/lib/charts/visxRenderers/ArcRenderer.tsx), [`FunnelRenderer`](../../client/src/lib/charts/visxRenderers/FunnelRenderer.tsx), [`BoxRenderer`](../../client/src/lib/charts/visxRenderers/BoxRenderer.tsx), [`WaterfallRenderer`](../../client/src/lib/charts/visxRenderers/WaterfallRenderer.tsx), [`ComboRenderer`](../../client/src/lib/charts/visxRenderers/ComboRenderer.tsx)) wired to `dispatchCrossFilter`. Each imports `useDashboardTileContext` + `dispatchCrossFilter` + `toFilterValue`; reads the dashboard-tile context once in the renderer body; preserves the type-original category value (`rawKey` / `rawLabel` / `rawCategory` / `rawX`) alongside the stringified one so the cross-filter predicate compares apples-to-apples; and adds an `onClick` on its categorical mark that dispatches `CROSS_FILTER_EVENT` with `{ column: <xField>, value: toFilterValue(<raw>), sourceTileId: dashboardTile.tileId }` when a dashboard-tile context is present. Outside a dashboard tile (chat / explorer / share preview) the click is a no-op — same invariant as BarRenderer. WaterfallRenderer additionally skips dispatch on running-total bars (`b.isTotal === true`) via an intermediate `clickable` local; ArcRenderer's per-key aggregator now stores `{ value, rawKey }` so the first row's raw value survives the per-key reduction; BoxRenderer captures `rawCategory` from `rows[0]?.[enc.x.field]` because `groupBy(data, [enc.x.field])` only returns string keys; ComboRenderer keeps both `rawX` (for dispatch) and `xRaw = asString(rawX)` (for the band scale lookup). The bar mark is the categorical handle for the combo chart; the secondary-axis line stays click-inert. 36 tests across 7 suites in [`wd2WiringRestCat.test.ts`](../../client/src/pages/Dashboard/lib/wd2WiringRestCat.test.ts) source-inspect the per-renderer dispatch shape, the click-gate predicate (`dashboardTile` for 4 of 5; `clickable` for Waterfall), the `cursor: pointer` style on hover, and the raw-value preservation. Remaining 4 renderers: WD2-wiring-rest-rect (heatmap cells, needs a one-vs-two-dim filter decision), WD2-wiring-rest-trend (Line/Area with nearest-x lookup + brush-zoom guard), WD2-wiring-rest-point (Point conditional on `colorCh`). KPI / Radar / Regression have no meaningful categorical click target and are deliberately skipped. See `docs/WAVES.md` for full entry.
- **Wave WD2-wiring-bar (2026-05-18)** — BarRenderer cross-filter wiring proof. Atomic round-trip validation of the WD2 helper's chart-mark-click → `dispatchCrossFilter` → window `CROSS_FILTER_EVENT` → `DashboardView` `useEffect` → `setGlobalFilters((g) => applyCrossFilter(g, detail))` data path on the bar mark. New module [`client/src/pages/Dashboard/lib/dashboardTileContext.tsx`](../../client/src/pages/Dashboard/lib/dashboardTileContext.tsx) — `DashboardTileProvider` memoises `{ tileId }`; `useDashboardTileContext()` returns the value inside a provider or `null` outside (chat / explorer / share preview surfaces fall through cleanly). [`ChartTileBody`](../../client/src/pages/Dashboard/Components/ChartTileBody.tsx) wraps the pivot-view branch + the chart-shim (PremiumChart-v2 / legacy ChartRenderer) branch in a single `<DashboardTileProvider tileId={tile.id}>` block so every renderer the tile delegates to inherits the context. [`BarRenderer`](../../client/src/lib/charts/visxRenderers/BarRenderer.tsx) imports `useDashboardTileContext` + `dispatchCrossFilter` + `toFilterValue`; reads the context at the top of the body; widens the `interactive` flag to `grid.inGrid || !!dashboardTile`; the bar `onClick` runs `grid.toggleFilter({ field, value })` independently when inside a `<ChartGrid>` (chat / explorer multi-chart context preserved unchanged) AND dispatches `CROSS_FILTER_EVENT` carrying `{ column: enc.x.field, value: toFilterValue(c.outerRaw), sourceTileId: dashboardTile.tileId }` when inside a dashboard tile. [`DashboardView`](../../client/src/pages/Dashboard/Components/DashboardView.tsx) gains a `useEffect` right after the existing `DR4` captured-filter seed effect — attaches a `window.addEventListener(CROSS_FILTER_EVENT, handler)` with a `typeof window === 'undefined'` SSR guard, casts to `CustomEvent<CrossFilterEvent>`, filters malformed payloads (`!detail || typeof detail.column !== 'string'`), and dispatches `setGlobalFilters((prev) => applyCrossFilter(prev, detail))`; cleanup removes the listener on unmount. `toFilterValue` in [`crossFilter.ts`](../../client/src/pages/Dashboard/lib/crossFilter.ts) widened from a narrow `string | number | boolean | null | undefined` signature to `unknown` because chart-mark values flow as `unknown` from typed `BarCell.outerRaw`; the existing 29 WD2 tests stay green (runtime behavior unchanged for the narrower types), plus a new `Date` branch ISO-coerces temporal axis brushes. 18 tests across 5 suites in [`dashboardTileContext.test.ts`](../../client/src/pages/Dashboard/lib/dashboardTileContext.test.ts): module shape (4), BarRenderer source-inspection (6), ChartTileBody provider-wrap source-inspection (2), DashboardView subscription source-inspection (6). Remaining 12 visx renderers + ECharts adapter follow as WD2-wiring-rest with the same three-line addition per renderer. See `docs/WAVES.md` for full entry.
- **Wave WI2-server (2026-05-18)** — `POST /api/insight/regen` endpoint pairs with the WI2-cache client surface. The future WI2-wire hook will call this on cache miss and merge the response straight into the cache via `cache.set(buildCacheKey(tileId, hashGlobalFilters(filters)), response)`. New module [`server/lib/insightRegen.ts`](../../server/lib/insightRegen.ts) ships five pure helpers + one network boundary: strict zod `regenInsightRequestSchema` (`{ tileId, spec, filteredData ≤5000 rows, domainContext?, datasetContextHint? }`) + `regenInsightResponseSchema` (matches `InsightRegenEntry` from WI2-cache verbatim — `{ text, citations?, regeneratedAt, confidenceTier }`); `summarizeFilteredData(rows, { x, y })` computes deterministic anchors (top / bottom row by y, mean, distinct x-values preview); `inferConfidenceTier(rowCount)` mirrors WQ1's tier vocabulary (<10 low / <100 medium / ≥100 high); `extractInsightCitations(text)` mirrors W22's `CITATION_TOKEN_RE` byte for byte plus the hyphen-rule filter; byte-stable `buildInsightRegenPrompt(args) → { system, user }` (system carries persona + output contract; user embeds the stat anchors + optional DATASET/DOMAIN CONTEXT blocks); `regenInsightForFilteredView(request)` is the single MINI-tier `callLlm` boundary with deterministic post-processing (whitespace normalisation, blockquote strip, 1200-char cap, citations + tier inference, ISO timestamp). New `LLM_PURPOSE.INSIGHT_REGEN` MINI-tier purpose declared in [`llmCallPurpose.ts`](../../server/lib/agents/runtime/llmCallPurpose.ts); both W3.11 routing-regression and W18 default-stub-handler tests updated for the new purpose. Route wired in [`server/routes/insightRegen.ts`](../../server/routes/insightRegen.ts) — auth-gated via `getAuthenticatedEmail`, 400 on schema fail, 500 on regen failure with `agentLog` telemetry on both success + error. 36 tests across 8 suites. WI2 trilogy is now 2 of 3 shipped (cache + server); WI2-wire is the next wave — `useInsightRegen` hook + "✦ Re-explain this view" button in [`TileInsightFooter.tsx`](../../client/src/pages/Dashboard/Components/TileInsightFooter.tsx) merges everything into the user-facing surface. See `docs/WAVES.md` for full entry.
- **Wave WI2-cache (2026-05-18)** — per-tile insight regen cache pure helpers. New module [`client/src/pages/Dashboard/lib/insightRegenCache.ts`](../../client/src/pages/Dashboard/lib/insightRegenCache.ts) ships the data-plumbing layer for WI1's dynamic insight regeneration. Three exports: `hashGlobalFilters(filters)` — byte-stable serialisation of `ActiveChartFilters` (sorts column keys + categorical values; date / numeric have fixed-key serialisation with `c:` / `d:` / `n:` kind prefixes so the same column with different filter shapes never collides); `buildCacheKey(tileId, filterHash)` — `${tileId}::${filterHash}` concatenation with an unambiguous `::` delimiter; `createInsightRegenCache(opts?)` — factory returning a closure-backed LRU + TTL cache with default 64 entries (8 tiles × 8 filter combos upper bound; ~32 KB memory ceiling) + default 5-min TTL (matches Anthropic prompt-cache TTL). The cache stores `InsightRegenEntry = { text, citations?, regeneratedAt?, confidenceTier? }` so the future WI2-server + WI2-wire surfaces agree on the shape now. `get` refreshes LRU recency; `set` evicts the oldest when full; `has` / `get` lazy-delete stale entries; `evictExpired()` does an active sweep for the consuming hook to call periodically. `now` injection enables deterministic TTL tests without `setTimeout` or fake-timer libraries. 21 tests across 6 suites pin every contract surface (hash byte-stability, key composition, basic get/set, LRU eviction with recency refresh, TTL expiry, integration scenario with shared hits + invalidation). Foundation for the WI2-server + WI2-wire follow-ons; pure data-plumbing ships first (same pattern as W59a → W59b and WD2 helper → renderer wiring). See `docs/WAVES.md` for full entry.
- **Wave WD2 (2026-05-18)** — cross-filter brushing pure helper. New module [`client/src/pages/Dashboard/lib/crossFilter.ts`](../../client/src/pages/Dashboard/lib/crossFilter.ts) surfaces a one-way data path from a chart-mark click to the dashboard `globalFilters` map (`ActiveChartFilters`). Pure helpers — no React state, no DOM dependency outside the `dispatchCrossFilter` browser-guarded `CustomEvent` dispatch. `applyCrossFilter(global, event)` implements the toggle contract: clicking an active `(column, value)` pair removes it, clicking a new value on a column with an existing categorical filter appends, clicking on a column with no categorical filter installs a fresh categorical `[value]` selection. Date / numeric selections on the column are replaced (chart brush is explicitly discrete-value). `toFilterValue` coerces clicks to canonical string form — `null` / `undefined` → the literal `"null"` (matches the upstream `(null)` key normalisation in `breakdownRankingTool` + the existing chart filter UI's null-bucket handling); numbers / booleans → `String(v)`. Companion helpers: `removeCrossFilter` (remove one (column, value)), `clearCrossFilter` (drop a column entirely), `isCrossFilterActive` (membership test), `listActiveCrossFilters` (flat `{column, value}[]` enumeration for chip rendering). `CROSS_FILTER_EVENT = "marico:cross-filter"` is the canonical CustomEvent name a future wave will subscribe to in `DashboardView`. Module is pure on its inputs: `applyCrossFilter` / `removeCrossFilter` / `clearCrossFilter` all clone-before-modify (`{ ...global }`) so the caller's map is never mutated. 29 tests across 10 suites pin every contract surface (value coercion, toggle semantics on empty / categorical / non-categorical / mutation-purity / coercion edges, remove / clear / list edge cases, dispatch no-op in non-browser env). Wiring into the renderers + `DashboardView` is a follow-on wave; this wave isolates the data-plumbing concern so the wiring wave can be a thin call-site change. See `docs/WAVES.md` for full entry.
- **Wave WD1 (2026-05-16)** — `AddFilterPopover` ([client/src/pages/Dashboard/Components/AddFilterPopover.tsx](../../client/src/pages/Dashboard/Components/AddFilterPopover.tsx)) gives the DR4 global filter bar a `+ Add filter` trigger. Three inline editors (categorical / numeric / date), pure helpers in [dashboardGlobalFilters.ts](../../client/src/pages/Dashboard/dashboardGlobalFilters.ts) (`aggregateTileRowsForFiltering`, `availableFilterDefinitions`, `addCategoricalFilter`, `addNumericFilter`, `addDateFilter`). The bar renders even when `global` is empty if there are columns available to add — DR4's zero-pixels-when-nothing-to-do contract preserved for dashboards with no chart tiles. See `docs/WAVES.md` for full entry. Future waves (WD2–WD10) layer cross-filter brushing, drill-through, dynamic insights, fork-from-dashboard, mobile, linked sheets, saved views, comments, scheduled refresh.

### 5.4 Theme bridge (ECharts)

ECharts specialty marks (treemap, sankey, sunburst, parallel,
calendar, candlestick, choropleth, gauge) lazy-load and read CSS
variables on init.

`next-themes` toggles `class="dark"` on `<html>`, which doesn't fire
`prefers-color-scheme`. Fix-3 wired a `MutationObserver` on
`document.documentElement` watching the `class` attribute, so app-level
theme toggles re-apply ECharts options within ~16ms (one frame). The
`prefers-color-scheme` listener stays as a fallback for OS-driven
changes.

### Recent changes

- Dashboard day-grain trend (2026-06-10) — `dashboardFeatureSweep.pickStrongestDateColumn` no longer returns a Month facet unconditionally: it validates the facet's distinct-bucket count for the dataset's actual span (`distinctBucketsForGrain`) and falls to the span-appropriate finer facet (Day on a single-month daily span) so a real multi-point trend is charted instead of a flat 1-bucket "no trend". Companion planner-side fix (`patchExecuteQueryPlanTrendGrain` `isDashboard` opt) in `docs/architecture/agent-runtime.md`.
- Wave WHov-area-crosshair (2026-05-25) — add hover tooltip + vertical cross-hair indicator to AreaRenderer, mirroring LineRenderer's WHov-line-crosshair pattern; promotes the hover-crosshair convention to documented status. See `docs/WAVES.md`.
- Wave WHov-line-crosshair (2026-05-22) — add hover-time vertical cross-hair indicator to LineRenderer; first instance of the hover-crosshair pattern. See `docs/WAVES.md`.
