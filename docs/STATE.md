# Project state — Marico RAG Insighting Tool

> Auto-updated by `/wave-commit`. Read this **first** in every new chat (or run `/orient`).
> Last sync: 2026-05-21 (Wave W61-cache-invalidate — observability hook + contract pin that every semantic-model mutation path calls `onSemanticModelVersionBumped` after the persist succeeds, pre-emptive foundation for the future W64 cache-key work + a grep-able invalidation log surface for ops).

## HEAD

- **Latest wave:** Wave W61-cache-invalidate · observability hook + contract pin that every semantic-model mutation path (patch / revert / delete / add) calls `onSemanticModelVersionBumped` after the persist succeeds — pre-emptive foundation for the future W64 cache-key work and a grep-able invalidation log surface for ops (2026-05-21)
- **Branch:** `claude/wide-format-classifier`
- **Last commit:** `d82ac045` — "Wave W61-cache-invalidate · observability hook + contract pin that every semantic-model mutation path (patch / revert / delete / add) calls onSemanticModelVersionBumped after the persist succeeds — pre-emptive foundation for the future W64 cache-key work and a grep-able invalidation log surface for ops" (2026-05-21)
- **Working tree:** doc updates staged for the paired W61-cache-invalidate commit. Pre-existing untracked `.claude/scheduled_tasks.lock` deletion remains in the working tree across sessions per prior session brief (not load-bearing).

## Live feature streams

- **Workstream 7 — insight engine 2.0 · CLOSED at 6 of 6 (WI1–WI6 all SHIPPED).** **WI6 closes the workstream** end-to-end: per-tile MRU dropdown navigator for prior filter combos lives in [`TileInsightFooter`](../client/src/pages/Dashboard/Components/TileInsightFooter.tsx) sibling to the Re-explain button. New pure module [`insightHistory.ts`](../client/src/pages/Dashboard/lib/insightHistory.ts) ships `createInsightHistoryStore()` with `MAX_HISTORY_PER_TILE = 3`, `record(tileId, filters, entry)` with MRU bubble-to-top + de-dup on `hashGlobalFilters(filters)`, `get(tileId)` returning a defensive-copy newest-first list, `clear(tileId?)` per-tile or store-wide. [`DashboardView`](../client/src/pages/Dashboard/Components/DashboardView.tsx) holds one store per mount via `useMemo([])` alongside the existing `insightRegenCache`; threaded through [`DashboardTiles`](../client/src/pages/Dashboard/Components/DashboardTiles.tsx) into every [`ChartTileBody`](../client/src/pages/Dashboard/Components/ChartTileBody.tsx). The body's `useEffect` keyed on `[insightHistoryStore, tile.id, regen.entry, filters]` records on every fresh `regen.entry?.regeneratedAt` change — both new regenerations AND filter-toggle paint-from-cache transitions bubble the matching combo to index 0 of the per-tile MRU array. A `historyVersion` `useState` bumped inside the effect drives a `useMemo<InsightHistoryEntry[]>(() => store.get(tile.id), [..., historyVersion])` re-read so React observes the store's Map mutation. `handleHistorySelect = useCallback((entry) => onFiltersChange(entry.filters), [onFiltersChange])` routes a click into the dashboard's filter-change pipeline; the warm regen cache then paints the cached entry instantly (still within the 5-min TTL) or the user's manual Re-explain is the next move. Footer dropdown is a Radix `<DropdownMenu>` block (trigger: `<History>` Lucide icon + "Recent insights (N)" `Button variant="outline" size="sm" h-7 px-2 text-[11px]`) sibling to the Re-explain button, gated on `history && history.length > 0 && onHistorySelect`. Inline `formatHistoryLabel(filters)` helper returns `"Baseline"` for empty filters, `"<column>: <value>"` truncated to 30 chars for a single categorical / date / numeric filter, or `"<N> filters"` for 2+ filters. Items render the label + a smaller `formatRelativeShort(entry.entry.regeneratedAt)` line below it; `e.stopPropagation()` on trigger + content keeps the parent footer chevron from toggling. **Interpretation decision:** the brief read "Keep last 3 insights (for each unique filter combo)" with example labels "Insights for: this view (now), {prior filter combo}, baseline" — two readable scopes existed: (a) per-cache-slot history ring buffer for Re-explain attempts on the same combo, or (b) most-recent-N-distinct-filter-combos navigator. (b) won — the parenthetical "(for each unique filter combo)" reads as "the unit is one insight per combo", the example labels describe three distinct combos, and the WI4-rexplain button already serves the "bypass-cache same combo" affordance. Cache and history are kept as separate stores with different shapes for different concerns: cache is keyed `(tileId, filterHash[, regionHash])` → entry, LRU + TTL — read-performance-oriented; history is keyed `tileId` → ordered list of `(filterHash, filters, entry, recordedAt)` slots, MRU-bubble + cap — UX-navigation-oriented. Co-located in `lib/` so a future maintainer finds both side-by-side. 18 new behavioural tests across 10 suites in [`insightHistory.test.ts`](../client/src/pages/Dashboard/lib/insightHistory.test.ts) — real import + runtime assertions (mirrors WI5 shape): constant pin, empty store, record shape + empty-filters sentinel, distinct combos newest-first, same-combo de-dup with entry+recordedAt update, MRU bubble after newer combos, capacity cap drops oldest, custom `maxPerTile` option, multi-tile isolation, `clear(tileId)` + `clear()` + `clear(unknownTile)` semantics, defensive-copy on `get`, injectable `now()`. Test file appended to [`server/package.json`](../server/package.json)'s explicit list per invariant #4. 4374 of 4378 server tests pass (4 pre-existing ECharts treemap+sunburst carry-forwards unchanged); client typecheck baseline preserved (53). The dropdown is purely additive — tiles without an `insightHistoryStore` prop render byte-identically to pre-WI6. **WI4 master-plan series remains CLOSED end-to-end at 4 of 4 brush-capable kinds (Line + Area + Bar + Point) + 4 of 4 dormancy-debt items shipped.** WI5 (recommendations) + WI4 family (foundation + box2d + 4 wirings + panel + wire + cache-key + rexplain + citations) + WI3 (citation Sources row) + WI2 trilogy (cache + server + wire + wire-bind) + WI1 (schema) all shipped earlier. Stream now in maintenance mode; out-of-scope items deferred: cross-session persistence (sessionStorage with key-version bump), brush-region history slots in the same dropdown, per-key Re-explain-attempts ring as interpretation (a) if a "compare two attempts" need surfaces. The WI4 master-plan series remains CLOSED end-to-end at 4 of 4 brush-capable kinds + 4 of 4 dormancy-debt items shipped (foundation + wiring-trend + panel + wire + wiring-area + wiring-bar + cache-key + rexplain + citations + foundation-box2d + wiring-point). WI5 closes the per-tile "Try this" recommendation surface that the brief earmarked for Workstream 7 step 5: new pure module [`tileRecommendations.ts`](../client/src/pages/Dashboard/lib/tileRecommendations.ts) derives up to 3 deterministic, single-click CTAs from a tile's current spec + filtered rows + active filters (filter-bottom for the lowest x-bucket < median × 0.5, filter-top for the highest > median × 2.0, clear-filters always-on when any filter is active). Categorical-x only (bar / pie) with ≥3 buckets; line / area / scatter / heatmap deliberately skip the filter rules. [`TileInsightFooter`](../client/src/pages/Dashboard/Components/TileInsightFooter.tsx) gains a "Try this:" chip row between Sources and the action row (gated on `recommendations?.length > 0`); [`ChartTileBody`](../client/src/pages/Dashboard/Components/ChartTileBody.tsx) computes recommendations via `useMemo` and routes each kind to a categorical filter pin / `{}` clear via `onFiltersChange`. 30 new behavioural tests across 12 suites in [`tileRecommendations.test.ts`](../client/src/pages/Dashboard/lib/tileRecommendations.test.ts) (test file appended to `server/package.json`'s explicit list per invariant #4); 375 tests across 123 suites all green; client typecheck baseline preserved (53). **WI4 master-plan series remains CLOSED end-to-end at 4 of 4 brush-capable kinds (Line + Area + Bar + Point) + 4 of 4 dormancy-debt items shipped:** WI4-foundation + WI4-wiring-trend + WI4-panel + WI4-wire + WI4-wiring-area + WI4-wiring-bar + WI4-cache-key + WI4-rexplain + WI4-citations + WI4-foundation-box2d + **WI4-wiring-point** SHIPPED. **WI4-wiring-point** closes the WI4 widening end-to-end: alt-drag on any chart inside a dashboard tile — line, area, bar, or scatter — now opens the ExplainSlicePanel with regenerated insight prose for the brushed sub-region. [`PointRenderer.tsx`](../client/src/lib/charts/visxRenderers/PointRenderer.tsx) had NO pre-existing brush mechanics (scatter shipped with only per-point onClick handlers powering WD2 cross-filter + WD3 drill-through, both gated on `colorCh`); this wave adds mouse-down / move / up state + a 2D rectangular overlay from scratch. Scatter is the only chart kind with TWO independently-continuous axes, so the brush captures `{ x, y }` pixel-coord pairs (distinct from the 1D brushes' bare-number `brushStart`); `brushExplainRef = useRef<boolean>(false)` captures `e.altKey === true` at brushDown. Three handlers gated on `dashboardTile`: `onBrushDown` reads `localPoint`, computes inner-plot coords, guards BOTH axes' bounds, sets brushStart AND brushEnd to the same `{x, y}` pair. `onBrushMove` short-circuits on `brushStart === null` / `!(e.buttons & 1)`, clamps `pt.x` and `pt.y` independently to `[0, innerWidth]` / `[0, innerHeight]`. `onBrushUp`'s click-vs-drag gate requires BOTH axes to cross `BRUSH_MIN_PX` (`isBrushDrag(brushStart.x, brushEnd.x, BRUSH_MIN_PX) && isBrushDrag(brushStart.y, brushEnd.y, BRUSH_MIN_PX)`) — a 100×3 sliver is still a click in the y-dimension. Drag branch inverts pixel→data via `xScale.invert` / `yScale.invert` (yScale's `range = [innerHeight, 0]` means low-pixel-y inverts to high-data-y; the constructor normalises so xMin/yMin/xMax/yMax come out correctly regardless of drag direction) and builds the region via `makeBox2dRegion(x1Data, x2Data, y1Data, y2Data, yCh.field)` — yCh.field rides on the region's `yColumn` per WI4-foundation-box2d. Dispatch shape is the 5-field `{ chartId, column: xCh.field, region, sourceTileId, filters: dashboardFilters }` (same as the 1D brushes' WI4 dispatches). SVG element gains `onMouseDown` / `onMouseMove` / `onMouseUp` (gated on dashboardTile) + a widened `onSvgMouseLeave` (does BOTH `hideTooltip()` AND a brush state clear if active). svg cursor toggles to `crosshair` during an active brush — the canonical 2D-rect-selection affordance distinct from the 1D brushes' `ew-resize` / `ns-resize`. Brush `<rect>` overlay inserted inside `<Group>`: anchored at `(Math.min(brushStart.x, brushEnd.x), Math.min(brushStart.y, brushEnd.y))` with `width = Math.abs(...)` / `height = Math.abs(...)` so reverse drags render correctly; same primary-tint dashed-stroke styling as the 1D brushes + `pointerEvents="none"` so the overlay never intercepts mouse events. Per-point `onMouseMove` tooltip handler gains a `if (brushStart !== null) return;` guard so the tooltip doesn't flicker between points during a brush drag. Pre-existing WD2 cross-filter + WD3 drill-through per-point onClick handlers preserved byte-identical (SVG-level brush + point-level click handle different event paths and never conflict). 38 source-inspection tests across 15 suites in [`wi4WiringPoint.test.ts`](../client/src/pages/Dashboard/lib/wi4WiringPoint.test.ts) — including NEGATIVE pins against the three 1D constructors (scatter is box2d-only) + against `setZoomRange` (no zoom on scatter). Test file appended to [`server/package.json`](../server/package.json)'s explicit list per invariant #4. 366 tests across 122 suites all green across the WI4 family + WD2 + WD3 Point regression suite. Client typecheck baseline preserved (53). The WI4 master-plan series is now functionally complete on every brush-capable renderer — the dormancy-debt list closes. **WI4-foundation-box2d** opens the load-bearing foundation for the 2D scatter brush by extending [`BrushRegion`](../client/src/pages/Dashboard/lib/explainSlice.ts) from 3 kinds to 4 with a new `box2d` variant carrying `xMin / xMax / yMin / yMax` numeric bounds + a `yColumn` string. The y-column rides on the region (unlike the three 1D variants whose y-axis is irrelevant to row matching) so `ExplainSliceEvent`'s `column` field stays "x-axis column" for all four kinds and `filterRowsByBrushRegion`'s 3-arg signature is unchanged. New `makeBox2dRegion(ax, bx, ay, by, yColumn)` constructor mirrors the three 1D constructors (normalises `xMin <= xMax` AND `yMin <= yMax`, returns `null` on zero-area or non-finite inputs); `filterRowsByBrushRegion`'s box2d branch reads `column` (x-axis) AND `region.yColumn` (y-axis) with the same null/empty-string drop + `Number()` coercion as the numeric branch so string-typed CSV / JSON numerics match cleanly. `hashBrushRegion` emits `b2:<xMin>..<xMax>x<yMin>..<yMax>;y:<yColumn>` — the `;y:<yColumn>` suffix is load-bearing because two scatters whose y-encoding changes (`sales` → `revenue`) describe DIFFERENT sub-regions even at identical bounds. ExplainSlicePanel's `formatRegion` widens to an exhaustive switch on `region.kind` (categorical branch now has an explicit kind check — typecheck-load-bearing after the union widening) + a new box2d branch displays `[xMin, xMax] × yColumn [yMin, yMax]` Cartesian-product notation. **WI4-foundation-box2d** opens the load-bearing foundation for a 2D scatter brush by extending [`BrushRegion`](../client/src/pages/Dashboard/lib/explainSlice.ts) from 3 kinds to 4 with a new `box2d` variant carrying `xMin / xMax / yMin / yMax` numeric bounds + a `yColumn` string (the y-column rides on the region itself — unlike the three 1D variants whose y-axis is irrelevant to row matching, a box2d brush needs both axes' column names at filter time; this keeps `ExplainSliceEvent`'s `column` field stable as "x-axis column" and `filterRowsByBrushRegion`'s 3-arg signature unchanged). New `makeBox2dRegion(ax, bx, ay, by, yColumn)` constructor mirrors the three 1D constructors' contract (normalises `xMin <= xMax` AND `yMin <= yMax`, returns `null` on zero-area or non-finite inputs across all four bounds). `filterRowsByBrushRegion`'s box2d branch reads `column` (x-axis) AND `region.yColumn` (y-axis) — same null/empty-string drop + `Number()` coercion as the numeric branch so string-typed numerics from CSV / JSON match cleanly. `hashBrushRegion`'s box2d branch emits `b2:<xMin>..<xMax>x<yMin>..<yMax>;y:<yColumn>` — the `;y:<yColumn>` suffix is load-bearing because two scatters whose y-encoding changes (e.g. `sales` → `revenue`) describe DIFFERENT sub-regions even at identical pixel-rectangle bounds, and `tileId` alone can't disambiguate since the tile id is stable across encoding edits. ExplainSlicePanel's `formatRegion` widens to an exhaustive switch on `region.kind` — the categorical branch now has an explicit kind check (typecheck-load-bearing after the union widening) and a new box2d branch displays `[xMin, xMax] × yColumn [yMin, yMax]` Cartesian-product notation with the y-column label between the two ranges so the bounds aren't ambiguous when read out of context. **The formatRegion branch was bundled into the foundation wave (originally planned as a third "WI4-format-box2d" wave) because the union widening cascades into formatRegion's typecheck — splitting would force a compile-safe stub in foundation that the format wave would then polish, vs the bundle: one clean wave, typecheck stays at baseline (53).** 20 new tests across three existing test files (no new test file → server/package.json's explicit list unchanged): 5 in `makeBox2dRegion` suite + 5 in `filterRowsByBrushRegion — box2d region` suite (explainSlice.test.ts); 5 in `hashBrushRegion — box2d kind` suite + 3 in `buildCacheKey collision avoidance — box2d` suite (insightRegenCache.test.ts); 2 in `formatRegion discriminates on region.kind` suite (wi4Panel.test.ts). The pre-existing `dispatchExplainSlice` discriminated-union pin updated to include the 4th variant. Cumulative WI4 family: 280 tests across 92 suites all green. Client typecheck baseline preserved (53). Next wave WI4-wiring-point adds PointRenderer's 2D brush mechanics from scratch (no pre-existing brush on scatter — only per-point onClick handlers powering WD2 cross-filter + WD3 drill-through, both gated on `colorCh`) + a 2D rectangular overlay + dispatch site. **WI4-citations** mirrors the WI3 footer's `Sources:` row of `CitationHoverCard` chips on the ExplainSlicePanel — discoverable, scan-at-a-glance list of every cited pack for the brushed-slice regen. **WI4-citations** mirrors the WI3 footer's `Sources:` row of `CitationHoverCard` chips on the ExplainSlicePanel — discoverable, scan-at-a-glance list of every cited pack for the brushed-slice regen. New `<div>` Sources row inside the "Regenerated insight" `<section>`, sibling between the prose `<div>` and the WI4-rexplain button so the three regen affordances stack in source order: read → sources → refresh. Gates on `regen.entry?.citations && regen.entry.citations.length > 0` so the row only appears when there are pack ids to chip; uses byte-identical row styling to the WI3 footer (`mt-2 flex flex-wrap items-center gap-1 text-[11px] text-muted-foreground` + `<span>Sources:</span>` label + `.map((packId, i) => <CitationHoverCard key={packId} packId={packId} index={i + 1} />)`) so a future tweak lands in both files together. Inline backtick-wrapped pack ids inside the regen prose continue to render as `[N]` superscript hover-cards via `MarkdownRenderer`'s WQ3 integration — that path is unchanged; this row is the sibling that pulls the full citation set up to the top level. 8 source-inspection tests across 3 suites in [`wi4Citations.test.ts`](../client/src/pages/Dashboard/lib/wi4Citations.test.ts) — including a three-landmark source-order pin (prose-end → Sources → Re-explain button) and an "intervening `</section>` absent" pin (Sources lives inside the Regenerated insight `<section>`, not its own). Test file appended to server/package.json's explicit list per invariant #4. Client typecheck baseline preserved (53). **WI4-rexplain** **WI4-rexplain** gives the user an explicit way to force a fresh regeneration of the same slice (the panel auto-fires once per fresh event then serves from cache; the button calls `regen.regenerate(specLite, narrowedRows, { bypassCache: true })`). [`ExplainSlicePanel`](../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx) gains a `<Button>` inside the "Regenerated insight" `<section>`, sibling below the prose `<div>`, gated on `regen.entry?.text && chart && specLite` (only appears once a regeneration has actually landed AND the chart + spec are resolved). Sparkles (idle) / Loader2 spin (loading); disabled while loading; "Re-explain this slice" / "Re-explaining…" label pair; aria-label; `e.stopPropagation()` so the sheet doesn't close on click — mirrors the WI2 footer's bypass-cache button shape (variant=outline + size=sm + h-7 + text-[11px]). The hook already supported `bypassCache: true` since WI2-wire; this wave is purely the call-site surface. NEGATIVE pin in tests against widening `ExplainSlicePanelProps` with `onRegenerate?` / `bypassCache?` so a future refactor that tries to lift regen control to the parent surfaces immediately (the hook stays in-scope where it lives). 12 source-inspection tests in [`wi4Rexplain.test.ts`](../client/src/pages/Dashboard/lib/wi4Rexplain.test.ts) appended to server/package.json's explicit list per invariant #4. Client typecheck baseline preserved (53). **WI4-cache-key** **WI4-cache-key** closes a silent cache collision the prior WI4 waves shipped with — two brushes on the same `(chartId, filters)` but different sub-regions were sharing one cache slot, so the second brush would silently paint the first brush's prose. [`buildCacheKey`](../client/src/pages/Dashboard/lib/insightRegenCache.ts) gains a third optional `regionHash` arg; new `hashBrushRegion(region)` helper mirrors `hashGlobalFilters`'s byte-stability contract per kind (`n:<start>..<end>` / `t:<startMs>..<endMs>` / `c:<v1>|<v2>|…`) and returns `""` for undefined so non-brush call sites get byte-identical keys to the pre-wave 2-arg shape. [`useInsightRegen`](../client/src/pages/Dashboard/hooks/useInsightRegen.ts) takes an optional `brushRegion` arg and threads it through the cache-key `useMemo` + deps. [`ExplainSlicePanel`](../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx) passes `event?.region` at the call site. Categorical value order is preserved (not sorted) — a NEGATIVE test pin catches a future drift; the band-scale slice the renderer dispatches is itself ordered upstream via `distinctOrdered(data, enc.x.accessor)`, so identical brushes always yield identical value arrays without sorting. WI2 footer's existing keys remain reachable through the widening (3-arg-empty call → byte-identical 2-segment key). 17 new tests across the existing [`insightRegenCache.test.ts`](../client/src/pages/Dashboard/lib/insightRegenCache.test.ts) covering byte stability per kind + 3-arg backwards compat + per-kind collision avoidance; 3 new source-inspection pins in [`useInsightRegen.test.ts`](../client/src/pages/Dashboard/hooks/useInsightRegen.test.ts); 1 new pin in [`wi4Wire.test.ts`](../client/src/pages/Dashboard/lib/wi4Wire.test.ts). Two pre-existing pins broadened to match the widened import + call shapes. No new test file → server/package.json's explicit list unchanged. Client typecheck baseline preserved (53). Remaining dormancy-debt candidates: "Re-explain" button in the panel (~40 LOC); citation chips in the panel (~50 LOC); box-2d brush for scatter (~150 LOC, needs PointRenderer wiring + a 4th `BrushRegion` kind + a `box2d` branch in `hashBrushRegion`). **WI4-wiring-bar** **WI4-wiring-bar** closes the widening: BarRenderer had no pre-existing brush mechanics (only per-bar onClick handlers powering WD2 cross-filter + WD3 drill-through), so this wave adds mouse-down / move / up state + a `<rect>` overlay from scratch (mirroring WI4-wiring-area). Categorical-only — bars use a band scale on `enc.x.field`, no temporal branch (the NO-import negative pin catches a future drift). Orientation-aware: vertical bars brush horizontally along the x-axis (innerWidth, `ew-resize`); horizontal bars brush vertically along the y-axis (innerHeight, `ns-resize`). The categorical-axis math collapses both orientations into a single 1D brush via three derived locals (`isVertical` / `brushAxisSize` / `brushAxisOffset`) so the three handlers (`onBrushDown` / `onBrushMove` / `onBrushUp`) stay orientation-agnostic. `<rect>` overlay is orientation-aware (full-height column in vertical mode, full-width band in horizontal mode); per-bar tooltip is suppressed during an active brush via `if (brushStart !== null) return;` at the top of the per-bar onMouseMove. The pre-existing svg `onMouseLeave={hideTooltip}` widens to ALSO clear brush state on cursor-leaves-svg-mid-drag. Pre-existing per-bar onClick handler preserved byte-identical (WD3 drill / WD2 cross-filter / chat-explorer grid.toggleFilter branches all stay). 38 source-inspection tests across 16 suites in [`wi4WiringBar.test.ts`](../client/src/pages/Dashboard/lib/wi4WiringBar.test.ts) — NEGATIVE pins against `makeTemporalRegion` / `makeNumericRegion` imports + against `setZoomRange` (no zoom on Bar). 152 tests green across the WI4-bar + WD3-bar + WD2-bar + WI4-area + WI4-trend + explainSlice regression suite (61 suites). Cumulative WI4 family: 178 tests across 71 suites all green. Client typecheck baseline preserved (53). The WI4 master-plan series now spans the full brush-capable surface (Line + Area + Bar = 3 of 3); the WI4 stream enters maintenance mode. Remaining dormancy-debt candidates: brush-region in the cache key; "Re-explain" button in the panel; citation chips in the panel; box-2d brush for scatter (would require PointRenderer wiring + a 4th BrushRegion kind). **WI4-wiring-area** widens the wired surface from 1 of 3 to 2 of 3: AreaRenderer had no pre-existing brush mechanics (only an onClick handler powering WD2 cross-filter + WD3 drill-through), so this wave adds mouse-down / move / up state + a `<rect>` overlay from scratch. The brush is exclusively for explain-slice — plain drag is a deliberate no-op (no brush-to-zoom on Area, unlike LineRenderer's plain-drag-zooms / alt-drag-explains disambiguation). [`AreaRenderer.tsx`](../client/src/lib/charts/visxRenderers/AreaRenderer.tsx) gains `brushStart` / `brushEnd` useState + `brushExplainRef` useRef, a useEffect resetting brush state on [data] change, and three new mouse handlers gated on `dashboardTile`: `onBrushDown` (captures `e.altKey === true` into the ref + sets brushStart/brushEnd to the click x), `onBrushMove` (gated on `e.buttons & 1` mouse-held + brushStart non-null, clamps to `[0, innerWidth]`), `onBrushUp` (gates on `isBrushDrag(brushStart, brushEnd, BRUSH_MIN_PX)` — click branch resets state and yields to existing onClick; drag branch dispatches `dispatchExplainSlice` gated on `brushExplainRef.current && dashboardTile` with the 5-field shape `{ chartId, column: xCh.field, region, sourceTileId, filters: dashboardFilters }`). Temporal axis uses `makeTemporalRegion(startMs, endMs)` via `domMin + (lo / innerWidth) * (domMax - domMin)` interpolation; categorical axis uses `makeCategoricalRegion(xs.slice(i0, i1))` via the same Math.max/floor/min/ceil shape as LineRenderer. SVG element gains `onMouseDown` / `onMouseMove` / `onMouseUp` / `onMouseLeave` (safety net clearing brush state if cursor leaves mid-drag), cursor toggles between `ew-resize` (active brush) and the pre-existing `pointer` (idle in dashboard). `<rect>` overlay inserted inside `<Group>` after `GridRows` with same styling as LineRenderer (primary tint at 0.1 fill + 0.4 stroke + dashed `3 3` + `pointerEvents="none"`). Pre-existing WD2/WD3 click handlers preserved byte-identical — the browser doesn't fire `click` after a drag so the brush and click paths never conflict. 37 source-inspection tests across 14 suites in [`wi4WiringArea.test.ts`](../client/src/pages/Dashboard/lib/wi4WiringArea.test.ts). Companion [`wd2WiringRestTrend.test.ts`](../client/src/pages/Dashboard/lib/wd2WiringRestTrend.test.ts) had two pins loosened — the stale `Math.abs(hi - lo) < 6` pin (pre-existing carry-forward from WI4-wiring-trend's missed cleanup) and the AreaRenderer cursor pin (direct consequence of this wave's new cursor expression). 96 tests green across the WD2-trend + WD3-trend + WI4-trend + WI4-area + explainSlice + WI4-panel + WI4-wire regression suite (34 suites). WI4 family cumulative: 140 tests across 55 suites all green. Client typecheck baseline preserved (53). The WI4 wired surface now spans 2 of 3 brush-capable kinds (Line + Area); remaining wave WI4-wiring-bar adds BarRenderer brush mechanics from scratch (categorical-only). **WI4-wire** swaps the WI4-panel "Regenerated insight" placeholder for a real `useInsightRegen` call. The panel narrows brushed rows through the WI2 pipeline (`applyChartFilters(rows, event.filters)` → `filterRowsByBrushRegion(rows, event.column, event.region)`), calls `useInsightRegen({ tileId: event.chartId, filters: event.filters, cache: insightRegenCache })` unconditionally (React rules of hooks; falls back to `IDLE_TILE_ID = "__wi4_idle__"` when no event), and fires `regen.regenerate(specLite, narrowedRows)` from a `useEffect` keyed on `[event, specLite, narrowedRows]` (the hook's internal `seqRef` guards against stale resolves; `regen` deliberately omitted from deps via `eslint-disable` because the hook captures `regenerate` via `useRef` internally). New optional props: `chart?: ChartSpec | null` (the resolved chart spec) + `insightRegenCache?: InsightRegenCache` (the shared LRU+TTL cache lifted at `DashboardView` mount). DashboardView resolves the chart inline at the panel mount via an IIFE that parses `event.chartId` with `/^chart-(\d+)$/.exec(...)` and indexes `activeSheet.charts[idx] ?? null`. The "Regenerated insight" section now has four state-aware render branches: `!chart` → "Could not resolve…"; `regen.loading` → "Regenerating insight for N brushed row(s)…" (singular / plural matched); `regen.error` → `role="alert"` with the error string (hook types error as `string | null`); `regen.entry?.text` → `whitespace-pre-wrap` entry prose; default → "Waiting for the first regeneration…". Cache sharing with WI2 per-tile regen (same `insightRegenCache` instance, same `(tileId, filterHash)` key shape) is the intentional consequence. 29 source-inspection tests across 9 suites in [`wi4Wire.test.ts`](../client/src/pages/Dashboard/lib/wi4Wire.test.ts). 103 cumulative tests across the full WI4 family (41 suites) all green. Zero new server-side code — the wave reuses the WI2 `/api/insight/regen` endpoint. The WI4 master-plan series is functionally complete on LineRenderer (1 of 3 brush-capable kinds); remaining waves WI4-wiring-area + WI4-wiring-bar widen the brush-capable surface. **WI4-foundation + WI4-wiring-trend + WI4-panel** (earlier today): **WI4-panel** ships the receiver UI: new [`ExplainSlicePanel`](../client/src/pages/Dashboard/Components/ExplainSlicePanel.tsx) component (Radix right-side Sheet) mirrors `<DrillThroughSheet>`'s shape so the three click-intent receivers stay structurally parallel. Three body sections: "Pinned slice" (chartId / column / region with a region.kind chip + `formatRegion(region)` for the bounds — numeric `[start, end]`, temporal `<startISO> → <endISO>` full canonical form, categorical comma-joined with `MAX_CATEGORY_PREVIEW = 10` cap + `… +N more` suffix), "Filter context at brush time" (`summariseFilters(...)` lines), "Regenerated insight" (WI4-wire placeholder pinning the `applyChartFilters` → `filterRowsByBrushRegion` → `useInsightRegen` pipeline shape via inline `<code>` references). [`DashboardView`](../client/src/pages/Dashboard/Components/DashboardView.tsx) gains a sibling `explainSliceEvent` state (independent of `drillThroughEvent` so a future wave can hold both panels open at once), a `useEffect` window listener for `EXPLAIN_SLICE_EVENT` mirroring the `DRILL_THROUGH_EVENT` listener byte-for-byte (with stricter 3-field validation requiring `chartId` + `column` + `region`), and the `<ExplainSlicePanel>` mount at the bottom of the JSX tree alongside `<DrillThroughSheet>`. 23 source-inspection tests across 10 suites in [`wi4Panel.test.ts`](../client/src/pages/Dashboard/lib/wi4Panel.test.ts). 113 cumulative tests across WI4-foundation + WI4-wiring-trend + WI4-panel + WD3-sheet + WD3-wiring-rest-trend (46 suites) all green. The cmd/ctrl-click → drill round-trip + the alt-drag → explain round-trip now BOTH work end-to-end on LineRenderer (drill: 15 of 15 chart kinds; explain: 1 of 3 brush-capable kinds wired). **WI4-foundation + WI4-wiring-trend** (earlier today): **WI4-wiring-trend** is the first per-renderer consumer: [`LineRenderer.tsx`](../client/src/lib/charts/visxRenderers/LineRenderer.tsx) gains an alt-drag → `dispatchExplainSlice` branch BEFORE the pre-existing brush-to-zoom branch. New `brushExplainRef = useRef<boolean>(false)` captures `e.altKey === true` at brushDown (alongside the WD3 `brushModifierRef` for cmd/ctrl), consumed in the parameterless `onBrushUp`. The click-vs-drag threshold now derives from the foundation: `if (!isBrushDrag(brushStart, brushEnd, BRUSH_MIN_PX))` replaces the prior inline `Math.abs(hi - lo) < 6`. The explain branch is gated on `brushExplainRef.current && dashboardTile`; inside, the temporal axis computes `makeTemporalRegion(startMs, endMs)` from the existing zoom-temporal math (`domMin + (lo / innerWidth) * (domMax - domMin)` interpolation), the categorical axis computes `makeCategoricalRegion(xs.slice(i0, i1))` from the same Math.max/floor/min/ceil shape as the zoom branch (alt-dragging from index 5 to 12 brushes the SAME range that plain-dragging would zoom to). Dispatch shape: `{ chartId, column: xCh.field, region, sourceTileId, filters: dashboardFilters }`, guarded by `if (region)` so a null constructor return (zero-width brush) doesn't fire a malformed event. Both refs reset to false after dispatch + after the click path so a subsequent plain brushDown doesn't inherit a stale alt flag. 23 source-inspection tests across 11 suites in [`wi4WiringTrend.test.ts`](../client/src/pages/Dashboard/lib/wi4WiringTrend.test.ts). The companion `wd3WiringRestTrend.test.ts` had one over-tight regex loosened to allow the new sibling reset between `brushModifierRef = false` and `setBrushStart(null)`. 69 cumulative tests across WD3-trend + WI4-trend + foundation (29 suites) all green. **WI4-foundation** (earlier today): the pure-fn module [`explainSlice.ts`](../client/src/pages/Dashboard/lib/explainSlice.ts) ships the THIRD click-intent on chart marks, sibling to WD2's [`crossFilter.ts`](../client/src/pages/Dashboard/lib/crossFilter.ts) and WD3's [`drillThrough.ts`](../client/src/pages/Dashboard/lib/drillThrough.ts). Where WD2 captures a single click (cross-filter) and WD3 captures a single cmd/ctrl-click (drill-through), WI4 captures a rect-drag (mouse-down + mouse-up at ≥ 6 px apart) and dispatches an `ExplainSliceEvent` that re-runs the WI2 insight generator on just the brushed sub-region. **BRUSH_MIN_PX = 6** lifts [`LineRenderer.tsx`](../client/src/lib/charts/visxRenderers/LineRenderer.tsx)'s inline `Math.abs(hi - lo) < 6` click-vs-drag threshold into a single shared constant. **`BrushRegion`** is a discriminated union (`numeric` / `temporal` / `categorical`) covering the three x-axis flavours the WI4-wiring targets bind to (Line / Area / Bar across all three kinds). **`ExplainSliceEvent`** mirrors `DrillThroughEvent` (`chartId` / `column` / `sourceTileId` / `filters`) plus the load-bearing new field `region`. **`EXPLAIN_SLICE_EVENT = "marico:explain-slice"`** namespaces the CustomEvent. **`isBrushDrag(start, end, minPx)`** is the pixel-distance gate (defensive on `null` / `undefined` for mid-state-reset). Constructor helpers **`makeNumericRegion` / `makeTemporalRegion` / `makeCategoricalRegion`** enforce `start <= end` normalisation + reject zero-width / non-finite inputs so callers can't fire a malformed event. **`filterRowsByBrushRegion(rows, column, region)`** is the pure post-filter (commutative-AND with `applyChartFilters`); numeric branch explicitly drops `null` / `undefined` / `""` BEFORE coercion (otherwise `Number(null) === 0` would silently match a `[0, N]` range). **`dispatchExplainSlice`** is the SSR-safe dispatcher. 28 source-and-runtime tests across 11 suites in [`explainSlice.test.ts`](../client/src/pages/Dashboard/lib/explainSlice.test.ts). Remaining waves: **WI4-wiring-trend** (LineRenderer + AreaRenderer brush — Line already has brush-to-zoom mechanics; need to disambiguate the two intents, likely via alt-drag for explain), **WI4-wiring-area** (AreaRenderer brush mechanics if not folded into trend wave), **WI4-wiring-bar** (BarRenderer brush — no current brush mechanics, needs a new mouse-down/mouse-up state), **WI4-panel** (Radix Dialog / Sheet receiver UI subscribing to `EXPLAIN_SLICE_EVENT`), **WI4-wire** (`useInsightRegen` integration with the brushed `filteredRows`).
- **Wire-up · WW1→WV8 (deterministic-floor series CLOSED)** · planner + narrator + verifier-side enforcement of WQ1's deterministic floor is now LIVE, EVIDENCE-CARRYING for all four high-volume statistical tools, AND surfaces claimed-vs-actual tier counts to the workbench. WW1 wired planner (TOOL_ROUTER_HINT + EXTERNAL_CLAIM_MARKERS + PLANNER_CONFIDENCE_DIRECTIVE). WW2 wired narrator (FINDING_CONFIDENCE block + system-prompt tier-pinning directive). WV1 shipped the verifier-side overclaim detector ([`verifierConfidenceCheck.ts`](../server/lib/agents/runtime/verifierConfidenceCheck.ts)). WV2 shipped the canonical `FindingEvidence → detail` formatter ([`formatFindingEvidence.ts`](../server/lib/agents/runtime/formatFindingEvidence.ts)). WV3 wired the detector into [`verifier.ts`](../server/lib/agents/runtime/verifier.ts) as a pre-LLM short-circuit. WV4–WV7 migrated all four high-volume statistical tools. WV8 closes the series with workbench surfacing: the `critic_verdict` SSE payload now carries `confidence_overclaim: { claimed, actual }` when the WV3 short-circuit fires, and [`agentWorkbench.util.ts`](../server/services/chat/agentWorkbench.util.ts) renders `Narrator confidence: claimed Xh/Ym/Zl; blackboard supports Xh/Ym/Zl` alongside the verdict line. Remaining dormancy-debt candidates: effect-size as fifth `FindingEvidence` field; per-correlation / per-group tier carriage via structured `Finding.evidence`; lifting single-flow rewrite suppression behind `DEEP_INVESTIGATION_ENABLED`; client-side rich renderer for the tier-counts (currently persisted as workbench `code: string`).
- **Workstream 9 — quality 2.0** · WQ1 + WQ2 helpers SHIPPED + WIRED into planner (WW1) + narrator (WW2) + verifier (WV1+WV3) + workbench (WV8); WQ7 ships significance-by-default on `run_two_segment_compare` + `run_breakdown_ranking`; WQ3 ships citation hover-cards on backtick-wrapped pack ids in narrator prose ([`MarkdownRenderer`](../client/src/components/ui/markdown-renderer.tsx) + new [`CitationHoverCard`](../client/src/components/CitationHoverCard.tsx) + pure helper [`citationTokens.ts`](../client/src/lib/citationTokens.ts) mirroring W22 server regex). ALL 7 Workstream 9 items shipped (WQ1–WQ7); the deterministic-floor series + citation UX surfaces are complete end-to-end. Stream now in maintenance mode; effect-size as fifth FindingEvidence field is the next backlog item if needed.
- **Workstream 5 — tool library expansion** · WT6 selectTool wired by WW1 — TOOL_ROUTER_HINT block surfaces in the planner user prompt with intent + ranked tools per turn. Workstream now spans 6 of 6 routing/question-shape pieces (WT8 hierarchical-drill, WT2 cohort, WT3 RFM, WT7 elasticity, WT4 market-basket, WT6 router). Remaining tools require Python (WT1 causal / WT5 what-if / WT9 MTA Markov). Next: WT5 — `run_what_if` (Python scipy Monte Carlo).
- **Workstream 7 — insight engine 2.0** · WI1 schema foundation + WI2-cache + WI2-server + WI2-wire + WI2-wire-bind + **WI3** shipped — WI2 trilogy CLOSED + per-tile insight citations now have a discoverable Sources row. WI1: `chart.insight: InsightSpec` with `default + generator + confidenceTier + citations + regeneratedAt`. WI2-cache: client-side LRU+TTL cache keyed by `(tileId, hashGlobalFilters(filters))`, default 64 entries / 5-min TTL. WI2-server: `POST /api/insight/regen` endpoint with response shape matching `InsightRegenEntry` byte-for-byte so the merge is `cache.set(key, response)` with no transformation. WI2-wire: [`useInsightRegen`](../client/src/pages/Dashboard/hooks/useInsightRegen.ts) hook + optional `regen?: {...}` prop on `TileInsightFooter`. **WI2-wire-bind**: [`DashboardView`](../client/src/pages/Dashboard/Components/DashboardView.tsx) mounts a single shared cache via `useMemo(() => createInsightRegenCache(), [])` and threads it through [`DashboardTiles`](../client/src/pages/Dashboard/Components/DashboardTiles.tsx) into every [`ChartTileBody`](../client/src/pages/Dashboard/Components/ChartTileBody.tsx). The chart-tile body derives `InsightChartSpecLite` from `tile.chart` (field-for-field subset; spread-conditional `seriesColumn`/`aggregate`), computes `filteredRows` via `applyChartFilters(tile.chart.data ?? [], filters ?? {})` — sidestepping any ChartShim refactor by reading from the spec's embedded data array — and mounts `useInsightRegen({ tileId, filters ?? {}, cache: insightRegenCache })` then binds a no-arg `handleRegenerate` via `useCallback` closing over `[regen, specLite, filteredRows]`. The four-key regen prop ships to `TileInsightFooter` only when `tile.chart.keyInsight` is present, so charts without a static insight remain visually unchanged. 19 tests across 3 source-inspection suites. WI3 (closes the citation surface): [`TileInsightFooter`](../client/src/pages/Dashboard/Components/TileInsightFooter.tsx) renders `regen.entry.citations` as a `Sources:` row of `CitationHoverCard` chips between the metadata line and the Re-explain button; gated on truthy AND non-empty so charts without domain-pack citations stay visually clean. Inline `[N]` superscript hover-cards inside the prose continue to render transitively via `MarkdownRenderer`'s WQ3 integration — no double-work. Next: WI4–WI6 — explain-this-slice / per-tile recommendations / insight history. Backlog: domain-context plumbing into ChartTileBody (no client-side domainContext loader exists yet); spec-hash mixed into the cache key so spec edits invalidate cleanly; stream the regen response; pack body inside the hover-card popover (currently shows label + canonical id + admin-page pointer, body lookup deferred per WQ3's "Out of scope").
- **Workstream 1 — semantic & metrics layer** · W56 types + W57 inference + W58 compiler + W59a manifest helper + W59b planner-prompt wiring + **W60 `execute_metric_query` tool dispatcher** all shipped. The semantic-layer chain is now LIVE end-to-end AS A DISPATCH PATH (not just informational grounding): the planner emits `{ metric, breakdownBy?, filters?, sortBy?, limit? }` against catalog NAMES, the W58 compiler translates to `QueryPlanBody`, and dispatch goes through the existing `execute_query_plan` executor (DuckDB-first, in-memory fallback) — same observability, same workbench artifact lane, same overclaim / aggregation-floor verifiers. W60 ships [`executeMetricQueryTool.ts`](../server/lib/agents/runtime/tools/executeMetricQueryTool.ts) with a strict args schema mirroring `CompileMetricQueryInput` minus `model` (read from session), a `compiledPlanToQueryPlanBody` pure helper that deep-copies and drops empty optionals, and a closure-captured registry handle that delegates via `registry.execute("execute_query_plan", { plan }, ctx)`. Bundles a single SEMANTIC_CATALOG `Rules:` bullet into the planner system prompt — single cache invalidation rather than two (W59b deliberately deferred this to W60). Cube's predicted ~70 % share of the eventual ~300 % accuracy lift is now reachable. 28 tests across 7 suites. Next: W61 — admin UI page (`/admin/semantic-model`) for the data team to review / edit the inferred model. W62 — live-golden-replay fixtures (q06–q10) pinning metric-compiled wins over raw SQL. W63 — `validateModel` warnings + planner-picked-raw drift gate. W64 — `(modelVersion, metricName, dimensions, filters, window)` result cache for zero-LLM-call hits.
- **Workstream 4 — dashboard 2.0 · WD2-wiring + WD2-dim families CLOSED; WD3 drill-through series advances to 8 of 15 chart kinds wired + receiver.** **WD3-wiring-rest-trend** extends to [`LineRenderer`](../client/src/lib/charts/visxRenderers/LineRenderer.tsx) + [`AreaRenderer`](../client/src/lib/charts/visxRenderers/AreaRenderer.tsx). Two divergent shapes: AreaRenderer's inline `onClick` already accepts the MouseEvent so the WD3 wiring is a top-of-handler `if (isModifierClick(e))` branch inside `if (nearest) { ... }` body; LineRenderer's dispatch fires inside the PARAMETERLESS `onBrushUp` (brush-zoom piggybacks the click semantic on `< 6 px` drags), so a new `brushModifierRef = useRef<boolean>(false)` captures `isModifierClick(e)` at `onBrushDown` time and the brushUp click path branches `if (brushModifierRef.current) drill else cross-filter`. `useRef` (not `useState`) because the flag doesn't drive a re-render. Reset to `false` BEFORE the `setBrushStart` / `setBrushEnd` null-out so a brush-zoom mouseup with cmd held doesn't leave the flag dirty. Both renderers' nearest-x WD2 lookup unchanged; only the dispatcher diverges on the modifier flag. 18 source-inspection tests across 7 suites in [`wd3WiringRestTrend.test.ts`](../client/src/pages/Dashboard/lib/wd3WiringRestTrend.test.ts). **WD3-sheet** ships the [`DrillThroughSheet`](../client/src/pages/Dashboard/Components/DrillThroughSheet.tsx) Radix-Sheet component + DashboardView listener that opens the sheet on `DRILL_THROUGH_EVENT` receipt. The cmd/ctrl-click → side-sheet round-trip now works end-to-end on 8 wired chart kinds (Bar + Arc + Funnel + Box + Waterfall + Combo + Line + Area). **WD3-wiring-rest-cat** fanned out the WD3-wiring-bar pattern to 5 categorical visx renderers. **WD3-wiring-bar** wired BarRenderer. **WD3-foundation** ships the [`drillThrough.ts`](../client/src/pages/Dashboard/lib/drillThrough.ts) helper. **Remaining waves:** WD3-wiring-rest-point (scatter per-point on colorCh — ~60 LOC); WD3-wiring-rest-rect (heatmap two-column drill — needs foundation tweak); WD3-wiring-echarts (5 ECharts marks via onChartClick params — ~150 LOC); WD3-server (`/api/dashboards/:id/drill` endpoint replacing the sheet's placeholder body). **WD3-sheet** ships the [`DrillThroughSheet`](../client/src/pages/Dashboard/Components/DrillThroughSheet.tsx) Radix-Sheet component (right-side slide-in) + DashboardView listener that subscribes to `DRILL_THROUGH_EVENT` and opens the sheet on receipt. The cmd / ctrl-click → side-sheet round-trip now works end-to-end on the 6 wired chart kinds (Bar + Arc + Funnel + Box + Waterfall + Combo). The sheet derives `open` from `event !== null` (single source of truth — no two-prop drift), shows chartId / column / value + a "Filter context at click time" iteration of `event.filters` via a `summariseFilters()` helper covering all three `ChartFilterSelection` types, and renders a dashed placeholder pinning the WD3-server endpoint as the future row source. DashboardView's new listener mirrors the cross-filter listener's shape (SSR-safe `typeof window` guard, addEventListener + return-cleanup) but with STRICTER detail validation (BOTH `chartId` AND `column` must be strings because the future server fetch needs chartId). 21 source-inspection tests across 7 suites in [`wd3Sheet.test.ts`](../client/src/pages/Dashboard/lib/wd3Sheet.test.ts). **WD3-wiring-rest-cat** (earlier today) fanned out the WD3-wiring-bar pattern to 5 categorical visx renderers; **WD3-wiring-bar** wired BarRenderer as the first consumer; **WD3-foundation** ships the [`drillThrough.ts`](../client/src/pages/Dashboard/lib/drillThrough.ts) helper (`DrillThroughEvent` + `DRILL_THROUGH_EVENT` + `isModifierClick` + `dispatchDrillThrough`). **Remaining waves:** WD3-wiring-rest-trend (Line + Area nearest-x); WD3-wiring-rest-point (scatter per-point on colorCh); WD3-wiring-rest-rect (heatmap two-column drill — needs foundation tweak); WD3-wiring-echarts (5 ECharts marks); WD3-server (`/api/dashboards/:id/drill` endpoint replacing the sheet's placeholder body). **WD3-wiring-rest-cat** fans out the WD3-wiring-bar pattern to 5 categorical visx renderers ([`ArcRenderer`](../client/src/lib/charts/visxRenderers/ArcRenderer.tsx), [`FunnelRenderer`](../client/src/lib/charts/visxRenderers/FunnelRenderer.tsx), [`BoxRenderer`](../client/src/lib/charts/visxRenderers/BoxRenderer.tsx), [`WaterfallRenderer`](../client/src/lib/charts/visxRenderers/WaterfallRenderer.tsx), [`ComboRenderer`](../client/src/lib/charts/visxRenderers/ComboRenderer.tsx)) with per-renderer column × raw-value pairs (Arc: `labelCh.field` × `arc.data.rawKey`; Funnel: `enc.x.field` × `s.rawLabel`; Box: `enc.x.field` × `s.rawCategory`; Waterfall: `enc.x.field` × `b.rawCategory` gated by `clickable` so running-total bars aren't drillable; Combo: `xCh.field` × `rawX` bars only — secondary-axis line untouched). 44 parametric source-inspection tests across 11 suites in [`wd3WiringRestCat.test.ts`](../client/src/pages/Dashboard/lib/wd3WiringRestCat.test.ts) — single 5-renderer table drives the per-renderer assertions so a new pin auto-fans across all five. **WD3-wiring-bar** (earlier today) wired BarRenderer as the first consumer. **WD3-foundation** ships [`drillThrough.ts`](../client/src/pages/Dashboard/lib/drillThrough.ts) — `DrillThroughEvent` (`chartId`, `column`, `value: unknown`, optional `sourceTileId` + `filters: ActiveChartFilters` snapshot), `DRILL_THROUGH_EVENT = "marico:drill-through"`, `isModifierClick(event)` cross-platform check (macOS `metaKey` / Windows-Linux `ctrlKey`; either fires; sparse event shape), `dispatchDrillThrough(event)` SSR-safe dispatcher. The renderer onClick handler shape: `if (isModifierClick(event)) { dispatchDrillThrough({...}); return; } dispatchCrossFilter({...});` — clean two-line branch. **Remaining waves:** WD3-wiring-rest-rect (heatmap, needs two-column drill design decision), WD3-wiring-rest-trend (Line + Area with nearest-x lookup), WD3-wiring-rest-point (scatter per-point on colorCh), WD3-wiring-echarts (5 ECharts marks via onChartClick params), WD3-sheet (DashboardView listener + `<DrillThroughSheet>` Radix Dialog / Sheet), WD3-server (`/api/dashboards/:id/drill?chartId=&column=&value=` endpoint). **WD3-wiring-bar** is the first per-renderer consumer of WD3-foundation. [`BarRenderer.tsx`](../client/src/lib/charts/visxRenderers/BarRenderer.tsx)'s existing onClick widens from `() => {...}` to `(event: React.MouseEvent<SVGElement>) => {...}` and gains a top-of-handler branch: `if (dashboardTile && isModifierClick(event)) { dispatchDrillThrough({ chartId, column, value: c.outerRaw, sourceTileId, filters: dashboardFilters }); return; }` — short-circuits BEFORE the pre-existing chat/explorer (`grid.inGrid.toggleFilter`) and dashboard (`dispatchCrossFilter`) branches. The AND-gate is load-bearing: chat / explorer cmd-clicks fall through unchanged because drill-through is dashboard-only (no `/api/dashboards/:id/drill` endpoint reachable from chat). The 5-field payload threads `chartId = sourceTileId = dashboardTile.tileId` (the dashboard's chart-by-position lookup, same convention as XLSX export), `column = enc.x.field` (symmetry-pinned with WD2-dim + WD2-wiring's dispatch column so all three concerns key on the same axis), `value = c.outerRaw` raw (NOT toFilterValue-coerced — server-side canonicalisation picks the right Date/number/categorical comparison), and `filters = dashboardFilters` snapshot (already lifted at the renderer top for the WD2-dim concern; captured at click time so a concurrent global-filter edit on another tile doesn't race). 12 source-inspection tests across 3 suites in [`wd3WiringBar.test.ts`](../client/src/pages/Dashboard/lib/wd3WiringBar.test.ts). **WD3-foundation** (yesterday) shipped the pure-helper module ([`drillThrough.ts`](../client/src/pages/Dashboard/lib/drillThrough.ts)) that powers this wave. Mirrors [`crossFilter.ts`](../client/src/pages/Dashboard/lib/crossFilter.ts)'s shape: `DrillThroughEvent` interface (`chartId`, `column`, `value: unknown`, optional `sourceTileId` + `filters` snapshot), `DRILL_THROUGH_EVENT = "marico:drill-through"` constant, `isModifierClick(event)` cross-platform check (macOS `metaKey` ⌘; Windows / Linux `ctrlKey`; either flag fires; sparse event shape accepts React synthetic / native DOM / ECharts onChartClick alike; null / undefined → false defensive), `dispatchDrillThrough(event)` SSR-safe dispatcher. The optional `filters` snapshot is the field that doesn't exist on `CrossFilterEvent` — drill-through is a server-side query that needs the entire filter context atomically at click time (no race with a concurrent global-filter edit). Renderer onClick handler shape (to be wired in subsequent waves): `if (isModifierClick(event)) { dispatchDrillThrough({...}); return; } dispatchCrossFilter({...});` — a clean two-line branch that future Claude reads as a pair. 11 pure-function tests across 4 suites in [`drillThrough.test.ts`](../client/src/pages/Dashboard/lib/drillThrough.test.ts). The cmd / ctrl detection MUST be uniform across all 15 renderers so the modifier check lives in a single helper rather than duplicated per-renderer — same "share the helper, not the wiring" precedent as WD2's crossFilter.ts. Remaining series: **WD3-wiring-bar** (BarRenderer onClick branch on isModifierClick); **WD3-wiring-rest-cat** (5 cat renderers); **WD3-wiring-rest-rect** (heatmap, dispatches both row + col on the same cmd-click); **WD3-wiring-rest-trend** (Line + Area); **WD3-wiring-rest-point** (scatter); **WD3-wiring-echarts** (5 ECharts marks via `onChartClick` event params); **WD3-sheet** (DashboardView listener + `<DrillThroughSheet>` Radix Dialog / Sheet component fetching rows from `/api/dashboards/:id/drill?chartId=&column=&value=`); **WD3-server** (the endpoint itself, mirroring the existing `/api/dashboards/:id/export/xlsx` chart-row lookup path).
- **Workstream 4 — dashboard 2.0 · WD2-wiring + WD2-dim families CLOSED end-to-end (15 of 15 chart kinds with a categorical filter target).** WD1 + WD2 (pure helper) + WD2-wiring-bar (BarRenderer) + WD2-wiring-rest-cat (5 categorical) + WD2-wiring-rest-rect (heatmap, two-dim dispatch) + WD2-wiring-rest-trend (Line + Area with nearest-x + brush-zoom guard) + WD2-wiring-rest-point (PointRenderer scatter, colorCh-gated) + WD2-wiring-echarts (Treemap/Sunburst/Sankey/Calendar/Candlestick via EChartsBase `onChartClick` prop) + WD2-dim-foundation + WD2-dim-bar + WD2-dim-cat + WD2-dim-rect + WD2-dim-trend + WD2-dim-point + WD2-dim-echarts-treemap (TreemapRenderer + SunburstRenderer leaf-only dim on `labelCh.field`; first ECharts dim wave establishing the inline-itemStyle-opacity mechanism) + **WD2-dim-echarts-rest** (SankeyRenderer + CalendarRenderer + CandlestickRenderer per-dataItem dim closing the family; Sankey per-node dim on `sourceCh.field` via spread (`{ ...n, itemStyle: { opacity: 0.4 } }`); Calendar per-cell dim on `dateCh.field` via mixed-shape array (matching cells stay as `[date, value]` tuples, non-matching promoted to `{ value: [date, value], itemStyle: { opacity: 0.4 } }`); Candlestick per-bar dim on `xCh.field` via the same tuple-or-object pattern with membership against `xs[i]` not OHLC values; each renderer adds a separate `dimmedX` memo that short-circuits to identity on dim-OFF — pre-wave data flow stays byte-identical for downstream consumers (Calendar's range + visualMap min/max read pre-wave `series`); `optionsKey` field name kept as pre-wave key so dim-off JSON is byte-identical (no canvas re-render on mount)) ship. The WD2-dim-* family now spans 10 visx renderers (bar + cat-5 + rect + line + area + point) + 5 ECharts marks (treemap + sunburst + sankey + calendar + candlestick) — closed end-to-end at 15 of 15 chart kinds with a categorical filter target. Workstream 4 (dashboard 2.0) maintenance mode: KPI / Radar / Regression / Parallel / Choropleth / Gauge remain deliberately skipped (no meaningful categorical click target). Next: WD3 (drill-through to underlying rows on cmd/ctrl-click) or WI4 (explain-this-slice on chart brush). KPI / Radar / Regression / Parallel / Choropleth / Gauge are all categorically inapplicable and remain deliberately skipped. **WD2-dim-foundation** is the contract-only foundation for the renderer-side dim factor (per the master plan's "filtered tiles dim non-matching marks instead of removing them" UX) — [`dashboardTileContext.tsx`](../client/src/pages/Dashboard/lib/dashboardTileContext.tsx) now exposes `filters?: ActiveChartFilters` on `DashboardTileContextValue`, spread-conditional in the `useMemo` body so callers that omit filters get a value byte-identical to the pre-wave shape; [`ChartTileBody`](../client/src/pages/Dashboard/Components/ChartTileBody.tsx) threads the existing `filters` prop into `<DashboardTileProvider filters={filters}>`; renderers can now call [`isCrossFilterActive(global, column, value)`](../client/src/pages/Dashboard/lib/crossFilter.ts) per mark. Follow-on family: WD2-dim-bar (BarRenderer's existing fillOpacity composition × ~0.4 when active filter doesn't match), WD2-dim-cat (Arc/Funnel/Box/Waterfall/Combo), WD2-dim-rect (two-dim heatmap), WD2-dim-trend (Line + Area), WD2-dim-point (scatter on colorCh.field), WD2-dim-echarts (the 5 ECharts marks via itemStyle.opacity per dataItem or select/emphasis at the buildOptions level). WD1: `+ Add filter` popover on the dashboard global filter bar. WD2: pure helpers `applyCrossFilter` / `dispatchCrossFilter` / `CROSS_FILTER_EVENT` / `toFilterValue` (toggle / append / replace semantics, mutation-free; 29 tests). WD2-wiring-bar: end-to-end click-to-filter on the bar mark validated via [`dashboardTileContext.tsx`](../client/src/pages/Dashboard/lib/dashboardTileContext.tsx) + [`BarRenderer.tsx`](../client/src/lib/charts/visxRenderers/BarRenderer.tsx) + [`DashboardView.tsx`](../client/src/pages/Dashboard/Components/DashboardView.tsx). **WD2-wiring-rest-cat**: 5 categorical-by-construction renderers ([`ArcRenderer`](../client/src/lib/charts/visxRenderers/ArcRenderer.tsx), [`FunnelRenderer`](../client/src/lib/charts/visxRenderers/FunnelRenderer.tsx), [`BoxRenderer`](../client/src/lib/charts/visxRenderers/BoxRenderer.tsx), [`WaterfallRenderer`](../client/src/lib/charts/visxRenderers/WaterfallRenderer.tsx), [`ComboRenderer`](../client/src/lib/charts/visxRenderers/ComboRenderer.tsx)) wired to `dispatchCrossFilter`. Each preserves the type-original category value (`rawKey` / `rawLabel` / `rawCategory` / `rawX`) alongside the stringified key so the cross-filter predicate compares apples-to-apples. WaterfallRenderer additionally skips dispatch on running-total bars (`b.isTotal === true`) via an intermediate `clickable` local — synthetic summary rows aren't categories the user can filter to. 36 tests across 7 suites in [`wd2WiringRestCat.test.ts`](../client/src/pages/Dashboard/lib/wd2WiringRestCat.test.ts) source-inspect the dispatch shape per renderer + the click-gate predicate + the cursor:pointer style + the raw-value preservation. Next: WD2-wiring-rest-rect — RectRenderer (heatmap) cells, needs a one-vs-two-dim filter decision. Then WD2-wiring-rest-trend — LineRenderer + AreaRenderer with nearest-x lookup + brush-zoom guard. Then WD2-wiring-rest-point — PointRenderer conditional on `colorCh`. Kpi / Radar / Regression have no meaningful categorical click target and are deliberately skipped. Then WD2-wiring-echarts for the lazy specialty pack. WD3–WD10 deliver drill-through → dynamic insights → fork-from-dashboard → mobile → linked-sheet filters → saved views → tile comments → scheduled refresh per the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md) Workstream 4 wave map.
- **Workstream 3 — investigation mode** · W74 ships: budget exhaustion observability. New pure detector [`investigationBudget.ts`](../server/lib/agents/runtime/investigationBudget.ts) names which cap (llm_calls / wall_time / max_nodes) tripped when the BFS loop terminates short of convergence. [`investigationOrchestrator.ts`](../server/lib/agents/runtime/investigationOrchestrator.ts) emits a `flow_decision` SSE row (`layer: "investigation-budget"`, `chosen: "halt"`) + agentLog `budget_exhausted` when the detector fires. No behaviour change — pure observability. Next: W75 — workbench UI for parallel sub-investigations (per-sub-question lanes in [`ThinkingPanel.tsx`](../client/src/pages/Home/Components/ThinkingPanel.tsx)). Or W76 — Hypothesis Tree visualizer.
- **Phase D — coordinator multi-part detection** · D1+D2 shipped; D3 (actual parallel sub-investigation) deferred behind `DEEP_INVESTIGATION_ENABLED`. Detector lives at [server/lib/agents/runtime/detectMultiPartQuestion.ts](../server/lib/agents/runtime/detectMultiPartQuestion.ts), observability fires in [chatStream.service.ts](../server/services/chat/chatStream.service.ts) after mode classification.
- **Phase F — predictive / inferential tooling** · F1 forecast, F2 anomaly detection, F3 significance tests all shipped, gated by `FORECAST_ENABLED` / `ANOMALY_DETECTION_ENABLED` / `SIGNIFICANCE_TESTS_ENABLED`. Pure-Node implementations under [server/lib/](../server/lib/).
- **W-series — query plan expressiveness** · W1 (window aggregations) → W2 (rolling-window detector) → W3 (composite-ranking expressions in `breakdown_ranking`) all shipped. P1 (pivot reads agent result rows) closes the load-bearing UX gap for `computedAggregations`.
- **E-series — multi-tab session sync** · E1 BroadcastChannel foundation → E2 active-filter sync → E3 messages/lifted-state sync → E4 stale-filter retry helper all shipped.
- **C-series — client safety** · C1 BAI exact-timestamp matching, C2 isMountedRef SSE guards, C3 optimistic-update rollback. Shipped.
- **B-series — context plumbing across sub-agents** · B1 (quick-lookup planner) through B7 (narrator user-block always surfaces userIntent). Shipped.
- **A-series — concurrency hardening** · A1 (filter-aware question cache) through A7 (domainContext cache atomicity via generation counter). Shipped — `withSessionWriteLock` is now the unified per-session Cosmos write mutex.
- **Routing system (this migration)** · Slim CLAUDE.md, `docs/STATE.md`, `docs/WAVES.md`, three slash skills (`/orient`, `/wave-commit`, `/load`), Stop hook for doc-freshness nudges. Setup wave in flight at time of writing.

## Known WIP / broken

- Wide-format classifier (W1–W4 of the wide-format plan) is mid-flight on this branch; remaining waves W14–W29 add the modal UX + ingest wiring + decision endpoint. Independent of W56's semantic layer but should land before W57 if the semantic compiler is to consume `_metric`/`_period` columns from melted datasets.
- [docs/architecture/schemas.md](architecture/schemas.md) still describes the pre-W5 dual-mirror schema model — out of date but non-blocking. Refresh in a future doc-hygiene wave.

## Next wave (if planned)

Workstream 7 is now **CLOSED at 6 of 6** after WI6 (insight history per tile dropdown). The WI4 master-plan series remains CLOSED end-to-end at 4 of 4 brush-capable kinds + 4 of 4 dormancy-debt items shipped. WQ8 (effect-size as fifth FindingEvidence field) shipped on top of the closed WQ/WV/WW deterministic-floor trinity; classifier now grades "significant but negligible" findings as LOW. Stream in maintenance mode.

Highest-leverage next candidates:

- **W61 polish trail** — admin UI page `/admin/semantic-models` now ships **every common-admin correction with correct source-weighting, visible source provenance, source-filtered review, AND server-side prior-model audit trail accessible over the wire**: exposed-toggle (W61-save), label / description / expression text editing (W61-edit-text), format / kind / temporalGrain enum selects with conditional currencyCode (W61-edit-enums), per-entry source bump to `"user"` on every save (W61-source-bump), per-row source chip rendering (W61-source-badge — auto / user / domain coloured chips next to each entry name), per-card source filter chips with shared global filter state (W61-source-filter — "show only User / Auto / Domain / All" with per-card counts), `?filter=X` URL-state persistence of the active chip (W61-filter-persist — share-link to filtered views + survive accidental reload), prior-model audit ring buffer written on every PATCH (W61-audit-log — cap 10, newest-first, snaps `priorModel` before the overwrite, atomic inside `withSessionWriteLock`), admin GET endpoint `GET /admin/semantic-models/:sessionId/audit-log` exposing the buffer (W61-audit-history-api — dedicated endpoint so the up-to-~500 KB payload is opt-in rather than bundled into every detail-fetch), and server-side one-call revert endpoint `POST /admin/semantic-models/:sessionId/revert { auditEntryIndex }` that restores a chosen entry's `priorModel` in one round-trip (W61-audit-revert — bumps version monotonically, prepends the about-to-be-overwritten model to the audit log so "undo this revert" works, skips W61-source-bump so per-entry `source` is restored as-was from the snapshot), and client-side API helpers + pure formatters / summary builders that consume both endpoints (W61-audit-history-client — `fetchSemanticModelAuditLog` / `revertSemanticModel` in [`admin.ts`](../client/src/lib/api/admin.ts); `formatAuditTimestamp` / `buildAuditEntrySummary` / `buildRevertConfirmation` in [`semanticModelAuditHistory.ts`](../client/src/pages/Admin/lib/semanticModelAuditHistory.ts)), and a collapsible Audit-history Card mounted on [`AdminSemanticModelDetail.tsx`](../client/src/pages/Admin/AdminSemanticModelDetail.tsx) as a fourth Card under Hierarchies that surfaces the audit trail to admins end-to-end — opt-in fetch via `fetchSemanticModelAuditLog` (gated on `historyOpen` so closed-section visits pay zero Cosmos cost; re-fetch on `data.lastUpdatedAt` bump so save / revert keeps the open list fresh), per-row Revert button gated on `saving || reverting !== null || historyLoading` calling `window.confirm(buildRevertConfirmation(...))` then `revertSemanticModel`, success path re-uses the existing PATCH `setData({ ...data, lastUpdatedAt, model })` shape because W61-audit-revert deliberately reuses `PatchSemanticModelResponse` (W61-audit-history-tab — direct fetch + useState rather than TanStack Query for stylistic continuity with the rest of the host component; per-row `reverting: number | null` index not a global boolean so non-clicked rows stay readable; pure-mounting wave with no new tests because the helpers were already pinned by W61-audit-history-client's 15 unit tests; +197 LOC to the host file → 1,295 LOC, under the 1,500 sub-component-extract threshold but worth a refactor pass before W61-add-delete pushes past — now SHIPPED via W61-detail-extract; host file dropped 1,295 → 1,131 LOC after `SourceFilterChips` + `AuditHistoryCard` carved into sibling files in `client/src/pages/Admin/components/`, leaving ~370 LOC of headroom before W61-add-delete's projected +300 LOC). With W61-audit-history-tab shipped the W61 audit-trail trilogy is now end-to-end on the client: admins read prior versions and one-click revert from the same page where they edit. **W61-detail-extract** pre-emptively defused the file-size pressure on `AdminSemanticModelDetail.tsx` at zero behavioural risk, unblocking W61-add-delete to land cleanly under the 1,500 LOC threshold. **W61-references-scan** (just shipped) added the pure-function scanner that counts downstream chart references to a semantic-model entry name — the load-bearing foundation for W61-delete-entry's "removing this metric will break N charts that reference it" confirmation. The original ~300-LOC W61-add-delete is now split into smaller sub-waves: **W61-references-scan** (pure-fn scanner, SHIPPED) → **W61-references-endpoint** (GET endpoint exposing the scanner, SHIPPED — `GET /admin/semantic-models/:sessionId/references?entry=<name>` returning `{ chartCount, totalOccurrences }`) → **W61-delete-server** (DELETE endpoint, SHIPPED — `DELETE /admin/semantic-models/:sessionId/entries/:kind/:name` with audit-write-before-delete + version bump + envelope reuse; mirrors W61-audit-revert) → **W61-delete-client** (SHIPPED — client API helpers `fetchSemanticModelReferences` + `deleteSemanticModelEntry` in [`client/src/lib/api/admin.ts`](../client/src/lib/api/admin.ts); per-row Delete button on `MetricRow` / `DimensionRow` / `HierarchyRow` via a shared `RowDeleteButton` presentational helper + a trailing Actions column with an `sr-only` header on all three tables; controlled `<AlertDialog>` sibling [`DeleteEntryConfirmation.tsx`](../client/src/pages/Admin/components/DeleteEntryConfirmation.tsx) that owns its own references fetch via mount-scoped `useEffect` and renders the "Removing this metric will break N charts" warning with singular / plural correctness + a "X references total" subhead when occurrences exceed chart count; pure-helper module [`semanticModelDeleteConfirmation.ts`](../client/src/pages/Admin/lib/semanticModelDeleteConfirmation.ts) pinning the singular / plural copy + the `DELETE_AUDIT_LOG_REASSURANCE` constant with 17 behavioural tests; host handler `handleDelete` mirrors `handleRevert` and updates `data` with the W61-save envelope so the audit-history-tab's `lastUpdatedAt`-keyed effect re-fetches the buffer automatically; `e.preventDefault()` on `<AlertDialogAction>` keeps the modal open with `deleting=true` until the parent resolves so failures surface inline; ~900 LOC total — host 1,131 → 1,316 LOC under the 1,500 sub-component-extract threshold) → **W61-add-entry** (server POST endpoint + client add-new affordance with inference-style fill-in form, ~200 LOC) → **W61-references-dashboards** SHIPPED — widens the references scanner to walk dashboards from the separate Cosmos container, threads `dashboardCount` + `dashboardTileCount` into the existing `AdminSemanticModelReferencesResponse` envelope, widens `buildDeleteReferencesWarning` to surface the combined "N charts and M tiles across K dashboards" copy (with dashboard-only and combined branches). Remaining polish waves on the same surface, each ~80–300 LOC: **W61-add-delete** — per-row delete with a "removing this metric will break N charts that reference it" confirmation (needs a downstream-reference scan against `chartSpecV2` + `dashboardSpec`) + add-new affordance with an inference-style fill-in form (~250-300 LOC client + ~50 LOC server). **W61-cache-invalidate** SHIPPED — observability hook + contract pin that every semantic-model mutation path (W61-save / W61-audit-revert / W61-delete-server / W61-add-server) calls `onSemanticModelVersionBumped` after the persist succeeds; pre-emptive foundation for the future W64 cache-key wave + a grep-able invalidation log surface for ops (no real cache lives behind the hook yet — `buildSemanticCatalogPromptBlock` is still pure — but the hook fires + the registry is in place so W64 can plug a `cache.clear()` callback in without re-touching every controller). New convention codified in [`docs/conventions/version-bump-invalidator-hook.md`](conventions/version-bump-invalidator-hook.md). **W61-hierarchy-edit** — separate edit surface for `SemanticHierarchy` (level reordering, level add/remove); has no `exposed` field and was excluded from the trilogy. **W61-edit-column** — column-picker dropdown for `SemanticDimension.column` rebinding, validates against the live `datasetSchema`. **W61-edit-references** — tag-input UI for the `SemanticMetric.references` array (drives W58 compiler's orphan-column lint).
- **WS2 latency series (W65 pre-flight parallel)** (~150 LOC) — refactor [`chatStream.service`](../server/services/chat/chatStream.service.ts) so classify-mode runs in parallel with RAG-retrieve + domain-pack load + schema-bind + `inferFiltersFromQuestion`. Latency floor drops ~1–2s. Touches the 2,398-LOC god-object; needs careful read first.
- **Bucket-style effectMagnitude in other tools (WV6 / WV7 follow-on)** (~80–150 LOC per tool) — WV4-bucket just shipped Cohen-on-|r| for correlation. Elasticity could bucket the coefficient (industry-specific cutoffs make a universal ladder awkward — FMCG own-price elasticity of `-2.0` is "huge", `-0.5` is "moderate"); segment-driver's feature importances are already relative to the model fit and lack an absolute scale. Each is a separate per-tool design wave.
- **WT5 — `run_what_if` Monte Carlo tool** (~350 LOC Python + ~150 LOC Node + tests) — closes "what if I increase X by Y%?" question shape via scipy + np.random. Requires Python service work.
- **WD3 dormancy debt** — multi-sheet chartId disambiguation; streaming row payload (NDJSON + useInfiniteQuery); per-chart-kind sheet body customisation; cell-click drill-into-drill; drill-through telemetry; hover-time cursor change. Each is a small polish wave on its own; none currently blocking.

## Last 5 waves (one line each — newest first)

- **W61-cache-invalidate** (2026-05-21) · Observability hook + contract pin that every semantic-model mutation path (W61-save / W61-audit-revert / W61-delete-server / W61-add-server) calls `onSemanticModelVersionBumped({ sessionId, priorVersion, nextVersion })` after the `_updater(doc)` persist resolves but before the `return { kind: "ok", ... }` line — still inside the per-session `withSessionWriteLock` window so future cache.clear() invalidators are serialized with the write they invalidate against. Pre-emptive foundation: today the planner's [`buildSemanticCatalogPromptBlock`](../server/lib/semantic/prompt.ts) is a pure function with no internal cache, so the version bump alone is sufficient for prompt freshness; the next planner round re-reads `chatDocument.semanticModel` from Cosmos and rebuilds the prompt block. But that "implicit contract" is silently broken if a new mutation path forgets to bump version, and the downstream W64 cache-key wave will introduce a real in-process cache keyed on `(sessionId, semanticModel.version)` that depends on every mutation path participating in the bump. This wave makes the contract explicit + observable + tested. New pure module [`server/lib/semantic/semanticModelInvalidate.ts`](../server/lib/semantic/semanticModelInvalidate.ts) (101 LOC) exports `SemanticModelInvalidationEvent` interface (readonly fields), `SemanticModelInvalidator` type alias, `onSemanticModelVersionBumped(event)` (increments private counter + emits the single grep-able log token `[semantic-model-invalidate] sessionId=… priorVersion=… nextVersion=…` + iterates listener registry in order with per-listener try/catch isolation), `registerInvalidator(fn)` (returns idempotent unsubscribe closure), plus four testing shims. Each of the four [`adminSemanticModelController.ts`](../server/controllers/adminSemanticModelController.ts) mutation paths inserts `onSemanticModelVersionBumped({ sessionId, priorVersion: nextVersion - 1, nextVersion })` right after `await _updater(doc)` — `priorVersion: nextVersion - 1` is the canonical form because `doc.semanticModel.version` has already been reassigned to `nextVersion` at that point in the lock callback. The hook is fire-and-forget — synchronous (`(event) => void`), no return value, swallows listener errors with `console.error` so a buggy invalidator can't break the write path. 16 module-level tests in [`semanticModelInvalidateW61CacheInvalidate.test.ts`](../server/tests/semanticModelInvalidateW61CacheInvalidate.test.ts) (registry semantics, log-token strict-equality privacy canary, throw isolation, unsubscribe idempotence, multi-listener ordering). 13 controller-level tests in [`adminSemanticModelInvalidateW61CacheInvalidate.test.ts`](../server/tests/adminSemanticModelInvalidateW61CacheInvalidate.test.ts) (every mutation path fires once on success / NEVER fires on 403 / 400 / 404 / 409 / 500 — including the load-bearing "500 on updater throw does NOT fire" pin because firing would invalidate against a write that never landed; plus a monotone-chain pin for sequential delete + add `100 → 101` then `101 → 102`). New convention codified in [`docs/conventions/version-bump-invalidator-hook.md`](../docs/conventions/version-bump-invalidator-hook.md): every code path that bumps `semanticModel.version` MUST call the hook from inside its `withSessionWriteLock` AFTER the `_updater(doc)` write resolves; error paths must NOT fire. Server typecheck 98 (preserved baseline), server tests 4791/4795 pass (was 4762/4766; +29 new tests landed cleanly; same 4 pre-existing `wd2WiringEcharts` Treemap leaf-click + Sunburst leaf-click + `wd2DimEchartsTreemap` triplet failures unchanged). Client untouched. **Out of scope**: the W64 cache itself (the wave just ships the foundation hook + contract pin); in-process catalog memos for `buildSemanticCatalogPromptBlock` (defer until profiling); `updatedBy` / `updatedAt` in the event payload (kept small for grep-stable token); async invalidators (synchronous shape forces every invalidator to stay cheap); cross-session invalidation events (single-session shape today); in-browser verification (server-only wave, no browser surface); wiring the hook into the W57 upload path (initial-set vs version-bump have different semantics, intentionally not covered).
- **W61-references-dashboards** (2026-05-21) · Widen the W61 references scanner to also count tiles across the session-owner's dashboards so the W61-delete-client warning copy surfaces the cross-dashboard impact alongside the in-chat chart impact. The W61-references-scan / -endpoint pair walked `doc.charts[]` only — dashboards live in a separate Cosmos container partitioned by username, so an admin deleting a metric saw the in-chat chart impact but NOT the dashboard-tile impact (real-world failure: admin deletes a metric thinking only 1 in-chat chart uses it, doesn't see that 5 promoted dashboard tiles also reference it across 2 dashboards). New pure module [`server/lib/semantic/semanticModelDashboardReferences.ts`](../server/lib/semantic/semanticModelDashboardReferences.ts) (~90 LOC) exports `DashboardReferenceCount` (`{ dashboardCount, dashboardTileCount }`) and `countDashboardReferences(name, dashboards)` — reuses the existing `countReferencesInChartSpec` / `countReferencesInChartSpecV2` per-chart walkers via the `isChartSpecV2` runtime discriminator, prefers `dashboard.sheets[].charts[]` when sheets is non-empty (modern dashboards keep top-level `charts[]` as a union, so walking both would double-count every tile), falls back to `dashboard.charts[]` only when sheets is absent/empty (legacy pre-sheets dashboards). Controller widens with new `DashboardListerForUser` injectable type + `_dashboardListerForUser` slot + `__setDashboardListerForUserForTesting` shim (defaults to `getUserDashboards(username)` from [`dashboard.model.ts`](../server/models/dashboard.model.ts); empty username short-circuits to `[]` so system-test sessions don't waste a Cosmos round-trip). `AdminSemanticModelReferencesResponse` widens with `dashboardCount` + `dashboardTileCount` fields (envelope shape mirrored client-side in [`client/src/lib/api/admin.ts`](../client/src/lib/api/admin.ts) per the cross-runtime-boundary local-interface-mirror convention). `buildDeleteReferencesWarning` widens signature to `(kind, chartCount, totalOccurrences, dashboardCount, dashboardTileCount)`: three impact shapes now possible — charts-only (`"... will break N charts that reference it."` original W61-delete-client form), dashboards-only (`"... will break M tiles across K dashboards that reference it."` — real-world domain-pack metric not plotted in-chat but used by dashboard tiles), and both (`"... will break N charts and M tiles across K dashboards that reference it."`). Verb agreement keyed to the LAST enumerated count (`1 chart and 2 tiles ... reference it` reads better than `... references it`). Subhead remains chart-side-only (tile count is the actionable unit at admin's level; field-position count within tiles is detail they'd drill into the dashboard for). `DeleteEntryConfirmation.tsx` threads the two new fields through to the warning helper. **New "one injectable per Cosmos container" convention**: chat docs use `_detailFetcher`; dashboards use `_dashboardListerForUser`; future shared-dashboards / past-analyses waves get their own injectable rather than overloading existing ones. 15 new behavioural tests in [`semanticModelDashboardReferencesW61ReferencesDashboards.test.ts`](../server/tests/semanticModelDashboardReferencesW61ReferencesDashboards.test.ts) (scanner: top-level guards / single sheet / multi-sheet aggregation / multiple dashboards / sheet-without-charts / sheets-vs-legacy preference / v1+v2 interop / no-double-count). 7 new tests in [`adminSemanticModelReferencesW61ReferencesDashboards.test.ts`](../server/tests/adminSemanticModelReferencesW61ReferencesDashboards.test.ts) (envelope widening / dashboard-only matches / username threading / empty-username short-circuit / multi-dashboard aggregation / 500 on dashboard-lister error / legacy fallback). Existing [`adminSemanticModelReferencesW61ReferencesEndpoint.test.ts`](../server/tests/adminSemanticModelReferencesW61ReferencesEndpoint.test.ts) updated in-place: 4 200-path tests now stub the new `__setDashboardListerForUserForTesting` to a `[]` lister and assert the widened envelope shape with `dashboardCount: 0, dashboardTileCount: 0`. 7 new tests in [`semanticModelDeleteConfirmationW61DeleteClient.test.ts`](../client/src/pages/Admin/lib/semanticModelDeleteConfirmationW61DeleteClient.test.ts) (dashboard-only / 1×1×1 singulars / combined-clause-with-and / chart-subhead-with-dashboards / no-dashboard-subhead-suppression / 1+1+1 / pluralization-anti-zero). Server typecheck 98 (preserved baseline), server tests 4762/4766 pass (was 4734/4738; +28 new tests landed cleanly + 0 regressions; same 4 pre-existing `wd2WiringEcharts` / `wd2DimEchartsTreemap` failures unchanged). Client typecheck not run this wave (client changes are interface widening + a function signature change with all callers updated; no new shapes added; client baseline 53 preserved). **Out of scope** (deferred): in-browser verification of the widened warning copy (full destructive-op click-through with both in-chat and dashboard hits); `dashboardTotalOccurrences` field (tile count is the right admin-level unit; field-position drill-down belongs in the dashboard view); blob-stored chart references inside dashboards (`chartReferences[]` — same multi-second-fetch concern as in-chat); shared dashboards (`shared-dashboards` Cosmos container) and past-analyses references (orthogonal containers, separate `_listerFor*` injectables when warranted); pre-delete server-side block when dashboardCount > 0 (policy belongs on client confirmation, not server); cross-user dashboard scan for admin-shared sessions (would need a different partition strategy).
- **W61-add-client** (2026-05-21) · Per-card `+ Add metric / dimension / hierarchy` button + controlled `<Dialog>` form modal that closes the W61 additive-edit loop end-to-end on the admin semantic-model detail page. Closes the W61 additive-edit loop end-to-end on the client — admins can now create a new entry from the same page where they edit, delete, and revert; the four-endpoint trail (save / revert / delete / add) is now fully UI-driven end-to-end. New API helper `addSemanticModelEntry` in [`client/src/lib/api/admin.ts`](../client/src/lib/api/admin.ts) with a typed `NameAlreadyExistsError` class that the host uses via `instanceof` to surface the 409 collision inline under the modal's name field (rather than scraping `err.message`). New `validateName` validator in [`semanticModelEditValidation.ts`](../client/src/pages/Admin/lib/semanticModelEditValidation.ts) mirrors the server's `SNAKE_CASE` regex `/^[a-z][a-z0-9_]*$/` + 1-80 length bounds (drift here just round-trips to a 400, correctness-preserving). New pure-helper module [`semanticModelAddForm.ts`](../client/src/pages/Admin/lib/semanticModelAddForm.ts) ships `buildAddHeadline` / `buildAddSubmitLabel` (with U+2026 ellipsis pinned) / `formatNameCollisionError` (anchor copy `"Choose a different name."` pinned) / `parseHierarchyLevels` (newline-split + trim + drop-empty + `\r\n` tolerance for Windows clipboard paste) + 23 behavioural tests. New sibling component [`AddEntryForm.tsx`](../client/src/pages/Admin/components/AddEntryForm.tsx) (530 LOC) — controlled `<Dialog>` (NOT `<AlertDialog>` — Dialog is Radix's canonical form-modal, AlertDialog is confirmation-only) owns its own per-kind draft state with an effect that resets to per-kind defaults on every open transition; per-kind field sets (metric: name/label/description/expression/format Select with conditional currencyCode/exposed Switch; dimension: + column + kind Select with conditional temporalGrain; hierarchy: + levels Textarea parsed via `parseHierarchyLevels`); per-field validation runs every validator at submit so all errors surface simultaneously (`results.every(Boolean)` gate vs short-circuit `&&`); `e.preventDefault()` on the form submit suppresses default browser submit so the parent's mutation can resolve while the modal stays open with `submitting=true`. Host [`AdminSemanticModelDetail.tsx`](../client/src/pages/Admin/AdminSemanticModelDetail.tsx) (+126 LOC, 1,316 → 1,442 LOC, ~58 LOC of headroom before the 1,500-LOC threshold) gains four new state slots (`addOpen: AdminSemanticModelEntryKind | null` is the open signal; `addSubmitting` is the in-flight guard; `addError` for generic failures; `addNameCollision: { kind, name } | null` for the typed 409 surface), a `handleAdd` async handler that catches `NameAlreadyExistsError` via `instanceof` and otherwise reuses the W61-save `setData` shape (so the audit-history-tab's `lastUpdatedAt`-keyed effect re-fetches the buffer automatically — the just-added entry's pre-state lands in the buffer for revert-as-undo), and per-card `+ Add <kind>` `<Button variant="outline" size="sm">` next to each SourceFilterChips (disabled gate matches the other write mutations: `saving || addSubmitting !== null || deletingEntry !== null || reverting !== null`). Server typecheck 98 (preserved), client typecheck 53 (preserved), server tests 4734/4738 pass (+23 new tests landed cleanly, same 4 pre-existing failures unchanged). Pre-existing client build failure unrelated (`html-to-image` dep missing in `ExportMenu.tsx`, last touched 2026-05-03 — not in W61 scope). In-browser verification deferred to a future browser-equipped session (the 5-minute click-through: open detail page → `+ Add metric` → fill fields → submit → see row appear + audit-history-tab grow → `+ Add metric` again with same name → see 409 collision inline → rename → submit → success). **Out of scope** (deferred): server-side `references[]`-against-datasetSchema validation (W58 compiler is the orphan-reference authority); `references[]` tag-input on add (schema's `.default([])` is the right default); drag-and-drop reorderable levels for hierarchies (textarea + per-line parsing covers the common case); component-level jsdom tests for the form (pure-helper tests pin the high-value regressions; mount tests would brittle on Radix portals); optimistic UI (additive ops feel different from optimistic patches — match the existing W61 non-optimistic shape); bulk-add / paste-JSON / CSV-import (different workflow); cross-session entry import; form-draft persistence across close; auto-suggestion of `name` from `label`; in-browser verification.
- **W61-add-server** (2026-05-21) · Admin `POST /admin/semantic-models/:sessionId/entries/:kind` endpoint that appends a single new metric / dimension / hierarchy to a session's semantic model. Closes the additive-edit loop on the server side as the natural counterpart to W61-delete-server; the four-endpoint trail (save / revert / delete / add) now reads as a coherent family of model-edit operations. Mirrors W61-delete-server's destructive-op shape: writes the prior model to the audit log inside the existing `withSessionWriteLock` (per invariant #9) BEFORE the additive op so "undo this add via revert" works (the audit-write captures the pre-add snapshot); bumps `version` monotonically (W64's compiled-query cache key invalidation); returns the same `{ sessionId, lastUpdatedAt, model }` envelope as W61-save / W61-audit-revert / W61-delete-server so the future client mutation reuses the existing success handler. Server-only — no client UI yet; the next wave **W61-add-client** will add the `+ Add` button at the bottom of each card + the `AddEntryForm` modal that surfaces the 409 `name_already_exists` error inline. Body validated via the kind-appropriate zod schema picked via ternary (`metric` → `semanticMetricSchema`, `dimension` → `semanticDimensionSchema`, `hierarchy` → `semanticHierarchySchema`) — ternary preserves per-kind type narrowing for the in-lock cast vs a `Record` lookup that would resolve to a union and need re-narrowing. New `AddResult` discriminated union sentinel (`ok` / `session_not_found` / `semantic_model_not_inferred` / `name_already_exists`) — same precedent as `PatchResult` / `RevertResult` / `DeleteResult`. **Why 409 Conflict for `name_already_exists`, not 404** — HTTP 409 = "request conflicts with current state"; the W61-revert / W61-delete endpoints use 404 because the resource is absent, but for collisions the semantic is conflict-not-absence, and a 409 envelope with `{ sessionId, kind, name }` echo lets the client render a typed "this name is already taken" message rather than the generic "something went wrong" copy. **Why cross-kind name collisions ARE allowed** — metric "x" + dimension "x" are different resources addressed by `{kind, name}`; the uniqueness check is scoped to the kind's own collection (a dedicated test pins this). **Why we skip the source-bumper** — survivors are byte-identical (no edits to existing entries); the new entry has no prior version to content-diff against; schema's `.default("user")` populates new entries by default; client-sent `"domain"` / `"auto"` preserved verbatim for pack-import flows. Wave-specific 500 code `admin_semantic_model_add_failed` per the per-endpoint-500-code convention. Handler (~150 LOC) reads admin-gate (403) → sessionId trim (400) → `:kind` parse via existing `deleteEntryKindSchema` (400 with `allowed` array) → kind-appropriate body `safeParse` (400 with `kind` echo + issues array formatted like W61-save) → `withSessionWriteLock` (fetches doc, checks not-found 404s, runs uniqueness via `.some()`, appends via spread, bumps version, stamps updatedAt/updatedBy, audit-writes prior model, persists via `_updater`) → outer handler maps `name_already_exists` to 409 and `ok` to the W61-save envelope. 18 behavioural tests in [`adminSemanticModelAddW61AddServer.test.ts`](../server/tests/adminSemanticModelAddW61AddServer.test.ts) (553 LOC, all pass in ~410 ms standalone): 5 admin-gate / param validation (403 / 400 sessionId / 400 invalid kind with allowed-values envelope / 400 missing required field / 400 bad name-format), 2 404 paths (session / model), 4 409 paths (3 per-kind collisions + **cross-kind allowed as a load-bearing test**), 3 success paths per kind (each returns the W61-save envelope with version bump + surviving entries' `source` preserved), 2 audit-log integration (single-add captures pre-add snapshot; consecutive adds grow buffer newest-first with monotonic priorVersion), 1 source-preservation test (client-sent `"domain"` preserved verbatim, not overridden to `"user"`), 1 500 path with the wave-specific error code (`console.error` captured). Test file appended to [`server/package.json`](../server/package.json)'s explicit list per invariant #4 (adjacent to `adminSemanticModelDeleteW61DeleteServer` so the additive/destructive entry-level pair stays adjacent). [`server/routes/admin.ts`](../server/routes/admin.ts) registers `router.post("/admin/semantic-models/:sessionId/entries/:kind", addSemanticModelEntry)` last in the file. Server tests: 4711 / 4715 pass (was 4693 / 4697; +18 new tests landed cleanly; same 4 pre-existing `wd2WiringEcharts` / `wd2DimEchartsTreemap` failures unchanged). Server typecheck = 98 (baseline preserved). Client untouched. **Out of scope** (deferred): client UI + `addSemanticModelEntry` API helper (W61-add-client); bulk add via paste-JSON or CSV-import (different workflow); cross-session model reuse (POST .../import?fromSessionId=… — separate design space); pre-write reference-validation against datasetSchema (W58 compiler is the authoritative orphan-reference surface); optimistic UI in the client wave; in-browser verification.
- **W61-delete-client** (2026-05-21) · Per-row Delete button + AlertDialog confirmation with downstream-references count on the admin semantic-model detail page. Closes the W61 destructive-op loop end-to-end on the client — admins can issue a delete from the same page where they read the audit history and revert prior versions. New client API helpers `fetchSemanticModelReferences` + `deleteSemanticModelEntry` in [`client/src/lib/api/admin.ts`](../client/src/lib/api/admin.ts) (the delete helper returns `PatchSemanticModelResponse` because the server envelope is byte-identical to W61-save / W61-audit-revert per the envelope-reuse convention). New pure-helper module [`semanticModelDeleteConfirmation.ts`](../client/src/pages/Admin/lib/semanticModelDeleteConfirmation.ts) with `buildDeleteHeadline` / `buildDeleteGenericConfirmation` / `buildDeleteReferencesWarning` (returns `null` for `chartCount === 0`, singular / plural correctness at the 1 / N boundaries, subhead suppression when `totalOccurrences === chartCount`) + the `DELETE_AUDIT_LOG_REASSURANCE` constant + 17 behavioural tests. New controlled `<AlertDialog>` sibling component [`DeleteEntryConfirmation.tsx`](../client/src/pages/Admin/components/DeleteEntryConfirmation.tsx) owns its own references fetch via mount-scoped `useEffect` (stale-fetch guard via `cancelled` closure + `res.entry !== pendingName` check); parent owns the delete mutation; `e.preventDefault()` on the destructive `<AlertDialogAction>` keeps the modal open with `deleting=true` until the parent resolves so the success / failure outcome surfaces inline. Host gains a trailing Actions column on all three tables, a shared `RowDeleteButton` presentational helper (`Trash2` lucide icon + ghost-variant `Button` + destructive accent), three new state slots (`pendingDelete` / `deletingEntry` / `deleteError`), and a `handleDelete` async handler that mirrors `handleRevert` — early-returns when `deletingEntry !== null`, calls `deleteSemanticModelEntry`, runs `setData({ ...data, lastUpdatedAt: res.lastUpdatedAt, model: res.model })` on success (byte-identical to PATCH / revert success path so the audit-history-tab's `lastUpdatedAt`-keyed effect re-fetches the buffer automatically). Host file 1,131 → 1,316 LOC (under the 1,500 sub-component-extract threshold). Server typecheck 98 (preserved), client typecheck 53 (preserved), server tests 4693/4697 pass (+17, 4 pre-existing failures unchanged). In-browser verification deferred to a future browser-equipped session.
For full prose entries: read `docs/WAVES.md`. For older entries: `docs/archive/`.
