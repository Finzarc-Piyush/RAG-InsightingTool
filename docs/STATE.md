# Project state — Marico RAG Insighting Tool

> Auto-updated by `/wave-commit`. Read this **first** in every new chat (or run `/orient`).
> Last sync: 2026-05-18 (Wave WI2-cache — per-tile insight regen cache).

## HEAD

- **Latest wave:** Wave WI2-cache · per-tile insight regen cache (pure helpers) (2026-05-18)
- **Branch:** `claude/wide-format-classifier`
- **Last commit:** `40602120` — "Wave WI2-cache · per-tile insight regen cache (pure helpers)" (2026-05-18)
- **Working tree:** doc updates staged for paired WI2-cache commit.

## Live feature streams

- **Wire-up · WW1→WV8 (deterministic-floor series CLOSED)** · planner + narrator + verifier-side enforcement of WQ1's deterministic floor is now LIVE, EVIDENCE-CARRYING for all four high-volume statistical tools, AND surfaces claimed-vs-actual tier counts to the workbench. WW1 wired planner (TOOL_ROUTER_HINT + EXTERNAL_CLAIM_MARKERS + PLANNER_CONFIDENCE_DIRECTIVE). WW2 wired narrator (FINDING_CONFIDENCE block + system-prompt tier-pinning directive). WV1 shipped the verifier-side overclaim detector ([`verifierConfidenceCheck.ts`](../server/lib/agents/runtime/verifierConfidenceCheck.ts)). WV2 shipped the canonical `FindingEvidence → detail` formatter ([`formatFindingEvidence.ts`](../server/lib/agents/runtime/formatFindingEvidence.ts)). WV3 wired the detector into [`verifier.ts`](../server/lib/agents/runtime/verifier.ts) as a pre-LLM short-circuit. WV4–WV7 migrated all four high-volume statistical tools. WV8 closes the series with workbench surfacing: the `critic_verdict` SSE payload now carries `confidence_overclaim: { claimed, actual }` when the WV3 short-circuit fires, and [`agentWorkbench.util.ts`](../server/services/chat/agentWorkbench.util.ts) renders `Narrator confidence: claimed Xh/Ym/Zl; blackboard supports Xh/Ym/Zl` alongside the verdict line. Remaining dormancy-debt candidates: effect-size as fifth `FindingEvidence` field; per-correlation / per-group tier carriage via structured `Finding.evidence`; lifting single-flow rewrite suppression behind `DEEP_INVESTIGATION_ENABLED`; client-side rich renderer for the tier-counts (currently persisted as workbench `code: string`).
- **Workstream 9 — quality 2.0** · WQ1 + WQ2 helpers SHIPPED + WIRED into planner (WW1) + narrator (WW2) + verifier (WV1+WV3) + workbench (WV8); WQ7 ships significance-by-default on `run_two_segment_compare` + `run_breakdown_ranking`; WQ3 ships citation hover-cards on backtick-wrapped pack ids in narrator prose ([`MarkdownRenderer`](../client/src/components/ui/markdown-renderer.tsx) + new [`CitationHoverCard`](../client/src/components/CitationHoverCard.tsx) + pure helper [`citationTokens.ts`](../client/src/lib/citationTokens.ts) mirroring W22 server regex). ALL 7 Workstream 9 items shipped (WQ1–WQ7); the deterministic-floor series + citation UX surfaces are complete end-to-end. Stream now in maintenance mode; effect-size as fifth FindingEvidence field is the next backlog item if needed.
- **Workstream 5 — tool library expansion** · WT6 selectTool wired by WW1 — TOOL_ROUTER_HINT block surfaces in the planner user prompt with intent + ranked tools per turn. Workstream now spans 6 of 6 routing/question-shape pieces (WT8 hierarchical-drill, WT2 cohort, WT3 RFM, WT7 elasticity, WT4 market-basket, WT6 router). Remaining tools require Python (WT1 causal / WT5 what-if / WT9 MTA Markov). Next: WT5 — `run_what_if` (Python scipy Monte Carlo).
- **Workstream 7 — insight engine 2.0** · WI1 schema foundation + WI2-cache shipped. WI1: `chart.insight: InsightSpec` with `default + generator + confidenceTier + citations + regeneratedAt` (coexists with legacy `keyInsight`). WI2-cache: new [`client/src/pages/Dashboard/lib/insightRegenCache.ts`](../client/src/pages/Dashboard/lib/insightRegenCache.ts) — `hashGlobalFilters(filters)` byte-stable hash, `buildCacheKey(tileId, filterHash)`, `createInsightRegenCache(opts?)` factory with LRU eviction (default 64 entries) + TTL expiry (default 5 min, matches Anthropic prompt-cache TTL). 21 tests across 6 suites. Next: WI2-server — `POST /api/insight/regen` endpoint that the hook will call on cache miss. Then WI2-wire — `useInsightRegen` hook + "✦ Re-explain this view" button in `TileInsightFooter.tsx`. WI3–WI6 deliver citation hover-cards in insights → explain-this-slice → per-tile recommendations → insight history.
- **Workstream 1 — semantic & metrics layer** · W56 types + W57 inference + W58 compiler + W59a manifest helper + W59b planner-prompt wiring all shipped. The semantic-layer chain is now LIVE end-to-end as informational grounding: every planner LLM call sees a byte-stable manifest of metrics + dimensions + hierarchies as a first-class user-prompt block (between the WW1 hints block and the P-A1 RAG block). The planner can route raw-column queries against canonical metric / dimension names without guessing synonyms. W59b adds `buildSemanticCatalogPromptBlock` to [`server/lib/semantic/prompt.ts`](../server/lib/semantic/prompt.ts) + a 3-line edit to [`server/lib/agents/runtime/planner.ts`](../server/lib/agents/runtime/planner.ts) (one import + one helper call + one inline template change; system prompt deliberately untouched so prompt-cache breakpoints stay stable). 9 tests across 5 suites including planner.ts source-inspection wiring. Next: W60 — register `execute_metric_query` tool dispatching through `compileMetricQuery` so the planner can call into the catalog instead of just reading it; bundle with a system-prompt SEMANTIC_CATALOG directive. W61–W64 deliver admin UI → drift gate → result cache.
- **Workstream 4 — dashboard 2.0** · WD1 + WD2 (pure helper) ship. WD1: `+ Add filter` popover on the dashboard global filter bar (categorical + numeric + date pickers). [DashboardGlobalFilterBar.tsx](../client/src/pages/Dashboard/Components/DashboardGlobalFilterBar.tsx) renders even when `global` is empty IFF availableFilters is non-empty. WD2: new module [`client/src/pages/Dashboard/lib/crossFilter.ts`](../client/src/pages/Dashboard/lib/crossFilter.ts) — pure helpers (`applyCrossFilter`, `removeCrossFilter`, `clearCrossFilter`, `isCrossFilterActive`, `listActiveCrossFilters`, `toFilterValue`) plus the browser-guarded `dispatchCrossFilter(CROSS_FILTER_EVENT)` CustomEvent dispatch. Toggle semantics: clicking an active (column, value) pair removes it; new value on categorical column appends; non-categorical selection replaced with categorical. Mutation-free contract on the global map. 29 tests across 10 suites. Next: WD2-wiring — connect the visx + ECharts renderer `onClick` handlers to `dispatchCrossFilter` and subscribe `DashboardView` to the event channel. WD3–WD10 deliver drill-through → dynamic insights → fork-from-dashboard → mobile → linked-sheet filters → saved views → tile comments → scheduled refresh per the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md) Workstream 4 wave map.
- **Workstream 3 — investigation mode** · W74 ships: budget exhaustion observability. New pure detector [`investigationBudget.ts`](../server/lib/agents/runtime/investigationBudget.ts) names which cap (llm_calls / wall_time / max_nodes) tripped when the BFS loop terminates short of convergence. [`investigationOrchestrator.ts`](../server/lib/agents/runtime/investigationOrchestrator.ts) emits a `flow_decision` SSE row (`layer: "investigation-budget"`, `chosen: "halt"`) + agentLog `budget_exhausted` when the detector fires. No behaviour change — pure observability. Next: W75 — workbench UI for parallel sub-investigations (per-sub-question lanes in [`ThinkingPanel.tsx`](../client/src/pages/Home/Components/ThinkingPanel.tsx)). Or W76 — Hypothesis Tree visualizer.
- **Phase D — coordinator multi-part detection** · D1+D2 shipped; D3 (actual parallel sub-investigation) deferred behind `DEEP_INVESTIGATION_ENABLED`. Detector lives at [server/lib/agents/runtime/detectMultiPartQuestion.ts](../server/lib/agents/runtime/detectMultiPartQuestion.ts), observability fires in [chatStream.service.ts](../server/services/chat/chatStream.service.ts) after mode classification.
- **Phase F — predictive / inferential tooling** · F1 forecast, F2 anomaly detection, F3 significance tests all shipped, gated by `FORECAST_ENABLED` / `ANOMALY_DETECTION_ENABLED` / `SIGNIFICANCE_TESTS_ENABLED`. Pure-Node implementations under [server/lib/](../server/lib/).
- **W-series — query plan expressiveness** · W1 (window aggregations) → W2 (rolling-window detector) → W3 (composite-ranking expressions in `breakdown_ranking`) all shipped. P1 (pivot reads agent result rows) closes the load-bearing UX gap for `computedAggregations`.
- **E-series — multi-tab session sync** · E1 BroadcastChannel foundation → E2 active-filter sync → E3 messages/lifted-state sync → E4 stale-filter retry helper all shipped.
- **C-series — client safety** · C1 BAI exact-timestamp matching, C2 isMountedRef SSE guards, C3 optimistic-update rollback. Shipped.
- **B-series — context plumbing across sub-agents** · B1 (quick-lookup planner) through B7 (narrator user-block always surfaces userIntent). Shipped.
- **A-series — concurrency hardening** · A1 (filter-aware question cache) through A7 (domainContext cache atomicity via generation counter). Shipped — `withSessionWriteLock` is now the unified per-session Cosmos write mutex.
- **Routing system (this migration)** · Slim CLAUDE.md, `docs/STATE.md`, `docs/WAVES.md`, three slash skills (`/orient`, `/wave-commit`, `/load`), Stop hook for doc-freshness nudges. Setup wave in flight at time of writing.

## Known WIP / broken

- Wide-format classifier (W1–W4 of the wide-format plan) is mid-flight on this branch; remaining waves W14–W29 add the modal UX + ingest wiring + decision endpoint. Independent of W56's semantic layer but should land before W57 if the semantic compiler is to consume `_metric`/`_period` columns from melted datasets.
- [docs/architecture/schemas.md](architecture/schemas.md) still describes the pre-W5 dual-mirror schema model — out of date but non-blocking. Refresh in a future doc-hygiene wave.

## Next wave (if planned)

- **W60** (proposed) — register `execute_metric_query` tool dispatcher in [`registerTools.ts`](../server/lib/agents/runtime/tools/registerTools.ts). Tool wraps W58's `compileMetricQuery({metric, breakdownBy, filters, window})` and dispatches the compiled `QueryPlanBody` through the existing `execute_query_plan` executor. Args schema mirrors `CompileMetricQueryInput`. Add a SEMANTIC_CATALOG `Rules:` line to the planner system prompt (single edit — bundle with tool reg so cache invalidates once, not twice).
- **WI2-server** (alternate) — `POST /api/insight/regen` endpoint taking `{ spec, filteredData, domainContext, tileId }` and returning `{ text, citations, regeneratedAt, confidenceTier }`. Goes through MINI-tier `callLlm` channel; reuses `promptCacheEligibility` so the system prompt caches across tiles in the same session. Pairs with WI2-cache (already shipped) — the client hook calls this endpoint on cache miss.
- **WD2-wiring** (alternate) — connect chart renderers + DashboardView to the WD2 helper. One-line-per-renderer change in the visx + ECharts mark `onClick` handlers (`dispatchCrossFilter({ column, value, sourceTileId })`) + a `useEffect` in `DashboardView` that subscribes to `CROSS_FILTER_EVENT` and runs `setGlobalFilters((g) => applyCrossFilter(g, event.detail))`. Pure helper is in place; this is the wiring.

## Last 5 waves (one line each — newest first)

- **WI2-cache** (2026-05-18) · per-tile insight regen cache (pure helpers). New [`client/src/pages/Dashboard/lib/insightRegenCache.ts`](../client/src/pages/Dashboard/lib/insightRegenCache.ts) — `hashGlobalFilters(filters)` byte-stable serialisation (sorts column keys + categorical values; date / numeric have fixed-key serialisation), `buildCacheKey(tileId, filterHash)`, `createInsightRegenCache(opts?)` factory returning a closure-backed LRU + TTL cache. Defaults: 64 entries, 5-min TTL (matches Anthropic prompt-cache TTL). `get` refreshes LRU recency; `set` evicts the oldest when full; stale entries lazy-delete on probe + `evictExpired()` does an active sweep. Stores `InsightRegenEntry = { text, citations?, regeneratedAt?, confidenceTier? }` so the future WI2-server endpoint + WI2-wire hook agree on the shape up front. 21 tests across 6 suites. Foundation for the WI2-server + WI2-wire follow-ons; pure data-plumbing first.
- **W59b** (2026-05-18) · wire `formatMetricCatalog` into the planner user prompt. Activates the dormant W56+W57+W58 chain: `ChatDocument.semanticModel` is now read on every planner LLM call and inlined as a byte-stable markdown manifest between the WW1 hints block and the P-A1 RAG block. 3-line `planner.ts` edit + new `buildSemanticCatalogPromptBlock` wrapper in [`server/lib/semantic/prompt.ts`](../server/lib/semantic/prompt.ts) (null/undefined → `""`; populated → manifest + `\n\n`). System prompt deliberately untouched (cache breakpoint preservation). 9 tests across 5 suites including source-inspection on the import + call + inline-template position.
- **WD2** (2026-05-18) · cross-filter brushing — pure helper module. New [`client/src/pages/Dashboard/lib/crossFilter.ts`](../client/src/pages/Dashboard/lib/crossFilter.ts) — pure data-plumbing for chart-mark-click → `globalFilters` toggle. Exports: `applyCrossFilter`, `removeCrossFilter`, `clearCrossFilter`, `isCrossFilterActive`, `listActiveCrossFilters`, `toFilterValue`, `dispatchCrossFilter`, `CROSS_FILTER_EVENT`. Toggle semantics: active (column, value) pair → remove; new value on categorical column → append; non-categorical (date / numeric) selection → replaced with categorical (chart brush is discrete-value). Mutation-free on the input map. Null / undefined coerce to `"null"` (matches upstream `(null)` key normalisation). 29 tests across 10 suites. Renderer + DashboardView wiring is the follow-on; this wave isolates the data plumbing.
- **W59a** (2026-05-18) · `formatMetricCatalog` pure helper (byte-stable manifest). New [server/lib/semantic/prompt.ts](../server/lib/semantic/prompt.ts) renders a `SemanticModel` as a markdown manifest for the planner prompt (W59b), admin UI (W61), and narrator citations. Sort + filter contract guarantees byte-stable output across identical models (prompt-cache contract). Exposes `formatMetricCatalog` + per-entry `formatMetricLines`/`formatDimensionLines`/`formatHierarchyLines`. Hidden entries dropped by default; empty-everything collapses to a single parenthetical marker line. W59b becomes a thin call site. 19 tests across 7 suites (empty model, metrics, dimensions, hierarchies, byte-stability, section-heading toggle, direct helpers).
- **WQ3** (2026-05-18) · citation hover-cards in narrator prose. New pure helper `client/src/lib/citationTokens.ts` mirrors W22's `CITATION_TOKEN_RE` byte-for-byte (`` /`([a-z][a-z0-9-]{4,})`/g `` + hyphen rule) and exposes `extractCitations` / `listCitedPackIds` / `formatCitationLabel`. New `client/src/components/CitationHoverCard.tsx` wraps Radix HoverCard around a numbered superscript `[N]` pill. `MarkdownRenderer` builds per-render `citationIndex` once + threads through bold/italic/plain spans. Metadata-only hover content (label + canonical id + admin breadcrumb); snippet body is deferred. 20 tests covering regex parity, hyphen-rule, segment shape, dedupe, humanisation.

For full prose entries: read `docs/WAVES.md`. For older entries: `docs/archive/`.
