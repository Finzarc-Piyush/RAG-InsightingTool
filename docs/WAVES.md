# Wave history — Marico RAG Insighting Tool

> Authoritative wave-by-wave changelog. Newest at top. Older waves rotate into `docs/archive/`
> via `/wave-commit` when this file crosses ~300 KB or ~50 waves.
>
> If you need historical context: read the relevant entries here, or ask Claude
> "what changed in Wave W<n>" — a subagent can fetch from `docs/archive/` if needed.
>
> **Maintenance contract:** every code change ships a paired entry here via `/wave-commit`.
> Do not edit CLAUDE.md's changelog inline — that file is the routing index, not the log.

---

- **2026-05-16** — **Wave WT3 · `run_rfm_segmentation` tool.** Closes the RFM segmentation question-shape gap from the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md) Workstream 5 wave map. A Marico marketing analyst asking "which customer segments should we focus on?" previously got an ad-hoc breakdown driven by the planner's intuition — accuracy varied turn-to-turn. WT3 ships a deterministic RFM scorer + canonical segment classifier so the answer is reproducible.

  **What landed.**
    - New [server/lib/agents/runtime/tools/rfmSegmentationTool.ts](server/lib/agents/runtime/tools/rfmSegmentationTool.ts) (~310 LOC). Pure-Node. Exported helpers:
      - `runRfmSegmentation(rows, args)` — pure function. Direct callable for tests + skills.
      - `classifyRfmSegment(r, f, m, buckets)` — pure segment-rule classifier. Exposed for unit testing the taxonomy independently.
      - `registerRfmSegmentationTool(registry)` — registers the tool as `run_rfm_segmentation`.
    - Args: `entityColumn`, `periodColumn` (ISO-like values — same rationale as WT2), `monetaryColumn`, `buckets` ∈ [3,7] default `5`, `frequencyMode` ∈ {`rows`, `distinct_periods`} default `distinct_periods`, `maxEntities` ∈ [10, 2000] default `100`, optional `dimensionFilters[]`.
    - Output: `table = { columns: [entityColumn, "last_period", "frequency", "monetary", "r_score", "f_score", "m_score", "rfm_score", "segment"], rows: [...capped at maxEntities, sorted by total score desc + monetary desc] }`. `rfm_score` is the concatenated string (e.g. `"555"`).
    - `numericPayload` carries `segmentBreakdown: Array<{segment, count}>` for the FULL entity population (not just the capped table) so dashboards can show segment-level totals without re-running.

  **Why this design.**
    - **Quintile rank scoring with tie handling.** Each entity's score is `ceil(rank/N * buckets)` where `rank = count of entities with value ≤ this entity's value`. Two entities with the same monetary spend get the same M score — the test suite pins this with a same-spend-everyone fixture.
    - **Recency uses period-index, not "days since last".** The tool doesn't parse dates; it sorts lexicographic period values (matching WT2's choice) and uses the index of each entity's last observation as the recency value. Larger index = more recent = higher R score.
    - **`frequencyMode` default is `distinct_periods`.** A single shopper buying three products on one day shouldn't show as frequency 3 — that overweights basket-size analysts. Counting distinct periods matches the analyst intuition of "how many times did this customer come back."
    - **Canonical segment taxonomy with priority order.** First-match-wins on the rule chain: Lost → Cant Lose Them → At Risk → Hibernating → New Customers → Champions → Loyal Customers → Potential Loyalist → About to Sleep → Regular. "Cant Lose Them" is intentionally tight (F=top AND M=top with low R) — a (2,4,4) entity is the looser "At Risk", not Cant-Lose-Them. The test suite pins each branch independently.
    - **`maxEntities` cap but full-population segment counts.** A 50k-customer dataset would produce a 50k-row table — too large for SSE / chart rendering. Cap the table at 100 by default; surface segment counts in `numericPayload` so the analyst still sees "Champions: 142, Loyal: 1820, At Risk: 980, ..." over the whole base.
    - **Sort by total score desc, then monetary desc.** Tie-break by spend matches the analyst question "show me my best customers" — among RFM=555 customers, the bigger spenders come first.

  **Tests.** [tests/rfmSegmentationToolWT3.test.ts](server/tests/rfmSegmentationToolWT3.test.ts) — 23 cases across 7 suites:
    - **Quintile scoring** (3): perfect-5-tier fixture (c1→111, c5→555), table sorted by total score desc, ties get identical scores.
    - **Frequency modes** (2): `distinct_periods` collapses duplicate-period rows; `rows` counts every row.
    - **Recency derivation** (1): last_period = entity's max period; ranks correctly.
    - **Segment classifier** (8): Champions / Lost / Cant Lose Them / New Customers / At Risk / Hibernating / Champions-over-Loyal priority / Regular fallback.
    - **Output shape** (3): expected columns, segmentBreakdown numericPayload, maxEntities cap with full segment counts.
    - **Filters + failures** (3): dimensionFilter prefilter, empty dataset, all-filtered-out.
    - **Schema** (3): defaults applied, buckets out-of-range rejected, `.strict()` rejects unknown keys.
    - 23/23 passing. Appended to [server/package.json](server/package.json) per invariant #4.

  **Verified.** `npx tsc --noEmit` reports 98 errors, identical to WT2 baseline — zero new from WT3. Commit `b42e5a2b`.

- **2026-05-16** — **Wave WT2 · `run_cohort_analysis` tool.** Closes the cohort/retention question-shape gap from the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md) Workstream 5 wave map. A Marico marketing analyst asking "of stores that started carrying Parachute in Q1 2024, how many are still ordering it 6 months later?" previously had no first-class tool — the answer required hand-rolled SQL plus a pivot. WT2 ships the cohort matrix as a first-class tool that the planner can pick directly.

  **What landed.**
    - New [server/lib/agents/runtime/tools/cohortAnalysisTool.ts](server/lib/agents/runtime/tools/cohortAnalysisTool.ts) (~290 LOC). Pure-Node — no Python service round-trip. Exported helpers:
      - `runCohortAnalysis(rows, args)` — pure function. Direct callable for tests + skills.
      - `registerCohortAnalysisTool(registry)` — registers the tool as `run_cohort_analysis` with strict zod-args.
    - Args: `entityColumn` (the unique entity to track), `periodColumn` (time dimension, lexicographic sort = calendar order — use ISO-like values), optional `cohortColumn` (explicit cohort label; if omitted, cohort = each entity's earliest observed period — classic acquisition cohort), optional `metricColumn` (required for sum/mean), `aggregation` ∈ {count_distinct, sum, mean} default `count_distinct`, `maxPeriods` ∈ [2, 24] default `12`, `retentionMode` (normalises every cell by the cohort's period_offset_0 value), optional `dimensionFilters[]` (categorical `in` / `not_in` prefilter).
    - Output: `table = { columns: ["cohort", "cohort_size", "period_offset_0", ..., "period_offset_N"], rows: [...per cohort] }`. `cohort_size` = the count of distinct entities in period_offset_0, regardless of aggregation, so renderers can label "Cohort 2024-01 (n=120)" without re-counting.
    - Registered in [registerTools.ts](server/lib/agents/runtime/tools/registerTools.ts) right after `registerHierarchicalDrillTool`. Duplicate-name guard test still passes.

  **Why this design.**
    - **Pure-Node, not Python.** Bucketing + aggregation is sub-millisecond in JS for datasets up to ~1M rows; a Python round-trip would add ~80 ms of HTTP latency for zero accuracy gain. Same rationale as WT8.
    - **Lexicographic period sort.** Marico's ingest layer normalises temporal columns to ISO-like values (`"2024-01"`, `"2024-W03"`, `"2024Q1"`) which already sort correctly. Parsing arbitrary date formats inside the tool would re-implement the upstream temporal-grain pipeline — better to require the caller to feed ISO-like values.
    - **`cohort_size` is always count_distinct.** The "cohort size" label is universally read as "how many entities started in this cohort," even when the aggregation is `sum(revenue)`. Mixing aggregation semantics with cohort size would confuse renderers.
    - **`retentionMode` divides by `cellValue(baseCell)`, not by `cohort_size`.** Under `aggregation: "sum"`, the period_offset_0 base IS the sum of revenue in the birth period, so retention = revenue_retained_fraction. Dividing by entity count would mix units.
    - **Zero-base guard.** When the period_offset_0 base value is 0 (e.g., metricColumn had nulls in the birth period), retentionMode emits 0 instead of NaN/Inf. Pinned by the test suite.
    - **Offset bounds.** Periods past `maxPeriods - 1` are dropped, not pushed into a final "12+" bucket. Keeps the output schema stable (column count = `maxPeriods + 2`) and matches how Excel pivot cohort tables are usually built.
    - **Cohort label can be non-period.** When `cohortColumn` is given, the cohort label may be a non-period string. The current impl computes offset by looking up the cohort string in the period index — so explicit-cohort mode works cleanly when the cohort labels are themselves period values (the common case: `cohortColumn: "signup_month"`). A future wave can add `cohortColumn` → "first observed period per cohort" computation; for now the inline comment on the bailout makes the limitation explicit.

  **Tests.** [tests/cohortAnalysisToolWT2.test.ts](server/tests/cohortAnalysisToolWT2.test.ts) — 20 cases across 7 suites:
    - **Acquisition cohort** (2): cohort = earliest period, cohorts sorted lexicographically.
    - **Explicit cohortColumn** (1): cohortColumn drives the cohort assignment with sum aggregation.
    - **Aggregation modes** (3): `sum` per cell, `mean` per cell, `count_distinct` dedupes same-entity rows.
    - **Retention mode** (2): normalises by period_offset_0, zero-base safety.
    - **Period offset bounds** (2): `maxPeriods` caps columns, rows past the cap are dropped.
    - **Dimension filters** (2): `in` prefilter, all-rows-filtered → `ok:false`.
    - **Failure modes + schema** (5): empty dataset, no period values, schema rejects sum/mean without metricColumn, schema accepts valid defaults, maxPeriods > 24 rejected.
    - **numericPayload metadata** (1): downstream-consumer-readable JSON.
    - 20/20 passing. Appended to [server/package.json](server/package.json) per invariant #4.

  **Verified.** `npx tsc --noEmit` reports 98 errors, identical to WT8 baseline — zero new from WT2. Full server suite: 3065 pass, 0 fail. Commit `67700f00`.

- **2026-05-16** — **Wave WT8 · `run_hierarchical_drill` tool.** Closes the "high-cardinality breakdown is unreadable" gap explicitly flagged in the agent-runtime audits and the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md) Workstream 5 wave map. A Marico marketing analyst asking "Sales by SKU" against a 200-SKU dataset previously got a matrix chart with 200 unreadable tick labels; WT8 rolls everything past the top-N into a single "Other" bucket so the chart speaks instead of yelling.

  **What landed.**
    - New [server/lib/agents/runtime/tools/hierarchicalDrillTool.ts](server/lib/agents/runtime/tools/hierarchicalDrillTool.ts) (~270 LOC). Pure-Node — no Python service round-trip. Exported helpers:
      - `runHierarchicalDrill(rows, args)` — pure function. Direct callable for tests + skills.
      - `registerHierarchicalDrillTool(registry)` — registers the tool as `run_hierarchical_drill` with strict zod-args.
    - Args: `dimension` (the high-card column to roll up), `metricColumn` (numeric column to aggregate), `aggregation` ∈ {sum, mean, count, min, max} default `sum`, `topN` ∈ [2, 50] default `10`, `direction` ∈ {asc, desc} default `desc`, `otherLabel` default `"Other"`, optional `dimensionFilters[]` (categorical `in` / `not_in` only — row-level numeric filtering belongs to `execute_query_plan`).
    - Output: `table = { columns: [dimension, metricColumn, _rank, _share], rows: [...top-N..., Other?] }`. `_rank` starts at 1 for kept buckets; the Other row carries `_rank: -1` so renderers can dim / mark it. `_share` is a 0..1 fraction of the grand total — chart labels read it directly without a second pass.
    - Registered in [registerTools.ts](server/lib/agents/runtime/tools/registerTools.ts) right after `registerForecastTool`. The W-F2 toolRegistry duplicate-name guard test still passes (verified).

  **Why this design.**
    - **Pure-Node, not Python.** Bucketing + aggregation is trivial in JS; a Python round-trip would add 60-100 ms of HTTP latency for zero accuracy gain. The MMM optimiser (W46-W55) lives in Python because scipy SLSQP requires it; this tool needs only sort + slice + sum.
    - **mean-of-the-Other-bucket is row-level, not mean-of-means.** When rolled-up buckets have different row counts (e.g., South has 100 rows averaging 30, East has 1 row averaging 100), mean-of-means gives the wrong answer (`(30 + 100) / 2 = 65`) while row-level mean is `(100×30 + 1×100) / 101 = ~30.7`. The test suite pins this with a discriminating fixture — getting this wrong was a real risk because every other aggregation (sum/count/min/max) is order-independent.
    - **`_rank: -1` for Other, not `null` or `0`.** Negative sentinel survives JSON round-trips, sorts cleanly to the bottom, and is unmistakable downstream. `null` would collide with renderers that already treat null as "missing."
    - **`_share` is computed across ALL buckets including Other.** Percentages always sum to 100 even when Other is invisible (rollup.length === 0).
    - **Null + empty dimension cells skipped, not bucketed.** A dataset where 30% of rows have a null Region shouldn't surface a "(null)" bar that dominates the chart. The tool's job is "make this readable" — silently skipping nulls + empties matches user intent.

  **Tests.** [tests/hierarchicalDrillToolWT8.test.ts](server/tests/hierarchicalDrillToolWT8.test.ts) — 19 cases across 5 suites:
    - **Bucketing + rollup** (3): top-N + Other, no-Other-when-buckets-fit-topN, same-bucket aggregation.
    - **Aggregation operations** (4): `mean` bucket-level, `mean`-of-Other uses row-level not mean-of-means (the discriminating fixture), `count` counts rows ignoring non-numeric metric cells, `min` returns smallest per bucket.
    - **Direction + share-of-total** (3): `asc` returns smallest first, _share fractions sum to ≈ 1.0, Other bucket flagged with `_rank: -1`.
    - **Filters + edge cases** (5): `in` filter, `not_in` filter, null/empty dimension cells skipped, empty input → `ok: false`, all-rows-filtered-out → `ok: false`.
    - **Args schema** (3): `topN` 2..50 bounds, defaults applied, `.strict()` rejects unknown args.
    - 19/19 passing; 26/26 across the broader tool-registry test suite (including the duplicate-name guard from Wave F2). Appended to [server/package.json](server/package.json) per invariant #4.

  **Verified.** `npx tsc --noEmit` reports 98 errors, identical to WI1 baseline — zero new from WT8. Commit `0fdc072c`.

  **Out of scope.** Explicit drill-*down* action (clicking "Other" expands into its own top-N) — the tool today is single-level rollup; drill-down is a follow-up wave that needs the dashboard tile chrome to know how to re-fire the tool with a tighter scope. Auto-trigger from the planner when `groupBy` cardinality > 20 — follow-up planner-prompt rewrite; the agent today must explicitly call `run_hierarchical_drill`. Skill composite that pairs this with a "follow-up: drill into Other" suggestion — follow-up; the agent skill registry (`server/lib/agents/runtime/skills/`) has the right home for it.

- **2026-05-16** — **Wave WI1 · InsightSpec schema — richer dynamic-aware insight.** Pre-WI1 the per-chart insight was a single static string ([`chart.keyInsight`](server/shared/schema.ts)) baked at chart creation; on filter change the user saw stale prose, and there was no place to attach confidence tier, domain-pack citations, or regen metadata. WI1 adds `chart.insight: InsightSpec` as a structured back-compat sibling. The legacy `keyInsight` stays — renderers prefer `insight.default` when present, fall back to `keyInsight` otherwise. This is the foundation wave for WI2 (dynamic regeneration on filter change), WI3 (citation hover-cards), WI4 (explain-this-slice), WI5 (per-tile "Try this" CTAs), WI6 (insight history).

  **What landed.** New [`insightSpecSchema`](server/shared/schema.ts) with five fields:
    - `default: string` (≤500 chars, matches the `businessCommentary` cap so renderers can share styling envelopes) — the baked-at-creation insight.
    - `generator?: { kind: "llm" | "deterministic"; args?: Record<string, unknown> }` — describes how to RE-derive the insight when state changes. Today the field is opaque + back-compat; WI2 will wire the `llm` kind to a MINI-tier LLM call.
    - `confidenceTier?: "low" | "medium" | "high"` — drives prose length + hedging in the renderer (W9 quality follow-up).
    - `citations?: string[]` (≤8, each non-empty) — domain pack ids referenced. The existing W22 `checkDomainLensCitations` infra validates these server-side; WI3 surfaces them as click-to-expand hover-cards.
    - `regeneratedAt?: string` — ISO timestamp of last regen, used by the client cache.
    - Plus a pure helper `legacyKeyInsightToInsightSpec(keyInsight)` that lifts a legacy string into the new shape (trim + 500-char truncate; returns undefined for empty/whitespace/undefined). Used by future migration paths.

  `chartSpecSchema` extended with optional `insight: insightSpecSchema.optional()` alongside the existing optional `keyInsight: z.string().optional()`. Auto-mirrored to client via the W5 re-export.

  **Why this design.** Considered (a) deprecating `keyInsight` and migrating all consumers in one wave — rejected because `keyInsight` is read in ChartTileBody, TileInsightFooter, ChartRenderer, ChartModal, the v2 PremiumChart, the answer envelope, the past-analyses doc, the dashboard export pipeline, and a dozen other call sites. A coordinated migration is a multi-wave undertaking; back-compat coexistence keeps the schema move tiny. (b) Making `default` a non-trimmed `string()` without a max — rejected because `businessCommentary.max(500)` is the existing nearby ceiling and renderers already style for that envelope; matching it keeps the visual contract stable. (c) Making `citations` strictly typed as `z.array(packIdSchema)` with a custom pack-id regex — over-specified for W56-class work; passthrough strings + W22 validator pattern keeps the schema simple. (d) Putting `generator.args` under a discriminated union per `kind` — over-engineered for today's single `kind`; the passthrough `Record<string, unknown>` lets WI2's LLM args + WI3's deterministic args evolve without schema bumps.

  Passthrough on `generator.args` is intentional: it's a forward-compatibility hook for the multi-shape generators coming in WI2–WI6. Schema validation lives at the generator-call site, not on the spec.

  **Tests.** [tests/insightSpecSchemaWI1.test.ts](server/tests/insightSpecSchemaWI1.test.ts) — 14 cases across 3 suites: schema bounds (8: minimal default-only parse, full round-trip, 500-char cap, generator.kind enum positive + negative, 8-citation cap, empty-citation rejection, passthrough generator.args with nested structures); legacy migration (3: lift plain string, undefined/empty/whitespace return undefined, trim + truncate); chartSpec coexistence (3: keyInsight-only / insight-only / both-populated all parse cleanly). 14/14 passing. 75/75 across all new-wave tests (W56 + W57 + W58 + WI1 + W73). Appended to [server/package.json](server/package.json) per invariant #4.

  **Verified.** `npx tsc --noEmit` reports 98 errors, identical to W58 baseline — zero new from WI1. Commit `7e703493`.

  **Out of scope.** Dynamic regeneration on filter change — **WI2** wires the `generator.kind === "llm"` path to a MINI-tier LLM call (~$0.001 per regen), cached by `(tileId, filterHash)`. Citation hover-card UI — **WI3** mounts a `CitationHoverCard` component that renders the cited pack snippet on click. Explain-this-slice context-menu — **WI4**. Per-tile "Try this" CTAs grounded in actionable findings — **WI5**. Insight history dropdown — **WI6**. UI consumers in [ChartTileBody.tsx](client/src/pages/Dashboard/Components/ChartTileBody.tsx) and [TileInsightFooter.tsx](client/src/pages/Dashboard/Components/TileInsightFooter.tsx) continue to read `chart.keyInsight` unchanged; the new `insight` field is wire-ready for WI2's consumer.

- **2026-05-16** — **Wave W58 · Semantic-layer compiler.** The wave that makes Workstream 1 actually usable. W56 shipped types, W57 auto-populated the catalog at upload, but no consumer existed. W58 ships `compileMetricQuery({ model, metric, breakdownBy?, filters?, sortBy?, limit? }) → CompiledQueryPlan` — a pure deterministic translator from a semantic query into a `QueryPlanBody` (the same shape `execute_query_plan` already eats). W59 will rewrite the planner prompt to emit semantic queries by default; W60 registers `execute_metric_query` that dispatches through the compiler.

  **What landed.** New [server/lib/semantic/compiler.ts](server/lib/semantic/compiler.ts) (~330 LOC including types + helpers). Two compilation paths based on expression shape:
    - **Simple single-aggregation metrics.** Expression is exactly one `<OP>(<col>)` or `COUNT(*)` or `COUNT(DISTINCT col)`. Emits one [`aggregationEntry`](server/lib/queryPlanExecutor.ts) under the metric's own name. Recognised operators: SUM, AVG, MEAN, MIN, MAX, COUNT, MEDIAN — mapped to the executor's `aggOpSchema` enum (`AVG` and `MEAN` both → `mean`; `COUNT(DISTINCT)` → `count_distinct`).
    - **Composite arithmetic metrics.** Expression like `SUM(gross_sales) - SUM(returns)` or `SUM(value_sales) / SUM(units)`. Compiler extracts every `<OP>(<col>)` occurrence via a global regex, dedupes by `(operation, column)` so `SUM(x) + SUM(x)` emits ONE aggregation, builds auto-aliases like `_sum_gross_sales` / `_sum_returns`, and rewrites the expression with the alias substitutions. The rewritten expression goes into one `computedAggregation` aliased to the metric name. Validates the rewritten expression against the same `ALLOWED_EXPRESSION_CHARS_RE` the executor uses — character mismatch returns a clear error rather than crashing the executor downstream.

  Breakdown dimensions and filter dimensions are looked up in the model's `dimensions` index; unknown names return a clear error listing the available options. Filters forward the `match` modifier (`exact` / `case_insensitive` / `contains`). Sort target can be the metric alias OR a breakdown dimension name; both resolve to the right column in the emitted `sort[]` entry.

  **Why this design.** Considered (a) emitting raw SQL strings instead of `QueryPlanBody` — rejected because that bypasses the existing W5 `contains` filter LIKE compilation, the W1 windowAggregations infrastructure, the W17 envelope-completeness gate, and the verifier's `numericPayload` re-check. Building on `QueryPlanBody` means every existing tool / validator / verifier works unchanged. (b) Letting the compiler emit arbitrary SQL inside `computedAggregations` — rejected because `computedAggregationSchema`'s character-class restriction is a defense-in-depth measure (admin-controlled expressions are auth-controlled but the executor still validates). The "NULLIF / CASE not supported yet" wall is intentional surface area for the future executor-schema widening wave.

  Dedupe of repeated aggregations matters more than it looks: ratio metrics like `SUM(value_sales) / SUM(category_value_sales)` and the inferred Marico FMCG model often pair a numerator with the *same* aggregation appearing in the denominator of a different metric. Dedupe keeps the plan minimal so the executor's grouping pass walks the data once per `(op, col)` instead of N times.

  **Tests.** [tests/semanticCompilerW58.test.ts](server/tests/semanticCompilerW58.test.ts) — 18 cases across 5 suites: simple single-aggregation (5: SUM / AVG → mean / COUNT(*) / COUNT(DISTINCT) / all operators MIN/MAX/MEDIAN/MEAN); composite arithmetic (4: subtract, ratio, dedupe, NULLIF rejection); breakdownBy + filters (4: dimension-name → column, `in` filter, `match` modifier forwarding, unknown-dimension error); sort + limit (2: sort by metric alias, sort by breakdown dimension); errors (3: unknown metric, unknown filter dim, unknown sort target). 48/48 tests pass when run with W56 + W57. Appended to [server/package.json](server/package.json)'s explicit `test` list per invariant #4.

  **Verified.** `npx tsc --noEmit` reports 98 errors, identical to the W57 baseline — zero new from W58. Commit `fe84c840`.

  **Out of scope.** Window aggregations (carry-through of `plan.windowAggregations`) — follow-up; the existing W1 schema is ready to layer on. Per-dimension nested aggregations (the `perDimension` shape on `aggregationEntrySchema`) — follow-up. `NULLIF` / `CASE` / commas in expressions — requires widening `computedAggregationSchema`'s allowed-character set in a paired executor wave. Multi-metric queries (asking for SUM(x), AVG(y), MEDIAN(z) together as one envelope) — follow-up; current shape is one metric per call. The compiler itself does NOT execute the plan — `executeQueryPlan` is the next-link consumer (today reached via `execute_query_plan` tool; W60 adds `execute_metric_query` that wraps the compiler + executor in one tool call).

- **2026-05-16** — **Wave WD1 · Dashboard global filter bar is now additive.** Closes the user's explicit "global slicers and dicers everywhere" requirement from the 1000x prompt. Pre-WD1 the DR4 bar at [DashboardGlobalFilterBar.tsx](client/src/pages/Dashboard/Components/DashboardGlobalFilterBar.tsx) was display-only — the header comment carried the explicit TODO `"Adding filters from the bar is a follow-up wave"`; this wave fills it. Users now open a dashboard with **0 filters · + Add filter** one click away from any chart tile's column, instead of being stuck with whatever filter was captured at dashboard creation.

  **What landed.**
    - New [client/src/pages/Dashboard/Components/AddFilterPopover.tsx](client/src/pages/Dashboard/Components/AddFilterPopover.tsx) (~340 LOC). Trigger button rendered inline next to the existing chips. Popover has two views:
      - **Column list** — searchable, scrollable, sorted by cross-tile frequency then alphabetically. Each row carries a type icon (`Tag` / `Hash` / `Calendar`) and label so the user can predict the picker shape before clicking.
      - **Per-column inline editor** — three variants:
        - `CategoricalEditor` renders a checkbox list capped at the first 30 distinct values (with a `+ N more values not shown` hint for high-cardinality columns).
        - `NumericEditor` accepts `min` + `max` number inputs with the observed range as placeholder + footnote.
        - `DateEditor` uses HTML5 native date inputs with observed range as footnote.
      - All three confirm through a single `onConfirm(next: ActiveChartFilters)` callback so the component stays stateless w.r.t. dashboard persistence — the parent (`DashboardView`) already owns `setGlobalFilters`.

    - [DashboardGlobalFilterBar.tsx](client/src/pages/Dashboard/Components/DashboardGlobalFilterBar.tsx) gains an `availableFilters?: ChartFilterDefinition[]` prop. The bar now renders even when `global` is empty *if* `availableFilters.length > 0`, preserving DR4's "zero pixels when there's nothing to do" contract for dashboards with no chart tiles or all-empty chart data. AddFilterPopover mounted inline next to the chips and the existing "Clear all" link.

    - [DashboardView.tsx](client/src/pages/Dashboard/Components/DashboardView.tsx) wires the new prop. A `useMemo` over `sections + globalFilters` calls the new pure helper `availableFilterDefinitions` and passes the result down. Memoised so the popover stays fast on large dashboards.

    - [dashboardGlobalFilters.ts](client/src/pages/Dashboard/dashboardGlobalFilters.ts) gains four pure helpers (all no-DOM / no-async / testable):
      - `aggregateTileRowsForFiltering(tiles, maxRowsPerTile=2000)` — concatenates chart-tile data with a per-tile row cap. Categorical/numeric/date inference converges fast on a few-thousand-row sample; the cap prevents pathological compute on dashboards with very large bundled datasets.
      - `availableFilterDefinitions(tiles, currentGlobal)` — runs `deriveChartFilterDefinitions` on the aggregated rows and excludes columns already in `currentGlobal` (re-adding an active filter is the chip's edit affordance, not Add). Sorts by cross-tile frequency from `dashboardFilterableColumns`, then alphabetically.
      - `addCategoricalFilter(current, column, values)` / `addNumericFilter(current, column, min, max)` / `addDateFilter(current, column, start, end)` — pure mutations that produce the next `ActiveChartFilters`. No-op on empty selections (don't pollute the global state with empty filters). Don't mutate input.

  **Why this design.** Considered (a) a single generic `addFilter(column, selection)` helper — rejected because it loses type narrowing at the call site; the three-variant API mirrors `ChartFilterSelection`'s discriminated union and keeps the popover editor calls type-safe. (b) Computing filter definitions inside `AddFilterPopover` — rejected because the inference walks every chart-tile's data and would re-run on every popover open; computing in `DashboardView` with `useMemo` lets the result amortize across opens. (c) Rendering the picker as a modal — rejected because the popover is anchored to the bar, lets the user keep the bar's chip context visible, and matches the inline-edit affordances elsewhere in the dashboard chrome.

  Categorical-cap-at-30 is a UX call: more than 30 checkboxes in a dropdown is unusable, and the picker's job is "narrow this dashboard to a useful slice", not "let me pick any value from a 5,000-entry catalog." The cap surfaces with a "+ N more values not shown" hint so the user knows there's more. Future wave can layer search-within-values when telemetry shows that 30-cap hits real users; today's data shape (Marico FMCG: brand × region × channel × period × metric) rarely cracks 30 distinct values per dimension.

  **Tests.** [client/src/pages/Dashboard/dashboardGlobalFiltersWD1.vitest.test.ts](client/src/pages/Dashboard/dashboardGlobalFiltersWD1.vitest.test.ts) — 17 cases across 3 suites: `aggregateTileRowsForFiltering` (5: order preservation across tiles, non-chart tile skip, maxRowsPerTile cap, empty dashboard, empty chart data), `availableFilterDefinitions` (4: categorical+numeric derivation, exclude-already-active, empty when no chart tiles, frequency-then-alphabetical sort), pure add helpers (8: each variant — one-bound / both-bounds / no-op / no-mutation / overwrite). 17/17 passing. 420/420 client vitest tests still pass overall — no regression. The `AddFilterPopover` component itself is exercised by manual smoke (consistent with `TileInsightFooter.vitest.test.ts` / `dashboardTileChrome.vitest.test.ts` pattern; vitest runs in node env without jsdom). `.vitest.test.ts` files are auto-discovered — NOT appended to `server/package.json` (invariant #4 applies to server `node:test` files only).

  **Verified.** `npx tsc --noEmit` reports 53 errors, identical to the pre-WD1 client-side baseline — zero new from WD1. `npm run theme:check` clean (semantic tokens only — `bg-popover`, `bg-muted`, `text-foreground`, `text-muted-foreground`, `text-popover-foreground`). Commit `d1ea692e`.

  **Out of scope.** Cross-filter brushing (click chart bar → adds filter to global) — that's **WD2**. Drill-through-to-detail modal — **WD3**. Dynamic per-tile insight regeneration on filter change — **WD4**. Fork-to-explorer from dashboard tiles — **WD5**. Mobile-touch UI — **WD6**. Multi-sheet linked filters — **WD7**. Saved views / filter presets — **WD8**. Inline tile comments — **WD9**. Scheduled refresh + notify — **WD10**. Each separately shipped per the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md) Workstream 4 wave map.

- **2026-05-16** — **Wave W73 · Investigation mode wired into the agent loop entry.** Re-wires the dormant [`runDeepInvestigation`](server/lib/agents/runtime/investigationOrchestrator.ts) orchestrator into [`dataAnalyzer.answerQuestion`](server/lib/dataAnalyzer.ts) behind the existing `DEEP_INVESTIGATION_ENABLED` flag — invariant #6 preserved, default OFF. The capability had been fully built and tested through Phases D1/D2 + the W9 orchestrator + W7 investigation tree + W11 budget caps, but had zero live callers; only the test file [investigationOrchestrator.test.ts](server/tests/investigationOrchestrator.test.ts) referenced it. The pre-W73 comment at [dataAnalyzer.ts:502-506](server/lib/dataAnalyzer.ts#L502-L506) explicitly flagged this entry point as the future hook — this wave fills it.

  **What landed.**
    - New [server/lib/agents/runtime/investigationDispatch.ts](server/lib/agents/runtime/investigationDispatch.ts) — `shouldDispatchDeepInvestigation(question)` is a pure deterministic decision returning `{ fire: boolean, reason: string, multiPart?: MultiPartIntent }`. Three layers of gating, evaluated in order: (1) `DEEP_INVESTIGATION_ENABLED` env var must be `"true"` or `"1"` (any other value, including `"yes"` / unset / `"false"`, returns `fire: false`); (2) question must be non-empty after trim; (3) [`detectMultiPartQuestion`](server/lib/agents/runtime/detectMultiPartQuestion.ts) (W11 D1 detector) must return a multi-part intent. Returns a structured reason string ≤ 500 chars for the `flow_decision` SSE payload.
    - [`dataAnalyzer.answerQuestion`](server/lib/dataAnalyzer.ts) `let loopResult: AgentLoopResult | null = null` declared, then `shouldDispatchDeepInvestigation(question)` called pre-`runAgentTurn`. On `fire: true`, emits a `flow_decision` SSE row (`layer: "investigation-dispatch"`, `chosen: "deep"`, `overriddenBy: "DEEP_INVESTIGATION_ENABLED"`) carrying the rationale and up to 8 sub-questions as `candidates`, then awaits `runDeepInvestigation`. Three fall-back paths to the single-flow `runAgentTurn`: (a) gate off / single-part question (fire=false); (b) `runDeepInvestigation` throws; (c) `runDeepInvestigation` returns null or empty answer. Single-flow path is unchanged from pre-W73 when the gate is off — zero behaviour change in default deployment.

  **Budget caps already in place** (defined in [investigationTree.ts:31-46](server/lib/agents/runtime/investigationTree.ts#L31-L46)): `DEEP_INVESTIGATION_MAX_LLM_CALLS=120`, `DEEP_INVESTIGATION_MAX_WALL_MS=1_500_000`, `DEEP_INVESTIGATION_MAX_DEPTH=3`, `DEEP_INVESTIGATION_MAX_NODES=15`, `DEEP_INVESTIGATION_MAX_PARALLEL=3`, `DEEP_INVESTIGATION_MAX_CHILDREN=3`, `DEEP_INVESTIGATION_PER_NODE_LLM=12`, `DEEP_INVESTIGATION_PER_NODE_WALL_MS=90_000`. All override-able via env. The Wave D plan reserved these caps to bound cost when the gate is lifted.

  **Why this design.** Considered (a) flipping `DEEP_INVESTIGATION_ENABLED` default to `"true"` — rejected because invariant #6 says re-wiring requires a feature flag; the spirit is "opt-in, not on-by-default". Operators flip the env var once they validate cost + quality in their environment. (b) Auto-triggering on `analysisBrief.questionShape` ∈ {`driver_discovery`, `variance_diagnostic`, `comparison`} — deferred to W74 because the brief is computed *inside* `runAgentTurn` (post-mode-classification, pre-planner) and is not available at the dispatch site without an architecture change. The W11 `detectMultiPartQuestion` detector is the right immediate trigger: it's pure, fast, and runs pre-turn cleanly. (c) Spawning the dispatcher inside `runAgentTurn` — rejected because dispatching at the loop entry keeps the orchestrators decoupled and lets the SSE `flow_decision` row land before the loop's other workbench events.

  Telemetry pattern: every dispatch decision (fire-true or fire-false-with-multi-part-question-but-gate-off) emits a `flow_decision` SSE row visible in the workbench timeline. Operators can grep logs for `investigation-dispatch` to count multi-part questions vs single-flow ones.

  **Tests.** [tests/investigationDispatchW73.test.ts](server/tests/investigationDispatchW73.test.ts) — 13 cases across 3 suites. **Gate semantics (4):** unset → fire=false; `"false"` → fire=false; `"yes"` → fire=false (only `"true"`/`"1"` accepted); `"1"` → fire=true on a multi-part question. **Multi-part detection with gate on (7):** two-part conjunction fires, three-part conjunction fires with ≥ 2 sub-questions, bare-`and`-noun-phrase (`"sales and growth by region"`) does NOT fire, descriptive question does NOT fire, empty / undefined / whitespace-only questions all return fire=false with the right `reason`. **Decision shape (2):** reason string ≤ 500 chars (SSE envelope safety), multiPart carries the original question + trigger token. Helper `withEnv()` sets/restores `DEEP_INVESTIGATION_ENABLED` around each case so tests don't leak global state. Appended to [server/package.json](server/package.json)'s explicit `test` list per invariant #4.

  **Verified.** 38/38 adjacent investigation + routing tests still pass (`agenticRoutingNoLegacyFastPath`, `detectMultiPartQuestionD1`, `coordinatorAgent`, `investigationOrchestrator`). `npx tsc --noEmit` reports 98 errors, identical to W57 baseline — zero new from W73. Commit `d1de1647`.

  **Out of scope.** Shape-based auto-dispatch (W74): when `analysisBrief.questionShape ∈ {driver_discovery, variance_diagnostic, comparison}` auto-trigger investigation regardless of conjunction phrasing — requires hoisting the brief computation pre-dispatch or wiring dispatch inside `runAgentTurn`. Workbench UI for parallel sub-investigations (W75): existing [`ThinkingPanel.tsx`](client/src/pages/Home/Components/ThinkingPanel.tsx) renders linear timeline; per-sub-question lanes deferred. Hypothesis Tree visualizer (W76), narrator cross-investigation merge (W77), investigation memory carry-over (W78), live-LLM golden-replay fixture q11 (W79) — all separately tested per the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md).

- **2026-05-16** — **Wave W57 · Semantic model inference.** Builds on W56's type foundation to auto-populate a `SemanticModel` from upload-time signals — pure deterministic function, fast, no I/O, replayable. The model is the *catalog of things users can ask about*; W58's compiler will turn `(metric, breakdownBy, filters, window)` queries against this catalog into `QueryPlanBody` for DuckDB execution. Best-effort: an inference failure does NOT block the upload's understanding-ready checkpoint.

  **What landed.**
    - New [server/lib/semantic/inferModel.ts](server/lib/semantic/inferModel.ts) (~260 LOC including helpers) — entry point `inferModel({ summary, datasetProfile?, modelName? }) → SemanticModel`. Two paths:
      - **Long-form (tidy) datasets.** `SUM(<col>)` metric per numeric column (excluding indicators); currency-tagged columns get `format: "currency"` + `currencyCode` from `dataSummary.columns[i].currency.isoCode`. Categorical dimensions for low-cardinality strings (those with `topValues` populated). Temporal dimensions for date columns with grain mapped from `temporalDisplayGrain` (`'year'` → `'year'`, `'monthOrQuarter'` → `'month'`, `'dayOrWeek'` → `'day'`). Indicator columns become categorical dimensions (never metrics). Hidden `__tf_*` facet columns excluded. Always emits a `row_count` metric (`COUNT(*)`).
      - **Wide-format datasets** (detected via `dataSummary.wideFormatTransform`). Generic `value` metric (`SUM(value)`), `_metric` as a categorical splitter dimension, `_period` as a temporal dimension, id-vars as categorical dimensions. Per-`_metric` synthetic metrics (e.g. `value_sales = SUM(value) FILTER (WHERE _metric = 'Value Sales')`) deferred to W58 because they need distinct-value enumeration the compiler already does.
    - `toSnakeCase` helper handles separators, camelCase → snake, leading-digit guard (`123_metric` → `field_123_metric` to satisfy the schema regex), empty/punctuation fallback, and double-underscore stripping (`__tf_year` → `tf_year`, `_period` → `period`). Snake-case collisions resolved with numeric suffix (`sales` → `sales_2` → `sales_3`).
    - High-cardinality identifier columns (string-typed but no `topValues` — TransactionId / OrderId shape) are **not** exposed as dimensions. The planner shouldn't suggest "break down by TransactionId"; raw query plans can still reference them.
    - `ChatDocument` gains optional `semanticModel?: SemanticModel` field in [server/models/chat.model.ts](server/models/chat.model.ts) next to `datasetProfile?`. Auto-mirrored to client via the W5 re-export.
    - Upload pipeline wiring in [server/utils/uploadQueue.ts](server/utils/uploadQueue.ts) — `inferModel` runs at the understanding-ready checkpoint right before `updateChatDocument` persists `dataSummary + datasetProfile + sessionAnalysisContext`. Dynamic import matches the existing pattern; failure logs a warning and lets the rest of the checkpoint proceed.

  **Why this design.** Considered (a) deferring inference to first chat turn — rejected because the planner prompt rewrite in W59 expects the model to be available at session-load time, (b) running inference inside `inferDatasetProfile` (the existing LLM enrichment) — rejected because the semantic model should be deterministic and cheap, and the LLM-profile call already takes seconds, (c) requiring user confirmation before inference — rejected because the model is auto-source and editable later via the W61 admin UI; blocking on user input violates the non-blocking-startup invariant. Building inference as a pure function with the `inferModel({ summary, datasetProfile? }) → SemanticModel` shape lets the same code be re-run in the admin UI after a column-rename or wide-format reshape without any I/O coupling.

  Hierarchies stay empty by default. Multi-level chains (Country → Region → City) need either explicit declaration or LLM detection — column-name heuristics alone (e.g., looking for "country" / "region" / "city" suffixes) generate too many false positives on Marico's brand+market+SKU shapes. Future wave can layer LLM detection on top without changing the schema.

  **Tests.** [tests/semanticInferModelW57.test.ts](server/tests/semanticInferModelW57.test.ts) — 15 cases across 3 suites: `toSnakeCase` (5: free-form → snake, underscore stripping, leading-digit guard, fallback); long-form (10: SUM-per-numeric + row_count, currency tagging via ISO code, categorical-only-for-topValues, temporal grain mapping incl. no-hint case, hidden `__tf_*` exclusion, indicator-as-dimension, snake-case collision disambiguation, zod schema round-trip via [`semanticModelSchema`](server/shared/schema.ts), defaults for empty model + custom name, empty-dataset edge case); wide-format (1 end-to-end with generic value + `_metric` + `_period` + id-var dimensions). Appended to [server/package.json](server/package.json)'s explicit `test` list per invariant #4.

  **Verified.** W56 + W57 + 2 adjacent schema tests (`dimensionHierarchiesSchema`, `sessionAnalysisContextAssistantMerge`) 36/36 passing. `npx tsc --noEmit` reports 98 errors, identical to the W56 baseline — zero new errors from W57. Commit `ee647eb9`.

  **Out of scope.** No compiler — `inferModel` produces the catalog but cannot yet execute a metric query; W58 builds the SQL compiler. No planner prompt change — planner still emits `execute_query_plan` against raw columns; W59 rewrites the prompt to use the metric catalog. No `execute_metric_query` tool — W60. No admin UI — W61. No reference resolver that validates the metric's `references[]` against actual dataset columns at upload time — deferred to W63 (`validateModel`). No domain-pack-driven metric seeding — domain packs declare metrics declaratively; W64 plumbing.

- **2026-05-16** — **Wave W56 · Semantic & metrics layer — type foundation.** First wave of Workstream 1 from the [1000x master plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md). Pre-W56 the agent's planner generated SQL directly against raw DuckDB schemas; per [Cube's paired benchmarks](https://cube.dev/blog/why-semantic-layers-make-llm-analytics-reliable-a-paired-benchmark-across-three-frontier-models) and Looker's internal testing, a governed semantic layer cuts data errors ~66% and lifts accuracy ~300% across frontier models — the single biggest accuracy lever available. The Marico-tailored layer ships in 9 waves (W56–W64); this wave lands only the type foundation so subsequent waves (W57 `inferModel`, W58 compiler, W59 planner prompt rewrite, W60 `execute_metric_query` tool) have a stable canonical shape.

  **What landed.** Four new zod schemas in [server/shared/schema.ts](server/shared/schema.ts), placed adjacent to `dimensionHierarchySchema` so future readers see the two related-but-orthogonal hierarchy concepts side-by-side:
    - `semanticMetricSchema` — canonical Metric: `{name, label, expression, references, format, currencyCode?, decimals?, description?, exposed, source}`. Names are lowercase snake_case (regex `/^[a-z][a-z0-9_]*$/`). `expression` is a pure aggregation expression validated at compile time in W58. `format` ∈ {number, percent, currency, ratio, duration} drives the chart/pivot renderer. `currencyCode` is ISO 4217 (uppercase 3-letter). `source` ∈ {auto, user, domain} tracks provenance: auto-inferred from `datasetProfile`, user-edited via the admin UI (W61), or pulled from a domain pack.
    - `semanticDimensionSchema` — `{name, label, column, kind, temporalGrain?, ...}`. `kind` ∈ {categorical, temporal, numeric_binned, geo} drives the global filter picker's type-appropriate UI in Workstream 4 / WD1.
    - `semanticHierarchySchema` — multi-level drill-down chain like Country → Region → City. Distinct from `dimensionHierarchySchema` (in-column rollup totals such as 'All India' rolling up regions); the two coexist. `levels` references dimension *names* (not raw columns) so the chain stays stable across column renames.
    - `semanticModelSchema` — top-level container: `{version, name, metrics, dimensions, hierarchies, updatedAt?, updatedBy?}`. `version` is monotonic and serves as the cache key for compiled queries (W64). Caps: ≤200 metrics, ≤200 dimensions, ≤50 hierarchies — wide enough for Marico's portfolio scale, narrow enough to fail loudly on accidental cardinality explosion.

  Auto-mirrored to client via W5's single re-export ([client/src/shared/schema.ts](client/src/shared/schema.ts) → `export * from "../../../server/shared/schema"`). No client-side change required.

  **Why this design.** Considered Cube.dev integration vs roll-our-own; the user picked roll-our-own (see the [1000x plan's AskUserQuestion exchange](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md)) because Cube.dev would add an external service to operate, while the Marico-tailored layer reuses existing [`wideFormat/metricVocabulary.ts`](server/lib/wideFormat/metricVocabulary.ts), [`wideFormat/periodVocabulary.ts`](server/lib/wideFormat/periodVocabulary.ts), [`datasetProfile.ts`](server/lib/datasetProfile.ts), and the existing `dimensionHierarchies` array — net zero new dependencies. The schemas are intentionally small (~140 LOC of definitions); the surface that does real work lives in subsequent waves' modules under `server/lib/semantic/`.

  Snake-case name enforcement is load-bearing: prevents naming collisions across the metric catalog, simplifies parsing in the compiler's SQL builder, and matches the convention already used in `_period` and `_metric` columns from wide-format melt. The `semanticDimension` / `semanticMetric` / `semanticHierarchy` naming (rather than bare `Dimension` / `Metric` / `Hierarchy`) is deliberate: avoids clashing with existing identifiers used in chart specs (`chart.x` is informally called a "dimension") and the existing `dimensionHierarchySchema` rollup concept.

  **Tests.** [tests/semanticModelSchemaW56.test.ts](server/tests/semanticModelSchemaW56.test.ts) — 15 cases across 4 suites: metric (round-trip, defaults, snake-case rejection, ISO 4217 currency rejection, references-cap-at-20, expression-length-cap-at-2000); dimension (round-trip with temporal grain, all four kinds, unknown-kind rejection); hierarchy (multi-level chain, 2 ≤ levels ≤ 8 enforcement, snake-case on level names); model (defaults for an empty model, realistic Marico FMCG round-trip with two metrics + two dimensions + one geo hierarchy, ≤200 metrics + ≤50 hierarchies caps, version ≥ 1). Appended to [server/package.json](server/package.json)'s explicit `test` script per invariant #4.

  **Verified.** W56 test 15/15 passing. Adjacent schema tests (dimensionHierarchiesSchema, agentRuntimeSchemas, domainContextSchema) 18/18 still passing — no regression. `npx tsc --noEmit` reports 98 errors, all pre-existing; zero new errors related to the W56 changes. Commit `549b6610`.

  **Out of scope.** No `ChatDocument.semanticModel` field yet — lands in W57 alongside the auto-inference. No compiler, no planner prompt change, no tool. No admin UI. No reference resolver against actual column names. No domain-pack-driven metric seeding. Each is its own wave (W57–W64) per the [1000x plan](/Users/tida/.claude/plans/go-through-the-entire-partitioned-yao.md). Also out of scope: refreshing the stale "dual-mirror" description in [docs/architecture/schemas.md](docs/architecture/schemas.md) — W5 collapsed the client mirror into a single re-export but the doc still describes the old model. Picked up in a future doc-hygiene wave.

- **2026-05-16** — **Phase D · Coordinator multi-part detection + `flow_decision` observability (D1+D2 shipped; D3 wires actual parallel decomposition in a follow-up wave).** Pre-D the W11–W13 single-flow policy bypassed `coordinatorAgent.decomposeQuestion` entirely (the `runDeepInvestigation` orchestrator at [investigationOrchestrator.ts:99](server/lib/agents/runtime/investigationOrchestrator.ts) exists, was wired internally, but had no live caller). User-confirmed scope: re-enable behind a flag, validate the detector against real questions first, then commit to behaviour change in a separate cleanup wave once production telemetry confirms the detector matches user phrasings.

  **Wave D1 · `detectMultiPartQuestion`** at [server/lib/agents/runtime/detectMultiPartQuestion.ts](server/lib/agents/runtime/detectMultiPartQuestion.ts). Pure-regex deterministic detector returns split sub-questions when the user's question matches a multi-part conjunction shape: "X, and (also/then) Y", " and (verb-led second clause)", "additionally Y", "in addition Y". Recursive single-split for 3rd / 4th clauses; capped at 4 (W11 budget). Conservative — anything ambiguous returns null (caller proceeds to single-flow). Key gates: (a) compound metric noun phrases like "sales and growth by region" do NOT split (the second 'and' clause must be verb-led or question-word-led); (b) question length ≥ 20 chars; (c) each sub-clause ≥ 3 chars after split. 12 new tests in [tests/detectMultiPartQuestionD1.test.ts](server/tests/detectMultiPartQuestionD1.test.ts): primary phrasings (top brands AND why MARICO leads, compare A and tell me B, additionally check anomalies, three-part question into 3 sub-questions); single-shape questions correctly stay un-split (top 10 brands, compound noun-phrase 'and', adjective 'and', too short, empty input); explicit-question-word triggers (what + how + and clauses); cap at 4 enforced.

  **Wave D2 · `flow_decision` SSE observability** in [chatStream.service.ts](server/services/chat/chatStream.service.ts) right after the mode classifier. Gated by `DEEP_INVESTIGATION_ENABLED=true`. When the detector finds a multi-part shape, emits a flow_decision row with `{layer: "coordinator", chosen: "single_flow (N-part question — decomposition deferred to Wave D3)", overriddenBy: "single_flow_policy", candidates: subQuestions, reason: "Multi-part detected (trigger: ...). With DEEP_INVESTIGATION_ENABLED=true, this would decompose into N parallel sub-investigations once the orchestrator wiring lands."}`. The existing `agentWorkbench.util.ts:260` `flow_decision` converter renders this as a workbench row showing "Override: coordinator → single_flow (N-part question)" with the sub-questions in the candidates field — no client UI work needed. Users see WHEN the detector would have fired and exactly which sub-questions it would have spawned, validating the detector against real production traffic before committing to the parallel-execution path.

  **Wave D3 (deferred / minimum-viable observability stance):** the actual parallel sub-investigation execution + narrator merge are deliberately NOT shipped in this wave. Rationale: (a) `runDeepInvestigation` already exists at [investigationOrchestrator.ts:99](server/lib/agents/runtime/investigationOrchestrator.ts) — the infrastructure is there; (b) the cost-blast risk of wiring it without first observing detector accuracy on real questions is high (4 parallel agent turns × ~$0.50 each = $2 per multi-part question); (c) the user can opt-in to the flag, watch the workbench for false positives over a week of dogfooding, and authorise the actual decomposition cleanup wave with confidence. The D3 wiring would: (i) when `DEEP_INVESTIGATION_ENABLED=true` AND `detectMultiPartQuestion` returned a multi-part intent, call `runDeepInvestigation` per sub-question; (ii) merge the blackboards via the existing primitive; (iii) run ONE narrator call against the merged blackboard with explicit cross-references between sub-investigations; (iv) emit one envelope. All four steps reuse code that's already in tree — D3 is purely the wire-up, not new functionality. Recorded as a TODO so future-Claude doesn't duplicate work. **2952/2952 server tests pass** (12 new in D1) · **403/403 client tests pass** · `npx tsc --noEmit` 98 server / 53 client (matches pre-Phase-D baseline; zero new errors).

- **2026-05-16** — **Phase F · Forecasting + anomaly detection + statistical significance (3 tools, pure-Node, env-gated).** Three new tools that close the previously-empty "predictive / inferential" analytical category from the coverage audit. All three implemented in pure Node (no Python-service round-trip — keeps the production hot path lean; the Python-service `mmm/` directory remains the template if any of these need a statsmodels/scipy upgrade later).

  **Wave F1 · `run_forecast`** ([server/lib/forecasting/forecastSeries.ts](server/lib/forecasting/forecastSeries.ts) + [tools/forecastTool.ts](server/lib/agents/runtime/tools/forecastTool.ts)). Forecasts a numeric series N periods into the future via linear-trend regression + auto-detected seasonal pattern (4/7/12/52-period candidates picked by residual-variance reduction ≥ 5%) + bootstrap-style 95% CI bands that widen with sqrt(horizon). Input: `{timeColumn, valueColumn, granularity: "month"|"quarter"|"year", horizon, seasonality?: "auto"|"none"|4|7|12|52}`. Output: result table with `actual` (historical) + `forecast` + `lower_ci` + `upper_ci` columns + memorySlots (method, R², seasonal period). Gated by `FORECAST_ENABLED=true`. 9 tests in [tests/forecastSeriesF1.test.ts](server/tests/forecastSeriesF1.test.ts): perfect linear extrapolation, flat series → zero CI, sqrt(h) CI widening, auto-detect quarterly seasonality, explicit seasonality=12, `seasonality:"none"` skips detection, < 4 obs rejected, horizon clamps, null/NaN values cleaned during pre-process.

  **Wave F2 · `detect_anomalies`** ([server/lib/anomalyDetection.ts](server/lib/anomalyDetection.ts) + [tools/anomalyDetectionTool.ts](server/lib/agents/runtime/tools/anomalyDetectionTool.ts)). Surfaces outliers in a numeric column via IQR (Tukey's k=1.5) and/or z-score (k=2.5) — flags each observation with the method(s) that fired, severity score (distance beyond bound in IQR units / absolute z-score), and direction (high/low). "both" method returns union with combined flagging. Input: `{valueColumn, labelColumn?, method, iqrK?, zK?, dimensionFilters?, topN}`. Output: severity-sorted result table + stats payload (n, mean, median, std, q1, q3, IQR, bounds). Gated by `ANOMALY_DETECTION_ENABLED=true`. 10 tests in [tests/anomalyDetectionF2.test.ts](server/tests/anomalyDetectionF2.test.ts): IQR catches obvious outlier in tight cluster, z-score flags both extreme high + low, "both" tags by every triggering method, flat series → no anomalies, labels round-trip, severity sorts by magnitude, stats payload completeness, rejects < 5 obs / non-array input.

  **Wave F3 · `run_significance_test`** ([server/lib/significanceTests.ts](server/lib/significanceTests.ts) + [tools/significanceTestTool.ts](server/lib/agents/runtime/tools/significanceTestTool.ts)). Three test shapes via a Zod discriminated union: (a) Welch's two-sample t-test (unequal variances, Welch-Satterthwaite df) with Cohen's d effect size; (b) paired t-test; (c) chi-square test of independence with Cramér's V effect size. Pure-Node implementation includes Lanczos log-gamma, regularised incomplete beta (continued fraction, Press et al. NR 6.4) for the t-distribution survival function, and regularised lower incomplete gamma (series + continued fraction) for the chi-square survival function. Accuracy ~5 decimal places — plenty for narrative "is this significant" framing. Output: statistic, p-value, df, effect-size value + magnitude bucket (negligible/small/medium/large), `significant` boolean, narrator-ready `interpretation` string. Gated by `SIGNIFICANCE_TESTS_ENABLED=true`. 14 tests in [tests/significanceTestsF3.test.ts](server/tests/significanceTestsF3.test.ts): clearly-separated groups significant, overlapping groups not significant, identical samples p≈1, paired-t consistent improvement, paired-t noise → not significant, mismatched-length paired rejection, chi-square strong vs balanced tables, 3×3 chi-square df=4, custom alpha threshold flips borderline cases.

  All three tools registered in [registerTools.ts](server/lib/agents/runtime/tools/registerTools.ts) right after `detectSeasonalityTool`. **40/40 tests pass across the F-phase plus adjacent regression** (`toolRegistryDuplicateGuard`, `toolManifest`). `npx tsc --noEmit` 98 server errors (matches W3 baseline; zero new errors). Convention added: **predictive/inferential tools live behind explicit env flags (`FORECAST_ENABLED`, `ANOMALY_DETECTION_ENABLED`, `SIGNIFICANCE_TESTS_ENABLED`)** — registered unconditionally so the planner sees them in its tool manifest, with a clear "is disabled (X is not 'true')" off-message inside the body. Same pattern as `web_search` (`WEB_SEARCH_ENABLED`) and `run_budget_optimizer`. Out of scope (recorded so future-Claude doesn't reopen): ARIMA/Prophet upgrade for forecast (statsmodels not in `python-service/requirements.txt`; current linear+seasonal is "good enough" for "what does next quarter look like"); cohort + funnel analysis (deferred — different shape of math, would need their own tools); paired chi-square test (McNemar) — current 3 shapes cover the common asks. The planner's prompt will naturally pick these up via the existing tool-manifest formatter; no system-prompt edit needed.

- **2026-05-16** — **Wave W3 · Composite-ranking expression support in `run_breakdown_ranking`.** Closes "rank brands by combined growth and share" / "top products weighted by margin × volume" / "rank by (revenue - cost) / revenue" — multi-metric weighted ranking where the score is a function of several aggregated values, not a single metric. Pre-W3 the tool ranked by `metricColumn + aggregation` (one metric per call); users wanting composite ranking had to ask twice and combine by eye, or write raw SQL via `run_readonly_sql`. Schema extension at [breakdownRankingTool.ts](server/lib/agents/runtime/tools/breakdownRankingTool.ts): new optional `rankBy: {metrics: [{column, operation, alias}], expression: string}` field; when present, overrides the simple `metricColumn` path. `metricColumn` is now optional with a superRefine ensuring EITHER it OR `rankBy` is set. Expression validation reuses `parseComputedAggregationExpression` from Wave QL7 — same restricted arithmetic mini-language (`+ - * /` + parens + numeric literals + identifier names), same SQL-keyword denylist, same character-set guard. The expression's identifier list is cross-checked against the declared `metrics[].alias` set so referencing an undeclared metric fails validation up-front. Caps: max 4 metrics per call (keeps planner prompt surface small + per-group compute bounded); expression ≤ 240 chars. Composite ranking path in the tool body: `aggregateComposite` per-group accumulates sum + count for each metric, computes the per-metric value (sum / mean / count) at the end, evaluates the expression with each alias substituted by its numeric value via `Function` constructor over a no-closure arithmetic expression (same pattern as `applyComputedAggregationsInMemory`). Groups whose expression evaluates to null (e.g. division-by-zero on a metric with zero rows) sink to the bottom regardless of sort direction. Result rows expose each metric's aggregated value + `_composite_score` + `_rowCount`. Description + argsHelp updated with the new shape so the planner LLM sees the example. 12 new tests in [tests/breakdownRankingCompositeW3.test.ts](server/tests/breakdownRankingCompositeW3.test.ts): simple path backwards-compat (metricColumn + aggregation still works; missing both fails); composite happy paths (2-metric weighted, single-metric degenerate, ratio expression, count-based); rejections (undeclared alias in expression, disallowed SQL injection chars, reserved SQL keyword, >4 metrics, empty metrics array, expression >240 chars). 43/43 across W3 + adjacent regression sweep (`breakdownRankingTopNUnlimited`, `extractRankingIntent`, `enforceRankingPlanShape`). `npx tsc --noEmit` 98 server errors (matches W2 baseline). Convention added: **the restricted arithmetic mini-language from QL7's `computedAggregations` is the standard expression surface across the codebase** — `breakdown_ranking.rankBy.expression` reuses it; future tools that need user-supplied arithmetic should follow the same pattern (validate via `parseComputedAggregationExpression`, substitute identifiers, evaluate via Function-constructor). Out of scope (recorded so future-Claude doesn't reopen): mean-of-aggregated-values (`SUM(x)/COUNT(distinct y)` — possible today via the existing QL7 + `count_distinct` op chain); rolling-window references in rankBy expressions (need lookup against `windowAggregations` output aliases — defer until production telemetry shows the combined use case); rankBy with `perDimension`-style nested aggregations (single-pass per-group sums/means/counts is enough for v1).

- **2026-05-16** — **Wave W2 · `detectRollingWindowIntent` recognises rolling / cumulative / period-to-date phrasings.** Closes the discoverability gap for Wave W1: the schema is live but the planner LLM doesn't see explicit examples for every rolling-window phrasing. W2 ships a deterministic detector that the planner-side repair layer (lands in a follow-up wave) can use to inject a `windowAggregations` entry before the main analytical pass. New `detectRollingWindowIntent` in [planArgRepairs.ts](server/lib/agents/runtime/planArgRepairs.ts) (next to `detectPerXIntent`) returns a partial `WindowAggregation` shape (operation + frame + temporalUnit + rawCapture) for these phrasings: (a) "rolling N-week/day/month/quarter/year average/total/sum/min/max" → `{rows: N}` frame; (b) "N-week rolling average" / "trailing N-day mean" / "moving average over last N days" — same shape with reordered regex; (c) "cumulative X" / "running total" → `{range: "unbounded_preceding"}` with year-grain default; (d) YTD / year-to-date → cumulative within year; QTD → quarter; MTD → month. Period-to-date branches evaluate BEFORE the generic cumulative branch (otherwise "month-to-date" would match the cumulative regex's `to-date` token and default to year grain — caught during test pinning). Conservative gates: window size capped at 365, no number → null, no rolling/cumulative cue → null. 16 new tests in [tests/detectRollingWindowIntentW2.test.ts](server/tests/detectRollingWindowIntentW2.test.ts): primary phrasings (rolling 4-week, 4-week rolling, trailing 7-day, moving over last 30 days, rolling 12-month sum); cumulative + running total + YTD + QTD + MTD; rejections (empty input, plain "average sales", "top 10 brands", N > 365, "rolling" without N). 46/46 across W2 + adjacent regression sweep (`detectPerXIntent`, `injectPerDimensionRepair`, `inferFiltersFromQuestion`). `npx tsc --noEmit` 98 server errors (matches W1 baseline). Out of scope, recorded for future enhancement: actual `injectRollingWindowRepair` that mutates the agent's plan to wire the detected window into `windowAggregations` automatically — deliberately deferred so the user can audit the LLM-emitted plans first and see whether the detector's intents match what the planner ALREADY produces (the planner is smart enough to emit windowAggregations directly from the W1 schema example in its prompt; the deterministic repair becomes valuable only when production telemetry shows the planner mis-emits). Multi-axis rates ("avg sales per store per quarter") and MoM/YoY via lag/lead also deferred to a follow-up — the W1 schema supports them, the detector doesn't yet.

- **2026-05-16** — **Wave W1 · `windowAggregations` on `QueryPlanBody` — rolling averages, cumulative sums, rank-within-group, lag/lead.** Closes the "rolling 4-week average", "cumulative sales", "rank brands within region", "MoM via lag()" question shapes. Pre-W1 these were only expressible via `run_readonly_sql` (the 1.2KB SQL escape hatch). New `windowAggregationSchema` ([server/lib/queryPlanExecutor.ts:windowAggregationSchema](server/lib/queryPlanExecutor.ts)) accepts: aggregates (`sum`/`mean`/`min`/`max`/`count`), ranking (`row_number`/`rank`/`dense_rank`), positional (`lag`/`lead`); two frame shapes (`{rows: N}` for rolling, `{range: "unbounded_preceding"}` for cumulative); `partitionBy[]` + `orderBy[]` for the window; capped 6 windows per plan + 4-column max partition/order budget so the planner LLM keeps the surface small. The schema's `.superRefine` rejects ranking-with-frame and aggregate-without-column at validation time. Plumbed into BOTH execution paths: **(a) in-memory** (`applyWindowAggregationsInMemory` in `queryPlanExecutor.ts`) runs partition-by-then-walk for each window, with a 100k-row safety cap that errors out and tells the caller to route through DuckDB instead; **(b) DuckDB** ([queryPlanDuckdbExecutor.ts](server/lib/queryPlanDuckdbExecutor.ts) — new `buildWindowAggregationSql` helper) wraps the source table in a derived subquery `(SELECT *, <window_clauses> FROM data) w1_windowed` so the rest of the SQL builder operates against the windowed source as if it were the base table. Window aliases are materialised onto every output row — clients reading `result.data` see them directly. `normalizeAndValidateQueryPlanBody` now accepts plans with ONLY `windowAggregations` (no aggregations / dimensionFilters / limit) so "show me rolling 4-week sales by brand" works without contrived filters. 12 new tests in [tests/windowAggregationsW1.test.ts](server/tests/windowAggregationsW1.test.ts): schema accepts each operation shape; rejects ranking-with-frame and aggregate-without-column; QueryPlanBody caps at 6 windows; rolling 3-week sum per brand walks partitions correctly with the right window boundary math (100 → 300 → 600 → 900); cumulative sum (unbounded preceding) per brand; dense_rank by Sales desc handles ties without gaps; lag(Sales, 1) per brand produces null on the first row + Sales[n-1] thereafter; window alias columns appear on every output row; 100k-row in-memory cap errors out with a "route through DuckDB" message. 19/19 pass across W1 + adjacent regression sweep (`queryPlanDuckdbExecutor`, `duckdbPlanExecutor`, `executeQueryPlanDimensionFiltersRepair`). `npx tsc --noEmit` 98 server errors (matches P1 baseline; zero new errors). Convention added: **window aggregations are first-class plan-level constructs**; future "rolling X" / "rank within Y" / "cumulative Z" intent detection (Wave W2) can repair-inject them automatically. Out of scope, recorded so future-Claude doesn't reopen: cross-window reference (one window's alias inside another's column) — currently each window evaluates independently against the source; window aliases referenced in `dimensionFilters[]` would need forward-lookup validation (validator rejects today because schema column allowlist runs before window execution); window functions on calendar-bucketed data — works if you pre-aggregate via `dateAggregationPeriod` in a separate step but not within a single plan. Planner-prompt awareness lands in W2 alongside the rolling-phrasing detector.

- **2026-05-16** — **Wave P1 · Pivot reads agent result rows when computed aliases exist.** Closed the load-bearing UX gap from the analytical-capability audit: the agent already computes derived metrics correctly today via Wave QL7 (`computedAggregations: [{alias, expression}]` on QueryPlanBody — e.g. `SUM(rev)/COUNT(DISTINCT order) AS aov`), and the TABLE view renders them. But the INTERACTIVE PIVOT silently dropped them on any grouped query because `executePivotQuery` ([pivotQueryService.ts:445](server/lib/pivotQueryService.ts)) re-queries the base `data` table where alias columns don't exist (the PVT2 guard at [pivotDefaultsFromExecution.ts:419-427](server/lib/pivotDefaultsFromExecution.ts) strips them; QL9.A surfaced aliases only for SCALAR 1-row results — see the explicit comment at [tests/pivotDefaultsRatioShapeQL9.test.ts:127-142](server/tests/pivotDefaultsRatioShapeQL9.test.ts)). Fix: new `dataSource: "base" | "agent_result"` discriminator + inline `sourceRows` / `agentResultRows` on `PivotQueryRequest` + `PivotDefaults` ([server/shared/schema.ts:2592, 898](server/shared/schema.ts), plus new `PIVOT_AGENT_RESULT_MAX_ROWS = 200` cap). The agent loop ([agentLoop.service.ts](server/lib/agents/runtime/agentLoop.service.ts) around the existing intermediate-artifact emit) detects when `tracePlan.computedAggregations.length > 0` AND result is non-scalar AND row count ≤ 200, sets `dataSource: "agent_result"`, embeds the rows, and unions the alias columns into `values` (overriding the PVT2 strip). The server's `executePivotQuery` short-circuits on `dataSource: "agent_result"`: skips DuckDB entirely, runs the existing in-memory `buildPivotTree` over the embedded rows with UI filters applied via a new `filterSourceRowsForAgentResult` helper (mirrors the existing `pivotDimensionStringKey` normalization). Client ([DataPreviewTable.tsx:798](client/src/pages/Home/Components/DataPreviewTable.tsx)) forwards `dataSource + sourceRows` from `pivotDefaults` into the pivot request body when the message carries them. 9 new tests in [tests/pivotResultSetRenderP1.test.ts](server/tests/pivotResultSetRenderP1.test.ts) (appended to the explicit-file-list `test` script): schema accepts the new fields; backwards-compat (omitted = base path); pivot aggregates embedded rows with correct row count + tree shape; filter selections narrow the embedded rows; empty source rows produce empty tree without crashing; cap honored at PIVOT_AGENT_RESULT_MAX_ROWS rows. **45/45 across the pivot regression sweep** (`pivotResultSetRenderP1`, `pivotDefaultsFromExecution[.scalar]`, `pivotDefaultsRatioShapeQL9`, `pivotQueryService`, `intermediatePivotFilterProjection`, `chatStreamScalarPivot`). **403/403 client vitest pass.** `npx tsc --noEmit` 98 server errors / 53 client errors (both matching the post-E4 baseline; zero new errors). Convention added: **the pivot has two data-source modes** — base (default; queries DuckDB) and agent_result (new; aggregates embedded rows in-memory). Future tools that produce result columns NOT on the base table (window function output, forecast columns, anomaly flags) should set `dataSource: "agent_result"` and embed rows when result is non-scalar AND row count is small. The cap is a deliberate trade-off — 200 rows × 10 columns × 30 bytes ≈ 60KB per message; multiplied across 100 messages per session that's 6MB which still fits the 2MB Cosmos doc cap when most messages don't trigger this branch (only `computedAggregations`-bearing turns do). Out of scope (recorded so future-Claude doesn't reopen): the pivot field panel still shows base-table columns as draggable fields — alias columns appear as pre-selected values but the user can't drag a NEW alias column from the panel (would need a separate field-panel extension to read `agentResultColumns`); blob-offloaded result rows for >200-row results (future enhancement when needed); the QL9.A non-scalar-strip behaviour is preserved as the FALLBACK when the cap is exceeded so existing behavior holds for large result sets.

- **2026-05-15** — **Wave E4 · Stale active-filter retry helper (opt-in defensive pattern).** Closes the multi-tab plan's last theoretical race: Tab B issues a pivot / chart / drillthrough query AFTER Tab A bumped the active filter version but BEFORE the E2 broadcast event lands in Tab B (broadcast delivery is async; the query was already in flight). Today the server doesn't reject queries on a stale filter version — it just reads the current filter — so no realistic 400 fires. But the future server contract (when conflict detection matters, e.g. ETag-based optimistic concurrency in a multi-instance world) MAY emit `400 { code: "active_filter_version_mismatch" }`. Wave E4 ships the client-side recovery helper ready for that day. New [client/src/lib/activeFilterRetry.ts](client/src/lib/activeFilterRetry.ts): `isStaleActiveFilterError(err)` recognises the literal code string in either `Error.message`, `(err).code`, or a plain-string error; `withActiveFilterRetry(sessionId, fn, deps)` wraps a fetch closure — on stale-filter error, refetches `sessionsApi.getActiveFilter`, invokes `deps.onFilterRefetched` + `deps.onRetryTriggered` callbacks, and retries `fn` exactly once (no looping). On refetch failure the ORIGINAL stale error rethrows (don't mask the underlying issue). Non-stale errors propagate immediately without retry. Helper is OPT-IN — today nothing wires it into a call site (would change live request behavior; let the wave decision land on a future toggle). 10 new tests in [activeFilterRetry.vitest.test.ts](client/src/lib/activeFilterRetry.vitest.test.ts): `isStaleActiveFilterError` accepts each form (Error.message, .code, plain string), rejects unrelated errors; happy path no-retry no-refetch; non-stale error propagates without retry; stale error → refetch → retry → success with callback events fired; retry-attempt failure propagates correctly; refetch failure rethrows the ORIGINAL stale error not the refetch one; retries only ONCE (no infinite loop). **403/403 client tests pass** (45 vitest test files; 10 new in E4); client `tsc --noEmit` 53 errors (matches baseline; zero new errors). Convention added: **future server-side optimistic-concurrency rejects use a canonical error code string** — when a future server wave wires Cosmos ETag-based version checks, the matching rejection MUST surface `code: "active_filter_version_mismatch"` (or new helper sibling for other resources following the same naming). The client wrapper is ready to handle it.

- **2026-05-15** — **Wave E3 · Messages + lifted-state cross-tab sync wired into `useHomeMutations`.** [useHomeMutations.ts](client/src/pages/Home/modules/useHomeMutations.ts) gains a `useSessionBroadcast(sessionId, handler)` subscription that invalidates the TanStack Query cache keys `['sessions', userEmail]` and `['session', sessionId]` on any peer-tab event of kind `messages` / `columns` / `hierarchies` / `permanent_context`. The invalidation triggers TanStack to refetch and the relevant selectors (Home.tsx) to re-read the fresh chat doc — peer-tab message appends, column changes, hierarchy declarations, and permanent-context edits all show up in this tab automatically. Emit sites: (a) onSuccess of the chat-stream mutation fires `emitSessionBroadcast('messages')` after the turn fully settles (charts + business actions + pivot artifacts all in place — the broadcast is positioned at the end of the onSuccess body, just after the auto-created-dashboard handling); (b) the inline SSE handler for `session_context_updated` fires `emitSessionBroadcast('hierarchies')` when either `priorInvestigations` or `dimensionHierarchies` changed in the payload. The `isMountedRef` guard from Wave C2 also wraps the broadcast handler so peer-event handlers stay safe on unmount. 393/393 client tests pass (44 vitest test files); client `tsc --noEmit` 53 errors (matches baseline; 3 pre-existing errors at useHomeMutations.ts lines 507/1126/1127 unchanged — same `spawnedQuestions` cast issues as before, line numbers shifted slightly by my added code). Convention added: **a successful turn-end / SAC update MUST emit a broadcast on the matching event kind.** Future kinds added in this hook (e.g. on data-ops persist → emit `columns`, on permanent-context PATCH → emit `permanent_context`) extend the same pattern — emit AFTER the local state mutation, never before (otherwise a peer tab refetch could land before this tab's UI catches up).

- **2026-05-15** — **Wave E2 · Active-filter cross-tab sync wired into ChatInterface.** First consumer of the Wave E1 broadcast channel. `ChatInterface.tsx` extracted the inline "load active filter" useEffect body into a reusable `refetchActiveFilter` callback. Added `useSessionBroadcast(sessionId, handler)` that subscribes to peer-tab `active_filter` events and calls `refetchActiveFilter()` on receive. After every successful `setActiveFilter` PUT or `clearActiveFilter` DELETE, the local `pushConditions` / `handleClearAllFilters` functions call `emitSessionBroadcast('active_filter')` to notify peer tabs. Net effect: Tab A applies a filter → server bumps `activeFilter.version` → server response updates Tab A's local state → broadcast fires → Tab B's handler receives `active_filter` event → Tab B refetches via `sessionsApi.getActiveFilter` → Tab B's chips + filtered row count update without a page reload. The pivot / chart query keys that depend on `activeFilter.version` (set by Wave-FA2's pivot cache key via `stableHashJson({...activeFilterVersion})`) automatically invalidate on the next render because the new ActiveFilterSpec object is a new reference. 393/393 client tests pass (44 vitest test files; no new test — the broadcast contract is pinned in E1, and the ChatInterface integration is verified via the existing client smoke tests + manual multi-tab smoke). Client `tsc --noEmit` 53 errors (matches baseline). Convention added: **every server-mutating user action MUST emit a broadcast** so peer tabs can refetch. Future writes (hierarchy add, permanent context edit, computed column persist) all need an `emitSessionBroadcast(<kind>)` after a successful server response. The kinds enum in `sessionBroadcast.ts` is the source of truth for what's covered today.

- **2026-05-15** — **Wave E1 · `BroadcastChannel`-based cross-tab session sync foundation.** Pre-E1 the same session open in two browser tabs silently diverged: Tab A applied a filter → server bumped `activeFilter.version` → Tab B's pivot query failed with `active_filter_version_mismatch`; Tab A added a hierarchy → Tab B's banner stayed stale; Tab A's chat turn appended a message → Tab B's history was incomplete until reload. Multi-tab is now in scope (user-confirmed at the planning gate). E1 lays the foundation: new [client/src/lib/sessionBroadcast.ts](client/src/lib/sessionBroadcast.ts) wraps the browser-native `BroadcastChannel` API (`session:<sessionId>` per session) into an `openSessionChannel(sessionId)` factory returning `{emit, subscribe, release}`. Typed event envelope `SessionBroadcastEvent` with 5 kinds: `active_filter`, `messages`, `columns`, `hierarchies`, `permanent_context`. Per-CALL `senderId` so the emitting handle's own subscribers don't see their own echo. Per-call BroadcastChannel allocation (NOT cached) because Node's BroadcastChannel only delivers to OTHER channel instances with the same name — a cached single channel makes the unit-test "two tabs" simulation a no-op. New React hook in [sessionBroadcast.hook.ts](client/src/lib/sessionBroadcast.hook.ts) `useSessionBroadcast(sessionId, handler)` returns `{emitSessionBroadcast}`; subscribes on mount, releases on unmount, latest-handler-via-ref pattern so handler changes don't re-subscribe. Older browsers without BroadcastChannel get a no-op surface so callers don't need defensive checks. 7 new tests in [sessionBroadcast.vitest.test.ts](client/src/lib/sessionBroadcast.vitest.test.ts): peer subscriber receives emitted message; emitting tab does NOT receive its own echo (senderId filter); messages on different sessions do not cross-pollinate; each unique event kind round-trips correctly; release closes the channel; a throwing subscriber doesn't poison other subscribers; unsubscribe stops further deliveries. 393/393 client tests pass (44 vitest test files); client `tsc --noEmit` 53 errors (matches baseline). Convention added: **cross-tab session sync uses BroadcastChannel with per-call senderId.** New event kinds added in future waves (e.g. `pivot_state`, `chart_keyInsight`) extend the `SessionBroadcastEventKind` union; the hook + factory shape stays unchanged. Single-instance per browser only — cross-device sync requires server-side push (SSE / WebSocket), explicitly out of scope.

- **2026-05-15** — **Wave C3 · Optimistic-update rollback on filter-API error.** The `handleConditionsChange` flow in [ChatInterface.tsx](client/src/pages/Home/Components/ChatInterface.tsx) does an optimistic local `setActiveFilter` before the debounced PUT flies. Pre-C3 if the server rejected the PUT (network error, 5xx, validation fail), only a toast fired — the optimistic local state STAYED, diverging from the server (server kept the OLD filter, UI showed the NEW one). Subsequent pivot / chart queries would use the server's old filter version, displaying numbers that didn't match the chips. Fix: on PUT failure, `pushConditions` now calls `sessionsApi.getActiveFilter(sessionId)` to refetch the server's authoritative state and rolls back the optimistic local change. Same refetch pattern added to `handleClearAllFilters`. Refetch is itself best-effort; if it ALSO fails we leave the toast in place and the UI stays divergent until the next successful op (acceptable — the toast already warned the user). Refetch is chosen over "capture previous snapshot" because debounce + rapid user edits can stack multiple optimistic mutations before any single server error surfaces; the server is the only reliable source of truth in that case. The audit also flagged `DimensionHierarchiesBanner.handleRemove` for the same anti-pattern — verified by ground-truth read that the hierarchy code is ALREADY correct: it sets a spinner via `removingColumn` state, calls the API, and only calls `onChange(next)` AFTER the await resolves (line 58-59). The row stays visible during the await with a spinner. No change needed there. 386/386 client tests pass (no new tests — the rollback path needs a mocked-API integration test that's better suited to a future Playwright pass; the current vitest suite covers the schema contract). Client `tsc --noEmit` 53 errors (matches baseline; zero new errors). Convention added: **optimistic local state changes MUST have a rollback path**, even if the rollback is a refetch rather than a captured snapshot. New optimistic surfaces (delete dashboard, rename session, edit permanent context inline) should follow the same toast-then-refetch pattern on server error.

- **2026-05-15** — **Wave C2 · `isMountedRef` guards every SSE callback in `useHomeMutations`.** The Home component had P-017 unmount cleanup that aborted the chat fetch AbortController, but bytes already in flight on the wire can still trigger one or two more `onAgentEvent` / `onThinkingStep` / `onIntermediate` / `onResponse` callbacks BEFORE the abort surfaces in the stream reader. Those callbacks call setX setters on an unmounted component → React 18 warning, plus leaked work and stale promise chains. Fix: new `isMountedRef = useRef(true)` in [useHomeMutations.ts](client/src/pages/Home/modules/useHomeMutations.ts) flipped to `false` in the existing unmount-cleanup useEffect BEFORE the abort fires. Four long-lived SSE callbacks gain an early `if (!isMountedRef.current) return;` guard: `onThinkingStep`, `onAgentEvent` (handles every SSE event type — workbench, workbench_enriched, session_context_updated, business_actions, answer_chunk, thinking, flow_decision, sub_question_spawned, persist_status), `onIntermediate`, and `onResponse`. The guard at `onAgentEvent` is the most load-bearing — it covers ALL SSE event handlers in one early-return, so future event types added in the same handler inherit the unmount safety automatically. The streamChatRequest's other event entry points (`onDashboardCreated`, `onQueued`) are guarded by react-query's existing onSuccess/onError lifecycle (which handles unmount via internal flags). 386/386 client tests pass (43 vitest test files); client `tsc --noEmit` 53 errors (matches baseline; zero new errors). Convention added: **long-lived SSE callbacks in React components must guard setState calls with an `isMountedRef` early-return** — the standard React pattern (abort + ignore in finally) isn't enough when the callback fires from a streaming reader that's been queueing chunks. Future React surfaces that consume long-running streams (download progress, dashboard live-edit) should follow the same pattern.

- **2026-05-15** — **Wave C1 · `business_actions` SSE handler prefers exact-timestamp match over recency.** The client-side handler in [useHomeMutations.ts](client/src/pages/Home/modules/useHomeMutations.ts) for the post-verifier `business_actions` SSE event picked "the most recent assistant message" by recency. The server payload INCLUDES `messageTimestamp` for the message the items belong to but the client was ignoring it — the comment said "the user only has one turn in flight at a time" but that contract broke under: (a) user regenerates a message while the prior turn's BAI promise is still in flight (12s timeout window — BAI agent has plenty of headroom to resolve AFTER a regenerate); (b) user fires a fresh turn rapidly. In both cases items attached to the wrong message. Fix: new pure helper [client/src/lib/chat/findBusinessActionsTarget.ts](client/src/lib/chat/findBusinessActionsTarget.ts) `findBusinessActionsTargetIndex(messages, serverTs, toleranceMs=2000)` — Pass 1 looks for an assistant message whose `timestamp` matches `serverTs` within ±2000ms tolerance (absorbs the W38 streaming-narrator client/server clock divergence where each side independently calls `Date.now()`), iterating newest-first so the most-recent matching message wins under ties; Pass 2 falls back to "most-recent assistant" when no match exists or `serverTs` is null/non-finite. The inline matching logic in `useHomeMutations.ts` was extracted to the helper for testability. 12 new tests in [findBusinessActionsTarget.vitest.test.ts](client/src/lib/chat/findBusinessActionsTarget.vitest.test.ts): exact match wins over recency; ±2000ms tolerance honored; boundary inclusive; >2000ms drift falls to Pass 2; tied matches go to newest; null serverTs → Pass 2; non-finite serverTs → Pass 2; Pass 2 skips intermediate (planning) assistant messages; returns -1 with no eligible message; empty array returns -1; regenerate-mid-stream scenario attaches BAI to ORIGINAL message (timestamps >2s apart); rapid double-turn each BAI attaches to its own turn. **386/386 client tests pass** (43 vitest test files); client `tsc --noEmit` 53 errors (matches client baseline; zero new errors). Convention added: **client SSE event handlers must prefer server-shipped correlation IDs (timestamp / turnId / messageId) over positional heuristics when available** — recency is a fallback for missing IDs, never the primary strategy. Future SSE events (`pivot_update`, `session_context_updated`, business-actions retries) should follow the same pattern.

- **2026-05-15** — **Wave B7 · Narrator user-block always surfaces `userIntent`.** The narrator's user-block builder in [buildSynthesisContext.ts](server/lib/agents/runtime/buildSynthesisContext.ts) `buildUserBlock` rendered `permanentContext` and `suggestedFollowUps` but skipped `userIntent.verbatimNotes` and `userIntent.interpretedConstraints` (lives on `ctx.sessionAnalysisContext.userIntent`). Pre-B7 the narrator only saw these constraints when the hypothesis planner had encoded them in the blackboard; when the blackboard was empty (synthesis-fallback path) or thin (single-tool turn), the constraints were LOST — and the narrator could emit findings that violated user-stated rules ("always include Q4 metro stores", "exclude e-commerce", "MARICO/PARACHUTE only"). Fix: `buildUserBlock` now appends two new sections when present: "User-stated intent (verbatim from earlier turns):" + the `verbatimNotes` (capped 800 chars) and "User-stated constraints (interpreted from prior turns):" + the bulleted `interpretedConstraints` (top-8, each capped 200 chars). These flow through to BOTH the synthesis-context bundle's USER CONTEXT section AND the narrator's user prompt, regardless of blackboard state. Empty `verbatimNotes` / empty `interpretedConstraints` → no leak. 5 new tests in [tests/narratorUserIntentFallbackB7.test.ts](server/tests/narratorUserIntentFallbackB7.test.ts) appended to the explicit-file-list `test` script: verbatim-intent appears under labelled section; constraints appears as bulleted list; both together both present; absent userIntent → no leak; `formatSynthesisContextBundle` includes USER CONTEXT section with both fields when set. 5/5 pass; 47/47 across `narratorUserIntentFallbackB7`, `buildSynthesisContext`, `synthesisFallback`, `synthesisEnvelopeW8`, `narratorAgent`. **Full server suite: 2858/2858 pass** (cleared every adjacent regression). `npx tsc --noEmit` 98 errors (matches B6 baseline; zero new errors). Convention added: **the narrator's user block is the persistent surface for user-stated rules** — anything the user said in a prior turn (whether parsed into structured constraints or verbatim) gets surfaced regardless of whether the current turn's blackboard captured it. Future surfaces (deck planner via Wave B6, business actions via BAI agent) already had explicit userIntent threading; Wave B7 fills the gap at the narrator/synthesis-fallback layer where constraints are most load-bearing.

- **2026-05-15** — **Wave B6 · Deck planner sees user preferences + dimension hierarchies + wide-format shape + domain context.** The W-EXP-2 deck planner ([deckPlanner.ts](server/lib/agents/runtime/deckPlanner.ts)) builds a `SlideDeckPlan` from a saved Dashboard for the PPTX/PDF export pipeline. Pre-B6 it saw only the dashboard contents (charts, narrative blocks, answer envelope, business actions, captured filter). The dashboard may have been created on a PRIOR turn — by the time the user clicks "Download", they may have: added permanent context notes ("flag Q1 data quality in any export"), declared dimension hierarchies (BAI-style "FEMALE SHOWER GEL is a category total"), or uploaded a fresh wide-format file that's been melted. None of those would flow into the deck. Concrete failures: deck slides could refer to original wide-format column names ("Q3 2024 Value Sales") that no longer exist post-melt; recommendation slides could treat a rollup row as a peer competitor; exec-summary slides ignore user's standing notes. Fix: `DeckPlannerInputs` accepts 4 new optional fields — `permanentContext`, `domainContext`, `dimensionHierarchies`, `wideFormatShape`. `buildDeckPlannerUserPrompt` renders each as a labelled block (USER PREFERENCES / DIMENSION HIERARCHIES / DATASET SHAPE) with explicit guidance about how the planner should USE the block ("treat rollup values as category totals, NEVER as peers", "original wide column names NO LONGER EXIST — never reference"). [buildDashboardDeck.ts:buildAndVerifyDeckPlan](server/lib/exports/buildDashboardDeck.ts) does best-effort lookup of all four context fields from the session that owns the dashboard (via `dashboard.sessionId` → `getChatBySessionIdEfficient`): permanentContext + dimensionHierarchies + wideFormatTransform shape from the chat doc, domain context from the process-memoised `loadEnabledDomainContext()`. Failures wrap in try/catch with `agentLog` — export must always work even on dashboards from sessions that have been archived. 6 new tests in [tests/deckPlannerContextB6.test.ts](server/tests/deckPlannerContextB6.test.ts) appended to the explicit-file-list `test` script: USER PREFERENCES appears when permanentContext set; DIMENSION HIERARCHIES block appears with rollup vocabulary AND the "category totals" warning; DATASET SHAPE block appears with post-melt vocabulary AND the "NO LONGER EXIST" warning; DATASET SHAPE stays out when wideFormatShape.detected is false; absent blocks DO NOT leak; all three together produces a well-formed prompt with the closing "Compose the SlideDeckPlan" rule reminder still present. 6/6 pass; 38/38 across `deckPlannerWEXP2`, `deckPlannerContextB6`, `deckPlanVerifierWEXP3`, `exportGoldenFixtureWEXP13`. `npx tsc --noEmit` 98 errors (matches B5 baseline; zero new errors). Convention added: **deck export inherits session ambient context** — the dashboard is a snapshot at creation, but the deck reflects the user's CURRENT session state at export time. Future export agents (e.g., PDF slide notes, narrative regeneration) follow the same pattern: best-effort context lookup, swallow failures, always ship a deck.

- **2026-05-15** — **Wave B5 · Dataset profile (upload-time LLM) sees user notes + domain vocabulary.** WF8 added ambiguous-currency-symbol handling to the upload-time profile LLM call, but the LLM had no DOMAIN context to pick the right ISO code for symbols like `$` / `kr` / `¥`. A Marico-Vietnam dataset with `Region: "Off VN"` cells and `$` in the Sales column should override to VND — but without "Marico/FMCG/Vietnam" in the prompt, the LLM had nothing to ground on. Same for suggested questions: without permanent context ("treat budget as cost_cap_eur") or domain vocabulary (MAT / L12M / sub-brand cannibalisation), the suggested-questions list was generic. Fix: [datasetProfile.ts](server/lib/datasetProfile.ts) `inferDatasetProfile` accepts optional `permanentContext` (capped 800 chars) and `domainContext` (capped 2000 chars). Both are embedded in the user payload as labelled JSON fields (`userContext` and `domainContext`) so the LLM reads them as part of the same input doc. System prompt now references both: "userContext" (verbatim notes the user set on the session — apply when describing the dataset and choosing suggested questions; standing instructions like 'always exclude Central region' should NOT override the schema but SHOULD influence which questions you suggest), "domainContext" (FMCG / Marico domain vocabulary — use only to resolve metric/dimension names the dataset's column headers borrow from this vocabulary, NEVER to invent fields not present in "columns"). Upload-pipeline caller in [uploadQueue.ts](server/utils/uploadQueue.ts) resolves both: reads `permanentContext` from the existing chat doc (if any — handles re-uploads to sessions with prior context), and resolves `domainContext` via the process-memoised `loadEnabledDomainContext()` (near-free). Both lookups are wrapped in try/catch so a failure here can never abort the upload. 5 new tests in [tests/datasetProfileContextB5.test.ts](server/tests/datasetProfileContextB5.test.ts) (appended to the explicit-file-list `test` script): permanentContext appears as `userContext` field in LLM payload; domainContext appears; absent fields DO NOT leak; system prompt source-level check for both field names; backwards-compat without context. 5/5 pass; 15/15 across `datasetProfileContextB5`, `wideFormatPipeline`, `datasetProfileIndicatorEnrichment`. Tests pass `timeoutMs: 2000` so the runtime is ~2s instead of the 45s default. `npx tsc --noEmit` 98 errors (matches B4 baseline; zero new errors in datasetProfile.ts or uploadQueue.ts). Convention added: **upload-time LLM calls inherit the same per-session ambient context as the analysis-turn LLM calls** — when the user has standing notes or the system has loaded domain packs, even the upload "first look" benefits. Future similar upload-time agents (data-cleaning suggestion, anomaly preview) should follow the same pattern.

- **2026-05-15** — **Wave B4 · Mode classifier sees user notes + domain vocabulary + user intent.** The top-level mode classifier ([server/lib/agents/modeClassifier.ts](server/lib/agents/modeClassifier.ts)) routes every user message to one of `analysis | dataOps | modeling` — but pre-B4 it saw only the question + last 5 chat messages + the columns list. Ambiguous follow-ups ("yes do it", "use MAT for this") would surface-form regex-match and frequently mis-route: a session in mid-modeling-conversation would route a "yes" to dataOps if the most recent message happened to contain a dataOps keyword. Concrete failures the gap caused: "yes do it" after "Train a polynomial regression" might route to analysis when the user has `userIntent.verbatimNotes: "I'm building a model"`; "compute MAT for PARACHUTE" would route to dataOps (because of "compute") when the domain pack tells us MAT is a Moving-Annual-Total metric — that's analysis. Fix: `classifyMode` accepts a new optional fifth parameter `context?: ClassifyModeContext` with four optional fields: `permanentContext` (user's "always exclude Central" notes, capped 600 chars), `domainContext` (FMCG/Marico vocabulary, capped 1500 chars), `userIntentVerbatim` (current SAC's `userIntent.verbatimNotes`, capped 400), `userIntentConstraints` (current SAC's `userIntent.interpretedConstraints`, top-8 + 800-char cap). Each block is rendered as a labelled section in the user prompt (USER NOTES, DOMAIN VOCABULARY, USER INTENT) with explicit guidance about how to interpret it for routing. System prompt unchanged (byte-stable). Tight caps because mode classification is high-volume MINI-tier — prompt stays under ~3KB even with full context. Caller wiring: [chatStream.service.ts:1038](server/services/chat/chatStream.service.ts) resolves `domainContext` via `loadEnabledDomainContext()` (process-memoised; near-free) and threads `chatDocument.permanentContext`, `chatDocument.sessionAnalysisContext.userIntent.verbatimNotes`, and `chatDocument.sessionAnalysisContext.userIntent.interpretedConstraints` into the new context arg. The non-streaming `chat.service.ts` doesn't call `classifyMode` (verified by grep), so no parallel change needed. 6 new tests in [tests/modeClassifierContextB4.test.ts](server/tests/modeClassifierContextB4.test.ts) (new file, appended to the explicit-file-list `test` script): each of the four blocks appears in the LLM prompt when provided; absent blocks DO NOT leak (clean baseline); backwards-compat: omitting the context arg works (pre-B4 signature still valid); all four blocks together produces a well-formed prompt with correct block ordering. 6/6 pass; 7/7 across `modeClassifierContextB4` + `chatStreamModeContract` regression. `npx tsc --noEmit` 98 errors (matches B3 baseline; the pre-existing `spawnedQuestions` cast error at chatStream.service.ts line 1656 (was line 1630 pre-B4 edits — line shift is from my added code, not a new error) is unchanged). Convention added: **mode classification is now a contextual decision, not a pure surface-form match.** Future routers in this app (chart-mode, modeling-mode-detail, etc.) should follow the same pattern: accept an optional `context` arg, render each block with a brief usage note, keep the system prompt byte-stable.

- **2026-05-15** — **Wave B3 · Chart-insight `synthesisContext` audit + contract pin.** The audit flagged that `generateChartInsights` ([insightGenerator.ts](server/lib/insightGenerator.ts)) had an optional `synthesisContext` parameter that "most call sites pass undefined". Ground-truth verification: PVT1 already wired the chart-key-insight endpoint ([sessionController.ts](server/controllers/sessionController.ts)), and the agent-turn hot path (`enrichCharts` in [chatResponse.service.ts:88-95](server/services/chat/chatResponse.service.ts)) was ALREADY passing the full context bundle (userQuestion + sessionAnalysisContext + permanentContext + domainContext) from both `chat.service.ts:200-210` and `chatStream.service.ts:1265-1276`. The audit was based on a stale read; the live paths are correct. Remaining unfixed callers: (a) [analyticalChartSpec.ts:136](server/lib/analyticalChartSpec.ts) `mergeDeterministicAnalyticalCharts` — verified DEAD (imported in `dataAnalyzer.ts` but no live call site invokes it); Wave B3 added an optional `synthesisContext` parameter so a future re-wiring inherits the contract automatically. (b) [dataAnalyzer.ts:183, 324](server/lib/dataAnalyzer.ts) — upload-time first-look chart insights; no user question / SAC available yet because enrichment hasn't completed (acceptable to pass undefined). Wave B3 deliverables: (1) forward-compat wiring in `mergeDeterministicAnalyticalCharts` (signature accepts `synthesisContext?: ChartInsightSynthesisContext`, body forwards it positionally to `generateChartInsights`); (2) audit-documenting docstring at the top of `analyticalChartSpec.ts` enumerating live vs upload-time vs dead callers and the contract for each; (3) new [tests/chartInsightsSynthesisContextB3.test.ts](server/tests/chartInsightsSynthesisContextB3.test.ts) with 5 source-level pins — chatStream.service / chat.service `enrichCharts` callsites both pass all four context fields; sessionController PVT1 contract holds (userQuestion + sessionAnalysisContext); correlationAnalyzer forwards `synthesisContext`; `mergeDeterministicAnalyticalCharts` signature includes the optional param + the internal `generateChartInsights` call forwards it. 5/5 pass; 20/20 across `insightGeneratorBusinessCommentaryW12`, `chartInsightsSynthesisContextB3`, `insightGeneratorSeriesDimension`, `chartKeyInsightSerialisation`. `npx tsc --noEmit` 98 errors (matches B2 baseline; zero new errors). Convention added/refined: **the `synthesisContext` parameter is a contract that all LIVE chart-insight call sites must honor** — the new contract test makes any future regression (a new caller forgetting to pass it, or an existing caller stripping it) into a CI failure at the source-level inspection layer.

- **2026-05-15** — **Wave B2 · Business actions agent — dimension hierarchies + wide-format shape.** The post-verifier business-actions agent ([businessActionsAgent.ts](server/lib/agents/runtime/businessActionsAgent.ts)) saw `userIntent` + `permanentContext` + `domainContext` + `priorInvestigations` + the envelope, but was BLIND to two load-bearing data-shape blocks: `dimensionHierarchies` (H1+ — user-declared rollup rows like "FEMALE SHOWER GEL" being the category total in the Marico-VN dataset) and `wideFormatTransform` shape block (WPF1 — when the dataset arrived wide and was melted at upload time). Concrete failures: without hierarchies, BAI could recommend "deep dive on FEMALE SHOWER GEL vs MARICO" — treating the rollup AS a competing brand instead of recognizing it as the category whole; without wide-format shape, BAI could reference original column names ("Q3 2024 Value Sales") that no longer exist post-melt, or recommend SUM(Value) without scoping by Metric on compound-shape datasets. Fix: `buildUserMessage` in businessActionsAgent.ts appends `formatDimensionHierarchiesBlock(ctx)` and `formatWideFormatShapeBlock(ctx.summary)` after the existing USER NOTES section. Both helpers return `""` when the corresponding context is absent — gracefully no-ops. System prompt got two new NEVER rules: (1) "Treat a dimension's rollup-row as a peer in any action — actions on that dimension MUST treat the rollup as the category whole." (2) "Reference original wide-format column names — the post-melt column names are Period / PeriodIso / Value / Metric; the original Q1/Q2/Q3 columns no longer exist." System prompt remains byte-stable (the rules are static text). 4 new tests in [tests/businessActionsAgent.test.ts](server/tests/businessActionsAgent.test.ts) appended to the existing suite: DIMENSION HIERARCHIES block appears when ctx has declared rollups; DATASET SHAPE block appears when dataset was wide-format melted; absent blocks DO NOT leak (clean baseline); source-level check that the system prompt warns NEVER to treat rollup as a peer and NEVER to reference wide-format column names. 12/12 pass on the agent test (was 8 → +4); 23/23 across `businessActionsAgent`, `businessActionsHints`, `agentLoopBusinessActionsIntegration`. `npx tsc --noEmit` 98 errors (matches B1 baseline). Convention added: **all post-verifier sub-agents inherit the same data-shape context blocks as the synthesis surface** — `formatDimensionHierarchiesBlock` + `formatWideFormatShapeBlock` are the contract; future sub-agents (e.g., follow-up question generation, what-if scenarios) must thread both blocks or risk recommending against rollup peers / referencing dead column names.

- **2026-05-15** — **Wave B1 · Quick-lookup planner full context.** The QL1 fast path's planner ([quickAnswerPlanner.ts](server/lib/agents/runtime/quickAnswerPlanner.ts)) was blind to four standardized context blocks that the FULL planner sees: `permanentContext` (user free-text notes from the "Add additional context" surface), `domainContext` (FMCG/Marico domain packs hoisted per-turn into `ctx.domainContext` at W34), `priorInvestigations` (W21 digest of last-N answered questions on the session), and `userIntent` (verbatim notes + interpreted constraints from `ctx.sessionAnalysisContext.userIntent`). Concrete failures the gap caused: a session with `permanentContext: "always exclude Central region"` would have QL1 return all-region rollups; a session that already asked "show brand-level Marico" and now asks "what's our top product" would have QL1 re-emit a SKU-level breakdown instead of seeing the prior; VND/MAT/L12M jargon from domain packs couldn't be resolved without re-asking. Fix: `buildUserPrompt` now calls the existing `formatUserAndSessionJsonBlocks(ctx, {maxUserChars: 800, maxJsonChars: 1500, maxDomainChars: 2000})` helper from [context.ts](server/lib/agents/runtime/context.ts) — same helper the full planner / reflector use, just with tighter caps because QL1 is MINI-tier and we want the prompt under ~5KB. The helper composes permanentContext, domainContext, and prior-investigations into one block; appended to the user prompt right after the existing active-filter block, keeping the schema + wide-format + hierarchy + filter blocks at the top of the prompt (for cache-friendly prefix stability across turns where only the new block changes). System prompt also got 3 new bullet-rules instructing the planner how to USE the new blocks ("Honour the User-provided notes block ...", "Honour the Domain knowledge block ...", "Honour the Prior investigations block ..."). System prompt remains byte-stable. 5 new tests in [tests/quickAnswerPlanner.test.ts](server/tests/quickAnswerPlanner.test.ts) appended to the existing Wave QL1 suite: permanentContext appears in user prompt; domainContext appears; prior-investigations appears; absent blocks DO NOT leak (clean baseline); source-level check that the system prompt references the three new rules. 11/11 pass on the planner test (was 6 → +5); 76/76 across the full quick-answer regression sweep (`quickAnswerDetector`, `quickAnswerPathE2E`, `quickAnswerWideFormatGuard`, `quickAnswerFollowUps`). `npx tsc --noEmit` 98 errors (matches A7 baseline; zero new errors). Token budget: full-context user prompt is ~3-5KB vs pre-B1's ~1.5-2KB — still well within the MINI tier's prompt-cache window. Convention added: **fast-path agents inherit the SAME context-block contract as the full agent loop.** Future QL-style fast paths (e.g., a "quick chart" or "quick summary" path) must call `formatUserAndSessionJsonBlocks` with appropriate caps so user intent isn't silently lost.

- **2026-05-15** — **Wave A7 · `domainContext` cache atomicity (generation counter).** Closed a stale-data race in the process-memoised domain-context loader. Pre-A7 [loadEnabledDomainContext.ts](server/lib/domainContext/loadEnabledDomainContext.ts) held `cache: Promise<CacheEntry> | null`. The race: LLM call A awaits, `cache` is null so `build()` starts and `cache := <new promise>`. A yields on the `getToggleOverrides()` network round-trip. Admin PATCH toggle fires `invalidateDomainContextCache()` which sets `cache = null`. LLM call B starts, sees `cache` is null, builds a FRESH promise with the new toggles, awaits it, gets NEW context. LLM call A's promise eventually resolves with the OLD pre-toggle data. Net effect: two LLM calls in the same turn (or close-spaced turns) see DIFFERENT domain context across an admin toggle. Fix: tag every in-flight build with a `cacheGeneration` counter at spawn. `invalidateDomainContextCache()` bumps the counter AND sets cache to null. When a build resolves, it compares the counter — if bumped, discard the result and recursively call `loadEnabledDomainContext()` to rebuild against the now-current overrides. Bounded recursion (in the worst case, a chain of admin toggles produces a chain of rebuilds, but each rebuild eventually wins because Node is single-threaded between awaits). 4 new tests in [tests/domainContextCacheAtomicityA7.test.ts](server/tests/domainContextCacheAtomicityA7.test.ts) (appended to the explicit-file-list `test` script): invalidate increments the generation by 1 (twice, asserting +2); after invalidate, the next call returns a fresh build with the same content (idempotent in test env); concurrent callers without invalidate share the cache entry; invalidate FIRED BETWEEN two awaiting callers — both resolve to fresh data without orphaning. New test seam `__domainContextGenerationForTesting`. 4/4 pass; 19/19 on adjacent regression sweep (`domainContextLoader`, `domainContextWiring`, `adminDomainContextRoute`). `npx tsc --noEmit` 98 errors (matches A6 baseline). Convention added: **process-memoised caches with admin-driven invalidation MUST use a generation counter, not a single nullable promise.** Future caches with the same shape (e.g., feature-flag overrides, env-derived config) should follow this pattern. Single-instance correctness only — multi-instance scaling would need an external watcher (Cosmos change feed, Redis pub/sub) to propagate invalidates across processes; explicitly out of scope.

- **2026-05-15** — **Wave A6 · RAG indexing snapshots `dataVersion` at spawn + targets `ragIndex` writes through the unified lock.** Closed a stale-snapshot race in the fire-and-forget RAG indexing pipeline. Pre-A6 `indexSessionRag` ([server/lib/rag/indexSession.ts](server/lib/rag/indexSession.ts)) did: (1) read chat doc, (2) call `updateChatDocument(doc)` to set `ragIndex.status = "indexing"` (full-doc upsert from stale snapshot), (3) embed for ~3s (network call), (4) `updateChatDocument(doc)` again with the final status — clobbering any concurrent upload / chat-turn writes that happened to the same doc during the embedding phase. Plus a secondary concern: the final `dataVersion` written into `ragIndex.dataVersion` was read from the same stale snapshot, so embeddings could end up tagged with a `dataVersion` that didn't match the data actually sampled (race between read at step 1 and re-read at step 4). Fix: new internal helper `updateRagIndexField(sessionId, patch)` acquires `withSessionWriteLock` (Wave A2 unified map), re-fetches the doc inside the lock, sets ONLY the `ragIndex` field via shallow merge, and writes. The version is captured ONCE immediately after the initial read (line 91: `const ver = dataVersion(doc)`) and reused in the embedding loop + the final patch — so embeddings are guaranteed to be tagged with the version of the data we actually sampled, never a later version that bumped mid-flight. All three call sites (mid-flight `indexing`, terminal `ready`, error `error`) go through the helper. Concurrent writes to `messages[]`, `sessionAnalysisContext`, `dataSummary`, etc. are preserved because we only touch `ragIndex`. `upsertUserContextChunk` later in the same file only writes to AI Search (not Cosmos) so it's already safe — verified, no change. 4 new tests in [tests/ragIndexingLockA6.test.ts](server/tests/ragIndexingLockA6.test.ts) (appended to the explicit-file-list `test` script): indexSession.ts imports `withSessionWriteLock`; `updateRagIndexField` exists with the lock + re-fetch pattern; `indexSessionRag` captures `ver` via `dataVersion(doc)` BEFORE calling `embedTexts` (positional check on source string); `indexSessionRag` uses `updateRagIndexField` ≥ 2× and contains NO direct `await updateChatDocument(` calls in its body. 4/4 pass; 9/9 on adjacent regression sweep (`chunking`, `ragRetrieve`, `ragIndexingLockA6`). `npx tsc --noEmit` 98 errors (matches A5 baseline; zero new errors in indexSession.ts). Convention pinned: **RAG indexing — and any other long-running fire-and-forget Cosmos updater that's NOT in the synchronous request path — MUST capture state at spawn time and write only the field it owns through the unified lock.** Future similar agents (e.g., past-analysis cleanup, scheduled reindex jobs) should follow the `updateRagIndexField` pattern: lock + re-fetch + shallow-merge a single named field. Out of scope for this wave (recorded so future-Claude doesn't reopen): RAG indexing currently DELETES old chunks before upserting new ones (line 98); two concurrent reindexes could both delete and both upsert with the same `ver`, wasting embedding cost but never corrupting state. A future cleanup wave could add an in-flight reindex flag.

- **2026-05-15** — **Wave A5 · `sessionDocCache` contract pinned (audit's race was a false positive).** Investigated the audit finding that `sessionDocCache` (in-process TTL cache in [chat.model.ts:25](server/models/chat.model.ts)) was "not invalidated on hierarchy / permanent-context updates" — supposedly a HIGH-severity race where a freshly-declared hierarchy would be invisible to the next turn for up to 5 minutes. Ground truth: the cache TTL is 5 SECONDS (the auditor misread `5_000` as 5 min), AND every write path correctly delegates to `updateChatDocument` which REPOPULATES the cache with the freshly-written resource on line 646 of chat.model.ts. The "race" doesn't exist. Verified via grep: only ONE `items.upsert` call on the chat container in the entire server tree (inside `updateChatDocument`), and `waitForContainer` (the chat-container resolver) is not imported anywhere outside `chat.model.ts`. All session-write paths (`sessionAnalysisContext.ts`, `activeFilterController.ts`, `patchAssistantBusinessActions.ts`, `dataPersistence.ts`) route through `updateChatDocument` and inherit cache freshness automatically. Wave A5 deliverables: (a) load-bearing doc comment on `updateChatDocument` explaining the cache contract — future code must NOT call `containerInstance.items.upsert(chatDoc)` directly because the cache would hand out stale state for up to 5s after such a bypass-write. (b) new [tests/sessionDocCacheContractA5.test.ts](server/tests/sessionDocCacheContractA5.test.ts) with 3 contract tests: chat.model.ts contains exactly ONE `items.upsert` in code (excluding doc comments) AND it must use the `chatDocument` variable; no code outside chat.model.ts imports `waitForContainer` (the chat-container resolver stays module-private); `updateChatDocument` writes to `sessionDocCache.set(result.sessionId, ...)` before returning. 3/3 pass. `npx tsc --noEmit` 98 errors (matches A4 baseline; the 5 pre-existing `chat.model.ts` "No overload matches this call" errors at lines 573/969/1053/1483/1623 are stale Cosmos SDK type annotations unchanged by this wave). Convention pinned: **all chat-document writes flow through `updateChatDocument`; the cache update is part of the write atomically.** Any future code that tries to write to the chat container directly will trip these tests. The audit's finding was a false positive driven by a misreading of the TTL constant and missing the cache repopulation in `updateChatDocument`'s body — a useful lesson that audit summaries should always be ground-truthed against the live code (this lesson was hard-earned during waves A1 + A5 specifically).

- **2026-05-15** — **Wave A4 · `saveModifiedData` acquires the unified per-session write lock.** Closed the race the audit specifically called out: a `add_computed_columns(persistToSession:true)` tool call (or any other dataOps persist — there are dozens of `saveModifiedData` callsites in [dataOpsOrchestrator.ts](server/lib/dataOps/dataOpsOrchestrator.ts)) could race against turn-end `persistMergeAssistantSessionContext` because both did `getChatBySessionIdEfficient → mutate → updateChatDocument` against the same Cosmos chat doc with no shared lock. Last-writer-wins on the upsert meant a turn-end persist following a computed-column write could silently drop the new column from `dataSummary.columns` (and similarly drop session context updates if the persist order was reversed). Fix: [dataPersistence.ts](server/lib/dataOps/dataPersistence.ts) imports `withSessionWriteLock` from the unified Wave-A2 module; the public `saveModifiedData` now delegates to a private `saveModifiedDataLocked` inside the lock. Every dataOps callsite (computed-column persist, remove_nulls, filter, sort, dedupe, etc — all the orchestrator paths that ultimately call `saveModifiedData`) inherits the lock for free. Same lock map as A2, so dataOps writes serialise against assistant-merge persist + BAI patch + active-filter PUT/DELETE — closing every cross-write-path race window. 4 new tests in [tests/computedColumnPersistLockA4.test.ts](server/tests/computedColumnPersistLockA4.test.ts) (appended to the explicit-file-list `test` script): source-level inspection that dataPersistence.ts imports `withSessionWriteLock` from the unified module; that `saveModifiedData` wraps its body in `withSessionWriteLock(sessionId, ...)`; that the locked-body helper `saveModifiedDataLocked` is non-exported (so future code can't bypass the lock by reaching internals); per-session isolation behavioural sanity (two distinct sessions hold INDEPENDENT lock entries — proves A4 uses A2's primitive). Source-level assertions chosen to avoid 30s Cosmos timeouts in tests; the lock primitive's own contracts are exhaustively pinned in [sessionWriteLockA2.test.ts](server/tests/sessionWriteLockA2.test.ts). 31/31 pass across the new test + adjacent regression sweep (`sessionWriteLockA2`, `pastAnalysisDocLockA3`, `computedColumns`, `addComputedColumnsDatetimeConcat`). `npx tsc --noEmit` 98 errors (matches A3 baseline; zero new errors in dataPersistence.ts). Out of scope, recorded as a follow-up: callers in `dataOpsOrchestrator.ts` still pass a pre-fetched `sessionDoc` parameter that may be stale by the time the lock is acquired. The lock guarantees write SERIALISATION, but not freshness of the writer's view; future cleanup wave should re-fetch inside the lock and copy any caller-mutated fields onto the fresh doc. For Wave A4 the minimal fix is just the lock — concurrent fields rarely overlap in practice, and the analytical-correctness bug (computed columns disappearing under cross-path race) is closed.

- **2026-05-15** — **Wave A3 · `PastAnalysisDoc` per-doc write lock.** Closed a silent-data-loss race in the cache mirror layer. Pre-A3 the chat stream fired three independent fire-and-forget writes to the same `past_analyses` Cosmos document (id = `${sessionId}__${turnId}`): (1) `upsertPastAnalysisDoc` — initial doc body, fired from `maybeWritePastAnalysisDoc` at [chatStream.service.ts:327](server/services/chat/chatStream.service.ts); (2) `patchPastAnalysisBusinessActions` — RMW chain (`get → modify whole doc → upsert`) firing later when the post-verifier `businessActionsPromise` resolved; (3) `patchPastAnalysisPivotArtifacts` — RMW chain firing inside the same `.then` branch as the initial upsert. All three issued FULL-document upserts; if patch (2) read state X and patch (3) read state Y (== X + businessActions) but patch (2) wrote X back AFTER patch (3), the pivotArtifacts field disappeared. Symmetric collision dropped businessActions. Result: cache mirror ended up with at most one of the three fields when patches raced. Plus a fourth race: `setPastAnalysisFeedback` uses Cosmos PATCH (sets `/feedbackDetails`, `/feedback`, `/feedbackReasons`, `/feedbackComment`) but a concurrent BAI/pivot upsert writes the WHOLE doc and clobbers the just-PATCHed feedback. Fix: new file-private `pastAnalysisDocChain = new Map<string, Promise<unknown>>()` in [pastAnalysis.model.ts](server/models/pastAnalysis.model.ts) with `withPastAnalysisDocLock(docId, fn)`; all four writers acquire it. Recursion is avoided via a new internal `upsertPastAnalysisDocUnlocked` helper that the patch wrappers call (the public `upsertPastAnalysisDoc` locks then delegates to it). Different docs (different turns / sessions) still parallelise — this lock is specifically about the four concurrent writers for the SAME doc. Scope decision: **NOT** the session-level `withSessionWriteLock` (Wave A2) because this cache-mirror should never block the live response path's chat-doc writes; the chat-doc lock and the past-analyses lock are orthogonal (different Cosmos containers). 6 new tests in [tests/pastAnalysisDocLockA3.test.ts](server/tests/pastAnalysisDocLockA3.test.ts) (appended to the explicit-file-list `test` script): size counter / reset seam; the four locked writers exist as async fns; patch helpers no-op early on empty input WITHOUT acquiring the lock (so unrelated work doesn't accidentally serialise on garbage); schema-validation rejection short-circuits before lock acquisition (invalid input shouldn't take a lock). 44/44 pass across the new test + adjacent regression sweep (`pastAnalysisModel`, `pastAnalysisAmr2Capture`, `pastAnalysisAmr3PivotArtifact`, `feedbackController`, `businessActionsAgent`, `agentLoopBusinessActionsIntegration`). `npx tsc --noEmit` 98 errors (matches A2 baseline; 2 pre-existing `enableCrossPartitionQuery` errors at lines 422/481 of pastAnalysis.model.ts unchanged — they're stale Cosmos SDK type annotations from before A3, not regressions). Convention added: **all RMW writes to a `PastAnalysisDoc` MUST acquire the per-doc lock** via the public surface (the four exported functions). New writers added in the future should follow the same pattern: pass `id = ${sessionId}__${turnId}` to `withPastAnalysisDocLock`, call `upsertPastAnalysisDocUnlocked` inside the locked body to avoid recursion.

- **2026-05-15** — **Wave A2 · Unified per-session Cosmos write mutex via `withSessionWriteLock`.** Closed a structural concurrency gap discovered during the system-wide race-condition audit. Pre-A2 the codebase had THREE independent `Map<sessionId, Promise<unknown>>` maps that each serialised their own call site only: `sessionPersistChain` in [sessionAnalysisContext.ts](server/lib/sessionAnalysisContext.ts) (covered `persistMergeAssistantSessionContext`, `persistMidTurnAssistantSessionContext`, `extractAndPersistUserHierarchies`, `updateSessionDimensionHierarchies`, `updateSessionSchemaAnnotations`); `sessionPatchChain` in [patchAssistantBusinessActions.ts](server/lib/patchAssistantBusinessActions.ts) (covered the post-verifier BAI patch); `activeFilterLocks` in [activeFilterController.ts](server/controllers/activeFilterController.ts) (covered FA1 PUT/DELETE). All three paths called `getChatBySessionIdForUser → mutate → updateChatDocument` against the SAME Cosmos document but did not coordinate with each other. Whenever a turn's `businessActionsPromise` outlived the `response` SSE event (BAI's whole architecture), its eventual patch could RMW concurrently with a fresh turn's `persistMergeAssistantSessionContext` and silently corrupt `messages[]` (BAI patch reads stale messages, target message moves because turn B already appended new ones, BAI patch upserts the stale messages back as last-write-wins). Same race window opened between active-filter PUT and any concurrent turn-end persist. Fix: new [server/lib/sessionWriteLock.ts](server/lib/sessionWriteLock.ts) exporting `withSessionWriteLock<T>(sessionId, fn): Promise<T>` that holds ONE per-session promise chain shared across every Cosmos-facing RMW. All five callsites in `sessionAnalysisContext.ts`, the BAI patch, and the active-filter PUT/DELETE migrated to it. The `__resetActiveFilterLocksForTests` test seam now drains the unified chain via the new `__resetSessionWriteChainForTesting` export. `materializeLocks` in `ensureSessionDuckdbMaterialized.ts` deliberately stays separate (different concern: DuckDB table create/replace, not Cosmos writes; those paths don't touch the chat doc). 6 new tests in [tests/sessionWriteLockA2.test.ts](server/tests/sessionWriteLockA2.test.ts) (appended to the explicit-file-list `test` script per CLAUDE.md): FIFO serialisation across 3 concurrent calls on the same session; return value passthrough; failure isolation (a throwing fn rejects to its caller but the next caller still runs); per-session isolation (writes on different sessionIds run concurrently — no head-of-line blocking); the migrated callers all share one map (proven by SYMBOL identity + cross-module size assertion); `__sessionWriteChainSizeForTesting` reports active locks. 53/53 pass across the new test + adjacent regression sweep (`optimisticConcurrencyW40`, `sessionAnalysisContextAssistantMerge`, `sessionAnalysisContextMergeHierarchy`, `putSessionHierarchies`, `businessActionsAgent`, `applyActiveFilter`, `buildActiveFilterSql`, `agentLoopBusinessActionsIntegration`). `npx tsc --noEmit` 98 errors (matches Wave A1 baseline; the 3 pre-existing `sessionAnalysisContext.ts` "Two different types with this name exist, but they are unrelated" errors at lines 131/204/415 are unchanged — they're from the client+server parallel `SessionAnalysisContext` definitions). Convention added: **the unified `withSessionWriteLock` is THE per-session Cosmos write mutex.** Future code that does any RMW on `ChatDocument` from outside an existing locked path MUST acquire it. Single-instance correctness only — multi-instance horizontal scaling still needs Cosmos `ifMatch` ETag or external lock (recorded as a future concern).

- **2026-05-15** — **Wave A1 · Question cache (exact + semantic) is filter-aware.** Closed an analytical-correctness bug discovered during a system-wide race-condition audit: the LLM-answer cache (`tryExactQuestionCacheHit` / `trySemanticQuestionCacheHit` in [server/lib/cache/questionCacheLookup.ts](server/lib/cache/questionCacheLookup.ts) and the cache writer `maybeWritePastAnalysisDoc` in [chatStream.service.ts](server/services/chat/chatStream.service.ts)) was keyed only on `(sessionId, dataVersion, normalizedQuestion)`. After Wave-FA1 introduced the non-destructive active-filter overlay, this meant: (a) a user with a filter applied could hit a cached answer that was actually computed against unfiltered data, silently serving wrong numbers; (b) a user without a filter could hit a cached answer that was actually computed against a filter (cache pollution). Fix: new optional `activeFilterConditionCount?: number` on `CacheLookupInput`; both lookup functions return `null` early when count > 0, and `maybeWritePastAnalysisDoc` now skips its fire-and-forget upsert when the chat doc has any active-filter conditions. Threaded from the two call sites in `chatStream.service.ts` reading `chatDocument.activeFilter?.conditions?.length ?? 0`. Cache benefit lost for filter+question repeats — acceptable because filtered analysis is exploratory and the cache is for stable repeated questions; the cache stays warm for the unfiltered baseline. Pivot cache (`pivotQueryService.ts`) was already correct — Wave-FA2 included `activeFilterVersion` as one of the inputs to `stableHashJson` for the cache key's `configHash` component; the audit that flagged it was a false positive (the auditor read the outer template literal `${sessionId}_${dataVersion}_${configHash}` and missed that filter version was in the hashed inputs). 3 new tests in [tests/questionCacheLookup.test.ts](server/tests/questionCacheLookup.test.ts) (8 → 11 cases): exact-cache gates on count > 0, semantic-cache gates on count > 0, count=0 / undefined behaves identically to absent (pre-A1 behavior). 11/11 pass; broader chat-stream regression sweep across `chatStreamModeContract`, `chatStreamScalarPivot`, `pastAnalysisAmr2Capture`, `normalizeQuestion`, `sessionContextUpdatedSseW31` 37/37 pass; `npx tsc --noEmit` 98 errors (1 fewer than baseline 99 — line-shift coincidence; zero new errors in any touched file). Out of scope (deferred): persisting `activeFilterVersion` on the past_analyses doc shape so the cache CAN serve repeat-question hits under the same filter — would need a `PastAnalysisDoc` schema extension + reindex of existing rows; option (a) the user can authorize separately if cache hit-rate matters more than the simplicity of "active filter ⇒ no cache."

- **2026-05-12** — **Wave QL1 · Quick-lookup fast path.** Closes the user-reported "1.5min for a top-10 lookup" latency wart. Simple lookup questions ("top 10 states by sales", "list customers in Texas", "how many orders last month", "average price by category") now bypass the full hypothesis → brief → planner → reflector → narrator → verifier pipeline (5-7 sequential LLM calls, ~60-120s) with one Mini-tier LLM call + one DuckDB query and return in ~2-3s. Output is the preview table + 3 deterministic "want to analyze further?" follow-up chips. Clicking a chip submits a fresh turn that flows through the full agentic loop unchanged.
  - **Detection.** New [quickAnswerDetector.ts](server/lib/agents/runtime/quickAnswerDetector.ts) — pure regex/heuristic, no LLM. Gates: question length ≤ 140 chars, opens with a lookup verb (top/bottom/highest/lowest/list/show/how many/count/sum/total/average/avg/mean/latest/most recent/which N), and contains NO analytical-intent keyword (why/driver/cause/compare/vs/correlation/trend/over time/breakdown/decompose/explain/forecast/predict/optimize/redistribute/scenario/what if/dashboard/recommend/should we/seasonal/attribution/mmm/regression/hypothesis/deep dive/analyze/root cause) AND no multi-part conjunction (`and why`, `and tell me`, `and explain`, etc.). False-positive rate near zero by construction: "Top 10 states **and why they grew**" → rejected; "Top 10 states" → accepted.
  - **Plan generation.** New [quickAnswerPlanner.ts](server/lib/agents/runtime/quickAnswerPlanner.ts) — single `completeJson` call with the new `LLM_PURPOSE.QUICK_LOOKUP_PLANNER` (MINI tier, default `gpt-4o-mini` / Haiku via the existing `OPENAI_MODEL_FOR_<PURPOSE>` env pattern). System prompt is byte-stable (~1.6KB, prompt-cache eligible across turns) and encodes the 8-rule lookup-plan grammar. User prompt carries a slim schema (max 60 columns, max 8 top values per dimension), the WPF1 wide-format shape block via `formatWideFormatShapeBlock`, the H4 dimension-hierarchies block via `formatDimensionHierarchiesBlock`, and the FA1 active-filter summary. Output validated by a new local `quickLookupPlanResponseSchema = { plan: queryPlanBodySchema, questionRestated: string }`. `completeJson` owns the 3-attempt repair loop; planner returns null on terminal failure.
  - **Deterministic post-pass.** The fast path reuses the existing planner-side repairs — [`injectRollupExcludeFilters`](server/lib/agents/runtime/planArgRepairs.ts) (H3 — auto-excludes user-declared rollup rows from peer aggregations) and [`injectCompoundShapeMetricGuard`](server/lib/agents/runtime/planArgRepairs.ts) (WPF2 — adds a Metric filter on compound-shape wide-format datasets) — so wide-format and hierarchy invariants are honoured for free. No new repair logic is introduced; the existing contract is the contract.
  - **Execution.** Reuses [`executeQueryPlanOnDuckDb`](server/lib/queryPlanDuckdbExecutor.ts) (preferred, inherits FA2 `data_filtered` view + WPF3 PeriodIso ORDER BY + hidden-column stripping) with in-memory `executeQueryPlan` fallback when DuckDB isn't materialised. Zero-row results fall through to the full agent loop (often signals planner-mini misread the schema).
  - **Response composition.** New [quickAnswerPath.ts](server/lib/agents/runtime/quickAnswerPath.ts) orchestrates detect → plan → repairs → validate → execute → compose. Emits `mode`, `thinking` (drafting / running), `plan`, `tool_call`, `tool_result`, `response` SSE events — same skeleton the full loop emits for a one-step analytical turn. Does NOT emit `answer_chunk` (no streaming narrator), `business_actions` (BAI agent off for fast path), or `agent_workbench` enrichment. The `AgentLoopResult` carries `answer: ""` (table is the answer; AnswerCard's `if (!env) return null` cleanly omits the structured envelope), `table: rows[0..200]`, `agentTrace.steps: [execute_query_plan]` (so `derivePivotDefaultsFromExecution` in chatStream picks up the pivot shape for free), and `followUpPrompts: 3 strings`. ThinkingPanel / InvestigationSummaryCard / BusinessActionsCard / StreamingPreviewCard all render-gate on their data field — verified that none mount on the fast-path output. Zero client-side changes.
  - **Follow-ups.** New [quickAnswerFollowUps.ts](server/lib/agents/runtime/quickAnswerFollowUps.ts) — pure function, no LLM. Templates filled from the executed plan's groupBy + measure + top-row value. Ranking shape: (a) "What's driving the gap between top and bottom <dim>?", (b) "How has <topValue>'s <measure> trended over time?" (date-gated; only when `dataSummary.dateColumns.length > 0`), (c) "Which <otherDim> contributes most to <topValue>'s <measure>?" (where `<otherDim>` is picked from remaining dimensions by `topValues.length` cardinality proxy, low-cardinality first). Aggregate shape: distribution + over-time (date-gated) + top-N. Filter-projection shape: drivers + entity-max + over-time. Falls back to [`suggestedFollowUpsFromDataSummary`](server/lib/suggestedFollowUpsFromSummary.ts) to top up to 3 when templates can't produce enough (single-dim dataset, etc.). Always capped at 3, duplicates removed.
  - **Wiring.** Single call to `tryQuickAnswer({ ctx, turnId, onLlmCall, safeEmit })` at the very top of `runAgentTurn` in [agentLoop.service.ts:1180](server/lib/agents/runtime/agentLoop.service.ts) — after the turn-id and trace setup but BEFORE pre-planning. Non-null result early-returns. Any throw from the fast path is caught, logged via `agentLog("quick_lookup.path_threw")`, and the request flows through the existing pre-planner/planner/etc. — failure isolation is total.
  - **Feature flag.** `QUICK_LOOKUP_ENABLED=true` (default ON, documented in [server/.env.example](server/.env.example)). Set to `"false"` to force every question through the full loop (rollback). Per-purpose model override: `OPENAI_MODEL_FOR_QUICK_LOOKUP_PLANNER=` (defaults to the MINI tier).
  - **LLM routing.** New `LLM_PURPOSE.QUICK_LOOKUP_PLANNER` enum entry in [llmCallPurpose.ts](server/lib/agents/runtime/llmCallPurpose.ts) (MINI category). Pinned in [tests/llmRoutingRegression.test.ts](server/tests/llmRoutingRegression.test.ts) `EXPECTED_CATEGORY` — guarantees a future model swap doesn't silently route the planner to Opus.
  - **Tests.** 5 new files, all appended to the explicit-file-list `test` script in [server/package.json](server/package.json):
    1. [`tests/quickAnswerDetector.test.ts`](server/tests/quickAnswerDetector.test.ts) — 54 cases pinning the regex contract: 17 accept cases, 25 analytical rejects, 6 multi-part rejects, length budget, empty/non-string input, and the env-flag kill switch.
    2. [`tests/quickAnswerFollowUps.test.ts`](server/tests/quickAnswerFollowUps.test.ts) — 5 cases pinning ranking-with-dates, ranking-without-dates (trend gated out), aggregate-with-distribution, duplicate filtering, and single-dim fallback.
    3. [`tests/quickAnswerPlanner.test.ts`](server/tests/quickAnswerPlanner.test.ts) — 6 cases via W18 LLM stub: happy path, completeJson internal-retry recovery, terminal Zod fail → null, LLM throw → null, wide-format block in prompt, dimension-hierarchies block in prompt.
    4. [`tests/quickAnswerPathE2E.test.ts`](server/tests/quickAnswerPathE2E.test.ts) — 4 integration cases driving the full `runAgentTurn` with stubbed LLMs: fast path fires (hypothesis/planner/narrator/verifier do NOT), analytical question rejects, env kill-switch bypasses, zero-row fall-through.
    5. [`tests/quickAnswerWideFormatGuard.test.ts`](server/tests/quickAnswerWideFormatGuard.test.ts) — 2 cases on a compound-shape Marico-VN fixture: WPF2 guard injects Metric filter when planner forgets one (verified MARICO=2200 + PURITE=1700 = value_sales-only sums, not mixed); existing Metric filter preserved without double-injection.
  - **W18 stub harness.** Default `QUICK_LOOKUP_PLANNER` handler in [tests/helpers/llmStub.ts](server/tests/helpers/llmStub.ts) emits an intentionally-invalid plan (empty body, no aggregations / no filters / no limit) so unrelated tests don't suddenly fast-path through. Tests that exercise the path override with a concrete plan.
  - **What was deliberately scoped OUT** (recorded so future-Claude doesn't reopen): (a) **Auto-chart on the fast path.** User explicitly opted not to include this; table view only. Future "fast-path-with-chart" is one flag away (set `emitChart: true` in the orchestrator). (b) **Lite-narrator LLM call for a 1-line tldr.** The table IS the answer; an LLM preamble isn't justified at this latency budget. (c) **Caching identical fast-path questions across sessions.** The existing `past_analyses` cache already covers this for the full loop; not extending here. (d) **Extending the fast path to "compare A vs B" shape questions.** Those benefit from the planner+reflector loop's repair semantics; the detector deliberately routes them to the full loop. (e) **A `/deep` slash prefix to force-route a lookup-shape question through the full loop.** Could add later if users complain the fast path is over-eager — for now the gate is conservative enough that this hasn't shown up in dogfooding. (f) **Deleting the dormant [`simpleAnalysisFastPath.ts`](server/lib/agents/runtime/simpleAnalysisFastPath.ts)** which references the removed legacy orchestrator (commit `9422bed7`); separate cleanup wave. Verified: 71/71 new tests pass; `tests/llmRoutingRegression.test.ts` updates and passes; `npx tsc --noEmit` 99 errors (identical to baseline — zero new errors in any touched file); zero client-side changes required.

- **2026-05-06** — **Audit pass · CLAUDE.md ground-truthed against HEAD.** No code changes; structural reference sections updated in lockstep with the actual tree after the W-EXP / BAI / AD / FA / WF / WPF / H / PVT / PF / DWD / W56 stream. Routes table now lists [`superadmin.ts`](server/routes/superadmin.ts) (was implicit in the AD7 changelog entry only) and [`automations.ts`](server/routes/automations.ts) (replay flows — surface was undocumented). Controller list updated to all 21 actual files including `activeFilterController`, `automationController`, `dashboardExportController`, `superadminController`, `superadminMetricsController`. Models list now includes [`usageEvent.model.ts`](server/models/usageEvent.model.ts) (AD3) and [`automation.model.ts`](server/models/automation.model.ts). Tool registry table adds `compute_growth` and `detect_seasonality` (registered in [`registerTools.ts`](server/lib/agents/runtime/tools/registerTools.ts) but not previously listed). Skills table adds `growthAnalysis`. `server/lib/` topic list now mentions [`activeFilter/`](server/lib/activeFilter/), [`exports/`](server/lib/exports/), [`admin/`](server/lib/admin/), [`detectRollupHierarchies.ts`](server/lib/detectRollupHierarchies.ts), [`patchAssistantBusinessActions.ts`](server/lib/patchAssistantBusinessActions.ts), and the expanded `wideFormat/` file list (`currencyVocabulary`, `classifyDataset`, `meltDataset`, `applyWideFormatToSummary`, `applyWideFormatMeltIfNeeded`). Agent runtime phase 1 corrected: `inferFiltersFromQuestion` is a per-step repair invoked from `planArgRepairs.ensureInferredFiltersOnStep`, not a pre-planning phase; both pre-planner paths (`runHypothesisAndBriefMerged` and the legacy `generateHypotheses` + `maybeRunAnalysisBrief`) read `ctx.domainContext` since Wave WHD1. Phase 2 now mentions `injectRollupExcludeFilters` (H3) and `injectCompoundShapeMetricGuard` (WPF2). Phase 8 added — post-verifier `businessActionsAgent` (BAI1). LLM provider section now lists `BUSINESS_ACTIONS` and `DECK_PLANNER` purposes. Client routing adds `/superadmin*`. Home components section: moved `InteractiveChartCard.tsx` and `ChatChartCard.tsx` to [`client/src/components/charts/`](client/src/components/charts/) (where they actually live), added `BusinessActionsCard`, `BuildDashboardCallout`, `AutomationReplayBanner`, `PercolatingIndicator`, `ColumnSidebar`, `ColumnsDisplay`, `DataPreview`, `DataPreviewTable`, `ContextModal`, `DataSummaryModal`, `DashboardModal`, `InsightCard`, plus a definitive list of SSE event types `useHomeMutations` actually handles. Top-level shared `client/src/components/` block added with the cross-cutting banners (`FilterDataPanel`, `ActiveFilterChips`, `WideFormatBanner`, `DimensionHierarchiesBanner`). Dashboard surface block adds `CapturedFilterBanner` and notes the W-EXP-12 client-side handleExport slimming. New "Superadmin surface" subsection. Header banner status line refreshed to enumerate every shipped wave family currently reflected in HEAD. **No behaviour change** — the changelog entries below this one remain the authoritative wave-by-wave history; this entry just resyncs the structural reference sections that had drifted as new files landed.

- **2026-05-05** — **Waves AD1–AD7 · Admin (superadmin) dashboard + feedback completion.** Closes the user-visible feedback bug AND ships the KPI / quality / cost observability surface the user asked for. Hardcoded admin access to `piyush@finzarc.com` (single source of truth).
  - **AD1 · feedback fix.** `FeedbackButtons` was mounted but invisible on dataOps / synthesis-fallback / cache-hit assistant messages because the gate `sessionId && message.agentTrace?.turnId` failed when `agentTrace` was missing. Fix: [chatStream.service.ts:1208](server/services/chat/chatStream.service.ts) and [chat.service.ts:340](server/services/chat/chat.service.ts) now synthesize `agentTrace.turnId = randomUUID()` when missing, so EVERY persisted assistant message carries a stable per-turn id; cache-hit messages stamp `agentTrace.fromCache=true` (no turnId) so the client deliberately skips them (no `past_analyses` doc exists at the current sessionId — clicking thumbs would 404; cacheSource-redirect tracked as a follow-up). New pure helper [computeFeedbackTurnId.ts](client/src/pages/Home/Components/computeFeedbackTurnId.ts) consolidates the mount-gate logic with a `ts-<timestamp>` defense-in-depth fallback. Comment textarea always visible on thumbs-down ([FeedbackButtons.tsx:293-302](client/src/pages/Home/Components/FeedbackButtons.tsx)) — pre-fix it was gated to `selectedReasons.includes("other")`, which made the field invisible to users who picked any other reason chip OR none. New vitest [feedbackTurnIdAndCommentContract.vitest.test.ts](client/src/pages/Home/Components/feedbackTurnIdAndCommentContract.vitest.test.ts) — 11 cases pinning both bugs against future regression.
  - **AD2 · admin gate consolidated.** [server/lib/superadmin.ts](server/lib/superadmin.ts) `SUPERADMIN_EMAILS` reduced to `["piyush@finzarc.com"]` only (removed `piyush.kumar@finzarc.com`). [server/utils/admin.helper.ts](server/utils/admin.helper.ts) `isAdminRequest` now delegates to `isSuperadminRequest` — the env-driven `ADMIN_EMAILS` path is retired (env stays defined but no longer affects auth, so existing deploys don't crash). `isSuperadminRequest` switched to `getAuthenticatedEmail` so DISABLE_AUTH+`x-user-email` works in tests. New escape hatch `__setSuperadminEmailsForTesting` / `__resetSuperadminEmailsForTesting` for the existing [adminDomainContextRoute.test.ts](server/tests/adminDomainContextRoute.test.ts) (5 cases, all pass) which previously toggled `process.env.ADMIN_EMAILS`. New [server/middleware/requireSuperadmin.ts](server/middleware/requireSuperadmin.ts) — wraps Azure AD auth + superadmin check for new admin endpoints.
  - **AD3 · usage_events container + writer.** New Cosmos container partitioned by `/dateKey` (UTC `YYYYMMDD`) for events the existing canonical containers can't derive: `dashboard.exported`, `dashboard.opened`, `pivot.generated`, plus reserved types for `dashboard.shared` / `analysis.shared` / `message.regenerated` / `message.edited` / `admin.session.viewed`. New schema in [server/shared/schema.ts](server/shared/schema.ts) (`usageEventDocSchema`, `usageEventTypeSchema`); new model [server/models/usageEvent.model.ts](server/models/usageEvent.model.ts) with lazy container init mirroring the `pastAnalysis.model.ts` pattern; new fire-and-forget writer `recordUsageEvent` (never throws — telemetry must not break the live request path) + cross-partition `listUsageEvents(fromDateKey, toDateKey, eventType?, userEmail?)` for the aggregator. Emitters wired at three minimal seams: [getDashboardController](server/controllers/dashboardController.ts) → `dashboard.opened`; [exportDashboardController](server/controllers/dashboardController.ts) (both pdf + pptx branches) → `dashboard.exported` with `{format, sheetCount, sizeBytes}` metadata; [/api/data/:sessionId/pivot/query](server/routes/dataApi.ts) → `pivot.generated` with per-(sessionId, configHash) in-process dedupe + 5-minute TTL so pivot-grid drag/resize storms count as ONE event. Env kill-switch: `USAGE_EVENTS_ENABLED=false`.
  - **AD5 · metricsAggregator + cache.** New [server/lib/admin/metricsAggregator.ts](server/lib/admin/metricsAggregator.ts) computes daily-grain series from existing canonical containers — sessions/messages/charts via `getAllSessions()`, dashboards via `listAllDashboardsForSuperadmin()`, LLM cost via cross-partition query on `llm_usage`, feedback via cross-partition query on `past_analyses`, sharing via cross-partition queries on `shared_analyses` / `shared_dashboards`, and exports/pivots/opens via `listUsageEvents`. Aggregate API: `aggregateSessionMetrics` / `aggregateLlmMetrics` / `aggregateFeedbackMetrics` / `aggregateUsageEventMetrics` / `getDashboardsCreatedByDay` / `getChatsSharedByDay` / `getDashboardsSharedByDay`. New [metricsCache.ts](server/lib/admin/metricsCache.ts) — in-process Map<key, {value, expiresAt}> with 60s TTL + FIFO eviction at 200 entries. New pure-fn [bucketing.ts](server/lib/admin/bucketing.ts) — `rebucketDailySeries` collapses daily into weekly (ISO Mon–Sun) / monthly / quarterly / yearly per the user's granularity toggle, server-side. Marico-tenant scale: live aggregation is well within Cosmos RU budget; no pre-aggregation container needed (boring first).
  - **AD6 · two new endpoints, hardened by `requireSuperadmin`.** `GET /api/superadmin/metrics/overview?from=YYYYMMDD&to=YYYYMMDD&granularity=daily|weekly|monthly|quarterly|yearly` returns 13 KPI counters (active users + DAU/WAU/MAU subline, sessions, messages, charts, pivots, dashboards-created/exported/opened, chats/dashboards shared, thumbs up/down + down-rate, LLM cost + tokens + calls, avg messages per session) + every time series at the user-chosen granularity. The daily payload caches 60s; granularity rebuckets at response time so toggling is essentially free. `GET /api/superadmin/feedback?from=&to=&sentiment=&userEmail=&limit=` returns the recent feedback stream (newest 50 by default) with full feedbackDetails + `(sessionId, turnId)` link-back. Both wired in [server/routes/superadmin.ts](server/routes/superadmin.ts). New controller [server/controllers/superadminMetricsController.ts](server/controllers/superadminMetricsController.ts).
  - **AD7 · KPI dashboard at `/superadmin`.** Replaced the pre-AD7 2-card stub at [SuperadminLanding.tsx](client/src/pages/Superadmin/SuperadminLanding.tsx) with the full observability surface: toolbar (date range + granularity toggle Daily | Weekly | Monthly | Quarterly | Yearly), 12-card KPI grid, recharts `LineChart` time-series with metric selector (defaults to "Active users"), top-users-by-activity + top-users-by-cost tables, recent feedback stream with click-through to the existing `/superadmin/sessions/:sessionId` viewer (Wave AD8 was rendered moot by the existing `SuperadminSessionViewer`). Footer keeps the All sessions / All dashboards links to the existing pages. New API client [superadminMetrics.ts](client/src/lib/api/superadminMetrics.ts).
  - **What was deliberately deferred (out of scope, recorded so future-Claude doesn't reopen).** (a) Round-2 emitters — `message.regenerated`, `message.edited`, explicit `dashboard.shared` / `analysis.shared` events. The current AD5 aggregator counts shares directly from the existing `shared_*` invite tables, so the `*.shared` event types are reserved but not emitted yet; regenerate / edit rates are the only KPI not surfaced today. (b) Cache-hit feedback redirect to source `past_analyses` doc — current AD1 fix correctly hides feedback buttons on cache-hit (was hidden before AD1 too); proper fix routes feedback to source via `cacheSource: {sessionId, turnId}` hint on the request, but is a separate concern. (c) Pre-aggregated daily rollup container — single-tenant Marico scale doesn't justify it; the 60s `withMetricsCache` window absorbs hot-reload storms. (d) Audit logging of `admin.session.viewed` — schema slot reserved on `usageEventTypeSchema`, emitter wiring deferred. (e) Daily LLM cost spike alerting integrated into the new dashboard — existing [/admin/costs](client/src/pages/Admin/AdminCosts.tsx) page already covers per-day-cost with W6.3 alerts; the new dashboard surfaces total cost + cost time-series but doesn't replicate the alerts table.
  - **Conventions added / reaffirmed.** (1) **Single hardcoded admin allowlist.** Both `isAdminRequest` and `isSuperadminRequest` resolve through the same `SUPERADMIN_EMAILS` set; new admin endpoints use `requireSuperadmin` middleware; tests use `__setSuperadminEmailsForTesting`. The `ADMIN_EMAILS` env var is retired in production behavior (preserved as a no-op for back-compat). (2) **Telemetry must be fire-and-forget.** `recordUsageEvent` never throws; it logs warnings and drops the event when Cosmos is unreachable, the schema is malformed, or the user is unauthenticated. New event types added by future waves must follow the same contract. (3) **Daily-grain is the canonical aggregator output.** All `*ByDay` functions return `Array<{dateKey, value}>` sorted ascending; the HTTP layer rebuckets to user-chosen granularity at response time. Don't pre-aggregate at write time — boring first beats a scheduled job at this scale. (4) **The user-visible "admin" surface lives at `/superadmin/*`** — the codebase already used that namespace before AD2; we extended it rather than introducing a parallel `/admin` namespace. The existing `/admin/costs` and `/admin/context-packs` keep their URLs (now also gated by the consolidated allowlist).
  - **Verified.** Client vitest 332/332 passing (11 new in `feedbackTurnIdAndCommentContract`); server `tests/adminDomainContextRoute.test.ts` 5/5 (rewritten to use the new test helper). Server `npx tsc --noEmit` 99 errors (identical to baseline — zero new errors in any AD-touched file). Client `npx tsc --noEmit` zero new errors over the working-tree baseline.

- **2026-05-05** — **Waves W-EXP-4 through W-EXP-14 · Dashboard export rewrite SHIPPED end-to-end.** The 11 remaining waves of the 14-wave dashboard-export rewrite plan ([the-dashboard-download-feature-cozy-flask.md](../../.claude/plans/the-dashboard-download-feature-cozy-flask.md)). Three foundation waves (W-EXP-1/2/3 — schema, planner, verifier) shipped earlier the same day; this entry covers the renderers + wiring + cleanup + golden fixture.
  - **W-EXP-4 · ECharts SVG SSR helper.** New [`server/lib/exports/chartSsr.ts`](server/lib/exports/chartSsr.ts) — `renderChartSpecToSvg(spec, opts)` translates a `ChartSpec` into a self-contained SVG string via Apache ECharts SSR (`renderer: 'svg', ssr: true`). Pure JS, no native deps. Vector everywhere. Supports bar / line / area / scatter / pie / heatmap with the brand palette mirrored from `client/src/pages/Dashboard/exportTheme.ts`. Used by the PPT renderer for chart types pptxgenjs can't render natively (heatmap), and as the primary chart engine for PDFs (every chart is inline SVG → razor-sharp at print zoom).
  - **W-EXP-5/6 · pptxgenjs renderer (10 layouts + master + addChart wrapper).** New [`server/lib/exports/pptx/master.ts`](server/lib/exports/pptx/master.ts) defines a single named slide-master (`MARICO_BASE`) with running header (top thin rule + brand line), running footer (date · slide x of N · confidentiality), and `LAYOUT_WIDE` page geometry. Brand palette + font + slide dims are constants other layouts import from — no inline literals. Ten layout files under `server/lib/exports/pptx/layouts/` — one per `LayoutKind`. Each takes a typed `SlideSpec` slice (the discriminated union from W-EXP-1 makes type-narrowing automatic). Native chart handling lives in [`chartSpecToAddChart.ts`](server/lib/exports/pptx/chartSpecToAddChart.ts): `bar` / `line` / `area` / `pie` / `scatter` route through `pres.addChart` with embedded XLSX data so recipients get editable charts (right-click → Edit Data); `heatmap` returns false and the layout falls back to inline SVG via the W-EXP-4 helper. Tables always render via native `addTable` — no image fallback. Speaker notes attached via `slide.addNotes` on every slide.
  - **W-EXP-7 · PPTX wiring + legacy delete.** New [`server/lib/exports/pptx/render.ts`](server/lib/exports/pptx/render.ts) — `renderDeckPlanToPptxBuffer(plan, dashboard, opts)` switches on `slide.layout` exhaustively (compile-time guarantee — adding the 11th LayoutKind fails to compile here). New top-level orchestrator [`server/lib/exports/buildDashboardDeck.ts`](server/lib/exports/buildDashboardDeck.ts) wraps `runDeckPlanner` (W-EXP-2) → `verifyDeckPlan` (W-EXP-3) → repair-once-on-fail → render. Falls back to a deterministic 3-slide minimal deck (`buildFallbackDeckPlan`) when the planner can't produce a valid plan even after one repair round, so the user always gets a download. Both legacy service entry points ([`pptxExport.service.ts`](server/services/dashboardExport/pptxExport.service.ts) and [`dashboardExport.service.ts`](server/services/dashboardExport.service.ts)) become thin shims delegating to the new pipeline — both old endpoints (`POST /api/dashboards/:id/export` body `{format:"pptx"}` AND `GET /api/dashboards/:id/export/pptx`) get the upgrade with zero controller change required. Existing [`tests/dashboardExport.test.ts`](server/tests/dashboardExport.test.ts) updated to install the W18 LLM stub so its PPTX assertions stay offline + deterministic. New [`tests/exportPptxRenderWEXP7.test.ts`](server/tests/exportPptxRenderWEXP7.test.ts) exercises (a) the full 8-slide rich plan against a Marico-VN Q3 fixture (asserts ZIP-shaped output > 25KB), (b) fallback deck path when the LLM emits a Zod-invalid plan (verifies planner returns null + fallback renders cleanly), (c) verifier-fail → repair-branch → ok-plan rescue (asserts the planner is called twice and the second call's output passes verification).
  - **W-EXP-8/9/10 · @react-pdf renderer (10 PDF page components + render entry point).** New [`server/lib/exports/pdf/master.tsx`](server/lib/exports/pdf/master.tsx) — palette + page-size constants + `<PageHeader>` + `<PageFooter>` shared components using @react-pdf's `<Text render={...}>` token resolution for `Page x of N`. Ten page-component files under `server/lib/exports/pdf/layouts/` — symmetric to the PPT side. Charts embed via `<Image src=data:image/svg+xml;utf8,...>` from the W-EXP-4 SSR helper — vector at print zoom. Tables render natively via flexbox grid + zebra rows. New [`server/lib/exports/pdf/render.tsx`](server/lib/exports/pdf/render.tsx) — `renderDeckPlanToPdfBuffer(plan, dashboard, opts)` switches on `slide.layout` exhaustively (same compile-time guarantee as PPT). Async stream-to-Buffer wrapper handles @react-pdf's Node Readable output cleanly. New [`tests/exportPdfRenderWEXP11.test.ts`](server/tests/exportPdfRenderWEXP11.test.ts) asserts `%PDF-` magic bytes + non-trivial size + fallback path correctness.
  - **W-EXP-11 · PDF wiring.** [`buildDashboardDeckPdf`](server/lib/exports/buildDashboardDeck.ts) replaces the W-EXP-7 stub-throw with the real renderer chain: plan → verify → render via @react-pdf. Symmetric to PPT. The legacy [`buildDashboardPdf`](server/services/dashboardExport.service.ts) shim delegates here, so the existing client PDF download button (`POST /api/dashboards/:id/export` body `{format:"pdf"}`) lights up automatically — no controller change.
  - **W-EXP-12 · Client cleanup.** Deleted ~510 LOC of client-side rasterized-screenshot deck code from [`client/src/pages/Dashboard/Components/DashboardView.tsx`](client/src/pages/Dashboard/Components/DashboardView.tsx) (was lines 436-948 — `handleExport` body using `html-to-image.toPng` + `PptxGenJS`). New `handleExport` is ~30 lines and just calls [`dashboardsApi.exportDashboard(dashboard.id, 'pptx')`](client/src/lib/api/dashboards.ts) which routes to the new server pipeline. Dropped `html-to-image` and `pptxgenjs` from [`client/package.json`](client/package.json). Removed `EXPORT_BRAND` / `EXPORT_FONT_FAMILY` / `EXPORT_PIXEL_RATIO` / `SLIDE_WIDTH_IN` / `SLIDE_HEIGHT_IN` imports + the `PPT_LAYOUT` constant (no consumers remain). Sheet-selection UI in the export dialog is preserved as informational — the server-side planner picks layouts intelligently and ignores per-sheet filters. Per-sheet exports remain a future enhancement. Verified: `cd client && npm test` 321/321 vitest pass.
  - **W-EXP-13 · Golden Marico-VN Q3 fixture.** New [`tests/fixtures/maricoVnQ3Deck.ts`](server/tests/fixtures/maricoVnQ3Deck.ts) exports a stable `Dashboard` + `SlideDeckPlan` literal pair representing the Marico-VN Q3 review case study. Pinned in [`tests/exportGoldenFixtureWEXP13.test.ts`](server/tests/exportGoldenFixtureWEXP13.test.ts) — asserts (a) the plan passes the deterministic verifier, (b) PPTX render is ZIP-shaped + non-trivial, (c) PDF render starts with `%PDF-` + non-trivial, (d) the fixture covers ≥ 8 of the 10 layouts. The fixture is a literal (not LLM output) so the test is byte-stable across CI runs — perfect contract pin for any future planner-prompt change that subtly degrades output.
  - **W-EXP-14 · Action-title CI quality gate.** Same test file (W-EXP-13's `exportGoldenFixtureWEXP13.test.ts`) carries 4 additional cases that exercise [`checkActionTitle`](server/lib/agents/runtime/deckPlanVerifier.ts) against the golden fixture: (a) every action title individually passes the verb+number rule, (b) mutating slide #2's title to "Findings" trips the verifier (topic-title placeholder), (c) moving Methodology to position 2 trips back-third-placement, (d) dropping speaker-notes below the 20-char floor trips the floor. Future regression in the fixture, the planner stub default, OR the verifier rules will fail CI here.
  - **Pivot during W-EXP-7→8 (recorded for posterity).** The original plan called for Puppeteer + `@sparticuz/chromium-min` over a print-styled React route, with `@react-pdf/renderer` as a no-Chromium fallback. During W-EXP-7 wiring it became clear that two engines for one job is hacky (CLAUDE.md "Demand Elegance" rule). `@react-pdf` alone — purely server-side, no Chromium binary, no temp tokens / print-route auth dance, no Vercel cold-start anxiety — is the elegant solution. Symmetric to PPT (one renderer per format). The original Puppeteer+fallback waves W-EXP-8/9/10/11 collapsed into the four `@react-pdf`-only waves shipped here. Plan file updated to reflect the pivot.
  - **Conventions added across the W-EXP series.** (1) **Closed `LayoutKind` enum, expanded via single-file PR** — adding a layout requires schema + slot type + verifier exhaustiveness case + PPT renderer case + PDF renderer case + fixture update + golden test update. The compile-time exhaustiveness check makes "I forgot to render this layout" impossible in any of three places (PPT switch, PDF switch, verifier overload-bullets switch). (2) **The deck planner is the ONLY LLM call in the export pipeline** — renderers are deterministic. New export agents must NOT call the LLM at render time — push reasoning into the planner schema. (3) **Chart-id allocation is one-way deterministic** — `s{sheetIdx}c{chartIdx}` allocator + `resolveChartIdToSpec` inverse live in `deckPlanner.ts` only; renderers consume but never re-derive. (4) **Fallback decks always ship** — `buildFallbackDeckPlan` is the last-resort 3-slide path so the user always gets a download even when the planner + repair both fail. (5) **One brand palette source per format**, mirrored across `chartSsr.ts`, `pptx/master.ts`, `pdf/master.tsx`, and `client/src/pages/Dashboard/exportTheme.ts` — four sources is one too many; unification is a follow-up cleanup. (6) **Repair calls preserve the prefix** — appending the issues block to the END of the user message keeps the dashboard-description prefix byte-identical so prompt cache holds across initial + repair pair. (7) **Native > raster, always** — pptxgenjs `addChart` + `addTable` for everything pptxgenjs can express; SVG fallback only for chart types that genuinely can't be expressed natively. Tables-as-image is forbidden by the verifier.
  - **Out of scope (recorded so future-Claude doesn't reopen).** (a) Inter-font registration in @react-pdf — currently falls back to Helvetica; visually adequate, requires bundling a font file. (b) Per-sheet PPT exports — the server endpoint takes the whole dashboard; sheet selection in the client dialog is informational. (c) PowerPoint→PDF server-side conversion (LibreOffice headless) — two independent renderers is simpler than one + conversion. (d) Page numbers / TOC links in PDF — @react-pdf supports them but the current 8-page exec deck doesn't need a TOC. (e) Multi-language exports — assumes English. (f) Email-the-deck flow — out of scope; user downloads, attaches manually.
- **2026-05-05** — **Wave W-EXP-3 · Deterministic deck-plan verifier.** Third wave of the export-rewrite stream. Encodes the hard rules that separate a professional analytical deck from a "shitty" one. Sits between [`runDeckPlanner`](server/lib/agents/runtime/deckPlanner.ts) and the renderers (W-EXP-5/6/8/10) in the export controller flow. Mirrors the spirit of [`checkEnvelopeCompleteness`](server/lib/agents/runtime/checkEnvelopeCompleteness.ts) (W17): deterministic, objective, no LLM-as-judge — feeds a structured repair payload back into the existing planner LLM via the `runDeckPlanner` repair branch (W-EXP-2). Bounded at one repair round at the call site (lands in W-EXP-7/11) so a misbehaving planner can't loop the budget away.
  - **Rules encoded.** [`verifyDeckPlan(plan)`](server/lib/agents/runtime/deckPlanVerifier.ts) walks every slide and returns either `{ ok: true }` or a structured repair payload `{ description, courseCorrection, slideIssues[] }`: (1) Action title — verb-led, contains a number OR an uppercase-acronym/proper-noun, ≥ 5 words, must start with capital letter or digit (the "3 takeaways shape …" pattern is a legitimate sentence start), and is NOT a topic-title cliché ("Findings", "Overview", "Performance Overview", "Conclusion", …) — denylist matched case-insensitively after trimming trailing punctuation. (2) `TitleSlide` must be the first slide if any TitleSlide exists. (3) `Methodology` slides must live in the back third (`index ≥ floor(total * 2/3)`) — unconditionally; multiple methodology slides all checked. (4) `ExecSummary` must live in the first half (`index < ceil(total / 2)`). (5) `speakerNotes` ≥ 20 chars at runtime (also schema-enforced; double-check defends against hand-built plans bypassing the schema). (6) One-message-per-slide on bullet/caption fields: `findOverloadedBullets` counts ONLY unit-bearing magnitudes ("12%", "9.1pp", "₫68.7B", "1.8x") — bare integers in partition prose ("8 of the 12pp decline") count as ONE magnitude, so the rule fires on genuinely two-message bullets ("Sales fell 12% AND volume fell 4.2pp") without false-positiving on valid prose.
  - **Regex correctness.** First implementation used `\b` after every unit; that fails between two non-word chars (`%` followed by space — both non-word, so `\b` doesn't anchor). Final regex splits the alternation by unit type: percent unconditional, word-char units (`pp`/`pt`/`bps`/`KMB` scale letters) with `\b`, multipliers (`x`/`×`) unconditional, currency-prefixed (`$`/`₫`/`€`/`£`/`¥`) with optional scale suffix.
  - **Compile-time guarantee.** `findOverloadedBullets` switches on `slide.layout` exhaustively — adding the 11th LayoutKind to W-EXP-1's enum fails to compile here, forcing a verifier-side decision (continue layout's existing semantics, or add a new flag rule). Same pattern as the renderer-side exhaustiveness check pinned in `tests/exportSchemaWEXP1.test.ts`.
  - **Tests.** New [`tests/deckPlanVerifierWEXP3.test.ts`](server/tests/deckPlanVerifierWEXP3.test.ts) — 16 cases pinning: action-title verb+number rule (verb-led ✓, all-caps brand ✓, topic-title ✗, missing-specificity ✗, lowercase-start ✗, leading-digit ✓, < 5 words ✗); deck-level positional rules (TitleSlide-first, Methodology in back third of a 6-slide deck, ExecSummary in first half); speaker-notes runtime floor; one-message-per-slide on ExecSummary bullets; KpiRow numbers explicitly NOT flagged (numbers are the point of the layout); repair payload includes per-slide line + courseCorrection helper. Verified: 16/16 new tests pass; 82/82 across the touched suites (W-EXP-1/2/3 + W18 stub harness + LLM-purpose regression); `npx tsc --noEmit` 99 errors, baseline preserved.
  - **Conventions added.** (1) **Verifier checks are objective and cheap.** No LLM-as-judge. Future export-quality rules added here must be expressible as regex / count / position — anything subtler belongs in the planner system prompt instead. (2) **Repair payload shape is symmetric across deterministic gates.** `{ ok: false, code, description, courseCorrection, slideIssues[] }` — same structure as W17/W22/W35 envelope checks so the call site (W-EXP-7/11) can reuse the existing repair-loop pattern. (3) **Magnitude-detection counts unit-bearing numbers only.** Partition prose ("8 of the 12pp decline") is one message, not two. New rule additions that count "magnitudes" must use the same regex or they'll diverge from the verifier's definition.
- **2026-05-05** — **Wave W-EXP-2 · Dashboard export deck-planner agent.** Second wave of the export-rewrite stream. Wires Claude Opus 4.7 (structured-output mode) to compose a `SlideDeckPlan` from a saved Dashboard.
  - **Architecture.** [`runDeckPlanner(inputs, opts, repair?)`](server/lib/agents/runtime/deckPlanner.ts) — single LLM call, schema-validated via `slideDeckPlanSchema` (W-EXP-1), one-round repair branch on validation fail, returns `null` on terminal failure (caller's fallback responsibility — W-EXP-7/11 will land a deterministic minimal-deck path so the user always gets a download). Mirrors the [`narratorAgent.ts`](server/lib/agents/runtime/narratorAgent.ts) pattern (system prompt byte-stable across calls so the W4.2 prefix-cache threshold holds; `completeJson` retry loop owns its own attempts) but is decoupled from `AgentExecutionContext` because export runs at a separate request → response cycle, not inside an agent turn.
  - **System prompt — the load-bearing piece.** ~6KB byte-stable. Encodes the action-title rule with worked MBB examples (✓ "Sales fell 12% in Q3 driven by category mix shift" — verb + number + complete sentence, vs. ✗ "Sales by Quarter" — topic title, no verb), the closed `LayoutKind` enum, the MBB section template (TitleSlide → ExecSummary → KpiRow → ChartWithInsight×N → ImplicationsByHorizon → Recommendations → Methodology → Appendix), and 8 hard rules (one message per slide, methodology in back third, never invent numbers, never use placeholder titles, refer to charts only by inventory id, etc.). Lives entirely in the `system` parameter so prompt caching kicks in across repeat exports of similar dashboards.
  - **Slim representation — the cost lever.** [`buildSlimDashboard`](server/lib/agents/runtime/deckPlanner.ts) strips inline `ChartSpec.data` rows, `_agentProvenance.toolCalls`, and `gridLayout` before serialising into the user prompt — the planner doesn't need raw points to pick a layout. Surfaces ONLY the fields the planner actually uses: chart type/title/encodings/insight/businessCommentary, narrative-block summaries (capped 1.2KB each), table dimensions (caption + col count + row count), the full `answerEnvelope` (TL;DR/findings/magnitudes/implications/recommendations/methodology/caveats/domainLens), `businessActions[]`, and a one-line `capturedActiveFilter` summary. Charts get stable ids `s{sheetIdx}c{chartIdx}` (e.g. `s0c0`, `s1c2`); tables `s{sheetIdx}t{tableIdx}`. Renderers resolve back via [`resolveChartIdToSpec`](server/lib/agents/runtime/deckPlanner.ts) — keeping the id-allocation logic in one file prevents planner/renderer drift.
  - **Repair branch.** When invoked with `repair: { issues, priorPlan }` (the W-EXP-3 verifier's output, when it lands), the user message gets a focused suffix `"The previous plan failed verification. Fix these issues: …\n\nPrior plan (rewrite to fix the issues, do not start over from scratch): …"`. The dashboard-description prefix stays byte-identical so the prompt cache still hits across initial + repair calls. Same pattern as the W4 narrator-repair branch.
  - **Tests.** New [`tests/deckPlannerWEXP2.test.ts`](server/tests/deckPlannerWEXP2.test.ts) — 8 cases pinning: (a) slim representation strips raw chart data and preserves chart-id stability; (b) legacy `dashboard.charts[]` (no `sheets[]`) falls back to a synthetic "Overview" sheet; (c) `resolveChartIdToSpec` is the inverse of the id allocator (round-trip + null-on-garbage); (d) user prompt surfaces every load-bearing block (envelope, magnitudes, business actions, captured filter, chart inventory by id); (e) default stub returns a valid 3-slide plan that schema-validates; (f) `LLM_PURPOSE.DECK_PLANNER` fires for cost telemetry; (g) returns `null` when the LLM emits a Zod-invalid plan; (h) repair branch threads issues + prior plan into the user message. The W18 default-stub-handler map gets a new `DECK_PLANNER` entry that emits a minimal-valid 3-slide plan (TitleSlide + ExecSummary + Methodology) — keeps existing tests green when they run through code paths that touch the planner. Verified: 33/33 pass in the new + adjacent suites; `npx tsc --noEmit` 99 errors, identical to baseline; zero new errors in any touched file.
  - **Conventions added.** (1) **The deck planner is the ONLY LLM call in the export pipeline.** Renderers are deterministic. New export agents must NOT call the LLM at render time — if you need more reasoning, push it into the planner schema. (2) **Chart-id allocation lives in `deckPlanner.ts` and is a one-way function from (sheetIdx, chartIdx) → id.** Renderers resolve back via `resolveChartIdToSpec`. Don't introduce alternate id schemes; renderers and the planner must agree byte-for-byte. (3) **Repair calls preserve the prefix.** New repair branches in this codebase (narrator W4, this planner) all append the issues block to the END of the user message and keep the dashboard/blackboard description byte-identical, so the system + early-user prefix stays prompt-cache eligible across the initial + repair pair. Out of scope, recorded so future-Claude doesn't reopen: an LLM-driven verifier (the W-EXP-3 verifier is deterministic on purpose — regex + structural checks — to keep the repair-loop budget bounded and decisions auditable), a multi-agent planner (research toys; one well-prompted Opus 4.7 call beats them on cost, latency, and reviewability), and per-deck domain-pack injection (the dashboard's `answerEnvelope.domainLens` already carries the relevant pack citation; the planner is told to weave one phrase from it without re-running pack-loading).
- **2026-05-05** — **Wave W-EXP-1 · Dashboard export rewrite — SlideDeckPlan schema foundation.** First wave of a 14-wave rewrite that replaces the three sub-par dashboard download paths (text-only `pdf-lib` PDF via [`buildDashboardPdf`](server/services/dashboardExport.service.ts#L57); text-only server-side PPTX via [`buildDashboardPptxBuffer`](server/services/dashboardExport/pptxExport.service.ts#L55) — the one registered at `GET /api/dashboards/:id/export/pptx` but unused by the client; client-side raster screenshots via `html-to-image` at [`DashboardView.tsx:440-934`](client/src/pages/Dashboard/Components/DashboardView.tsx#L440-L934) — what users actually click) with a single LLM-driven pipeline. Full plan + decision rationale in [`/Users/tida/.claude/plans/the-dashboard-download-feature-cozy-flask.md`](../../.claude/plans/the-dashboard-download-feature-cozy-flask.md). This wave is schema-only; no user-visible behaviour change yet.
  - **What landed.** New [`server/shared/exportSchema.ts`](server/shared/exportSchema.ts) — `SlideDeckPlanSchema` + `LayoutKind` enum (closed: TitleSlide, ExecSummary, KpiRow, ChartWithInsight, TwoChartCompare, TableSlide, ImplicationsByHorizon, Recommendations, Methodology, Appendix). Per-layout slot shapes are typed via `z.discriminatedUnion("layout", …)` so renderers (W-EXP-5 onwards) get TS exhaustiveness checks at compile time — adding an 11th layout will fail to compile in every renderer that forgets it. `slideDeckPlanSchema` caps slides at 2–30; the verifier (W-EXP-3) enforces the structural rules (action titles must be verb + number, methodology in back third, speaker notes ≥ 20 chars). Re-exported through [`server/shared/schema.ts`](server/shared/schema.ts) so the client side picks it up automatically via the existing `export * from "../../../server/shared/schema"` shim in [`client/src/shared/schema.ts`](client/src/shared/schema.ts). New `LLM_PURPOSE.DECK_PLANNER` in [`llmCallPurpose.ts`](server/lib/agents/runtime/llmCallPurpose.ts), categorised PRIMARY (Opus 4.7) — action-title quality and structural reasoning are quality-sensitive enough that a Mini-tier downgrade would re-create the exact "shitty deck" failure mode this rewrite exists to fix. Decision-review gate: [`tests/llmRoutingRegression.test.ts`](server/tests/llmRoutingRegression.test.ts) `EXPECTED_CATEGORY` map updated in lockstep so the test fails until any new purpose has its category explicitly declared.
  - **Tests.** New [`tests/exportSchemaWEXP1.test.ts`](server/tests/exportSchemaWEXP1.test.ts) — 18 cases pinning: closed enum membership, per-layout slot validation (chartId required on ChartWithInsight; 3–6 bullets on ExecSummary; 2–5 KPIs; tableRef ref-vs-inline discriminator on TableSlide; horizon-enum on Recommendations; 4-entry-per-column cap on ImplicationsByHorizon; 20-char body floor on Methodology), top-level deck cap of 30 / floor of 2, and a renderer-shaped exhaustiveness switch over `LAYOUT_KIND` so adding the 11th layout breaks this test. Appended to [`server/package.json`](server/package.json) `test` script per the explicit-file-list rule. Verified: 50/50 pass (18 new + 32 existing in the touched LLM-purpose suites); `npx tsc --noEmit` 99 errors total — identical to baseline, zero new errors.
  - **Conventions added.** (1) **Closed `LayoutKind` enum, expanded via single-file PR.** Adding a new layout requires (a) one entry in `LAYOUT_KIND`, (b) one slot schema, (c) one variant in the `slideSpecSchema` discriminated union, (d) renderer additions in W-EXP-5/6/8/10, (e) update to the exhaustiveness test in this file. The compile-time exhaustiveness check makes "I forgot to render this layout" impossible — same pattern the W F3 verifier-verdict constants enforce. (2) **The LLM picks layout from a closed vocabulary; renderers own pixels.** Same architecture Beautiful.ai's "Smart Slides" use under the hood — separation of structural reasoning (LLM) from layout/typography (deterministic). Future export agents must respect this seam: never let the LLM emit free-form HTML/CSS or position coordinates. (3) **`businessActionItemSchema` is intentionally NOT reused for the Recommendations layout slots.** Action items live on the message; Recommendations slots are the planner's compressed view of `answerEnvelope.recommendations[]` + `businessActions[]` with horizon required (the message-level shape allows `horizon: undefined` for older entries). Single source of truth was tempting but the load-bearing differences (horizon required, no `dependencies` field, smaller caps) made one-shape-fits-all the wrong call. Out of scope, recorded so future-Claude doesn't reopen: an LLM `repair` round on schema validation fail (lands in W-EXP-2 alongside the planner agent), the verifier's regex for action titles (lands in W-EXP-3), and the chart-id resolution layer that maps `slots.chartId` → `Dashboard.charts[]` (lands in W-EXP-5).
- **2026-05-04** — **Wave PVT1 · intermediate-card pivot stops auto-exploding row-level slices.** Reproducible bug: user asks "Which TSOE has not uploaded the PJP yet?", the agent emits `execute_query_plan` with `groupBy: ["TSO_TSE Name"]` + `dimensionFilters: [{column: "PJP Adherence", op: "in", values: ["No PJP Available"]}]` and **no `aggregations`** — a filter-projection ("list rows of these dimensions") rather than a true aggregate. The DuckDB executor returns the full filtered slice: 396 rows × 44 columns. The chart and final answer are correct; the **Pivot** tab on the intermediate analytical card auto-fills every column (COLUMNS=Cluster Name, ROWS={all temporal facets · Date / TSOE-Date Combo / Clock-In Time + every text dimension}, VALUES={all 9 numerics}). User's rule, now encoded as a system invariant: "never should a pivot fill everything in rows and values when it doesn't understand what to do."
  - **Root cause** — two parallel pivot-defaults paths existed; only the final-message one was wired correctly. (1) The chat-message pivot at [`chatStream.service.ts:1209`](server/services/chat/chatStream.service.ts#L1209) → `mergePivotDefaultsForResponse` → `derivePivotDefaultsFromExecutionMerged` consults the trace plan's `groupBy` + `aggregations`. (2) The intermediate-card pivot at [`agentLoop.service.ts:2113`](server/lib/agents/runtime/agentLoop.service.ts#L2113) bypassed the trace plan entirely and fed the 396×44 slice straight into [`derivePivotDefaultsFromPreviewRows`](server/lib/pivotDefaultsFromPreview.ts#L74) — a helper designed for the dataset-level 50-row preview, not for analytical results. It blindly classifies every column → all numerics → values, all dimensions → rows, then `suggestPivotColumnsFromDimensions` lifts one categorical into columns. Exact match for the screenshot. A second hole compounded it: even on the merged path, when the trace had `groupBy.length > 0` but `aggregations.length === 0`, `mergePivotDefaultRowsAndValues` silently fell back to `fromPreview.values` (line 153-155) — still dumping all 9 numerics into VALUES. A third gap: the intermediate path never propagated `dimensionFilters` into `filterFields` / `filterSelections`, so the user couldn't see "PJP Adherence = No PJP Available" in the FILTERS shelf either.
  - **Fix (three surgical changes, both paths now share one helper).** (1) [`pivotDefaultsFromExecution.ts`](server/lib/pivotDefaultsFromExecution.ts) `mergePivotDefaultRowsAndValues`: new `isFilterProjection` check — when `traceValues.length === 0` AND `rowOut.length > 0` AND the normalized plan has no `aggregations`, set `rawValues = []` instead of falling back to `fromPreview.values`. The existing `if (rowOut.length === 0 && valueOut.length === 0) return undefined` short-circuit at line 166 still fires for plans with no groupBy at all. Filter-projection now returns `{rows: ["TSO_TSE Name"], values: [], filterFields: ["PJP Adherence"], filterSelections: {"PJP Adherence": ["No PJP Available"]}}`. (2) [`agentLoop.service.ts:2098-2153`](server/lib/agents/runtime/agentLoop.service.ts#L2098): for `execute_query_plan` steps, reconstruct the trace plan from `step.args.plan` and route through `mergePivotDefaultRowsAndValues` instead of `derivePivotDefaultsFromPreviewRows`. The merged helper now respects groupBy, suppresses fabricated values for filter-projection, and propagates `filterFields` / `filterSelections`. For tools without a `QueryPlanBody` (`run_segment_driver_analysis`, `run_readonly_sql`, `run_analytical_query` whose parsed plan isn't on `step.args`), keep the preview path **but** wrap with a universal safety guard: if `rows.length + values.length > 8` AND there's no trace-plan-derived shape, discard (`pivotDefaults: undefined`) and log `[pivot_defaults] suppressed_unclear { tool, rows: N, values: M }` for diagnostics. The `hasPivotHint` gate was relaxed from `rows.length > 0 AND values.length > 0` to `rows.length > 0 OR values.length > 0` so filter-projection (rows-only, values empty) survives the gate. The intermediate emission spread now includes `filterFields` / `filterSelections` alongside `rows` / `values` / `columns`. (3) [`chatStream.service.ts:684`](server/services/chat/chatStream.service.ts#L684) `flushIntermediateSegment`: the segment-merge condition was `segmentPivotDefaults?.rows?.length && segmentPivotDefaults?.values?.length` — required BOTH, which silently dropped filter-projection segment pivots and replaced them with parser-side defaults via `filterProvisionalPivotDefaultsToPreviewKeys`. Changed to `||` so rows-only segment pivots flow through `mergeIntermediateSegmentPivotDefaults` (which already early-returns segmentPivot unchanged when values is empty, perfect for filter-projection).
  - **Conventions added.** (1) **Trace plan beats preview categorization for any analytical tool that exposes a `QueryPlanBody`.** New tools that produce row-level slices but accept structured plan args must thread `step.args.plan` to the merged helper, not the preview categorizer — otherwise filter-projection regresses on every tool with this shape. (2) **Empty `values` is honest for filter-projection.** Don't fabricate `count(*)` or first-numeric to "give the user something" — empty values means "drag a measure if you want one"; the user's question was answered by the row list. (3) **Universal "when in doubt, don't auto-explode" safety guard.** For any pivot defaults derivation path that doesn't have a trusted trace-plan shape, if the result accumulates more than 8 fields across rows + values, suppress entirely. Log `[pivot_defaults] suppressed_unclear` so we can spot tools that need their own bespoke shape derivation.
  - **Tests.** New [`tests/intermediatePivotFilterProjection.test.ts`](server/tests/intermediatePivotFilterProjection.test.ts) (4 cases, appended to the explicit-file-list `test` script per CLAUDE.md): pin (a) the Marico TSOE-PJP scenario — full 14-column result row, expect rows=`["TSO_TSE Name"]`, values=`[]`, filterFields=`["PJP Adherence"]`, filterSelections=`{"PJP Adherence": ["No PJP Available"]}`; (b) regression — `groupBy + aggregations` still yields `rows=["Cluster Name"], values=["Total Visited OL's"]`; (c) filter-projection without dimension filter still rows-only with no fabricated values and no `filterFields`; (d) plan with no groupBy at all returns `undefined` via the existing empty-output guard. Verified: 4/4 new tests pass; all 58 pivot-defaults regression tests pass (`pivotDefaultsFromExecution.test.ts`, `.scalar.test.ts`, `pivotDefaultsFromPreview.test.ts`, `pivotEnvelope.test.ts`, `pivotSliceDefaults.test.ts`, `intermediatePivotCoalesce.test.ts`, `wideFormatPivotDefaults.test.ts`, `chatStreamScalarPivot.test.ts`, `lastAssistantPivotState.test.ts`); full server suite **2261/2261** pass (was 2257 + 4 new); client vitest **316/316** pass (no client change but verified the rows-only pivot doesn't crash `PivotGrid`); `npx tsc --noEmit` 99 errors — identical to baseline, zero new errors in any touched file.
  - **Out of scope** (recorded so future-Claude doesn't reopen): (a) bespoke pivot shapes for `run_correlation` / `run_segment_driver_analysis` / `breakdown_ranking` / `two_segment_compare` intermediates — the universal `>8 fields` safety guard prevents the worst auto-explode there for now; per-tool defaults are a separate wave. (b) Changing the executor to project to `groupBy` columns when no aggregations exist — risks breaking chart-spec authoring, narrator phrasing, and the answer envelope; solving the symptom at the pivot-defaults layer is safer. (c) Auto-adding a `count(*)` measure for filter-projection — rejected, fabricates a metric the user didn't ask for.
- **2026-05-03** — **Wave BAI2 · answerEnvelope plumbing fix (the latent bug BAI1 sidestepped).** During BAI1 implementation we discovered that `chatResponseSchema.parse(result)` was silently stripping `answerEnvelope` because the field wasn't part of the response schema (zod's default behavior strips unknown keys). The downstream effect: `validated.answerEnvelope` was always `undefined`, so the SSE response event sent envelope-less payloads, the Cosmos persist captured no envelope, and `AnswerCard` (which short-circuits with `if (!env) return null;`) rendered nothing during the active turn — users saw the markdown-only fallback even though `runAgentTurn` had produced a fully populated W7/W8 envelope. Refresh-after-persist showed nothing either. The comment at [`chatResponse.service.ts:248`](server/services/chat/chatResponse.service.ts) literally said "the envelope is stripped by chatResponseSchema.parse" and the `enrichPivotInsightFromEnvelope` workaround read it from raw `result` to derive insights — proof the bug was known but not fixed. Fix: (1) extracted the inline envelope shape on `messageSchema` into a new named export `messageAnswerEnvelopeSchema` ([server/shared/schema.ts:1123](server/shared/schema.ts)) so there's one source of truth; (2) added `answerEnvelope: messageAnswerEnvelopeSchema.optional()` to `chatResponseSchema` ([server/shared/schema.ts:1622](server/shared/schema.ts)); (3) also added top-level `businessActions` to `chatResponseSchema` so future paths that ship them inline (vs. via the dedicated SSE event) round-trip cleanly through validation; (4) updated `chatResponse.service.ts` to read envelope from `validated` first, falling back to raw `result` for non-schema synthesizer-fallback paths; (5) on the client side, [`useHomeMutations.ts`](client/src/pages/Home/modules/useHomeMutations.ts) now forwards `answerEnvelope` and `businessActions` through THREE bubble-construction paths — the text-first early-bubble at `onResponse` (W38 streaming-narrator path, fires when the agent splits answer text from charts), the final-merge that updates that early bubble in `onSuccess`, AND the new-bubble construction in `onSuccess` for non-streaming turns. The merge path uses a "incoming-OR-existing" pattern so a missing envelope on the final response payload can never erase one already attached by an earlier event (defensive against partial-payload regressions). **What changed user-visibly**: AnswerCard now renders the structured envelope (TL;DR pill, findings + magnitudes, implications grouped by horizon, methodology collapsible, caveats, recommendations) during the active turn, not just after refresh. **What did NOT change**: the legacy markdown fallback still fires when no envelope is produced (synthesizer-fallback / dataOps turns) — those paths were unaffected. Verified: 2215/2215 server tests pass; 217/217 client vitest tests pass; client tsc 52 errors (baseline preserved); server tsc 99 errors (baseline preserved); both production builds + theme:check clean.
- **2026-05-03** — **Wave BAI1 · Business Action Items.** New post-verifier sub-agent that surfaces concrete *business decisions* (things the user could do in the world) alongside the existing `recommendations[]` (which are analytical next steps inside the app). User report: "when I ask 'how do I rescue my falling sales' the app gives me what to analyze next, not what to actually do — sometimes I need both." The decision-grade envelope's `recommendations[]` field is unchanged; this is purely additive. The agent fires on every passing turn (post-verifier `pass`, post-deterministic-gates) using a cheap MINI-routed call (default `gpt-4o-mini`; opt into Haiku 4.5 with `OPENAI_MODEL_FOR_BUSINESS_ACTIONS=claude-haiku-4-5-20251001` in `server.env`) and self-gates to `{ items: [] }` when the question wasn't a strategy ask or evidence is too thin to ground actions. **Critically, there is no regex gate above the agent** — past iterations of that gate silently suppressed legitimate strategy phrasings ("the team's wondering what to do about LASHE — talk me through it"); regex is now used only as informational hints inside the agent's user message, never to decide whether the agent fires.
  - **Schema (server/shared/schema.ts:1216).** New top-level `Message.businessActions?: BusinessActionItem[]` (cap 5). Each item: `{ title (4–160ch), rationale (10–400ch), horizon: "now"|"this_quarter"|"strategic", confidence: "low"|"medium"|"high", dependencies?, expectedImpact? }`. Top-level rather than nested in `answerEnvelope.businessActions` so rendering and persistence are decoupled from the answer-envelope plumbing path (which strips through `chatResponseSchema.parse`'s default-strip behavior — verified with a zod `.parse({ x: 1 })` ad-hoc test; the section would never render if it depended on that). Optional + zero defaults — every existing persisted message round-trips unchanged.
  - **Agent (server/lib/agents/runtime/businessActionsAgent.ts).** Single `runBusinessActions(ctx, envelope, opts)` entry; mirrors the [`narratorAgent`](server/lib/agents/runtime/narratorAgent.ts) shape but smaller (`maxTokens: 1500`, `temperature: 0.3`). Reads `ctx.question`, the completed envelope (findings + magnitudes + implications + caveats + existing analytical recommendations + domainLens), `ctx.domainContext` (already hoisted per-turn at W34), `ctx.permanentContext` + `userIntent.{verbatimNotes, interpretedConstraints}`, and the top-3 `priorInvestigations` digests. System prompt has explicit confidence rubric (high = directly grounded, medium = grounded with assumption, low = under-evidenced; agent MAY emit low-confidence with mandatory `dependencies` describing what would lift confidence). Hard-skips (zero LLM cost) when the envelope has < 2 findings AND no magnitudes — the agent would self-gate anyway. Returns `[]` on any failure / schema mismatch / empty self-gate; never throws.
  - **Hint extractor (server/lib/agents/runtime/businessActionsHints.ts).** `extractStrategyIntentHints(question)` returns short labelled phrases for surface-form patterns (action verb + business outcome, imperative shape, explicit ask, decision framing). **Empty result is informational, not a gate** — pinned by [`tests/businessActionsHints.test.ts`](server/tests/businessActionsHints.test.ts). Intentionally over-recall, under-precision: false positives just hand the agent a hint it can ignore; false negatives are the failure mode the design actively avoids.
  - **Agent loop seam (server/lib/agents/runtime/agentLoop.service.ts:3441).** Right after `if (fv.verdict === VERIFIER_VERDICT.pass) { break; }`, the loop spawns `runBusinessActions(...)` as `Promise<BusinessActionItem[]>` (NOT awaited inline) and attaches it to `AgentLoopResult.businessActionsPromise`. Hard-skips when `BUSINESS_ACTIONS_ENABLED=false`, when synthesis cascaded to the fallback renderer (W4 — same skip path as the final verifier), or when the envelope has zero findings AND zero magnitudes. The skip set is intentionally narrow — every other passing turn pays one cheap LLM call (~2–4 s on Haiku/MINI) so non-obvious strategy phrasings never get suppressed by surface-form heuristics. `dataAnalyzer.answerQuestion` was extended to forward both `answerEnvelope` and `businessActionsPromise` explicitly through its return shape (the prior code dropped them via the explicit-field-list return — this also incidentally fixed the same drop on `answerEnvelope`, surfaced as a 4-error reduction in `tsc --noEmit` baseline).
  - **SSE bridge (server/services/chat/chatStream.service.ts:1726).** AFTER the `response` event fires (so the user sees the AnswerCard at the same moment they do today), the service awaits `result.businessActionsPromise` with `BUSINESS_ACTIONS_TIMEOUT_MS` (default 12000 ms). On non-empty resolution: emits a new `business_actions` SSE event `{ messageTimestamp, items }` AND fire-and-forget patches the persisted message via [`patchAssistantBusinessActions`](server/lib/patchAssistantBusinessActions.ts). On timeout / failure / empty: no event, no patch — the answer envelope is delivered exactly as today. Per-session in-process mutex on the patch chain mirrors the W40 `persistMergeAssistantSessionContext` pattern so concurrent writes serialise correctly.
  - **Client (client/src/pages/Home/Components/BusinessActionsCard.tsx + MessageBubble.tsx).** New bordered collapsible card (expanded by default) mounted in [`MessageBubble`](client/src/pages/Home/Components/MessageBubble.tsx) immediately after the `MagnitudesRow` — alongside `AnswerCard`, not inside it, so it renders independently of the envelope's presence. Header reads "Business action items" with a count chip; one-line muted subtitle: "Suggestions for business decisions, grounded in the findings above. These are starting points — not conclusions. Verify before acting." Each row carries a horizon chip (Now / This quarter / Strategic) + confidence chip (High / Medium / Low — low uses muted-tinted token classes so it reads as tentative). When ANY action is low-confidence, an inline ShieldCheck-iconed caveat row appears above the list reminding the user to validate. Semantic token classes only per [`client/THEMING.md`](client/THEMING.md). The new SSE handler in [`useHomeMutations.ts`](client/src/pages/Home/modules/useHomeMutations.ts) attaches `items` to the most-recent assistant message by recency rather than exact timestamp — server's `Date.now()` and client's `Date.now()` for the same turn diverge in the streaming-narrator path, but only one turn is in flight at a time so "last assistant message in state" is unambiguously correct.
  - **Tests (4 new files, all appended to the explicit `test` script per CLAUDE.md).** [`tests/businessActionsHints.test.ts`](server/tests/businessActionsHints.test.ts) — 8 cases pinning hint extraction; explicitly documents that empty hints is NOT a gate signal. [`tests/businessActionsAgent.test.ts`](server/tests/businessActionsAgent.test.ts) — 8 W18-stub cases pinning hard-skip on thin envelopes, empty self-gate, schema rejection on >5 items, malformed item rejection, the right LLM purpose firing, `onLlmCall` callback, and graceful failure on stub throw. [`tests/agentLoopBusinessActionsIntegration.test.ts`](server/tests/agentLoopBusinessActionsIntegration.test.ts) — 3 cases driving the full `runAgentTurn` with stubbed LLMs against a Marico fixture: promise attached on passing turn, promise absent under `BUSINESS_ACTIONS_ENABLED=false`, agent self-gate yields `[]`. [`client/.../BusinessActionsCard.vitest.test.ts`](client/src/pages/Home/Components/BusinessActionsCard.vitest.test.ts) — 8 vitest cases pinning the `Message.businessActions` schema contract (validates populated, empty, absent; rejects horizon/confidence enum violations, title < 4 chars, > 5 items; dependencies/expectedImpact stay optional). The default LLM-stub harness (`tests/helpers/llmStub.ts`) gets a `BUSINESS_ACTIONS: () => ({ items: [] })` default so existing tests don't suddenly grow a section.
  - **Conventions added.** (1) **Post-verifier sub-agents are async-decoupled, never blocking the envelope.** Future similar agents (follow-up question generation, what-if scenarios) follow the same pattern: spawn after `pass`, attach a promise to `AgentLoopResult`, the SSE bridge awaits AFTER the response event with a timeout, dedicated SSE event + Cosmos patch on resolve. Failure isolation: existing path is sacred. (2) **Self-gating beats classifier sprawl, and regex never gates.** One LLM call, one decision. Regex is hint extraction, not arbitration — the same regex that would miss "the team's wondering what to do about LASHE" must not be the thing deciding whether to ask. (3) **Top-level message fields beat nested envelope fields when the envelope plumbing is fragile.** The pre-existing `chatResponseSchema.parse` strips `answerEnvelope` (the `chatResponse.service.ts:248` comment confirms this); rather than fix that side-issue, BAI1 added `businessActions` as a top-level Message field so rendering doesn't depend on the broken envelope-flow path. The schema authors can fix the envelope flow separately without touching this feature. Verified: 24/24 new tests pass; server tsc 99 errors (unchanged baseline; `dataAnalyzer.ts` answerEnvelope explicit forward incidentally fixed 4 pre-existing errors → net 99 from 103 baseline); client tsc 52 errors (unchanged from 53 baseline — net +1 improvement); zero touched-file new errors.
- **2026-04-30** — **Waves DWD2-A/B/C · temporal-empties root cause + CSV format + row-cap auto-switch.** Three follow-up waves to the DWD1 download button. (A) [`parseFlexibleDate`](server/lib/dateUtils.ts) now recognises wide-format calendar PeriodIso labels — `2024-Q1` (→ first day of quarter), `2024-Hn` (→ first day of half), `2024-Wnn` (→ Monday of ISO week), `FY2024` / `CY2024` (→ Jan 1), `WE-YYYY-MM-DD` (→ exact date), `MAT-YYYY-MM` (→ first day of anchor month), `YTD-YYYY-MM` and `YTD-YYYY` (→ first day of anchor) — and strips comparative qualifiers (`-TY`, `-YA`, `-2YA`, `-3YA`) before lookup so `MAT-2024-12-YA` resolves the same as `MAT-2024-12`. Rolling / latest_n / bare-comparative shapes (`L12M`, `L12M-YA`, `MAT-TY`, `YTD-2YA`, `MTD-YA`, `XXXX-Q1`) intentionally remain unparseable: anchored to "now", no fixed calendar date — the temporal facet machinery correctly leaves those rows null rather than inventing an anchor. End-to-end value: when the dataset profile pass adds `PeriodIso` to `dataSummary.dateColumns`, `applyTemporalFacetColumns` now derives non-null `Year ·` / `Quarter ·` / `Month ·` facets for calendar rows — directly closing the "some temporal columns just seem empty" complaint the user raised alongside the download button ask. 11 new tests in [`tests/parseFlexibleDateIsoPeriods.test.ts`](server/tests/parseFlexibleDateIsoPeriods.test.ts) (appended to the explicit-file-list `test` script): pin every supported ISO shape, the qualifier-stripping behaviour, the latest_n / bare-comparative null cases, range checks (Q5, H3, W54, month 13 → null), and a `applyTemporalFacetColumns` integration test that proves mixed `PeriodIso` rows produce non-null facets only for calendar values. Adjacent regression sweep: `uploadDateEnrichment` / `dirtyDateEnrichment` / `temporalFacetColumns` / `parseRowDateOpaque` / `createDataSummaryDateInference` / `columnarStorageDateRoundtrip` 66/66 pass — no existing date parser case shifted. (B) `?format=csv` opt-in on `GET /api/data-ops/download-working/:sessionId` (default still xlsx). RFC-4180-ish CSV builder via `buildCsvBuffer` mirrors the inline serialiser in `downloadModifiedDataset`. Filename suffix `_working_<ts>.csv`, `Content-Type: text/csv; charset=utf-8`. Tested: comma-bearing fields quoted, embedded `"` doubled inside surrounding quotes, embedded `\n` preserved inside quoted cells, no trailing newline. Client API helper [`downloadWorkingDatasetXlsx(sessionId, format)`](client/src/lib/api/chat.ts) accepts `'xlsx' | 'csv'` (defaults `'xlsx'`) and returns `{ filename, rowCount }`. (C) Server now sets the `X-Working-Dataset-Row-Count` response header on every working-dataset download (added to [`middleware/cors.ts`](server/middleware/cors.ts) `exposedHeaders` so the browser actually surfaces it cross-origin alongside `Content-Length`). Client gate at [`ChatInterface.tsx:handleDownloadWorkingDataset`](client/src/pages/Home/Components/ChatInterface.tsx) reads `totalRows` (already prop-piped from `useHomeState`) and: stays on xlsx with no warning at <250k rows, stays on xlsx but toasts "Preparing large download — this may take a moment" at 250k–900k rows, and **auto-switches to CSV** at >900k rows with a toast explaining "Excel's per-sheet limit is ~1,048,576 rows" — closes the silent-truncation footgun where a 1.2M-row dataset would have lost rows in xlsx. The CSV switch is automatic; the user doesn't need to know the threshold. **Out of scope, recorded in [`/Users/tida/.claude/plans/follow-up-temporal-and-persist-policy.md`](/Users/tida/.claude/plans/follow-up-temporal-and-persist-policy.md):** (i) auto-promoting `summary.wideFormatTransform.periodIsoColumn` into `summary.dateColumns` at upload time so the Wave-A parser actually fires on every wide-format dataset (Wave A is the building block; deterministic auto-promotion is one more line in `uploadQueue.ts` but needs test-fixture triage across `wideFormatPipeline.test.ts` / `wideFormatShapeBlock.test.ts` / `wideFormatPostMeltPipelineE2E.test.ts` which pin exact `dateColumns` content), (ii) flipping `add_computed_columns` to persist-by-default — recorded as a product decision (Options A/B/C) the user picks explicitly, not a code-only wave, because of blob-storage / Cosmos-doc-size implications and the orthogonality of `derive_dimension_bucket`. Verified: 11/11 Wave-A tests, 5/5 download tests, 66/66 date-subsystem regression, server build clean, server tsc zero new errors over 99-error baseline, client tsc zero new errors over 52-error baseline.
- **2026-04-30** — **Wave DWD1 · Download Working Dataset.** New top-left "Download Dataset" button on the chat surface that streams the latest working dataset as XLSX, regardless of any active filter. Server route `GET /api/data-ops/download-working/:sessionId` ([`dataOpsController.ts:downloadWorkingDataset`](server/controllers/dataOpsController.ts)) reuses [`loadLatestData(chat, undefined, undefined, { skipActiveFilter: true })`](server/utils/dataLoader.ts#L202) so the file is identical to what the agent's tools see — same canonical row source priority chain (chunked → columnar → currentDataBlob → rawData → original blob), same wide-format auto-remelt via [`applyWideFormatMeltIfNeeded`](server/lib/wideFormat/applyWideFormatMeltIfNeeded.ts), same temporal-facet materialization via [`canonicalizeLoadedData`](server/utils/dataLoader.ts#L124-L134), and the Wave-FA non-destructive active filter is intentionally skipped so users always get the canonical unfiltered slice. Per-turn `add_computed_columns` outputs are present iff the agent passed `persistToSession: true` (matches next-turn agent visibility — by design). Client helper [`downloadWorkingDatasetXlsx`](client/src/lib/api/chat.ts) mirrors the existing [`downloadModifiedDataset`](client/src/lib/api/chat.ts) pattern (Content-Disposition filename parse → Blob → `<a download>` click). Button mounts as a third entry in the existing `absolute left-4 top-4` floating cluster at [`ChatInterface.tsx:728`](client/src/pages/Home/Components/ChatInterface.tsx#L728) alongside "Data Summary" and "Give Additional Context"; uses the `Loader2` spinner while the download is in flight. Filename: `<sanitizedFileName>_working_<YYYY-MM-DD_HHmmss>.xlsx` via the existing `sanitizeDownloadFileStem` + `downloadFilenameTimestamp` helpers. 4 new tests in [`tests/downloadWorkingDataset.test.ts`](server/tests/downloadWorkingDataset.test.ts) (appended to the explicit-file-list `test` script per CLAUDE.md): pin (a) ZIP magic bytes on the buffer, (b) Sheet1 with full unfiltered row count, (c) temporal facet columns (`Year · …`, `Month · …`) present in the header when `dataSummary.dateColumns` is non-empty, (d) **`skipActiveFilter:true` regression guard** — when an active filter is set on the chat document, the default `loadLatestData` narrows but the export still returns all canonical rows. Diagnosed-but-deferred (user can authorize separately): some temporal facet cells appear empty because (i) wide-format `latest_n` periods (`L12M`, `YTD-TY`) are unparseable by `parseFlexibleDate` so [`applyTemporalFacetColumns`](server/lib/temporalFacetColumns.ts#L430-L480) leaves Year/Quarter/Month null for those rows (CLAUDE.md WPF6-deferred), (ii) `add_computed_columns` is ephemeral unless `persistToSession:true`, (iii) `dirtyDateEnrichment` LLM batch fall-throughs leave `Cleaned_*` cells null and cascade into null facets. The download button now gives the user concrete evidence to decide which fix to authorize next.
- **2026-04-30** — **Wave WHD1 · domain-aware hypothesis planning.** Closed the only remaining domain-blind site in the agent loop: the hypothesis pre-planner. User asked "where is the FMCG/Marico/internet-search context — is it good enough?" Inventory confirmed packs flow into the planner prompt, the narrator W7 bundle, per-chart `businessCommentary` (W12), the chart-key-insight endpoint, and the `domainLens` envelope with W22 anti-hallucination citation check at [checkEnvelopeCompleteness.ts:117](server/lib/agents/runtime/checkEnvelopeCompleteness.ts#L117) wired at [agentLoop.service.ts:2833](server/lib/agents/runtime/agentLoop.service.ts#L2833) — but [hypothesisPlanner.ts](server/lib/agents/runtime/hypothesisPlanner.ts) and the merged W39 [runHypothesisAndBrief.ts](server/lib/agents/runtime/runHypothesisAndBrief.ts) didn't read `ctx.domainContext` even though it's already populated on the execution context (W34 hoist, [types.ts:175](server/lib/agents/runtime/types.ts#L175)). Hypotheses seed the entire investigation tree downstream, so a domain-blind hypothesis pass meant the agent often wouldn't think to test category seasonality, channel-mix shifts, commodity-cost lag, premiumisation, or sub-brand cannibalisation as candidate explanations even when the relevant pack was loaded into the same turn. Fix mirrors the W12 chart-commentary convention in [insightGenerator.ts:422-428](server/lib/insightGenerator.ts#L422): both `buildUserBlock` (legacy) and the merged W39 user-block now append a `FMCG / MARICO DOMAIN CONTEXT (background only — never numeric evidence; cite pack id when used)` block capped at 2 500 chars (a touch tighter than W12's 3k since the pre-planner is per-turn vs. per-chart). One matching rule line added to each system prompt: "When the FMCG / MARICO DOMAIN CONTEXT block is present, prefer hypotheses that test domain-relevant explanations (category seasonality, channel-mix shifts, commodity/input-cost lag, premiumisation, sub-brand cannibalisation, distribution gains/losses) over generic statistical fishing — but only when the data could plausibly answer them. Do not invent metric names; use only columns from the supplied schema." Cache-friendly: domain block lives in the *user* message (which varies per turn anyway); the system prompt only gets one new rule line, so the `ANALYST_PREAMBLE` static-prefix (>1024 token) cache eligibility is preserved on both paths. Zero plumbing change at call sites — `generateHypotheses(ctx, …)` and `runHypothesisAndBriefMerged(ctx, …)` already take the full execution context. 6 new tests in [`tests/hypothesisPlannerDomainContext.test.ts`](server/tests/hypothesisPlannerDomainContext.test.ts) (appended to the explicit-file-list `test` script per CLAUDE.md): pin (a) domain block in user prompt when set, (b) absent when undefined or whitespace-only — no empty-block leak, (c) cap at 2 500 chars on huge inputs, (d) same three on the merged W39 path. Verified: 6/6 new tests pass; 14/14 adjacent regression tests (existing hypothesis + W39 merged path) pass; 94/94 broader sweep across `agentTurnE2EW20`, `buildSynthesisContext`, `narratorAgent`, `checkDomainLensCitationsW22`, `domainContextWiring`, `synthesisFallback`, `insightGeneratorBusinessCommentaryW12`, `agentRuntimeSchemas`, `llmStubHarnessW18` passes; `npx tsc --noEmit` introduces zero new errors over baseline (the two errors at [hypothesisPlanner.ts:43-44](server/lib/agents/runtime/hypothesisPlanner.ts#L43) on `ctx.sessionAnalysisContext?.sessionContext` are pre-existing — verified by stashing the edit and counting). Out of scope, recorded in the plan: domain-awareness in the coordinator agent (currently dormant under single-flow policy), contextAgent round-2 RAG (data-finding-driven by design), and the mode classifier (orthogonal); a hard-coded FMCG-trigger heuristic for `web_search` (pure LLM judgment is working, hand-rolled keyword triggers tend to misfire); and question-driven pack selection (the 12k token budget + priority sort already handles selection well enough).
- **2026-04-29** — **Wide-format post-melt correctness (Waves WPF1–WPF8).** Closed six structural gaps where parts of the agent pipeline reasoned against stale wide-format mental models on auto-melted datasets (WF1–WF10 baseline). User report: "some answers correct, some wrong on the same wide-format dataset." Three parallel Explore-agent diagnostics confirmed the upload-time pipeline was sound but the planner / narrator / executors had zero awareness of the melt and zero guards against the new long-form schema's special semantics; one fallback data-load path silently re-read the original wide buffer without re-melting. Each gap was independently sufficient to produce wrong answers, fully explaining the "sometimes right, sometimes wrong" pattern.
  - **WPF1 — DATASET SHAPE prompt block.** New `formatWideFormatShapeBlock(summary)` in [`server/lib/agents/runtime/context.ts`](server/lib/agents/runtime/context.ts) emits a labelled block when `summary.wideFormatTransform.detected`. Teaches the LLM: (a) "this dataset arrived in WIDE format and was MELTED to LONG form at upload time", (b) Period (raw label) vs PeriodIso (canonical sortable) — always sort time queries by PeriodIso, (c) for compound shape, NEVER aggregate Value without filtering by Metric (lists distinct metric values from `topValues`), (d) original wide column names that NO LONGER EXIST (capped at 20). Wired into `summarizeContextForPrompt`. Mirrored into the narrator's data-understanding block via [`buildSynthesisContext.ts`](server/lib/agents/runtime/buildSynthesisContext.ts) so the answer envelope phrases magnitudes in metric-aware language ("value sales", "volume") not raw "Value". 6 unit tests in [`tests/wideFormatShapeBlock.test.ts`](server/tests/wideFormatShapeBlock.test.ts).
  - **WPF2 — deterministic compound-shape Metric-filter guard.** New `injectCompoundShapeMetricGuard(step, wideFormatTransform, question, distinctMetricValues)` in [`planArgRepairs.ts`](server/lib/agents/runtime/planArgRepairs.ts), runs in [`planner.ts`](server/lib/agents/runtime/planner.ts) right after `injectRollupExcludeFilters` (mirrors the H3 pattern). For compound-shape datasets, when a step touches `valueColumn` AND no `Metric` filter exists AND `Metric` isn't already in `groupBy`: (a) match the user question against a metric vocabulary (sales/revenue → value-sales family, volume/units → volume family, distribution/price/etc.), (b) inject `dimensionFilter: {column: Metric, op: in, values: [matched]}` for single-metric intent, (c) expand `groupBy` with `Metric` for cross-metric intent ("compare sales vs volume"), (d) fall back to value-sales heuristic when the question is metric-ambiguous (FMCG-pragmatic — better than letting the SUM mix everything). Covers `execute_query_plan`, `breakdown_ranking`, `run_two_segment_compare`, `run_correlation`, `run_segment_driver_analysis`. New `extractDistinctMetricValues(rows, metricColumn)` helper feeds the planner with the dataset's actual metric values; planner.ts hoists this once per turn. 21 unit tests in [`tests/compoundMetricGuard.test.ts`](server/tests/compoundMetricGuard.test.ts).
  - **WPF3 — Period-sort patch (chronological via PeriodIso).** Pre-fix: `dataTransform.applySort` lexicographic-sorted Period values, producing "Q1 24" before "Q2 23" — every trend chart was internally scrambled. Fix: [`applySort`](server/lib/dataTransform.ts) accepts an optional `summary` parameter and remaps Period sort to look up the parallel `PeriodIso` value on each row. The DuckDB SQL builder [`buildQueryPlanDuckdbSql`](server/lib/queryPlanDuckdbExecutor.ts) gets the same treatment: when groupBy includes the wide-format `periodColumn` AND the `periodIsoColumn` exists in the table, also add it to SELECT + GROUP BY (server-side hidden) and emit `ORDER BY periodIsoColumn ASC` by default; explicit Period sorts get rewritten to PeriodIso preserving ASC/DESC direction. Added `hiddenColumns?: string[]` to `BuildQueryPlanDuckdbSqlResult`; `executeQueryPlanOnDuckDb` strips them from result rows so callers see only the planner-requested columns. 9 unit tests in [`tests/wideFormatPeriodSort.test.ts`](server/tests/wideFormatPeriodSort.test.ts).
  - **WPF4 — fallback blob re-parse must re-melt.** Closed the silent-corruption hot path: large wide files (>10 000 rows) had empty `chatDocument.rawData` and no `currentDataBlob`, so [`dataLoader.loadLatestData`](server/utils/dataLoader.ts) re-parsed the original wide buffer via `parseFile`, returning **wide rows** which were then handed to in-memory tools alongside the post-melt `dataSummary.numericColumns: ["Value"]` — silent column-shape mismatch. New shared helper [`applyWideFormatMeltIfNeeded(rows, dataSummary)`](server/lib/wideFormat/applyWideFormatMeltIfNeeded.ts) re-classifies + re-melts when `dataSummary.wideFormatTransform.detected` and the rows look wide. Wired into both `dataLoader.ts` fallback paths (currentDataBlob CSV branch L323 + original blob CSV branch L397) AND the `revert` path in [`dataOpsOrchestrator.ts:5358`](server/lib/dataOps/dataOpsOrchestrator.ts) so "revert to original" returns the post-melt analytical canonical, not the raw wide buffer. Defensive: detects already-long-form rows by header presence (Period+PeriodIso+Value) so currentDataBlob's pre-stored long JSON isn't double-melted. New log line `[dataLoader] re-applied wide-format melt on ... re-parse path` for production grep. 5 unit tests in [`tests/applyWideFormatMeltIfNeeded.test.ts`](server/tests/applyWideFormatMeltIfNeeded.test.ts).
  - **WPF5 — column resolver refuses stale wide-column matches.** Pre-fix: [`columnMatcher.findMatchingColumn`](server/lib/agents/utils/columnMatcher.ts) and [`plannerColumnResolve.resolveToSchemaColumn`](server/lib/agents/runtime/plannerColumnResolve.ts) silently substring-matched agent-emitted "Q1 23 Value Sales" to the live "Value" column — running the analysis on the wrong axis. Fix: both functions accept an optional `wideFormatTransform` parameter; when the requested name matches an entry in `meltedColumns` (case-insensitive), refuse the match and return null (or the raw input passthrough) so downstream Zod / column-allowlist validation surfaces a clear error. Threaded through `normalizeExecuteQueryPlanStepArgs`, `normalizeCorrelationStepArgs`, `normalizeRunSegmentDriverStepArgs` in `planner.ts`. The legacy two-arg `findMatchingColumn` and `resolveToSchemaColumn` signatures still work — backwards-compatible opt-in. 8 unit tests in [`tests/wideFormatColumnResolverGuard.test.ts`](server/tests/wideFormatColumnResolverGuard.test.ts).
  - **WPF6 — PeriodIso surfaced as temporal axis on dateColumns line.** When `wideFormatTransform.detected`, `summarizeContextForPrompt` appends `PeriodIso (canonical period — see DATASET SHAPE block)` to the dateColumns line so the planner doesn't go hunting for a real date column that doesn't exist. Out of scope (deferred): teaching `parseFlexibleDate` to parse ISO period strings like "2023-Q1" / "L12M-2YA" so the existing temporal facet machinery (`Year · …` / `Quarter · …` derived columns) can auto-bucket on PeriodIso — that's a larger refactor and WPF1+WPF3 close the user-visible bug already. Test pinned in [`tests/wideFormatShapeBlock.test.ts`](server/tests/wideFormatShapeBlock.test.ts).
  - **WPF7 — pivot defaults pre-select a Metric for compound shape.** [`mergePivotDefaultRowsAndValues`](server/lib/pivotDefaultsFromExecution.ts) appends the `metricColumn` to `filterFields` with a value-sales-family value pre-selected when compound shape is detected AND the trace plan didn't already pin the Metric column to rows / columns / filters. Without this, the default rendered pivot SUMs Value across mixed metrics — same root cause as gap #2 but in pivot land. Heuristic prefers value-sales family; falls back to first distinct metric alphabetically. 5 unit tests in [`tests/wideFormatPivotDefaults.test.ts`](server/tests/wideFormatPivotDefaults.test.ts).
  - **WPF8 — golden e2e on compound-shape fixture.** New [`tests/wideFormatPostMeltPipelineE2E.test.ts`](server/tests/wideFormatPostMeltPipelineE2E.test.ts) parses an inline 2-brand × 4-quarter × 2-metric wide CSV through the full pipeline (parseFile → classifyDataset → meltDataset → applyWideFormatTransformToSummary → injectCompoundShapeMetricGuard → buildQueryPlanDuckdbSql → applyWideFormatMeltIfNeeded), asserts the contract end-to-end. Pins: (a) compound shape detected, (b) 16 long rows after melt with VND-tagged Value column, (c) compound guard injects `Metric IN ['Value Sales']` for sales-intent question, (d) DuckDB SQL adds `PeriodIso` to GROUP BY + `ORDER BY PeriodIso ASC` + strips it via `hiddenColumns`, (e) fallback re-parse helper restores 16 long rows from the wide buffer.
  - **Conventions added.** (1) **Wide-format awareness flows through three signals**, all gated on `summary.wideFormatTransform.detected`: the `formatWideFormatShapeBlock` prompt block (WPF1), the `injectCompoundShapeMetricGuard` planner repair (WPF2), and the executor-side hidden ISO column (WPF3). New tools that read the dataset shape should consume the same signal — never re-derive shape from heuristics. (2) **`Period` is for human display, `PeriodIso` is for sorting/joining.** This split is now load-bearing across the planner prompt (WPF1), in-memory `applySort` (WPF3), DuckDB ORDER BY (WPF3), and the dateColumns-line surfacing (WPF6). Don't sort by `Period` — always remap to `PeriodIso`. (3) **Compound-shape SUM(Value) without a Metric filter is forbidden.** The deterministic guard fails closed (heuristic value-sales fallback) rather than letting mixed-metric aggregation through. New tools that aggregate the value column on a compound dataset must check `wideFormatTransform.metricColumn` and either filter by Metric or expand groupBy to include it. (4) **All blob-re-parse paths must run `applyWideFormatMeltIfNeeded`.** Direct `parseFile` of the original blob returns wide rows; the post-melt `dataSummary` describes long form. New code reading from `chatDocument.blobInfo.blobName` outside the upload pipeline must call the helper or the analytical caller will silently see the wrong shape.
  - **Out of scope for this stream** (recorded so future-Claude doesn't reopen them): (a) `dataset.shape: 'long' | 'wide-melted'` field on `SessionAnalysisContext` — WPF1's prompt block carries the same information without a schema migration. (b) Snowflake-import auto-melt detection — Snowflake views are typically already long; pivoted views are out-of-scope edge case. (c) `parseFlexibleDate` understanding of ISO period strings (`2023-Q1`, `L12M-2YA`) so the temporal facet machinery can derive `Year · PeriodIso` / `Quarter · PeriodIso` buckets — WPF1+WPF3+WPF6 close the user-visible sort bug already; auto-buckets on PeriodIso would need a larger refactor of date inference. (d) RAG re-indexing for sessions uploaded before WF7 (2026-04-29) — fresh uploads since then index post-melt schema correctly; stale sessions need a one-shot migration script, not a code fix. Verified: all 1953 server tests pass (1862 baseline + ~91 new across WPF1–8); zero new TypeScript errors over baseline (8 pre-existing errors in `scripts/` and `services/chat/` unchanged); `tests/wideFormatPipeline.test.ts` (WF10 golden) still passes — upload pipeline shape was untouched.
- **2026-04-29** — **Dimension hierarchies — full v2 (Waves H6, AD1, EU1, RD1, ML1).** Closed all five deferred items from the H1–H5 stream so the rollup-aware analysis story is complete end-to-end.
  - **H6 · UI banner.** New [`DimensionHierarchiesBanner.tsx`](client/src/components/DimensionHierarchiesBanner.tsx) renders above the Dataset Columns chips when any hierarchy is declared. Collapsed default with a one-line summary; expanded shows column · rollup · `auto` badge (when `source: "auto"`) · children · description. Threaded via [`Home.tsx`](client/src/pages/Home/Home.tsx) (derives from lifted SAC) → [`ChatInterface.tsx`](client/src/pages/Home/Components/ChatInterface.tsx) → [`MessageBubble.tsx`](client/src/pages/Home/Components/MessageBubble.tsx) → [`DataPreview.tsx`](client/src/pages/Home/Components/DataPreview.tsx) → [`ColumnsDisplay.tsx`](client/src/pages/Home/Components/ColumnsDisplay.tsx). The H5 SSE handler in [`useHomeMutations.ts`](client/src/pages/Home/modules/useHomeMutations.ts) was widened to also push `dimensionHierarchies` into the lifted SAC, so the banner refreshes the same turn the user declares "X is the category" — no page reload needed.
  - **AD1 · upload-time auto-detection.** New [`detectRollupHierarchies.ts`](server/lib/detectRollupHierarchies.ts) runs after the SAC seed in [`uploadQueue.ts`](server/utils/uploadQueue.ts). For each (dimension, measure) pair it computes per-value sums and checks two strict thresholds — top value's share ≥ 70 % of column total AND top value ≥ 4× the runner-up — to reject false positives like 60 %-share market leaders or 80/20 splits. Picks the strongest measure when several exist. Stamps results onto `sessionAnalysisContext.dataset.dimensionHierarchies` with `source: "auto"`. The H2 immutability guard was simplified in lockstep: `withImmutableUserIntentFromPrevious` in [`sessionAnalysisContextGuards.ts`](server/lib/sessionAnalysisContextGuards.ts) now preserves ALL prior hierarchies (user + auto) across assistant merges — assistants never mutate dimension structure; only the user-merge LLM and the EU1 endpoint can change it. The pre-AD1 H2 logic dropped previous-auto entries on the first assistant merge, which would have wiped upload-time detections; the existing H2 test was rewritten to pin the new contract.
  - **EU1 · in-banner remove + PUT endpoint.** New `PUT /api/sessions/:sessionId/hierarchies` ([`sessions.ts`](server/routes/sessions.ts) + [`putSessionHierarchiesEndpoint`](server/controllers/sessionController.ts)) replaces the entire hierarchies array atomically via a new helper [`updateSessionDimensionHierarchies`](server/lib/sessionAnalysisContext.ts) that uses the same per-session mutex chain as the assistant merge. Body validated with `dimensionHierarchySchema`, capped at 20 entries. New client API [`sessionsApi.updateSessionHierarchies`](client/src/lib/api/sessions.ts). The banner shows a ✕ button per entry (mirrors the `wideFormatTransform` pattern); click → optimistic `onChange` callback updates the lifted SAC, banner re-renders without a refetch. Spinner during the network round-trip; inline error line with the failure reason if the PUT rejects. To ADD a hierarchy, the user re-states it in chat — the H5 chat-flow path extracts and persists it. The footer hint inside the banner tells the user this explicitly.
  - **RD1 · share-of-category denominator intent.** Extended H3's "skip rollup-exclude" check beyond the existing rollup-value mention. New [`shouldSkipRollupExclude`](server/lib/agents/runtime/planArgRepairs.ts) recognises share/contribution/%/fraction/portion/proportion patterns combined with EITHER the column name OR a generic category keyword (`category`/`total`/`overall`/`whole`/`entire`). When matched, the not_in filter is suppressed so the rollup row stays in the data, and a new `DETECTED INTENT — share-of-category` block is appended to the H4 hierarchy prompt instructing the LLM to use the rollup AS the denominator (`MARICO 6000 / FSG 68751 = ~9 %`, NOT `MARICO / sum-of-others`). Fix-along: an early version used `\b%\b` which fails because `%` is a non-word char (no word boundary between two non-word chars in `" % of"`); rewrote to `(?:\b(?:share|...)\b|%)\s+(?:of|...)\b`. Narrator system prompt got one matching guidance line. New [`classifyHierarchyIntent`](server/lib/agents/runtime/planArgRepairs.ts) reports per-hierarchy intent (`share-of-category` / `rollup-mention` / `peer-comparison`) used by the H4 block.
  - **ML1 · multi-level same-column hierarchies.** The schema already allowed multiple entries per column (`z.array(...).max(20)`), but H3's `find()` returned only the first match — so a 3-level Geography hierarchy (`World` → `Asia` → `India`) had only the top level excluded, the inner levels still polluted breakdowns. Switched to `filter()` that collects ALL rollup values for the groupBy column, applies per-value override checks (mention or share-of-category), and emits ONE consolidated `not_in` filter with all surviving values. Override-by-mention works at the level the user names: "show me Asia by country" excludes World + India but keeps Asia. Existing rollup values already in a `not_in` filter are detected and not double-added. Extended `MERGE_USER_SYSTEM` prompt with a multi-level example so the user-merge LLM emits one entry per nested rollup. The H4 banner already rendered each entry on its own line; no template change needed.
  - **Conventions added / changed.** (1) **`source: "user"` AND `source: "auto"` are both immutable across assistant merges.** Only the user-merge LLM (chat-flow extraction or PATCH endpoint) and the EU1 PUT endpoint can change `dimensionHierarchies`. The pre-AD1 distinction (user immutable, auto mutable) was a latent bug — once auto-detection landed, assistant merges would silently drop them. (2) **Auto-detection thresholds are intentionally strict.** Heuristic minimums of 70 % column share AND 4× runner-up ratio are tuned to reject market leaders and Pareto distributions. False negatives (heuristic misses a real category total) are acceptable because the user can always declare it in chat. False positives are unacceptable because they silently exclude the row from breakdowns and degrade the answer without explanation. (3) **Multi-level hierarchies are flat in storage, hierarchical in semantics.** No tree structure in the schema — multiple entries per column with independent `rollupValue` strings. The H3 logic treats them as a flat set during exclude-filter computation, which is correct for "give me peers" intent; the LLM handles the implicit nesting in narrative. Verified: 1862/1862 server tests pass (full suite, no flake this time); 198/198 client vitest pass; client tsc 52-error baseline preserved (zero new errors); both production builds OK.
- **2026-04-29** — **Excel-style non-destructive Filter Data overlay (Waves FA1–FA5).** The legacy "Filter Data" button mounted a 3-step modal (column → operator → value) that built a chat message like `"filter data where Products != FEMALE SHOWER GEL"` and routed through the data-ops orchestrator's `case 'filter'`, which called `saveModifiedData` — every filter wrote a new blob version, replaced `rawData`, regenerated `dataSummary` + `columnStatistics`, scheduled RAG reindex, and pushed an entry into `dataVersions`. The original was preserved in `blobInfo` (only used by `revert`), but each filter was a heavyweight blob round-trip with side-effects. Two visible problems: (a) the UI was a builder, not Excel; (b) once the chat scrolled, the user had no idea they were on a filtered slice.
  - **FA1** — schema + pure fns. Added `activeFilterSpecSchema` to [`server/shared/schema.ts`](server/shared/schema.ts) (`{conditions: ({kind:"in"|"range"|"dateRange", column, …})[], version, updatedAt}`) and `activeFilter?: ActiveFilterSpec` on `ChatDocument`. New [`server/lib/activeFilter/applyActiveFilter.ts`](server/lib/activeFilter/applyActiveFilter.ts) (in-memory predicate, reuses `pivotDimensionStringKeyForChartFilter` for value normalization) and [`server/lib/activeFilter/buildActiveFilterSql.ts`](server/lib/activeFilter/buildActiveFilterSql.ts) (composes existing `quoteIdent` / `escapeSqlStringLiteral` from [`pivotFilterSql.ts`](server/lib/pivotFilterSql.ts) — never hand-rolled escapes). 21 unit tests in [`tests/applyActiveFilter.test.ts`](server/tests/applyActiveFilter.test.ts) and [`tests/buildActiveFilterSql.test.ts`](server/tests/buildActiveFilterSql.test.ts) cover in / range / dateRange / SQL injection / multi-condition AND / empty result.
  - **FA2** — read-side enforcement. `loadLatestData` in [`server/utils/dataLoader.ts`](server/utils/dataLoader.ts) gained a `skipActiveFilter?: boolean` option; when not set, every analytical caller (chat turn, dashboard chart load, chart-key-insight refresh, data-ops bridge, public sessions endpoints) automatically receives filtered rows. The DuckDB rematerialize path (`ensureSessionDuckdbMaterialized`) opts out via `skipActiveFilter: true` so the canonical `data` table is never destroyed by filter changes. New [`resolveSessionDataTable`](server/lib/activeFilter/resolveSessionDataTable.ts) issues `CREATE OR REPLACE VIEW data_filtered AS SELECT * FROM data WHERE <expr>` (idempotent on `activeFilter.version`) and returns the right table name. [`queryPlanDuckdbExecutor`](server/lib/queryPlanDuckdbExecutor.ts), [`pivotQueryService`](server/lib/pivotQueryService.ts), [`duckdbPlanExecutor`](server/lib/duckdbPlanExecutor.ts), and the `pivot/drillthrough` endpoint in [`dataApi.ts`](server/routes/dataApi.ts) all route through it. The pivot `cacheKey` now includes `activeFilterVersion` so changing the filter invalidates pivot results without manual flushing. The pivot/fields distinct-values endpoint accepts `?excludeColumn=` so opening the Region filter shows all Regions narrowed only by *other* active conditions (Excel cross-column behavior).
  - **FA3** — controller + UI panel + API client. New [`activeFilterController.ts`](server/controllers/activeFilterController.ts) with `GET / PUT / DELETE /api/sessions/:sessionId/active-filter`, mutex-serialized per session (W40 pattern), wired in [`routes/sessions.ts`](server/routes/sessions.ts). The PUT/DELETE response includes the filtered row count + 50-row preview so the client doesn't need a second round-trip. Replaced the legacy modal with [`client/src/components/FilterDataPanel.tsx`](client/src/components/FilterDataPanel.tsx) — a right-side slide-in `Sheet` listing every column with per-column expand-to-filter (multi-select for categorical via `fetchPivotColumnDistincts(..., { excludeColumn })`, min/max range for numeric, date range for dates). Filter changes debounce ~250 ms and POST automatically; closing the panel does not clear the filter. New API client methods: `sessionsApi.getActiveFilter / setActiveFilter / clearActiveFilter`.
  - **FA4** — chip strip + button highlight + chat-input reroute + revert. New [`client/src/components/ActiveFilterChips.tsx`](client/src/components/ActiveFilterChips.tsx) renders above the chat scroll area when conditions are set; each chip has a ✕ to remove that one condition, plus a "Clear all" button. The "Filter Data" button in [`ChatInterface.tsx`](client/src/pages/Home/Components/ChatInterface.tsx) becomes solid-primary with a count badge when active. The natural-language `'filter'` data-op intent in [`dataOpsOrchestrator.ts`](server/lib/dataOps/dataOpsOrchestrator.ts) now translates to `ActiveFilterSpec` when all operators are representable (`=`, `in`, `>=`, `<=`, `>`, `<`, `between`) and writes via [`persistActiveFilter.ts`](server/lib/activeFilter/persistActiveFilter.ts) — no more `saveModifiedData`, no more blob/`rawData` mutation. Operators not modelable as active filter (`!=`, `contains`, `startsWith`, `endsWith`) keep the legacy destructive path with a console warning so we know which sessions still hit it. The `revert` data-op now also clears the active filter so users return to a clean canonical state in one operation.
  - **FA5** — direct-rawData reads + chartResponse + cache audit. Three places that bypass `loadLatestData` were wrapped in `applyActiveFilter`: the data-preview endpoints in [`dataRetrievalController.ts`](server/controllers/dataRetrievalController.ts) (so the preview matches what analyses see), the chart enrichment fallback at `chatResponse.service.ts:50` (so chart insights derive from the filtered slice), and `runDataOpsFromAgent` (so agent-routed data ops also see the slice). The `getAnalysisData*` endpoints now also return `activeFilter` so clients hydrate state on session load. Deleted dead `client/src/components/FilterDataModal.tsx`; slimmed `ColumnFilterDialog.tsx` to a type-only export consumed by `FilterAppliedMessage` and `MessageBubble` for rendering historical chat bubbles.
  - **FA6** — dashboard provenance capture. Added `capturedActiveFilter?: ActiveFilterSpec` to both `dashboardSchema` and `dashboardSpecSchema`. The agent-loop auto-create site at [`agentLoop.service.ts`](server/lib/agents/runtime/agentLoop.service.ts) and the `POST /api/dashboards/from-spec` controller both snapshot `chatDocument.activeFilter` onto the spec at creation time (controller falls back to looking up the session if the client didn't include it). [`createDashboardFromSpec`](server/models/dashboard.model.ts) persists the snapshot. The chart data inside the dashboard is already filtered (it was loaded via `loadLatestData` before being snapshotted), so the captured filter is **provenance metadata, not a re-applied predicate** — chart numbers don't change at view time. New [`CapturedFilterBanner.tsx`](client/src/pages/Dashboard/Components/CapturedFilterBanner.tsx) renders below the dashboard header when present ("Captured with filter: Region ∈ {North}. Numbers reflect the filtered slice at the time this dashboard was created."). Client `DashboardData` shape and `normalizeDashboard` in [`useDashboardState.ts`](client/src/pages/Dashboard/modules/useDashboardState.ts) carry the field through. Dashboards created without an active filter behave exactly as today.
  - **Conventions added.** (1) **The canonical `data` table is inviolate.** Filters create the `data_filtered` view via `CREATE OR REPLACE VIEW`; `data` is rebuilt only by upload / data-ops `revert`. Tests that materialize tables directly should call the rematerialize path with `skipActiveFilter: true` (already the default for `ensureAuthoritativeDataTable`). (2) **All analytical reads go through `loadLatestData` or `resolveSessionDataTable`.** New tools that read row data must pick one — never `chatDocument.rawData` directly, otherwise the filter silently bypasses (the three known direct-read sites are wrapped explicitly; new direct reads will fail to honor the filter and surprise users). (3) **RAG retrieval is unaffected by the active filter** — `retrieve_semantic_context`, the contextAgent multi-round, and `indexSession.ts` continue to embed canonical chunks. Documented in `applyActiveFilter`'s JSDoc to prevent a future "fix" from narrowing it. (4) **Cache keys must include `activeFilter.version`** — pivot cache already does; future caches around filtered data must follow suit, otherwise the agent will return yesterday's pre-filter aggregates.
- **2026-04-29** — **User-told dimension hierarchies (Waves H1–H5).** When a user uploads data where one row in a dimension column is a category total that rolls up the other rows in the same column (e.g. Marico-VN: `FEMALE SHOWER GEL` totals `MARICO + PURITE + OLIV + LASHE` in the `Products` column), the agent used to treat the parent and children as siblings, so every breakdown was dominated by the category at ~88% — mathematically just "the parent always wins". Now the user can tell the system once ("FEMALE SHOWER GEL is the entire category. Marico, Purite, Oliv, Lashe are products within it.") and that fact persists for the rest of the session: peer-comparison aggregations on `Products` deterministically exclude the rollup row, and the planner + narrator are explicitly told to frame the rollup as a category, not a competing item.
  - **H1** — schema. Added `dimensionHierarchySchema` and `dataset.dimensionHierarchies?: DimensionHierarchy[]` to [`sessionAnalysisContextSchema`](server/shared/schema.ts) (re-exported through [`client/src/shared/schema.ts`](client/src/shared/schema.ts)). Shape: `{ column, rollupValue, itemValues?, source: "user" | "auto", description? }`. Optional with no default — pre-existing Cosmos docs parse unchanged.
  - **H2** — capture. Extended `MERGE_USER_SYSTEM` in [`sessionAnalysisContext.ts`](server/lib/sessionAnalysisContext.ts) so the existing user-merge LLM populates `dimensionHierarchies` from "X is the category / Y is rolled up / are products within …" phrasings. Tightened [`sessionAnalysisContextGuards.ts`](server/lib/sessionAnalysisContextGuards.ts) `withImmutableUserIntentFromPrevious` to also pin `source: "user"` hierarchies across assistant merges (assistant-introduced `source: "auto"` entries can still be replaced — that's the H6+ auto-detect seam). The H5 chat-flow wiring (below) calls `extractAndPersistUserHierarchies`, which gates on a deterministic regex pre-check (`shouldExtractUserHierarchies`) before paying for the user-merge LLM call — routine analytical questions don't fire it.
  - **H3** — deterministic filter injection. New [`injectRollupExcludeFilters`](server/lib/agents/runtime/planArgRepairs.ts) is called from [planner.ts](server/lib/agents/runtime/planner.ts) right after `ensureInferredFiltersOnStep`. For every step with `groupBy` / `breakdownColumn` matching a declared hierarchy column, it appends a `not_in` filter against the rollup value (`match: "case_insensitive"`). Three skip cases: (a) the user's question text mentions the rollup value (override-by-mention — they're asking *about* the rollup), (b) an existing `in`-filter on that column already includes the rollup value (explicit user-driven inclusion wins), (c) an existing `not_in`-filter already excludes it. Targets `execute_query_plan.plan.groupBy` and `breakdown_ranking.breakdownColumn`; the executor's `not_in` op was already supported, so no SQL/exec changes were needed.
  - **H4** — prompt awareness. New [`formatDimensionHierarchiesBlock`](server/lib/agents/runtime/context.ts) emits a labelled `DIMENSION HIERARCHIES (declared by the user — treat as ground truth):` block listing each hierarchy with its column, rollup value, optional children, and a footer reminder about peer-comparison exclusion. Threaded into `summarizeContextForPrompt` for the planner and into the narrator user message in [`narratorAgent.ts`](server/lib/agents/runtime/narratorAgent.ts). The narrator system prompt also gets one new static rule line ("when the user message includes a DIMENSION HIERARCHIES block, treat the listed rollup values as category totals — never as competing items …") — kept static so the system prefix stays cache-stable.
  - **H5** — chat-input extraction + SSE + RAG. The user's clarification was typed into the chat input box (not the "Add context" PATCH endpoint), so the existing user-merge LLM never ran on it. Added `extractAndPersistUserHierarchies` to [`sessionAnalysisContext.ts`](server/lib/sessionAnalysisContext.ts) (regex-gated user-merge LLM call, persists via the same per-session mutex chain as `persistMergeAssistantSessionContext`, returns the new SAC only when `dimensionHierarchies` actually changed). Wired into [`chatStream.service.ts`](server/services/chat/chatStream.service.ts) right after mode classification — runs before the agent loop so the planner sees the hierarchy on the same turn the user declares it. The `session_context_updated` SSE event fires immediately with the new `dimensionHierarchies` field; the post-turn emit was widened to also include hierarchies. The RAG `user_context` chunk in [`rag/chunking.ts`](server/lib/rag/chunking.ts) appends a "Declared dimension hierarchies (treat as ground truth …)" section so retrieval-side context picks them up too; `buildChunksForSession` now emits the chunk when EITHER `permanentContext` OR hierarchies exist (was: only when permanentContext).
  - **H6 deferred (UI chip).** Plan flagged this as "polish, defer if tight". The end-to-end behavior change is visible in the analysis output (FEMALE SHOWER GEL no longer dominates the breakdown; the narrator phrases findings as "within the FEMALE SHOWER GEL category, MARICO leads at …"). A read-only "Defined hierarchies" chip in [`ColumnsDisplay.tsx`](client/src/pages/Home/Components/ColumnsDisplay.tsx) — mirroring the `wideFormatTransform` deep prop-drill through `useHomeState` → `useHomeMutations` → `Home.tsx` → `ChatInterface` → `MessageBubble` → `DataPreview` → `ColumnsDisplay` — is the natural follow-up wave.
  - **Out of scope for this stream**, recorded explicitly in [the plan](/Users/tida/.claude/plans/letting-the-app-know-bright-haven.md): (a) auto-detection of rollup rows at upload time (heuristic `max(value) ≈ sum(others)` plus an LLM check inside [`datasetProfile.ts`](server/lib/datasetProfile.ts) — would set `source: "auto"`); (b) rewriting share/contribution questions to use the rollup row as denominator (needs intent classification that can misfire); (c) multi-level hierarchies and cross-column hierarchies (e.g. `Region → All Regions`); (d) editable add/remove UI for hierarchies. v1 ships one user-told rollup per column, deterministically excluded from peer comparisons.
  - **Conventions added.** (1) **`source: "user"` SAC fields are immutable across assistant turns.** The `withImmutableUserIntentFromPrevious` guard now pins user hierarchies in addition to user intent — assistant-merge prompts that try to drop them get overridden. Auto-detected entries (H6+) will use `source: "auto"` and remain mutable. (2) **Deterministic filter injection runs in `planner.ts` after `ensureInferredFiltersOnStep`, never inside individual tools.** This keeps the rollup-exclude logic in one place and uniform across all `dimensionFilters`-aware tools. (3) **The user-merge LLM call is regex-gated for chat-input messages.** `shouldExtractUserHierarchies(text)` checks for hierarchy keywords ("category", "rollup", "are products within …", etc.) before paying for an LLM call — routine analytical questions like "what are sales by region?" don't trigger it. The PATCH `/api/sessions/:id/context` endpoint still runs the user-merge unconditionally because that flow is explicitly user-initiated for context updates. Verified: 250/250 pass across the new tests + adjacent regression sweep (schema, planArgRepairs, planner/narrator prompts, chunking, SSE, agent E2E); full server suite at 1776/1777 with one unrelated flake in `streamingNarratorW38.test.ts` ("Unable to deserialize cloned data" — node:test worker serialization hiccup that passes deterministically in isolation).
- **2026-04-29** — **Wave PF3 · pivot FILTERS shelf — strip paging/server-search complexity, always fetch full distincts.** User clarified that the FILTERS popover should access the full dataset's distinct values for any column — same authoritative DuckDB `data` table the agent's tools see — and that there's no need for the `hasMore`/paged-dimension/server-search-typeahead complexity introduced in PF1/PF2 ("the app should get access to the entire dataset like all the other internal tools and agents, and then choose the values to be filtered from there"). They also confirmed that the agent's final answer was always correct in production — the bug was strictly in the pivot section's chart and pivot UI rendering, never in the agent path. PF3 simplifies accordingly. (1) [`fetchPivotColumnDistincts`](client/src/lib/api/data.ts) signature collapsed back to `(sessionId, column, limit?) => Promise<string[]>` (dropped `q` parameter and the `PivotColumnDistinctsResult` envelope with `hasMore`/`cardinality`); default `limit` bumped 2000 → **100 000** (defensive ceiling, not a paging window — any realistic FMCG dimension is well below it). (2) [`syncFilterSelectionsWithFilters`](client/src/lib/pivot/buildPivotModel.ts) `hasMoreByField` parameter removed; restored the simple "no hint → auto-fill all loaded distincts" behavior, with a stale-hint guard (hint that doesn't intersect distincts → fall back to all) since distincts are now always authoritative. (3) [`DataPreviewTable.tsx`](client/src/pages/Home/Components/DataPreviewTable.tsx): dropped `sessionFilterDistinctsHasMore` state + `handleSearchPivotDistincts` callback (the server-search round-trip); restored the unconditional `isAll → omit field from payload` optimization (the snapshot is always the full set now, so `selected == snapshot` truly means "no filter applied"). The `sessionFilterDistinctsErrors` state, `filterDistinctsRetryNonce`, and the render-time-derived `filterDistinctsResolution: Record<string, 'loading' | 'loaded' | 'error'>` map all stay — those are the right guarantees regardless of paging. (4) [`PivotFieldPanel.tsx`](client/src/pages/Home/Components/pivot/PivotFieldPanel.tsx) `FilterFieldRow`: dropped `hasMore`, `onSearch`, the search-debounce `useEffect` + seq ref, and the `isPagedNoSelection` branch. Replaced the conditional "Showing partial list — type to search the full dataset" hint with a simple client-side substring filter `Input` (no network, no debounce) shown only when the loaded list has > 8 values. (5) [`PivotHeaderSliceFilter.tsx`](client/src/pages/Home/Components/pivot/PivotHeaderSliceFilter.tsx) updated for the new `string[]` return shape. (6) Tests: rewrote [`syncFilterSelections.vitest.test.ts`](client/src/lib/pivot/syncFilterSelections.vitest.test.ts) (6 tests now pinning the simpler contract: no-hint auto-fill, hint intersection, stale-hint fallback, sample-row fallback, field removal, selection preservation); rewrote [`agentFilterAutoApply.vitest.test.ts`](client/src/lib/pivot/agentFilterAutoApply.vitest.test.ts) (3 tests pinning the "show FEMALE SHOWER GEL only" → chip flow + the snapshot-equals-selection round-trip contract). Verified: `cd client && npm test` 198/198 pass; `npx tsc --noEmit` 52 errors total (identical to post-PF2 baseline — zero new errors). No server changes — the existing `/api/data/:sessionId/pivot/fields` endpoint already supports any limit value via the query string. Agent path (planner / executor / narrator / synthesis) untouched, as PF1/PF2/PF3 only ever modified client-side pivot rendering files.
- **2026-04-29** — **Wave PF2 · pivot FILTERS shelf — flicker fix, paged-dimension semantics, error retry, and auto-apply tests.** Critical follow-up to PF1 closing three real defects in PF1 plus locking in the agent-filter auto-apply contract. (1) **Sub-frame flicker** in [`PivotFieldPanel.tsx`](client/src/pages/Home/Components/pivot/PivotFieldPanel.tsx) `FilterFieldRow`: the `loading` flag was set inside the `useEffect` that fires *after* React commits, so the popover briefly rendered "No values to filter" before the loader appeared. Replaced the effect-derived `loading` boolean with a render-time-derived `filterDistinctsResolution: Record<string, 'loading' | 'loaded' | 'error'>` map computed in [`DataPreviewTable.tsx`](client/src/pages/Home/Components/DataPreviewTable.tsx) from `pivotSyncFields` membership minus `sessionFilterDistincts` keys minus the new `sessionFilterDistinctsErrors` keys — fields appear "loading" by construction the moment they enter the FILTERS shelf, no race window possible. (2) **Silent narrowing regression for paged dimensions** introduced by PF1: [`syncFilterSelectionsWithFilters`](client/src/lib/pivot/buildPivotModel.ts) auto-fills `selected = new Set(distinctNow)` when no agent-hint exists; pre-PF1 this paired with the "isAll → omit" optimization (auto-fill all + snapshot==selected → omit field from payload → server returns everything). PF1 correctly disabled isAll-omit for `hasMore=true` to avoid omitting based on a partial snapshot, but did NOT touch the auto-fill — net effect: for a 5000-product Markets column the loaded 2000 became the explicit payload, and the server filtered out the other 3000 silently. Fixed: `syncFilterSelectionsWithFilters` now takes `hasMoreByField` and for paged fields with no hint, leaves `next[f] = undefined` (server gets no filter, returns all). For paged fields with an agent hint, honors the hint as-is even when none of the hinted values made it into the loaded subset (the hint IS the explicit include-list intent — e.g. agent `dimensionFilters: [{column: 'Products', op: 'in', values: ['FEMALE SHOWER GEL']}]` survives even when 'FEMALE SHOWER GEL' is alphabetically beyond the 2000-row distincts cap). `FilterFieldRow` renders no checkboxes checked when `selected === undefined && hasMore`, with a clarifying line "All values included. Search and check to narrow." — matches Excel pivot semantics for paged dimensions. (3) **Error state with retry**: PF1's catch block silently swallowed `/pivot/fields` failures, which combined with the new flicker fix would leave the field stuck "Loading…" forever. Added `sessionFilterDistinctsErrors: Record<string, string>` state, a `handleRetryFilterDistincts` callback, and a `filterDistinctsRetryNonce` counter that re-fires the fetch effect. The popover now renders an inline error block with a Retry button when the resolution is `'error'`. (4) **Search seq-ref guard**: per-row search-debounce in `FilterFieldRow` now tracks an internal seq ref so fast-typing supersedes prior fetches and a late-arriving response can't clobber the active query's spinner. (5) **Tests**: confirmed end-to-end via the Explore subagent that the agent-dimensionFilters → `pivotSliceDefaultsFromDimensionFilters` → `mergePivotDefaultsForResponse` → message envelope `pivotDefaults.filterFields`/`filterSelections` → `createInitialPivotConfig` (adds field to `config.filters`) → `syncFilterSelectionsWithFilters` (hydrates the `Set`) → `PivotFieldPanel` chip pipeline is wired end-to-end with no env-flag gating; pinned the contract with two new vitest files: [`syncFilterSelections.vitest.test.ts`](client/src/lib/pivot/syncFilterSelections.vitest.test.ts) (7 tests covering paged/non-paged auto-fill semantics) and [`agentFilterAutoApply.vitest.test.ts`](client/src/lib/pivot/agentFilterAutoApply.vitest.test.ts) (3 tests pinning the "show FEMALE SHOWER GEL only" → chip flow). Verified: `cd client && npm test` 199/199 pass (was 189 → 10 new tests); `npx tsc --noEmit` 52 errors total (identical to post-PF1 baseline — zero new errors). No server changes.
- **2026-04-29** — **Wave PF1 · pivot FILTERS shelf — full-distincts + remove sample-based fallback.** Closed two linked bugs the user reported as "the pivot section is unable to apply filters correctly." (1) The FILTERS shelf popover in [`PivotFieldPanel.tsx`](client/src/pages/Home/Components/pivot/PivotFieldPanel.tsx) silently fell back to `distinctPivotFilterKeysFromRows(data, …)` over the agent's narrow output sample (50–200 rows) whenever `sessionFilterDistincts[field]` hadn't loaded yet — so users saw an incomplete value list and could not deselect values like `FEMALE SHOWER GEL` that exist in the full DuckDB session but not in the sample. (2) [`DataPreviewTable.tsx`](client/src/pages/Home/Components/DataPreviewTable.tsx)'s `effectivePivotModel = serverPivotModel ?? pivotModel` selector silently rendered a sample-based `buildPivotModel(pivotRows, …)` whenever the server pivot was loading or errored; selections for values not in the sample collapsed every row → "No data points produced." Fixes: in analysis variant (with `sessionId`), `FilterFieldRow` now sources values **only** from `filterDistinctsFromSession`, shows a "Loading values…" spinner until the fetch resolves, and surfaces a "Search values…" `Input` when the response carries `hasMore: true` (debounced 200 ms, calls `/pivot/fields?column=&q=&limit=200` and merges results). [`fetchPivotColumnDistincts`](client/src/lib/api/data.ts) was extended to return `{values, hasMore, cardinality}` and accept an optional `q` substring (server endpoint already supported it via the `q` query param at [`dataApi.ts`](server/routes/dataApi.ts#L354-L358), so no server change). `DataPreviewTable` now tracks per-field `sessionFilterDistinctsLoading` / `sessionFilterDistinctsHasMore` state and pipes them down. The `effectivePivotModel` selector now skips the client `pivotModel` entirely when in analysis variant with a sessionId — `serverPivotModel` is the sole source of truth, and existing `serverPivotError` / `serverPivotLoading` UI surfaces handle the loading/failure paths. Added a defensive guard on the "isAll → omit field from payload" optimization (lines 737-748 and 781-790): only short-circuits when `sessionFilterDistinctsHasMore[f] === false`, otherwise sends the explicit selection list (paged snapshots are not authoritative). Verified: `cd server && npm test` 1737/1737 pass; `cd client && npm test` 189/189 pass; `npx tsc --noEmit` introduces zero new errors in any file I touched (pre-existing baseline errors unchanged); theme:check pre-existing violations unchanged. Files touched: [`client/src/lib/api/data.ts`](client/src/lib/api/data.ts), [`client/src/lib/api/index.ts`](client/src/lib/api/index.ts), [`client/src/pages/Home/Components/pivot/PivotHeaderSliceFilter.tsx`](client/src/pages/Home/Components/pivot/PivotHeaderSliceFilter.tsx), [`client/src/pages/Home/Components/pivot/PivotFieldPanel.tsx`](client/src/pages/Home/Components/pivot/PivotFieldPanel.tsx), [`client/src/pages/Home/Components/DataPreviewTable.tsx`](client/src/pages/Home/Components/DataPreviewTable.tsx). Out of scope: the chart preview's "No data points produced for this chart configuration" copy when Line is paired with a non-temporal `Products`-on-COLUMNS layout — that's a `recommendPivotChart` validity issue, not a filter bug; leave for a separate wave.
- **2026-04-29** — **Wave W56 · thinking-panel coverage for silent agent phases.** Closed the long pause after the "Detecting query type" thinking row. The agent loop was doing 8–20 s of real work (data load, analysis brief, hypothesis generation, upfront RAG, planner LLM, narrator setup) before the first `plan` SSE event fired, and none of it surfaced — users perceived a freeze. Fix is pure instrumentation: emit `thinking` SSE events at each silent-phase boundary, reusing the existing `onThinkingStep` → `ThinkingPanel.StepRow` pipeline (no new event types, no schema change, no client-side changes). New active/completed step pairs added: **"Loading dataset"** (wraps `resolveAnswerQuestionDataLoad` in [chatStream.service.ts](server/services/chat/chatStream.service.ts) — completes with `"{N} rows · {M} columns"`); **"Generating hypotheses"** or **"Drafting analysis brief & hypotheses"** when `MERGED_PRE_PLANNER` is on (wraps `generateHypotheses` / `runHypothesisAndBriefMerged` in [agentLoop.service.ts](server/lib/agents/runtime/agentLoop.service.ts) — completes with hypothesis count); **"Retrieving session context"** (wraps the upfront RAG block — completes with `{N} hits`); **"Planning approach"** (wraps `runPlannerWithOneRetry` — completes with step count, before the existing `plan` SSE event flips "Agent plan" to completed); **"Synthesizing answer"** (wraps the narrator call — completes on first `answer_chunk` arrival, falling back to post-narrator on non-streaming repairs). The agent loop emits via `safeEmit("thinking", …)`; `chatStream.service.ts onAgentEvent` already falls through unknown events to `sendSSE`, so loop-emitted `thinking` events reach the existing client dispatcher unchanged. The client's `onThinkingStep` reducer dedupes by `step` name — `active → completed` updates the row in place rather than appending. Verified: `cd server && npm test` 1715/1715 pass; `cd client && npm test` 189/189 pass; `npx tsc --noEmit` introduces zero new errors over baseline (the lone remaining error at chatStream.service.ts:1365 is pre-existing, unrelated `spawnedQuestions` cast).
- **2026-04-29** — **Wide-format auto-melt + currency-aware parsing (Waves WF1–WF10).** Marico-VN Nielsen-style spreadsheets (period-as-column, đồng-prefixed cells) are now auto-transformed at upload time. The dormant [`server/lib/wideFormat/`](server/lib/wideFormat/) tagger is finally wired in.
  - **WF1** — extended [`periodVocabulary.ts`](server/lib/wideFormat/periodVocabulary.ts) to cover the Marico-VN shapes the screenshot exposed: `Latest 12 Mths` / `Latest 12 Mths YA` / `Latest 12 Mths 2YA` (new `latest_n` kind), `YTD TY` / `YTD YA` / `YTD 2YA`, and the `w/e DD/MM/YY` slash-DMY week-ending date format. Added a trailing-`w/e` decoration stripper so compound headers like `Q1 23 - w/e 23/03/23` and `Latest 12 Mths 2YA - w/e 23/12/23` resolve to the leading period token. New ISO labels: `L12M`, `L12M-YA`, `L12M-2YA`, `YTD-TY`, `YTD-YA`, `YTD-2YA`. 11 new test cases.
  - **WF2** — new [`currencyVocabulary.ts`](server/lib/wideFormat/currencyVocabulary.ts). `stripCurrencyAndParse(value)` peels leading/trailing currency symbols (đ→VND, R$→BRL, S$→SGD, HK$→HKD, RM→MYR, Rp→IDR, kr→SEK, plus 14 single-character symbols `$ € £ ¥ ₹ ₩ ₪ ₺ ฿ …`) and parses the remainder. `detectCurrencyInValues(samples)` votes the dominant `{symbol, isoCode, position}` over up to 200 sample strings (≥80% threshold). `AMBIGUOUS_SYMBOLS = {$, kr, ¥}` flags symbols the LLM must disambiguate. 35 unit tests.
  - **WF3** — new [`classifyDataset.ts`](server/lib/wideFormat/classifyDataset.ts). Tags every header via `tagColumn`; returns `{isWide, shape: 'pure_period'|'compound'|'pivot_metric_row', idColumns, periodColumns, …}`. Wide iff (a) period+compound cols ≥ max(3, 30%), (b) ≥2 distinct period ISOs, (c) ≥1 id column. Tests pin the screenshot's 21-column header list to `pure_period`.
  - **WF4** — new [`meltDataset.ts`](server/lib/wideFormat/meltDataset.ts). Pure fn that reshapes wide rows to `{...idCols, Period, PeriodIso, PeriodKind, Value}` (or `{…, Metric, Value}` for compound shape). Round-trip property test included.
  - **WF5** — [`fileParser.ts`](server/lib/fileParser.ts) string→number coercion now uses `stripCurrencyAndParse` (replaced the legacy `[%,$€£¥₹\s]` regex). Module-level `currencyTallyByColumn` captures the symbol per column during coercion; `finaliseCurrencyForColumn(name)` (exported) finalises votes into a `ColumnCurrency` annotation. `createDataSummary` attaches the resulting `currency` field to numeric `ColumnInfo`s and bypasses the `isIdentifierLikeNumericColumn` heuristic for currency-tagged columns (so 24 unique đX,XXX,XXX,XXX cells aren't misclassified as IDs).
  - **WF6** — extended [`server/shared/schema.ts`](server/shared/schema.ts) (client re-exports it via [`client/src/shared/schema.ts`](client/src/shared/schema.ts)). New optional fields: `ColumnInfo.currency: { symbol, isoCode, position, confidence }` and `DataSummary.wideFormatTransform: { detected, shape, idColumns, meltedColumns, periodCount, periodColumn, periodIsoColumn, periodKindColumn, valueColumn, metricColumn?, detectedCurrencySymbol }`.
  - **WF7** — wired the auto-melt into [`server/utils/uploadQueue.ts`](server/utils/uploadQueue.ts) right after `parseFile()` (the seam at the original L411). When `classifyDataset(headers).isWide`, `data` is replaced with the melted long form before profile inference, summary creation, and DuckDB materialisation. New [`applyWideFormatToSummary.ts`](server/lib/wideFormat/applyWideFormatToSummary.ts) helper votes the dominant currency across the original wide source columns and stamps it on the new long `Value` column. Feature-flag escape hatch: `WIDE_FORMAT_AUTO_MELT_ENABLED=false`.
  - **WF8** — extended the LLM dataset profile prompt in [`datasetProfile.ts`](server/lib/datasetProfile.ts) with `ambiguousCurrencyColumns` input (only set when `$`/`kr`/`¥` is detected). New `currencyOverrides: { columnName, isoCode }[]` output field — the pipeline applies overrides to `dataSummary.columns[i].currency.isoCode`. Enables Vietnam-context detection (`Markets="Off VN"` → VND) without hardcoding regional defaults.
  - **WF9** — client UI surfaces. New [`client/src/lib/currency.ts`](client/src/lib/currency.ts) (`formatCurrency`, `formatCurrencyCompact`). New [`client/src/components/WideFormatBanner.tsx`](client/src/components/WideFormatBanner.tsx) renders above the Dataset Columns card when `dataSummary.wideFormatTransform.detected` — collapsible "View original wide-format columns" lists the headers that were melted. [`ColumnsDisplay.tsx`](client/src/pages/Home/Components/ColumnsDisplay.tsx) accepts `currencyByColumn` + `wideFormatTransform` and renders `numeric · VND (đ)` chips on currency columns. Threaded through `DataPreview` → `MessageBubble` → `ChatInterface` → `Home` via new state on [`useHomeState`](client/src/pages/Home/modules/useHomeState.ts) populated by [`useHomeMutations`](client/src/pages/Home/modules/useHomeMutations.ts) from `dataSummary.columns[].currency` and `dataSummary.wideFormatTransform`.
  - **WF10** — end-to-end golden fixture in [`tests/wideFormatPipeline.test.ts`](server/tests/wideFormatPipeline.test.ts) pins the Marico-VN shape: parse → classify → melt → applyWideFormatTransformToSummary → 24 long rows, VND-tagged Value column, period ISOs `[2023-Q1, …, 2023-Q4, L12M-2YA, YTD-2YA]`. Five new test files appended to [`server/package.json`](server/package.json) `test` script (per the explicit-file-list rule). All 1642 server tests pass; all 171 client vitest tests pass; client TS error count went from 62 → 51 (no new errors introduced).
  - **Conventions added.** (1) **Wide-format detection is unconditional at upload (env-gated, default on).** Once a dataset trips `classifyDataset.isWide`, the long form is the canonical analysis surface — `dataSummary.columns`, `sampleRows`, `rawData`, the DuckDB `data` table, and RAG chunks all see the long shape. The original wide buffer survives in blob storage for download but is never re-parsed at chat time. (2) **Currency tags are heuristic-with-LLM-override.** `fileParser.currencyTallyByColumn` captures symbols at parse time (lossy — the symbol is stripped from numeric cells); `finaliseCurrencyForColumn` votes per column; the LLM dataset-profile pass overrides ambiguous symbols (`$`/`kr`/`¥`) using market/dataset context. Multi-currency columns return null and fall back to plain number formatting. (3) **DuckDB table name unchanged (`data`).** No agent-runtime changes were required — tools see a normal long-format table; the planner doesn't know a melt happened.
- **2026-04-28** — **Pivot Key Insight live-refetch fix.** Closed two bugs the user reported around stale / "basic" insights when pivot fields are mutated.
  - **Bug 1 (pivot view, frozen):** the "Key insight" card read from `message.insights[0]` (agent-turn-frozen) and never re-derived when the user dragged fields. Fix: new `pivotKeyInsight` state in [`DataPreviewTable.tsx`](client/src/pages/Home/Components/DataPreviewTable.tsx) drives a live refetch keyed off `pivotFlatRows` + a `pivotInsightConfigHash` (rows / columns / values / filter payload). Display selector becomes `analysisIntermediateInsight ?? pivotKeyInsight?.text ?? pivotInsight` (frozen → boot-state only). Loading affordance ("Re-deriving insight…") under the Lightbulb header.
  - **Bug 2a (chart view, "feels basic"):** `postChartKeyInsightEndpoint` passed `undefined`/`undefined` for `chatInsights` and `synthesisContext`, stripping all five context blocks from the LLM prompt; output mimicked the deterministic fallback. Fix: hydrate from session fields and the request body's `userQuestion` — parity with `enrichCharts`. See the new Conventions-that-bite entry.
  - **Bug 2b (chart view, sometimes missing):** empty pivot data returned `400` and wiped state; hash dedupe advanced on **all** outcomes so transient empty/error poisoned future refetches; optimistic `setChartInsight({ text: null, loading: true })` blanked prior text before the new fetch resolved. Fix: server returns `200 { keyInsight: "" }` on zero rows; outcome-aware dedupe ref (`{hash, outcome}`) only short-circuits on `success`/`pending`; functional setState preserves `prev?.text` across loading/error/empty transitions; `ChartKeyInsightCallout` keeps prior text with a muted "Couldn't refresh" subline on error.
  - **Conflicts and how they were resolved.** (1) **Hash-vs-data race** — `pivotInsightConfigHash` advances synchronously with the user's drag, but `pivotFlatRows` only updates after the server pivot query completes (~500ms). Resolution: effect deps include both; the React cleanup-on-rerender clears the stale-data timer before it fires, and a `pivotInsightSeqRef` aborts in-flight responses keyed to a superseded hash. (2) **Loading-overwrites-prior-text** — the existing `chartInsight` effect blanked text to null at the start of every fetch; if the new fetch failed, the prior good insight was lost. Resolution: every state setter uses `setX(prev => ({ ..., text: prev?.text ?? null }))`. (3) **Hash-poisoning** — pre-fix `lastInsightHashRef` was just `string` and advanced on empty/error too; once a hash had a transient failure, the same hash short-circuited forever. Resolution: outcome-aware ref `{hash, outcome}`; only `success`/`pending` short-circuit. (4) **`ChartKeyInsightCallout` UX** — old code unmounted the card on `loading: true`, which collapsed the card on every refresh. Resolution: pure-spinner only on first-load (`!insight.text`); when prior text exists, render it with an inline "Refreshing…" badge. (5) **Endpoint context starvation vs cost** — the lightweight chart-key-insight endpoint was deliberately stripped of context to be cheap, but the cost was the user-visible "feels basic" output. Resolution: hydrate via the process-memoised `loadEnabledDomainContext` (no measurable extra cost) and session fields already in scope. Verified: server build OK, `cd server && npm test` 1530/1530 pass; `cd client && npm test` 152/152 vitest pass; `cd client && npx tsc --noEmit` introduces zero new errors over baseline.
- **2026-04-28** — Promoted `CLAUDE.md` to comprehensive single-source-of-truth ("holy bible") with full server / client / agent-runtime / tool / skill / MMM / RAG / wide-format / domain-context / charting / deployment reference and a `docs/` index. Removed the legacy orchestrator description (deleted in commit `9422bed7`); added MMM pipeline (Waves W46–W55), narrator / AnswerEnvelope synthesis, investigation / blackboard subsystem, single-flow policy, Claude Opus 4.7 routing env vars, pivot subsystem, admin pages, expanded tool registry, new env flags, vitest convention. Fixed [`docs/agents-architecture-inventory.md`](docs/agents-architecture-inventory.md) in lockstep — removed the legacy-layer §3 and updated env-var defaults / counts. Added the "Last updated" header banner and this changelog. Removed the now-defunct `@assets/*` path alias.
- **2026-04-21** — Added the "tiny waves" working cadence (Wave W0.5).
- **earlier** — Initial repo layout, dev loop, and agent-architecture notes.
